-- DAT-008 (E5 secrets): the materialization TARGET carried alongside the kind on an
-- execution-secret handle — an env-var NAME for `env`, never a value. Generated by
-- `drizzle-kit generate`; the C14 idempotency guard (`IF NOT EXISTS` on the ADD COLUMN)
-- was hand-appended after generation so a partial-apply replay is a no-op (repo
-- convention, enforced by migration-idempotency.test.ts + migration-readiness). Schema
-- DDL is NOT hand-authored — only the idempotency guard is.
--
-- Nullable additive widen of an already-distributed table, so it inherits the existing
-- whole-table `aoa_app` grant and FORCE-RLS policy with no keystone reconciliation and
-- no new grant surface.
ALTER TABLE "job_secret_handles" ADD COLUMN IF NOT EXISTS "materialization_target" text;
