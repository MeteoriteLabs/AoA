{
  "$comment": [
    "WRK-008 slice 2b Step 9a — each D1 worker's declared DISPATCH gate posture, checked",
    "against docker-compose.d1.yml on every PR (scripts/check-d1-dispatch-declared.mjs).",
    "The plan's 'D1 must enable dispatch in its own compose file as a separate, attributable",
    "change' made mechanical. ALL FOUR landable gates are declared, not just the flag: a",
    "checker that parsed only AOA_WORKER_DISPATCH_ENABLED would stay green through the event",
    "it exists to catch (design §8 (i)).",
    "expect: 'absent' = the var must NOT be set; 'present' = set (any value); else the exact value."
  ],
  "workers": {
    "worker-a": {
      "dispatchEnabled": { "var": "AOA_WORKER_DISPATCH_ENABLED", "expect": "absent", "reason": "dispatch is OFF; enabling it is a separate attributable compose diff." },
      "keyStoreMode": { "var": "AOA_WORKER_KEY_STORE_MODE", "expect": "mounted_secret", "reason": "gate 3: mounted_secret with no OS-custody store means no device identity — the container's structural identity gate." },
      "sandboxProvider": { "var": "AOA_WORKER_SANDBOX_PROVIDER", "expect": "absent", "reason": "gate 1: the DEP-010 resolver reads this; absent ⇒ no provider. D1 runs bin/worker-daemon.js (the CONTAINER root), which has no resolver at all, so gate 1 stays STRUCTURAL through Sprint 2." },
      "e2bTemplate": { "var": "AOA_WORKER_E2B_TEMPLATE", "expect": "absent", "reason": "the second half of the DEP-010 provider construction; absent." },
      "providerUrl": { "var": "AOA_WORKER_PROVIDER_URL", "expect": "present", "reason": "PRESENT and DEAD: set to a live fake-provider for health, read by NO code (a full-tree grep finds only the two compose lines). NOT a gate. The first thing to re-examine is the day the CONTAINER root gains a provider path — not a compose diff." },
      "eventOutboxPath": { "var": "AOA_WORKER_EVENT_OUTBOX_PATH", "expect": "absent", "reason": "gate 4: absent ⇒ no durable outbox home ⇒ no_event_outbox_path (refuses first only once gates 1/3 pass)." }
    },
    "worker-b": {
      "dispatchEnabled": { "var": "AOA_WORKER_DISPATCH_ENABLED", "expect": "absent", "reason": "dispatch is OFF; enabling it is a separate attributable compose diff." },
      "keyStoreMode": { "var": "AOA_WORKER_KEY_STORE_MODE", "expect": "mounted_secret", "reason": "gate 3: mounted_secret with no OS-custody store means no device identity." },
      "sandboxProvider": { "var": "AOA_WORKER_SANDBOX_PROVIDER", "expect": "absent", "reason": "gate 1: absent ⇒ no provider; D1 runs the container root with no resolver." },
      "e2bTemplate": { "var": "AOA_WORKER_E2B_TEMPLATE", "expect": "absent", "reason": "absent." },
      "providerUrl": { "var": "AOA_WORKER_PROVIDER_URL", "expect": "present", "reason": "PRESENT and DEAD: read by no code; NOT a gate." },
      "eventOutboxPath": { "var": "AOA_WORKER_EVENT_OUTBOX_PATH", "expect": "absent", "reason": "gate 4: absent ⇒ no_event_outbox_path." }
    }
  }
}
