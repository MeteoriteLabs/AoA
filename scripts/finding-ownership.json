{
  "findings": {
    "E4-F007": {
      "status": "owned",
      "ticket": "WRK-010",
      "ownerStillOpen": "WRK-010 SLICE 1 (the server route) does not close this. The completeness critic established that after Sprints 1, 2 and 3 as originally sequenced the route would have had ZERO CALLERS -- slice 2b wires SessionStoreDeps.renew to Enroller.renew, the enrolment code replay, which only succeeds while the 10-minute code route is live. E4-F007's own statement of the defect ('a worker goes authority-less') therefore remains true until WRK-010 SLICE 2 ships the worker-side client plus the near-expiry threshold in SessionStore.ensureFresh. Closing it at slice 1 would convert a live problem into a settled one. Sequenced as go-book Sprint 2.5.",
      "reason": "The ownership call this entry was awaiting is made (go-book s8 D-2): ONE ticket, in E4, not a split across E3. The reason the E3-vs-E4 split lost its force is D-1 -- the renewal route reuses createWorkerSessionAuthenticator (server/src/middleware/worker-session-auth.ts:109), which already performs 9 of the 10 authority guards including the scope check at :165, so the route adds no new authority logic for an E3 ticket to own. CORRECTION to this entry's earlier text: IdentityLifecycle.acquireSession() is NOT 'already landed as the drop-in seam' -- grep finds that name in exactly two design documents and no source file. The real seam is SessionStoreDeps.renew, which is what WRK-010 targets."
    },
    "E4-F008": {
      "status": "owned",
      "ticket": "WRK-008",
      "ownerStillOpen": "Slice 2b - composing the poll loop and supervisor - is the live-dispatch wiring seam this finding names. Slices 1 and 2a have result docs; 2b does not exist yet.",
      "reason": "The finding says 'resolve at the live-dispatch wiring ticket (post-WRK-005)'. That ticket did not exist when it was filed; it does now, and it is WRK-008. A rotated provider-constraint digest going stale on a long-lived worker must be reconciled against in-flight leases when 2b composes the loop."
    },
    "E6-F003": {
      "status": "owned",
      "ticket": "DEP-011",
      "ownerStillOpen": "REPOINTED from DEP-010 to DEP-011 at DEP-010 completion. DEP-010 NARROWED this finding and DEFERRED the wire -- it answered WHICH PORT a networked worker->provider driver speaks (D1: worker-daemon's per-op SandboxProvider, transport-agnostic) and wired the desktop/self-hosted lane, but did NOT build the request/response wire a containerized worker speaks to adapter-manager. Leaving E6-F003 owned by the now-shipped DEP-010 would read as owned by nobody (E4-F013), so it is repointed to DEP-011, a filed scoping stub with a ticket file and a program node and no result doc. DEP-011 becomes required only when a containerized worker under docker-compose.staging.yml must dispatch: staging forbids E2B_API_KEY on any worker, so a container worker's provider cannot be key-backed and MUST be networked, reaching adapter-manager over control-net -- NOT provider-ctl-net, which is adapter-manager-only and a hard PROVIDER-CONTROL VIOLATION for a worker (docker-compose.d1.yml overloads that name to mean the opposite). No consumer exists today (adapter-manager has zero implementation; no worker dispatches).",
      "reason": "The composition-root decision this entry was awaiting was made by DEP-010 (go-book s8 D-3, decisions D1/D2), which resolved E6-F008 and E6-F004 and removed their entanglement with E6-F003, narrowing it from 'one of three ports' to 'the containerized worker->provider wire'. DEP-010 deferred that wire, so at completion its ownership passes to the successor that builds it, DEP-011 (E6). E6-F003 stays open (HIGH; a HIGH may never be accepted) until DEP-011 ships."
    },
    "E6-F005": {
      "status": "accepted",
      "reason": "LOW, documentation-only: add one line to the E6-D1-FOUNDATION gate noting that 'JOB-003 complete' transitively implies JOB-001/002/009 via the dep chain. The gate is closed and the dep chain is real; the missing sentence changes no behaviour and blocks nothing."
    },
    "E6-F006": {
      "status": "accepted",
      "reason": "LOW, and superseded by what actually shipped: DEP-000's acceptance named 'unschema-valid fixture rejected' and 'unknown op rejected' without dedicated RED files. The contract package now carries port-conformance, contract-nonvacuous and isolation-contract suites that exercise exactly those refusals. The gap was in the ticket's paperwork, not in the coverage."
    },
    "E6-F007": {
      "status": "accepted",
      "reason": "LOW, documentation-only: cite the drizzle --custom mechanism (E2-D01 precedent, Decision #122/C14) in DEP-003's RLS marker migration. DEP-003 shipped and the migrations exist; this is a citation, not a defect."
    },
    "E4-F009": {
      "status": "owned",
      "ticket": "WRK-008",
      "ownerStillOpen": "Slice 2b is the ticket that would compose the reconciler; it has a design doc and no result doc. The finding is the written-down reason it defers, so it stays open until a durable lease-candidate source exists.",
      "reason": "WRK-008 slice 2b is where createStartupReconciler would be composed, and its design defers exactly this. Filed now rather than at execution because the draft deferral stood on TWO reasons and one was false (ownershipSelector.organizationId IS available on the self-model); a deferral resting on a false reason is one nobody re-examines."
    },
    "E4-F010": {
      "status": "owned",
      "ticket": "WRK-011",
      "ownerStillOpen": "WRK-011 has a DESIGN doc and no RESULT doc, so nothing has shipped and this finding stays open. It is sequenced as go-book Sprint 2.75, between Sprint 2.5 and Sprint 3, and it cannot be pulled earlier: its success path replaces profile_hash, which the shipped middleware compares to the session claim at worker-session-auth.ts:167 (and job-leasing.ts:259/:297), so the caller's OWN session dies the instant the row is written -- and there is no SessionStore to hold the replacement until WRK-010 slice 2 ships one. What WRK-011 does NOT claim even after it ships: a composed daemon that polls, ACKs and supervises is Sprint 3 (createPollLoop still has zero production callers after WRK-011), and one real journey end to end on E2B is Sprint 5. The finding closes in WRK-011's result commit, which must flip the status in findings.md and DELETE this key in the same commit.",
      "reason": "HIGH. THE OWNERSHIP CALL THIS ENTRY WAS WAITING ON IS NOW MADE. This entry previously said 'the ticket that owns it should be written at Sprint 5 planning at the latest, and earlier if Sprint 3 surfaces it sooner'; that ticket now exists and is WRK-011, in E4, owning BOTH halves in ONE ticket -- the split is refused on purpose because the server route is not inert on success (it invalidates its caller's session), so a route without its first caller is worse than no route. The two halves, restated with the corrections WRK-011 verified at tip -- where this entry and the code disagreed, the code won. SERVER HALF: workers.profile_snapshot's only writers are still worker-enrollment.ts:444 (rotate) and :470 (insert), but at the repository layer those are two different operations (insertWorker :309, rotateWorker :313) and rotateWorker really is an UPDATE -- so 'no update channel' is not exact. The accurate statement is STRONGER: the only channel is enrolment rotation and a daemon can never travel it twice, because enroll-once.ts:193-202 short-circuits the steady-state boot before any network call, and a replay on the same code is refused at worker-enrollment.ts:325-327 once rotation advances the generation. The fix therefore has to be a NEW channel, not a way to reach the old one. WORKER HALF: poll-loop.ts:538 self-checks every offer against the worker's OWN self.report via offerSatisfiesWorker (poll/capacity.ts:143-157) -- correct as written -- but the shipped hello emits NO capabilities, not sandbox.*: desktop-hello.ts:144 takes capabilitiesForIsolation(isolation ?? 'none'), which is [] (isolation-capabilities.ts:50), and the only production call site (enroll-once.ts:255-261) passes no isolation. That makes the conclusion stronger, not stale -- ceiling-intersect-reported is empty for ANY ceiling. Second correction: 'false for 100% of offers' is true of the code and VACUOUS in production today, because createPollLoop has zero production callers and bin/worker-daemon.ts:344-345 hard-codes compose:false -- it is a correct prediction about the daemon Sprint 3 composes, not a live symptom anything is exhibiting. AND A THIRD BLOCKER THIS ENTRY NEVER NAMED, which fires EARLIER than either half: the enrolled all-zero capacity is a hard Math.min ceiling on the polled capacity (job-leasing.ts:566, :175-184), so deriveAdmissibleWorkloadTypes yields an empty workload list and repositories/tenant/job-control.ts:1810-1812 returns zero lease candidates before the static matcher is ever reached. A fix that refreshed only capabilities and policyHash would go green on every matcher test and still offer nothing. WHAT IT BLOCKS is unchanged: any claim that a distributed worker executes real work, Sprint 5's single-journey proof, and MIG-005/006/007 ACTIVE."
    },
    "E4-F012": {
      "status": "owned",
      "ticket": "WRK-010",
      "ownerStillOpen": "WRK-010 slice 2 (go-book Sprint 2.5) has no design doc yet; slice 1 does not touch it. The finding closes when a composed daemon can obtain its FIRST session, which is that sprint's acceptance clause.",
      "reason": "WRK-010 owns session lifecycle, and slice 2's stated acceptance -- 'the route has a production caller' -- is unreachable until this is answered. The answer is a DECISION, not plumbing: I13 discards the enrolment session on purpose so a bearer can never reach a log line, and every route to a first session either re-opens that or changes the SessionStoreDeps contract. Found by an independent codex review after two adversarial rounds had passed over the same seam."
    },
    "E4-F013": {
      "status": "unowned",
      "reason": "MED, and deliberately unowned: it is a hole in the ownership guard itself, so no product ticket is the natural owner and attaching it to one would be the same false-ownership move it describes. finding-ownership.mjs:118-120 tests only that ownerStillOpen is NON-EMPTY, so three live entries (E4-F008, E4-F009 -> WRK-008; E6-F003 -> DEP-010) will read as owned by shipped tickets the moment those tickets get result docs. The proposed fix is checkable -- when the owning ticket has a result doc, require a `successor` field naming a ticket that exists on disk -- but it is a guard change and needs its own RED test plus a deleted-guard mutation. Blocks nothing today."
    },
    "E4-F014": {
      "status": "unowned",
      "reason": "LOW, and deliberately unowned: a documentation inaccuracy in DSK-001's shipped design doc (it names IdentityLifecycle.acquireSession() as a landed seam at DSK-001-design.md:351 and :431; grep finds that symbol in no source .ts file). Filed by WRK-010 slice-1 terrain verification, the fourth documented-fact-with-no-code this programme has found. No product ticket is the natural owner -- remediation is a one-line cross-reference, not sprint work -- and the correction is already recorded in the E4-F007 reason above. The real seam is SessionStoreDeps.renew (packages/worker-daemon/src/identity/session.ts:55). Blocks nothing."
    },
    "E4-F015": {
      "status": "unowned",
      "reason": "MED, deliberately unowned: it documents that the gate-count checker the readiness audit recommended is UNSOUND (the shipped DispatchRefusalReason union has four members, the six-gate model is post-slice-2b prose no single source enumerates), and scopes the sound alternative (a declaration-based pin of the shipped union) which needs its own ticket because wiring a new check-*.mjs touches guard-inventory + execution-census + pr.yml. No product ticket is its natural owner. The interim rule -- label which enumeration any gate count means -- lives in the finding. Blocks nothing."
    }
  }
}
