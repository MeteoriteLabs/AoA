{
  "findings": {
    "E4-F008": {
      "status": "owned",
      "ticket": "WRK-012",
      "ownerStillOpen": "REPOINTED from WRK-008 to WRK-012 at WRK-008 slice 2b completion. Slice 2b composed the loop (the seam this finding named) but reads the self-model ONCE at boot and never re-reads it, so a mid-life provider-constraint rotation cannot be reconciled. WRK-012 is the filed scoping stub (ticket file + #### WRK-012 program node, no result doc) that adds the refresh channel + the lease-in-flight policy. Leaving it on shipped WRK-008 would read as owned by nobody (E4-F013).",
      "reason": "WRK-008 slice 2b is where the live-dispatch seam was composed, but the reconciliation of a rotated provider-constraint digest against in-flight leases is a self-model REFRESH channel + a lease-in-flight policy — design decisions with a blast radius, not a line in a composition ticket. LOW: the direction of failure is closed (a stale digest makes the worker unmatchable, not wrongly matched — capabilities.ts:466-467). Repointed to WRK-012 (E6/E4 pattern: DEP-011)."
    },
    "E6-F003": {
      "status": "owned",
      "ticket": "DEP-011",
      "ownerStillOpen": "REPOINTED from DEP-010 to DEP-011 at DEP-010 completion. DEP-010 NARROWED this finding and DEFERRED the wire -- it answered WHICH PORT a networked worker->provider driver speaks (D1: worker-daemon's per-op SandboxProvider, transport-agnostic) and wired the desktop/self-hosted lane, but did NOT build the request/response wire a containerized worker speaks to adapter-manager. Leaving E6-F003 owned by the now-shipped DEP-010 would read as owned by nobody (E4-F013), so it is repointed to DEP-011, a filed scoping stub with a ticket file and a program node and no result doc. DEP-011 becomes required only when a containerized worker under docker-compose.staging.yml must dispatch: staging forbids E2B_API_KEY on any worker, so a container worker's provider cannot be key-backed and MUST be networked, reaching adapter-manager over control-net -- NOT provider-ctl-net, which is adapter-manager-only and a hard PROVIDER-CONTROL VIOLATION for a worker (docker-compose.d1.yml overloads that name to mean the opposite). No consumer exists today (adapter-manager has zero implementation; no worker dispatches).",
      "reason": "The composition-root decision this entry was awaiting was made by DEP-010 (go-book s8 D-3, decisions D1/D2), which resolved E6-F008 and E6-F004 and removed their entanglement with E6-F003, narrowing it from 'one of three ports' to 'the containerized worker->provider wire'. DEP-010 deferred that wire, so at completion its ownership passes to the successor that builds it, DEP-011 (E6). E6-F003 stays open (HIGH; a HIGH may never be accepted) until DEP-011 ships."
    },
    "E6-F005": {
      "status": "accepted",
      "reason": "LOW, documentation-only: add one line to the E6-D1-FOUNDATION gate noting that 'JOB-003 complete' transitively implies JOB-001/002/009 via the dep chain. The gate is closed and the dep chain is real; the missing sentence changes no behaviour and blocks nothing."
    },
    "E6-F006": {
      "status": "accepted",
      "reason": "LOW, and superseded by what actually shipped: DEP-000's acceptance named 'unschema-valid fixture rejected' and 'unknown op rejected' without dedicated RED files. The contract package now carries port-conformance, contract-nonvacuous and isolation-contract suites that exercise exactly those refusals. The gap was in the ticket's paperwork, not in the coverage."
    },
    "E6-F007": {
      "status": "accepted",
      "reason": "LOW, documentation-only: cite the drizzle --custom mechanism (E2-D01 precedent, Decision #122/C14) in DEP-003's RLS marker migration. DEP-003 shipped and the migrations exist; this is a citation, not a defect."
    },
    "E4-F009": {
      "status": "owned",
      "ticket": "WRK-013",
      "ownerStillOpen": "REPOINTED from WRK-008 to WRK-013 at WRK-008 slice 2b completion. Slice 2b DEFERRED createStartupReconciler for ONE real blocker: leaseCandidates has no durable local source (the outbox persists events, not offers), so the lease-authority probe would run over [] every boot. WRK-013 is the filed scoping stub that adds the durable lease-candidate source and composes the reconciler. Leaving it on shipped WRK-008 would read as owned by nobody (E4-F013).",
      "reason": "WRK-008 slice 2b composed the loop but deferred the reconciler; reconstructing accepted lease offers from durable local state is its own ticket, not a line in a composition ticket. The ownershipSelector.organizationId concern was withdrawn (it IS on the self-model for org/owner-scoped targets). Repointed to WRK-013 (E6/E4 pattern: DEP-011)."
    },
    "E4-F014": {
      "status": "unowned",
      "reason": "LOW, and deliberately unowned: a documentation inaccuracy in DSK-001's shipped design doc (it names IdentityLifecycle.acquireSession() as a landed seam at DSK-001-design.md:351 and :431; grep finds that symbol in no source .ts file). Filed by WRK-010 slice-1 terrain verification, the fourth documented-fact-with-no-code this programme has found. No product ticket is the natural owner -- remediation is a one-line cross-reference, not sprint work. The real seam is SessionStoreDeps.renew (packages/worker-daemon/src/identity/session.ts:55), which WRK-010 slice 2 shipped the renewal client against; the phantom IdentityLifecycle.acquireSession() still appears only in DSK-001's design doc. Blocks nothing."
    },
    "E4-F016": {
      "status": "accepted",
      "reason": "LOW, documentation-only: three desktop-hello comments (desktop-hello.ts:28, :145, and the repeating comment in desktop-hello.test.ts) assert the enrolled all-zero capacity is byte-stability decoration the matcher overwrites, i.e. not a safety property. True of evaluateStaticLeaseEligibility (NEUTRAL_LEASE_MATCHER_CAPACITY, job-lease-eligibility.ts:213) but FALSE of the poll path: WRK-011 s0(d) proved the stored capacity is a hard Math.min clamp at job-leasing.ts:566, so zero stored capacity empties the admissible workload list before the static matcher. WRK-011 corrects the CODE the comments describe (a provisioned refresh writes a real capacity); a dated comment record is not silently rewritten mid-file, so this is the correction. Blocks nothing; remediation is a three-line comment fix no sprint carries."
    },
    "E10-F001": {
      "status": "unowned",
      "reason": "HIGH, deliberately unowned: making the first two Sprint-6 cutover designs (MIG-005 Commander, MIG-007 extraction) + reviews established that NO sink is buildable today. The two AGENTLESS sinks (extraction, Commander) refuse at the mint's guard 3 (agentless principal -> empty adapter; execution-secret-handle-mint.ts ~:167); CREW RIDES the mint (its principal is stamped {worker, agentId}, so guard 3 admits IFF the company's crew provider is v1 -- anthropic/openai, NOT google/opencode, per resolve-crew-adapter.ts; verified job-submission.integration.test.ts:77-99,307 + mint.test.ts:128-133,:138). All three still need distributed routing that only task_run has. The MIG-007 design corrected an earlier overstatement that extraction 'rides the mint' — it does not (agentless operationId), and it adds a sync->async result-return blocker that would drop every extracted item. Per-sink gap size: extraction (Company-key mint-runner change + thin value) < crew (agent-backed, RIDES the mint iff v1 provider; the shadow source_not_admitted was a fixture artifact -- no crew runs seeded) < Commander (net-new per-user credential class). The real Sprint 6 work is the SHARED prerequisites (routing seam + mint-runner generalization) + the sink-agnostic drain (the only landable item), not a sink flip. No current ticket fixes these; force-fitting onto one sink would be false ownership."
    },
    "E11-F001": {
      "status": "unowned",
      "reason": "LOW, deliberately unowned: it records that the 2026-08-27 breadth terrain audit (Sprint 9 section, docs/replatform/qa/2026-08-27-breadth-terrain-audit.md) still carries the pre-CI-green 'make the checker require the named release-test ticket to exist (flips E0 to honestly-red until E11 lands)' framing plus the imprecise '30/30 name REL-001/002/003/005' count (6 name the WRITTEN REL-004; only 24 name only-unwritten tickets). Both predate GO-BOOK §2.0's resolution (PR #327) that made ci-required green — after which a hard-strict foundation-checker red BREAKS a required check on every PR (the checker is a step of the always-on policy job). REL-FOUNDATION-GATE (S9 unit 1) ships the trackable-strict gate that corrects this at rest; the finding records that the dated QA snapshot is not silently rewritten. 'owned' is impossible — the non-numeric id REL-FOUNDATION-GATE is not a findTicketIds ticket, so it would red owner_ticket_missing. Blocks nothing."
    },
    "E11-F002": {
      "status": "owned",
      "ticket": "REL-003",
      "successor": "DBR-001",
      "ownerStillOpen": "REL-003 has a result doc (the verification core Lane A-E shipped + the DR rehearsal runbook), but E11-F002 stays OPEN because the restore leg is the owed operator leg: it resolves only when a real operator restore entrypoint (an `aoa db:restore` command or an exercised harness wrapper) lands AND the live staging rehearsal exercises it. Until then runDatabaseRestore has zero production callers. This is the WRK-008-style has-a-result-doc-but-part-still-open case the ownership guard's calibration allows. The runbook names the exact runDatabaseRestore/pg_restore invocation as the interim.",
      "reason": "MED: the database restore path has no operator entrypoint. Discovered by REL-003 (S9 unit 2) terrain: runDatabaseRestore (packages/db/src/backup-lib.ts) is exported from that module but has zero production/CLI callers, is NOT barrel-exported from @armyofagents/db, and no `aoa db:restore` command exists (aoa db:backup does). A DR ticket whose acceptance says 'prove database restore' thus has no operator invocation for the restore leg. Owned by REL-003, whose runbook (REL-003-dr-rehearsal-runbook.md step 4) wraps the invocation explicitly; resolves when a real restore entrypoint lands and the live rehearsal exercises it. Resolve = flip findings.md Status + DELETE this key in the SAME commit (C4)."
    },
    "E7-F003": {
      "status": "owned",
      "ticket": "CLI-008",
      "ownerStillOpen": "REPOINTED from `unowned` to CLI-008 on 2026-09-02, after a 39-agent scoping sweep produced the evidence a ticket could be written from. CLI-008 is a filed scoping stub (ticket file + #### CLI-008 program node, no result doc). The previous `unowned` reason was correct at the time - Unit 2 was sketched in a qa design with no ticket on disk, and naming a plausible existing ticket would have been a false claim of ownership. There is now an honest thing to point at.",
      "reason": "The distributed sandbox reaches the agent through argv only: no mcp__aoa__* tools, no instructions bundle, no workspace, no output capture. The acceptance verifier cannot see any of it - no clause reads workload, args, exitCode, stdout or any produced artifact, and clause 3 is terminal-agnostic - so a run that exits 127 with a context-free prompt satisfies every clause. MEDIUM as filed, but it is the last thing gating E7-1."
    },
    "E7-F007": {
      "status": "owned",
      "ticket": "MIG-010",
      "ownerStillOpen": "MIG-010 remains a filed scoping stub (ticket file + #### MIG-010 program node, NO result doc) and DELIBERATELY still has none: it closed E10-F002, E7-F004, E7-F005 and now E7-F006, but adding a -result.md would retire it as an owner exactly when this successor finding needs one. The divergence is between the pass verdict and the gate verdict over the same crosswalk, which is MIG-010 terrain and nobody else's. The fix is not settled (three non-equivalent shapes are on the record in the finding); choosing is the ticket work.",
      "reason": "reconcileCompanyLegacyResources computes closure over records it builds IN MEMORY; canary-preflight recomputes it over records PERSISTED in the append-only crosswalk. Delete an agent AFTER its lease was already recorded as mapped and the two disagree: onConflictDoNothing means the newly-unattributable record is never written, so the pass refuses forever while the gate opens. MEDIUM: the gate is arguably right (the record was true when written and the resource is unchanged), but the operator pass becomes permanently red with no command that can make it green, and the Unit 2.5 remedy cannot touch it - there is no unattributable record to resolve, and widening it to rewrite a mapped record is the forgeable transition design section 9.2 forbids. Measured on a real database in mig-010-unit-2-5-unattributable.integration.test.ts, not reasoned."
    },
    "E7-F008": {
      "status": "owned",
      "ticket": "CLI-008",
      "ownerStillOpen": "CLI-008 is a filed scoping stub (ticket file + #### CLI-008 program node, no result doc) and already owns E7-F003. This is the same argv surface CLI-008 unit D touches when the prompt stops being a positional, so splitting it to another ticket would split one edit across two owners. Fixable independently of the Unit B channel decision: chunked argv gives 8x without a new channel.",
      "reason": "buildTaskRunBatchWorkload refuses prompt_too_large above FROZEN_MAX_ARG_CHARS = 8192, mirroring the frozen schema's per-element cap, so a task whose assembled markdown exceeds ~7.4-7.7k description characters cannot dispatch distributed at all. Measured, not inferred. MEDIUM: a refusal rather than a truncation, and the protocol allows ~64 KiB per job across elements, so chunking is 8x headroom."
    },
    "E3-F034": {
      "status": "unowned",
      "reason": "MEDIUM, and deliberately unowned: every E3 ticket (JOB-001..JOB-014) has a result doc, so no open ticket can inherit this and naming a shipped one would be a false claim of ownership. job-leasing.integration.test.ts:1467 fires 100 concurrent service.poll() via Promise.all at ONE execution_targets row through a 24-connection pool (:829) against a 750ms lock_timeout connection GUC (packages/db/src/client.ts:107), each holding FOR UPDATE (job-control.ts:1819-1821) to COMMIT of the transaction opened at job-leasing.ts:542 -- so the test passes only while ~24 queued waiters all clear 750ms. Observed 93 succeed / 7 time out: it sits ON the threshold, and slower runner storage tips it. Non-determinism and pre-existence are both PROVEN rather than inferred: run 33723015836 attempt 2 re-ran the SAME sha dd03516d5 with no code change and went green, and the identical two-test failure with the identical assertion string already occurred on docs/replatform-program @ 811ee7ede on 2026-09-02. The load-bearing half is NOT the flake but the CASCADE: Promise.all rejects without cancelling the other 99 polls, whose transactions keep mutating the database while the NEXT test (:1493) calls resetRuntimeRows() and seeds; that test awaits its three polls SEQUENTIALLY (:1508/:1512/:1516) so it cannot be self-contending -- its failure is entirely imported, which is why one flaky test reports as two failures and six of seven 55P03s carry the wrong test name. Blocks nothing functionally; it degrades a REQUIRED check (verify) and cost a full investigation to attribute on its second sighting. Awaiting an E3 owner: fixing the cascade (await the stragglers alongside the existing assertions, not a bare Promise.allSettled swap, which would also stop rejections being failures) is cheap and unambiguous; moving the test off the threshold (lockTimeoutMs, pool cap, or claimer count) weakens what it proves and should say so in the test."
    },
    "E7-F011": {
      "status": "owned",
      "ticket": "CLI-008",
      "ownerStillOpen": "CLI-008 is a filed scoping stub (ticket file + program node, no result doc) and already owns E7-F003, E7-F008 and E7-F009. This is the seam Unit B built and the one Units C and D must cross first, so it belongs with them rather than split to another owner; splitting it would put the prerequisite and its consumers under different tickets.",
      "reason": "MEDIUM, corrected down from HIGH the same day it was filed. Unit B's staging channel has no route on the NETWORKED/container lane: stage_files is deliberately absent from the frozen ProviderOperation vocabulary (capabilities.ts:125-139), the wire's #post is typed to it, NetworkedProviderDriver declares fileStagingMode=none and its stageFiles throws (driver.ts:93,193-199), and supervisor.ts:700 fails closed with stage_input_failed. CORRECTION: as filed this claimed the shipped image boots the networked-host root and cited Dockerfile:13 - both false. Dockerfile:196 is CMD [node, dist/bin/worker-daemon.js] (the LOCAL daemon root) and :158-159 says the networked tree is \"Present but NOT entered\"; boot-roots-expectation.json, environment-variables.md:196 and DEP-010-design.md:704 all agree, and d1-compose-invariants.mjs:503-523 actively rejects the override that would enter it. A second gate: resolve-provider-url.ts:31 returns kind=none without AOA_WORKER_PROVIDER_URL, so no driver is constructed at all. So there is no shipped boot on which container-lane runs hard-fail, and Units C/D are NOT blocked - they run on the E2B/desktop lane. The fix is larger than first written: adapter-manager/src/server.ts:115-120 holds handlers for only create and execute and GATE_REQUIRED_OPS (:88-97) is the eight core ops, so a route must be added to the SERVER and its gate, not only to the driver type. Still no new provider and no change to the frozen vocabulary. Filed because the constraint existed ONLY as a prose comment in driver.ts:84-92 - no finding, no ticket, no gate clause - and therefore invisible to every register built to surface exactly this."
    },
    "E7-F009": {
      "status": "owned",
      "ticket": "CLI-008",
      "ownerStillOpen": "CLI-008 is a filed scoping stub (ticket file + #### CLI-008 program node, no result doc) and already owns E7-F003 and E7-F008. This is the same job-input-staging surface Unit B built and Units C/D will first exercise, so it belongs with them rather than split to another owner.",
      "reason": "pointerFitsExtension projects input.files only, while the lease offer is built from ALL committed rows for the attempt. Repeated stages therefore inflate the real extension past the 16,384-byte canonical limit while every individual call reports 'fits'; buildJobEnvelope then returns null and leasing throws internal_unavailable, leaving the job permanently unleaseable with nothing naming the cause. MEDIUM: latent until a producer stages more than once per attempt, but strictly worse than the duplicate-row defect it rides on - a dead job rather than a wrong file."
    }
  }
}
