{
  "$comment": [
    "Gate-clause wiring register. Seeded 2026-08-25 from the E0..E11 exit-gate audit.",
    "wired   = a production path reaches the named symbol; the checker verifies it and fails on 0.",
    "unwired = honestly dormant; reason must say what would have to change. Reported on green runs.",
    "Claiming wired when nothing calls it is the defect this register exists to make impossible.",
    "A CLAUSE IS THE UNIT OF CLAIM, AND HALF A CONJUNCTIVE CLAUSE MAY NOT BE ENROLLED. 2026-09-09: E0-de06-artifact-denial-audit was added here as `wired` for DE-06's audit clause and REMOVED the same day. The clause reads 'object put/get AND rejected-key attempts are audited'; only the rejected-key conjunct was wired, and a successful download grant still writes nothing. The symbol genuinely had callers, so the checker was green and correct -- what was false was the CLAUSE-LEVEL claim the key asserts. Enrolment here reads as delivery; a partly-delivered clause belongs in its finding until the whole clause holds. See docs/replatform/epics/E0-foundation/findings.md, E0-F010.",
    "2026-09-10, THE RELEASE CONDITION THAT PARAGRAPH NAMED IS NOW MET, and it is recorded here rather than left to make the paragraph above read as a live gap. The put/get conjunct was delivered by the object-access unit: a SUCCESSFUL upload or download grant now writes an attributable `security.object_access.*` row (server/src/services/artifact-object-access-audit.ts). BOTH conjuncts of DE-06's audit clause therefore hold, so the clause -- the unit of claim -- is enrolled, under the key E0-de06-object-access-audit below. The 2026-09-09 removal stands as written and is NOT retracted: it was correct on the evidence it had. What changed is the evidence, not the rule.",
    "providerCapabilityClaims (optional) = the provider-capability values this clause's reason asserts, checked against the source that declares them. Added 2026-09-06 (W4U1) after E5-2's reason described artifactExportMode for weeks after PR #353 changed it, because nothing read the sentence."
  ],
  "clauses": {
    "E9-1-service-reconciler": {
      "epic": "E9",
      "status": "wired",
      "symbol": "createServiceReconciler",
      "reason": "E9's exit gate opens with `desired state` and `placement`; SVC-002 is the control-plane half. TIMING: SVC-002-design.md 9.2 predicted this clause would land `unwired`, on the GO-BOOK's 2026-08-28 ruling that an E9 clause was PREMATURE `no real service-execution symbol to track until SVC-002 lands ... so a clause would be vacuous`. That ruling named its own release condition and it is now met: SVC-002 lands the symbol AND its arming path in one commit. PRODUCTION CALLERS: 0 at base (the symbol did not exist), 2 at head as the guard's own `countProductionCallers` measures it -- and those two lines are ONE composition site in server/src/index.ts, the dynamic-import binding plus the call, inside the `config.distributedExecutionEnabled && distributedExecutionDatabases` block. The measured figure is stated rather than the intuitive one because the guard counts the `const { createServiceReconciler } = await import(...)` line too (its import-stripping regex only blanks static `import ... from` forms). That block builds the reconciler, drives it on a `nextDelayMs` backoff timer, and stops it on SIGTERM/SIGINT. Registered inside that block for MIG-002's measured reason, not by copying it: flag-off allocates no `aoa_app` pool, so `runInTenant` would have nothing to open and unconditional registration would be a throw rather than a safety net. WHAT THE CLAUSE CLAIMS, NARROWLY: when a `services` row exists with `desired_state='running'` and a `service_generations` definition for its generation, the reconciler creates EXACTLY ONE `service_instances` row and EXACTLY ONE `service` job for it, in one transaction, and never a second -- idempotently across ticks, across concurrent passes, and across a writer that takes no lock at all. WHAT IT DOES NOT CLAIM, and each of these is the difference between this clause and the epic's gate: (1) NOTHING CREATES A SERVICE. `services` has one insert in the tree with ZERO production callers and SVC-002 adds no route (SVC-007 owns create/update/pause/resume/stop), so on a real deployment the tick reads an empty window and converges nothing -- E9's exit gate is NOT met by this ticket and this ticket does not claim it is. (2) `service_generations` likewise has ZERO writers, so even a hand-inserted service stalls at `{action:'none', reason:'no_generation'}` rather than starting with an invented command; the unreadable-intent branch is a deliberate stall, not a default. (3) It converges zero instances to one and then goes quiescent: `recordServiceHealth` is the only writer of `service_instances.status` and has no consumer, so nothing here drives an instance terminal (SVC-003). (4) `drain` means a draining target is not SELECTED for a new instance; moving or stopping an instance already running on a draining target is SVC-003/SVC-005. (5) A submitted service job is placed `queued`/`no_eligible_target` unless a daemon advertises `workload.service` with a free service slot, and E9-F002 stays open. PROVEN, NOT DECLARED: server/src/__tests__/service-reconciler.integration.test.ts (17 cases: SCHEMA, T1a-T1d, T2, T3a/T3b x3/T3c, T4, T5, T6, T7 + a NAMED positive control) plus packages/db/src/__tests__/service-instance-live-predicate.test.ts (4 cases). TWELVE mutants were applied to the shipped source, VERIFIED APPLIED in the rebuilt dist before their result was believed, run, and reverted; each killed at least one case while the named positive control (`T4 POSITIVE CONTROL`, pure `decideJobPlacement`) stayed green under every one: drop the partial unique index (2 red), widen its terminal predicate with 'pending' (2 red), widen the loser's catch to a bare `catch {}` (1 red, T1d), revert `reconciliationId` to `randomUUID()` (1 red, T1c), catch the 429 INSIDE the transaction (1 red -- T2's zero-INSTANCES half, with its zero-JOBS half still green, i.e. exactly the orphan wedge), delete the reconciler's sweep filter (1 red, T3a, on the reason and the no-error assertion rather than on a vacuous zero-instances one), delete the `desiredState` admission predicate (3 red -- and this is the one GENUINE base-tree red: before SVC-002 `serviceSourceIsAdmitted` had no such predicate), change it to `ne('stopped')` (2 red: `paused` and `deleted`, with `stopped` still green, which is the allow-list-vs-deny-list discrimination), and -- added in the review pass -- anchor the per-organization service sweep at the head instead of its cursor (1 red, T5: `expected [ ...(2) ] to deeply equal [ ...(5) ]`, two of five services converged and the other three starved forever, which was a REAL BUG in the first revision of this diff and is fixed), drop the `notExists` convergence predicate from `listReconcilableServices` (2 red, T4 + T6: a converged service never leaves the sweep window, so the window never shrinks and a restarted process re-walks finished work forever), and wrap the sweep cursor on a SHORT page without checking it was fully processed (1 red, T7: with three STALLED services -- and today every service stalls at `no_generation`, because `service_generations` has no writer -- a budget-truncated page restarts at the head every tick and the tail behind them is never reached), and revert the live-instance predicate from inlined SQL literals to drizzle's `notInArray` (2 red: the compiled statement carries three BIND PARAMETERS, and a parameterised `status NOT IN ($1,$2,$3)` cannot be proven to imply the LITERAL predicate of `service_instances_live_service_uq`, so once postgres-js's prepared statement is promoted to a generic plan the partial index is dropped and the sweep sequentially scans `service_instances` -- reproduced on PostgreSQL 18 in review). ★ TWO MUTANTS THE DESIGN NAMED DID **NOT** KILL, AND THAT IS RECORDED RATHER THAN DROPPED: (i) changing the observed-state predicate from non-terminal to `status='healthy'` leaves the suite GREEN (measured 14/14, before the review pass added T5), because that predicate and the index predicate are THE SAME CONDITION at two layers -- it reds only together with dropping the index, and then T4's `created` is [1,1,1]; (ii) removing the advisory lock leaves the suite GREEN, and so does removing the `FOR UPDATE` -- the serialization case reds only when BOTH go, so it pins the pair and neither member is individually necessary. Both corrections are written into the test file beside the cases they concern. THE AUTHORITY IS THE INDEX, NOT THE LOOP: `service_instances_live_service_uq` (migration 0275) binds every writer including ones not yet written, and T1a exercises it with two LOCK-FREE inserters on separate connections rather than through the reconciler."
    },
    "E9-2-service-supervisor": {
      "epic": "E9",
      "status": "wired",
      "symbol": "runServiceLifecycle",
      "reason": "E9's exit gate names supervision of a service instance; SVC-008b is the daemon half (SVC-008-design.md §11.2). WIRED, and enrolled as `wired` rather than the `unwired` the design predicted, because the design wrote that line when the constant widening was still hypothetical: SVC-008b actually dispatches to the symbol. PRODUCTION CALLERS: ONE, as the guard measures it — the `handoff.offer.job.workloadType === \"service\"` branch in `runLifecycle` (packages/worker-daemon/src/supervisor/supervisor.ts), which sits on the shipped lease path `createSupervisor` -> `accept` -> `runLifecycle`, and `createSupervisor` is composed in production by `lifecycle/dispatch-runtime.ts`. WHAT THE CLAUSE CLAIMS, NARROWLY: a service job reaches a worker and is SUPERVISED AS A SERVICE — launched via `startProcess` (an acknowledgement, not a completion), health-ticked from `processStatus`, stopped through a cancel->kill ladder whose verdict comes from an observation, and terminalized with the frozen service-instance events. It does NOT claim services are MANAGED: health semantics + the `service_health` -> `recordServiceHealth` projection are SVC-003's, restart/checkpoint are SVC-004's, drain/generation are SVC-005's, and there is still no human path to create a service (SVC-007). SVC-008b adds NO consumer of `recordServiceHealth` and no `service_health` state projection: the symbol's reference set is UNCHANGED by this diff, base to head, as the register's own counter reports. (The reason line previously said `recordServiceHealth` \"keeps its zero production callers\"; that number was wrong — the counter reports the same non-zero figure at base and at head. The substance, no new consumer, is what the clause needs and is what is asserted here.) PROVEN, NOT DECLARED: packages/worker-daemon/src/__tests__/svc-008b-service-supervisor.test.ts (T1-T7 + a NAMED positive control) was observed 12-red / 2-green against the unchanged tree, and six mutants were killed — deleting the dispatch (12 red), synthesizing `healthy` on an unwitnessed read, deriving the stop verdict from `ProcessSignalResult.accepted` instead of its observation, dropping the per-tick fence check, dropping the capability-freshness stop, and killing before the graceful rung. The positive control stayed green under every one. server/src/__tests__/u0-d1-placement-reachability.test.ts carries T0: the committed worker-b ceiling now leaves the shipped daemon's DERIVED hello offerable a real service job through the real `evaluateStaticLeaseEligibility`; reverting the constant reds T0 and the exact-equality pin while both batch cases stay green. RESIDUAL, ON THE RECORD: a service run is bounded at RUN_OP_DEADLINE_CEILING_MS (240 s) because the owned-labels capability is minted on one route with a 5-minute TTL and never re-minted; the loop stops on the teardown headroom rather than orphan a billable sandbox. Whether a four-minute service is worth advertising is SVC-008 §9.1 and is UNRULED."
    },
    "E9-3-service-health-projection": {
      "epic": "E9",
      "status": "wired",
      "symbol": "applyServiceProjectionForFence",
      "reason": "E9's exit gate names `health`, and this is the control-plane half of it: the projection that turns a durably-ingested service observation into `service_instances.status`. WHAT WAS TRUE BEFORE: SVC-008b emitted the five frozen service events and the JOB-005 ingest stored them, and they PROJECTED NO STATE CHANGE -- SVC-008b's own result says so in terms (\"adds NO consumer of `recordServiceHealth` and no `service_health` projection ... every service event emitted here is durably stored and projects no state change\"), and E9-1's reason records the consequence: SVC-002's reconciler converged zero instances to one and went quiescent, because nothing could drive an instance terminal. PRODUCTION CALLERS, measured with this guard's own `countProductionCallers`, base `6b39c77f6` vs head: `applyServiceProjectionForFence` 0 -> 1 (the per-event branch inside `acceptEvent`, itself on the shipped JOB-005 ingest path `workerControlRoutes` -> `createJobEventIngestService.ingest` -> `repos.jobControl.acceptEvent`); `writeServiceInstanceStatus` 0 -> 2 (the projection and `recordServiceHealth`, which now share the ONE writer of that column so the two cannot drift); `decideServiceProjectionForEvent` 0 -> 1; `predecessorsOf` 0 -> 1; and `canTransitionServiceInstanceStatus` 0 -> 1, which is the point of that last line: the FROZEN `SERVICE_INSTANCE_TRANSITIONS` table had ZERO production consumers at base, so the lifecycle it defines was a clause nothing enforced. `recordServiceHealth` is 2 at base and 2 at head -- unchanged, because the projection does not call it; both are entry points onto the shared writer. `createStartupReconciler`, the cautionary zero-caller neighbour, is untouched at 0. WHAT THE CLAUSE CLAIMS, NARROWLY: an accepted worker event that WITNESSES an instance fact moves `service_instances.status` inside the same fenced transaction that appends it, idempotently on a `service_instance_status` projection receipt, and every other event moves nothing. The authority for WHICH row is `service_instances.job_id`/`.attempt_id` (SVC-002's attribution), never the worker-controlled `payload.serviceInstanceId` -- which is unauthorized by construction (E9-F003), so a mismatched claim is REFUSED. The generation on the instance row is the FENCE SVC-002 scoped out by name, and a stale-generation event is refused. Legality is the frozen transition table, so no event can move an instance OUT of `stopped`/`failed`/`lost`: that is the DE-12 split-brain refusal, because a terminal instance has left `service_instances_live_service_uq` and SVC-002 has already replaced it. An attempt with no attributed instance answers `unattributed` -- an explicit unknown that writes nothing, never a default status. WHAT IT DOES NOT CLAIM, and each is the difference between this clause and E9's gate: (1) NOTHING CREATES A SERVICE -- `services` still has one insert with zero production callers and no route (SVC-007), so on a real deployment there is no instance for this projection to move; E9's exit gate is NOT met and this ticket does not claim it is. (2) The rest of SVC-003's Outcome is NOT delivered: no liveness deadline (a worker that goes silent without emitting anything is still only reaped at the LEASE level, and nothing terminalizes its instance), no graceful-stop or checkpoint request path, and no bounded lease-renewal change. SVC-003 stays OPEN. (3) Memory/context callbacks and connector refresh under the current fence are NOT built and are not buildable yet -- `MemoryActor` has no `service` kind and no memory op exists among the ten frozen worker operations (SVC-001-design.md section 4). (4) No partition test: no replica identity or partition detector exists anywhere in the tree. PROVEN, NOT DECLARED: server/src/__tests__/service-health-projection.integration.test.ts (11 cases over real embedded PostgreSQL and a real poll/ACK-minted ACTIVE fence, T1 driving the FULL `createJobEventIngestService` path so the decider is proven reachable from the wire -- and T1 is the ONLY case that drives that ingest, so the production wire is proven by exactly ONE arm: the `serviceProjection: null` mutant below reds T1 and nothing else, which is both the arming discrimination and the thinness) and server/src/__tests__/service-health-projection.test.ts (15 pure cases over the whole 9x9 transition table). SEVENTEEN mutants were applied to the shipped source, each VERIFIED APPLIED by a harness that tries both line-ending forms of its anchor and throws when it matches nothing, run, and reverted; every one killed at least one of the 26 cases while the NAMED positive control (`T8 POSITIVE CONTROL -- the batch attempt/job projection is byte-identical`) stayed green under all seventeen. The kills: delete the `attempt_started` -> `leased` arm (10 red -- without it `pending` is a dead end and the whole projection is production-unreachable), map `service_instance_started` to `healthy` (5), return the transition table unfiltered (7), project `stopping` from `service_graceful_stop_observed` (1), default an unreadable health verdict to `healthy` (2), REINSTATE `\"interrupted\"` in the health-status domain (2 -- this is the genuine BASE-TREE state, E9-F001), delete the legality check (1 -- T5, the split-brain case), trust `payload.serviceInstanceId` (1 -- T4), delete the generation fence (1 -- T3), return a definite `applied` for an unattributed observation (1 -- T7), have the projection bump `leases.expires_at` (1 -- T6, E9's \"health events do not extend ownership\" clause), delete the call site in `acceptEvent` (10 red, positive control still green), set `serviceProjection: null` in the ingest's `toAcceptInputs` (1 -- and ONLY T1, which is exactly the arming discrimination: the feature wired to nothing while every other case passes), and the four added by the review fixes -- revert `predecessorsOf` to the direct-edge filter (3), delete the attempt-terminal backstop (3), default `whenAlreadyTerminal` to `\"noop\"` (2, one of them T5 itself), and walk back through PROJECTABLE states too (3). ★★★ TWO REAL DEFECTS IN THIS DIFF WERE FOUND BY EXTERNAL REVIEW, NOT BY THE SUITE, AND BOTH WERE PERMANENT WEDGES -- recorded here because the shape of the miss is the point. (i) E9-F004: `SERVICE_INSTANCE_TRANSITIONS` makes `stopping` the SOLE predecessor of `stopped` and NO frozen worker event can assert `stopping` (the supervisor emits `service_instance_stopped` directly from `healthy` at `runServiceLifecycle`'s `case \"process_exited\"` arm at service-lifecycle.ts ~:293, and `gracefulStop`'s `verdict === \"stopped\"` branch at ~:362), so the first revision's direct-edge legality predicate refused EVERY NORMAL SERVICE STOP as `illegal_transition`, leaving the instance inside `service_instances_live_service_uq` where the reconciler could never replace it. It survived a named positive control and thirteen killed mutants because the only end-to-end case drove `service_instance_lost`, which the frozen table makes reachable from every non-terminal status -- a lifecycle table proven over the transitions a suite happens to exercise is not proven over the table. Fixed by admitting paths whose every INTERMEDIATE step is a status no event can project; the residual (`stopped` now admitted from `leased` too) is FILED as E9-F004 and stays open. (ii) E9-F005: a launch resolving no handle emits no `service_instance_started` (`runServiceLifecycle`'s §4.2a launch comment at service-lifecycle.ts ~:166 says so in terms), so a failed attempt left the instance `leased` forever; fixed with the attempt-terminal backstop and filed as E9-F005, resolved here."
    },
    "E9-4-service-liveness-deadline": {
      "epic": "E9",
      "status": "wired",
      "symbol": "sweepOrganizationServiceLiveness",
      "reason": "E9's exit gate names `health` AND `restart`; SVC-003a wired the first half and this is the piece between them: the deadline that notices an instance is no longer live WHEN NOTHING SAID SO. WHAT WAS TRUE BEFORE: every path SVC-003a built is edge-triggered by an accepted worker event, and SVC-003a's own result says the gap in terms -- \"a worker that goes silent WITHOUT EMITTING ANYTHING is still only reaped at the LEASE level, and nothing terminalizes its instance\". Measured at head before this ticket: `reapExpiredLeases` revokes the lease and terminalizes the ATTEMPT and the JOB and contains no write to `service_instances` at all, so the instance stayed inside `service_instances_live_service_uq` and SVC-002's reconciler could never replace it. That is the case L-T8 measures rather than asserts. The dangerous variant is worse: `lease-renewal.ts` renews on a driver separate from the supervise loop, and `runServiceLifecycle`'s health step handles an unanswerable `processStatus` read with `unknown => EMIT NOTHING` (its own comment closes \"SVC-003 owns the liveness DEADLINE policy\"), so a worker whose supervision has gone silent while renewal continues holds a lease that never expires and is invisible to the reaper entirely. PRODUCTION CALLERS, measured with this guard's own `countProductionCallers`, base `053f90fc8` vs head: `sweepOrganizationServiceLiveness` 0 -> 1 (the pass inside `createServiceReconciler`'s own `runTick`, ahead of the convergence pages); `sweepServiceInstanceLiveness` 0 -> 3, of which only ONE is a call -- the guard also counts the repository interface declaration and its implementation, the same reason SVC-003a records 2 for `recordServiceHealth`; `classifyServiceInstanceLiveness` 0 -> 1; `livenessVerdictTerminalizes` 0 -> 1; `livenessDeadlineAllowedFromStatuses` 0 -> 2 (the sweep and the module's own load-time coverage assertion); `SERVICE_LIVENESS_DEADLINE_TO_STATUS` 0 -> 3; `writeServiceInstanceStatus` 2 -> 3 at this ticket's head and 4 at the MERGED head (the deadline is the THIRD entry point onto the one writer of that column, and SVC-007a's `terminalizeServiceInstanceForCancelledAttempt` -- merged in afterwards -- is the FOURTH; four entry points, still one writer and one idea of a legal move); `predecessorsOf` 1 -> 2. UNCHANGED and re-measured rather than recited: `applyServiceProjectionForFence` 1 -> 1, `createServiceReconciler` 2 -> 2, `recordServiceHealth` 2 -> 2, `reapExpiredLeases` 3 -> 3, and the cautionary neighbour `createStartupReconciler` 0 -> 0. WHAT THE CLAUSE CLAIMS, NARROWLY: a live `service_instances` row whose worker has stopped being observed is driven to the frozen `lost` status by a clock rather than by an event, it leaves `service_instances_live_service_uq`, and SVC-002's unchanged reconciler creates its replacement -- in the SAME tick, because the sweep runs ahead of the convergence pages. TWO WINDOWS, AND THEY NEVER SUBSTITUTE: an instance that HAS been observed is aged against a short liveness window; one that has NEVER been observed has no liveness fact to age and is aged against `created_at` under a separate, longer admission window. Collapsing them in either direction is a defect with a name and both are mutation-pinned (L4/L15 for the short-window collapse, L5 for the infinitely-stale collapse). WHAT IT DOES NOT CLAIM, and each is the difference between this clause and the epic's gate: (1) NOTHING STILL CREATES A SERVICE -- `repos.services.insert` has the same ZERO production callers it had at base and this ticket adds no route (SVC-007), so on a real deployment the sweep reads an empty population and terminalizes nothing. (2) `service_generations` still has no writer, so a replacement stalls at `no_generation` on a real deployment exactly as SVC-002 records. (3) The deadline terminalizes the INSTANCE and does NOT fence the WORKER: it writes exactly one table (L-T11, mutant L17), so a silent-but-still-renewing worker keeps its lease while its replacement starts. The replacement is protected by SVC-003a's split-brain refusal; the old worker's external effects are not, and that residual is filed as E9-F007 and belongs to SVC-005's overlap clause. (4) WHETHER a terminalized instance should be replaced at all, and with what backoff, is SVC-004's crash-loop clause and this ticket has no opinion about it. PROVEN, NOT DECLARED: server/src/__tests__/service-liveness-deadline.test.ts (15 pure cases) and server/src/__tests__/service-liveness-deadline.integration.test.ts (13 cases, real embedded PostgreSQL, real poll/ACK-minted ACTIVE fence, the real shipped reaper). TWENTY mutants were applied to the shipped source by a harness that refuses to apply over an existing backup, tries BOTH line-ending forms of every anchor and THROWS on a miss, rebuilds `dist` for repository mutants, and byte-verifies each restore; a green unmutated baseline was measured before and after the campaign. Every one killed at least one case and the NAMED POSITIVE CONTROL (`L-T9 POSITIVE CONTROL -- SVC-002's convergence is untouched by the deadline`) stayed green under nineteen of the twenty. L7 is the exception and is a NON-RUN rather than a survival: it makes the module throw at load, so neither suite collects and no case executes, the control included -- said this way because a control that did not run is not a control that held. ★ ONE MUTANT SURVIVED THE FIRST CAMPAIGN AND IS RECORDED RATHER THAN DROPPED: L14 (delete the sweep's independent legality gate) killed NOTHING, because `lost` is legal from all six live statuses so the gate is unreachable through the shipped caller. The gate was kept and pinned by a case that performs the narrow call itself (L-T10), the same shape SVC-002's T1c uses for its own unreachable property. ★★★ AND EXTERNAL REVIEW OF PR #413 FOUND A REAL DEFECT IN THIS DIFF, RECORDED HERE BECAUSE OF WHAT IT WAS: the sweep read `ORDER BY created_at ASC LIMIT n`, and a HEALTHY instance never leaves the live set -- so for a tenant with more live instances than one batch holds, the oldest-created healthy rows filled the batch on every tick and a silent instance created after them was NEVER INSPECTED. That is a stuck service the deadline itself cannot see, i.e. the exact failure this clause claims to remove, and it is the SAME starvation bug review caught in `listReconcilableServices` on PR #406 -- whose correction note sits a few hundred lines above the new query in the same file. Fixed by ordering on `COALESCE(last_observed_at, created_at)` (an ORDERING, not a verdict: mutant L15 pins that collapsing the two instants in the SELECT is still a defect), pinned by L-T12, and mutant L18 restores the defect. Two smaller review points landed with it: a terminalizing tick no longer backs off to the idle delay (L19), and each condemned instance is now reported individually (L20) -- the DURABLE half of that record is deliberately NOT built and is filed as E9-F009."
    },
    "E9-4-service-create-and-desired-state": {
      "epic": "E9",
      "status": "wired",
      "symbol": "createServiceWithinTenant",
      "reason": "E9's exit gate opens with `desired state` and `generation`, and until this clause nothing on a real deployment could produce either. THE STATE AT BASE, in the words of three shipped documents that all said it and were all right: SVC-002-result.md section 7 (\"Nothing creates a service. `repos.services.insert` keeps its zero production callers and SVC-002 adds no routes. The reconciler reconciles rows only a test can create ... Nothing writes a generation.\"), SVC-003a-result.md section 2 (\"NOTHING CREATES A SERVICE ... so on a real deployment there is no `services` row, so no instance, so nothing for this projection to move\"), and E9-1's own reason above, which lists both as things it does NOT claim. SVC-002's reconciler and SVC-003a's projection were therefore SHIPPED AND UNREACHABLE. PRODUCTION CALLERS, measured with this guard's own `countProductionCallers`, base `053f90fc8` vs head: `createServiceWithinTenant` 0 -> 1 (`createService`, which the POST route calls); `createService`/`setServiceDesiredState`/`setServiceDesiredStateWithinTenant`/`readService`/`listServices`/`normalizeServiceDefinition` 0 -> 1 each (the four routes in `jobControlRoutes`); `insertServiceGeneration`, `updateServiceDesiredState`, `terminalizeServiceInstanceForCancelledAttempt`, `findServiceForCompany` and `listServicesForCompany` 0 -> 3 each and `findLiveServiceInstance` 0 -> 4 -- the MEASURED figures are stated rather than the intuitive ones, because for a tenant-repository method the guard counts the interface declaration and the implementation alongside the call site; `canTransitionServiceDesiredState` 0 -> 1, which is the second dead frozen fence this epic has armed (its table had ZERO production consumers, exactly as `canTransitionServiceInstanceStatus` did before SVC-003a); `lockServiceForReconcile` 3 -> 4 (the desired-state control REUSES SVC-002's per-service advisory lock rather than minting a second key -- two locks are one lock fewer); `decideServiceProjection` 1 -> 2. `repos.services.insert`, which is a property access the counter cannot see, has ZERO production call sites at base and ONE at head (`createServiceWithinTenant`), measured by grep over non-test sources. `createStartupReconciler`, the cautionary zero-caller neighbour, is untouched at 0; `createServiceReconciler` is 2 at base and 2 at head. THE COMPOSITION ROOT IS `jobControlRoutes` in `server/src/routes/job-control.ts`, mounted by `createApp` (`server/src/app.ts`) inside the `opts.distributedExecutionEnabled` block over the non-owner `aoa_app` pool -- the same router that carries JOB-008's operator surface, using the SAME `execution_target:manage` org-admin gate (`assertOrgAdmin`) run FIRST on every route -- literally first, since these four routes carry NO `validate(...)` middleware and parse their body schema INSIDE the handler after the gate. That is the fix for a P2 external review raised: `validate` is middleware, so it ran BEFORE the handler and therefore before the gate, answering an unauthorized caller with a 400 about their body while the same caller with a well-shaped body got the 403. A thrown ZodError reaches the same error handler `validate` relied on, so only the ORDER moves. WHAT THE CLAUSE CLAIMS, NARROWLY: an org admin can create a service and the immutable generation-1 definition that defines it, in ONE transaction; the reconciler then converges that service into exactly one instance and one `service` job built from the STORED definition; and the operator can move desired state through the FROZEN `SERVICE_DESIRED_TRANSITIONS` table, where a stop ALSO asks the live instance's job to stop through JOB-006's `requestCancellation` (graceful) and terminalizes the instance that cancellation orphaned, so a later resume actually starts a new one -- ALL OF IT IN ONE TRANSACTION under the service's own row lock, which is the fix for a P1 external review of PR #412 raised against the first revision (it committed the desired-state write, released the lock, and cancelled in a second transaction, so a concurrent `stopped -> running` in that gap meant the older stop drained a job the operator had already resumed). The lock order is stated rather than assumed because `requestCancellation`'s own header warns that getting it wrong deadlocks: service advisory + `services` row FIRST, then its untouched `lease -> attempt -> job` hierarchy, then `service_instances`; SVC-002's reconciler takes the service locks first exactly as this does and the JOB-005 ingest takes no service lock at all, so no cycle is constructible. WHAT IT DOES NOT CLAIM, and each is a difference from E9's gate: (1) NO GENERATION ROLLOUT. `update` does not mint generation N+1, because SVC-005's \"no two generations may perform external effects simultaneously\" fence does not exist and a rollout without it is the overlap E9's acceptance forbids. Generation 1 is the only generation this clause writes. (2) NO TTL AND NO CHECKPOINT ARE ACCEPTED. Both columns exist; nothing enforces a TTL (SVC-005) and nothing restores a checkpoint (SVC-004), so storing either would be a bound no code keeps. (3) `deleted` IS NOT REACHABLE from the control -- it is terminal in the frozen table and `service_generations`' RESTRICT FK makes a service with any generation undeletable, so the tombstone's semantics stay SVC-005's. (4) THE VIEW IS PARTIAL: desired state, generation, that generation's definition and the live instance (whose `status` IS SVC-003a's projected health). Checkpoint, budget and restart history are NOT in it. (5) NO SERVICE JOB IS LEASED ANYWHERE IN THIS SUITE. `E9-F002` keeps `workload.service` unofferable on a fleet like the test fixture's, so the DAEMON half of \"created, supervised, projected\" is NOT exercised and E9's exit gate is NOT met by this ticket. (6) CREATE IS NOT IDEMPOTENT: `services` has no natural key and no idempotency column, so two POSTs create two services -- bounded rather than prevented, because the second is visible in the list read and stoppable by the control shipped here. (7) NO `activity_log` ROW IS WRITTEN for a control action, and it is currently UNWRITABLE from here rather than merely unwritten: the shipped distributed-audit path `jobAuditBridge.recordAcceptedActivity` REQUIRES `fence: ActiveFenceRequest` because JOB-013 makes the `activity_audit` receipt the sole replay guard for a non-idempotent `insertActivityLog`, and a service create has no attempt while a desired-state change has no fence, so AGENTS.md's \"Activity logging for all mutating actions\" invariant is NOT met by these routes (external review of PR #412 raised it; the half that WAS fixable -- a required `reason` that the route discarded on every transition to `running` -- is now on the structured audit line); the audit is structured logger lines beside `drain`'s and `revoke`'s, and `jobAuditBridge` still has zero production callers (already on the register under DE-01). PROVEN, NOT DECLARED: `server/src/__tests__/service-management.integration.test.ts` (13 cases over real embedded PostgreSQL, including the create->reconcile->stop->resume->reconcile chain, a rollback probe proving the whole stop is ONE transaction, and a case driving `createServiceReconciler(...).tick()` -- the SAME sweeper the composition root drives, though with its admitted-organization enumerator stubbed and its backoff timer unexercised, so what that case proves is that a created service ENTERS `listReconcilableServices`'s window and converges through the sweep, not that the process wiring around the sweeper runs) and `server/src/__tests__/service-management.test.ts` (18 pure cases, including a walk of the WHOLE 4x3 desired-state table against the frozen predicate -- E9-F004's lesson applied to the other lifecycle). NINETEEN mutants were applied to the shipped source by a harness that refuses to apply over an existing backup, tries BOTH line-ending forms of its anchor and THROWS when neither matches (this tree is mixed: the repository file is CRLF and the new module is LF), verifies the bytes changed before believing the apply, and rebuilds `packages/db/dist` and confirms the mutation is PRESENT in it before any repository mutant's result is believed. Every one killed at least one of the 31 cases, and the NAMED POSITIVE CONTROL (`T9 POSITIVE CONTROL -- a service with no generation still stalls at no_generation`) stayed GREEN under all nineteen -- which is what distinguishes a working writer from a reconciler someone weakened until it started services without a definition. ★★★ E9-F006 WAS FOUND WHILE BUILDING THIS AND IS RESOLVED IN THE SAME COMMIT: `requestCancellation` FINALIZES rather than drains when there is no fenced worker, emitting no event, so SVC-003a's attempt-terminal backstop could not fire and the instance stayed non-terminal inside `service_instances_live_service_uq` forever -- making stop-then-resume converge nothing on every tick. That is the normal path today, not an edge case, because E9-F002 keeps service jobs unleased."
    },
    "E0-de14-startup-safety-audit": {
      "epic": "E0",
      "status": "wired",
      "symbol": "loadConfigWithStartupSafetyAudit",
      "reason": "DE-14's `audit` clause, \"the startup safety-assertion outcome is logged\" — the THIRD crossing whose audit clause is whole, out of the seventeen in the denial-audit class (E0-F010's cohort plus E0-F013's). ★ THE CLAUSE IS SINGLE-CONJUNCT, WHICH IS THE WHOLE REASON IT IS THE CHEAPEST ROW IN THE CLASS AND WHY THIS ENROLMENT PROVES LESS THAN THE TWO ABOVE IT. DE-19 and DE-06 each needed an attributable durable row; this one asks for a LOG and gets one, because `assertHostedExecutionStartupSafe` fires during config load BEFORE any database pool exists, so a durable row is structurally impossible at that point. Closing it moves no Group B/C/D blocker: not the separate-transaction lifecycle, not the tenantless sink, not the frozen worker-protocol hop. ★ BUT `the outcome` IS TWO-DIRECTIONAL and both directions are delivered, which is what the previous measurement of this row recorded as absent — \"Nothing, in either direction\". PRODUCTION CALLERS: ONE, as this guard's own countProductionCallers measures it — the entrypoint's single startup config load in server/src/index.ts. It is deliberately NOT inside `loadConfig`: `loadConfig` is not memoised and re-runs the assertion on every route and service call, so instrumenting it would have needed a once-only latch; instrumenting the entrypoint's one call leaves the per-request re-runs silent by construction. WHAT THE CLAUSE CLAIMS, NARROWLY: a PASS logs `distributed_execution.startup_safety.passed` at info carrying the deployment mode and the resolved distributed flag READ OFF THE ASSERTION'S OWN RETURN VALUE (`Config.hostedExecutionStartupSafety`), never re-derived from the same environment; a REFUSAL logs `distributed_execution.startup_safety.refused` at error carrying that branch's own machine reason (four branches, four codes) and the offending variable's NAME — never its value, because these are boolean flags and database URLs — and is then RETHROWN UNCHANGED, so the module-eval crash and the refusal to serve are byte-for-byte what they were. WHAT IT DOES NOT CLAIM: it is a log line, not an audit record; nothing here is durable, queryable or attributable to a tenant, and DE-14's other clauses are untouched, so DE-14 stays `partial` in the threat register. ★ CORRECTED LATER THE SAME DAY: this read \"exactly as DE-19 and DE-06 do\" and the analogy is FALSE. Those two each carry a SECOND clause MEASURED ABSENT and owned by another finding (E0-F016's, and E0-F012's `authentication`); DE-14's row names exactly ONE absent clause, `audit`, and it is the one this enrolment delivers. What holds the row at `partial` is the register's finding-citation contract -- check-distributed-execution-foundation.mjs clause 4 refuses `delivered` for any crossing named as a literal token in a live finding-ownership entry, and E0-F010's entry names DE-14 throughout. NO STATUS CHANGE IS TAKEN: it is entangled with E0-F010's resolution and with releaseTest REL-005 (zero files, deferred). ★ ONE INHERITED MEASUREMENT WAS REFUTED BY CONSTRUCTION, and it is named because the shape recurs: the 2026-09-10 note in findings.md said \"the failure outcome cannot be moved to the entrypoint\" because index.ts calls loadConfig() at module top level with no try/catch. A top-level call is not an uncatchable call — the try/catch lives inside the function the top-level statement now calls. Its other three measurements (no memoisation, the call-site census, middleware/logger.ts's module-level fs.mkdirSync + pino.transport making it un-importable from config.ts) STAND and each shaped the design: both config modules stay logger-free and the sink is injected. ★ ONE CODEX P2 ON PR #416, REAL AND FIXED IN THE SAME BRANCH: as first shipped, `loadConfig` read AOA_DISTRIBUTED_EXECUTION_ENABLED on its own line BEFORE the assertion, and that reader throws a PLAIN Error on a non-boolean value — so `=banana` refused startup one line too early, the recorder saw an unrelated load failure, and the `env_flag_unparseable` reason code was PRODUCTION-UNREACHABLE for that flag. A classified branch nothing could emit, shipped inside the unit whose subject is exactly that. Fixed structurally: the assertion already reads the flag and now RETURNS it, `loadConfig` derives the value from that outcome, and there is exactly one read. Nothing caught it because every reason-code arm asserted against the module that OWNS the codes, where all four are trivially reachable; only driving the REAL loadConfig sees the ordering. PROVEN, NOT DECLARED: server/src/__tests__/de-14-startup-safety-audit.test.ts, TEN arms — RE-COUNTED AT HEAD after the review fix, because this row is written in the same commit that changed the thing it counts. Observed RED against the unchanged tree, and the RED is a COLLECTION failure rather than a per-arm failure (neither the recorder module nor the assertion's new exports existed), which is a weaker first observation than DE-19's and DE-06's and is stated as such — the per-arm discrimination comes from the mutants instead. FIVE mutants, applied one at a time to the SHIPPED source and reverted with an md5-verified restore, each leaving named survivors. FOUR were run against the NINE-arm file (what it held at that moment, recorded at that count rather than restated at ten), and the FIFTH — restore the separate pre-assertion flag read, i.e. the Codex defect itself — at TEN arms, reddening exactly the REACHABILITY arm with 9 green: delete the pass log (exactly the 2 pass arms red, 7 green including the PRECISION CONTROL and the refusal arm); stamp a constant reason on one branch (exactly the distinctness arm red, 8 green — that arm asserts four branches produce four codes, which a single hard-coded reason would otherwise satisfy arm-by-arm); widen the refusal guard from `instanceof HostedExecutionStartupUnsafeError` to `instanceof Error` (exactly the PRECISION CONTROL red, 8 green — this is the arm that stops an unrelated loadConfig crash, a malformed port say, being filed as a safety refusal, and it exists because a scanner that fires on benign input is this programme's own measured failure); and restore the bare `loadConfig()` at the entrypoint (exactly the ARMING PATH arm red, 8 green — the arm that keeps this symbol from becoming another jobAuditBridge). The pre-existing server/src/__tests__/distributed-execution-policy.test.ts (17 arms over the same function's messages and throw shape) is the NAMED REGRESSION CONTROL and stayed green under all four."
    },
    "E0-de19-denial-audit": {
      "epic": "E0",
      "status": "wired",
      "symbol": "recordSecurityDenial",
      "reason": "DE-19's `audit` clause, \"context retrieval and denials are recorded in the retrieval audit\" — the first crossing closed out of the seventeen in the denial-audit class (E0-F010's eight + E0-F013's nine). ENROLLED IN THE SAME COMMIT AS THE SYMBOL, deliberately: `jobAuditBridge` is the cautionary tale — it was BUILT as this class's intended vehicle, has ZERO production callers to this day, and is not enrolled in this register at all, so nothing has ever reported on it (scripts/finding-ownership.json's separate MEDIUM finding). A writer nothing calls is the defect, not the fix. PRODUCTION CALLERS: ONE, and the number is stated as the guard measures it, not as it feels. The single textual call site is inside `handleMemoryGet`'s `denyMemoryGet` closure (server/src/mcp/tools/read-tools.ts), reached from THREE deny branches of the MCP `memory.get` tool (`not_visible`, `not_approved`, `actor_filter_denied`). An earlier draft of this sentence said \"2 callers\" by counting branches as call sites; `--counts` reports 1 and 1 is right. PROVEN, NOT DECLARED: server/src/__tests__/de-19-memory-denial-audit.integration.test.ts provokes the REAL cross-department refusal through the REAL handler against a REAL Postgres and asserts the durable row's WHO/TENANT/RESOURCE/WHY separately — it was observed RED before the change (3 failed / 4 passed) and four mutants were killed: deleting the write, stamping a constant reason, writing the row on the success path, and dropping the actor to \"system\". SCOPE, STATED NARROWLY: this wires ONE crossing. DE-19 itself stays `partial` because its authentication/revocation/integrity clauses are separately absent (E0-F016), and the other sixteen denial-audit crossings are untouched — four of them (DE-04/DE-12/DE-29 and DE-13's capacity half) additionally need a SEPARATE-TRANSACTION lifecycle this writer does not yet have, because their deny is a throw inside the tenant transaction that rolls back."
    },
    "E0-de06-object-access-audit": {
      "epic": "E0",
      "status": "wired",
      "symbol": "recordObjectAccessGrant",
      "reason": "DE-06's `audit` clause, \"object put/get AND rejected-key attempts are audited\" — the SECOND crossing whose audit clause is whole, out of the seventeen in the denial-audit class (E0-F010's cohort plus E0-F013's). ★ WHY THIS ENROLMENT IS LEGITIMATE WHERE THE 2026-09-09 ONE WAS NOT, stated first because the $comment above records that removal. That entry claimed the clause on the REJECTED-KEY conjunct alone while a successful download grant wrote nothing. BOTH conjuncts now hold: rejected-key is unchanged and was delivered by the earlier units (every returning refusal on both artifact services through the REQUIRED `denial` parameter, and all SIX `resolveWorkerFenceContext` throw sites), and object put/get is delivered by this symbol. The unit of claim is the clause; the clause is whole. PRODUCTION CALLERS: ONE, as this guard's own countProductionCallers measures it — the drain in `createArtifactTransferGrantService`'s `grant` (server/src/services/artifact-transfer-grant.ts), which runs on the POOL handle after the tenant transaction has closed and before the caller sees the grant. That call site sits on the shipped worker-control path, which is registered ONLY inside `if (opts.distributedExecutionEnabled)` in server/src/app.ts — a block that DYNAMICALLY imports ./routes/worker-control.js, so a flag-off startup never loads the module at all. AOA_DISTRIBUTED_EXECUTION_ENABLED is default FALSE, so this is a caller on a shipped path that no default deployment executes; that is a reach bound, not a dormancy claim, and it is exactly why the hot-path cost of a new write on a SUCCESS path was acceptable in this unit when three earlier units deferred it. WHAT THE CLAUSE CLAIMS, NARROWLY: the control plane AUTHORIZED an object operation, and the row says WHO (actor_id = the authenticated workerId, actor_type = system, because a worker has a row in neither `agents` nor `auth`), TENANT (company_id = the LOCKED LEASE's company, resolved under the worker's own organization GUC and never from the request; organization_id token-attested), RESOURCE (entity_type job_artifact + the artifact identity, plus details.objectKey = the key that was actually SIGNED), OPERATION (the ACTION itself: security.object_access.artifact_upload_grant vs …artifact_download_grant, so \"how many download URLs were issued in this tenant\" is an action predicate and not a jsonb dig), and the exposure window (details.grantExpiresAt). WHAT IT DOES NOT CLAIM, and this is the difference between the row and a proof of transfer: it records ISSUANCE, NOT REDEMPTION. The bytes move directly between worker and object storage — the entire purpose of the presigned design — so the control plane has exactly ONE observation point and it is issuance. The record therefore OVER-reports: an issued-but-never-redeemed GET still writes a row. Over-reporting is the correct direction for a disclosure audit, but a row means a capability was handed out, never that bytes moved, and no wiring creates a second observation point (only the object store's own access log could, and this tree's StorageProvider port has no such operation). SCOPE: this wires ONE clause. DE-06 itself stays `partial` in the threat register because its `authentication` clause is separately absent (E0-F012), exactly as DE-19 stays `partial` for E0-F016's. PROVEN, NOT DECLARED: server/src/__tests__/de-06-object-access-audit.integration.test.ts, TWELVE arms — RE-COUNTED AT HEAD after this unit's last edit, because this row was written in the same commit that changed the thing it counts — provoked through the real leasing/commit/grant services against real embedded PostgreSQL under the real aoa_app non-owner role. Observed 6-RED / 4-PASS against the unchanged tree AT TEN ARMS, which is what the file held at that moment; the eleventh (DE-11's kind arm) and the twelfth (the Codex redaction arm) were each observed RED against the code they were added for rather than against base, which is the narrower claim and is stated as such. 12/12 green after. The four greens in the RED run are the NAMED positive controls: anti-vacuity, the refused-grant mutation guard, cross-tenant non-disclosure, and the namespace reservation. SIX mutants killed, each with named controls green — THE MATRIX WAS RE-RUN IN FULL AT HEAD so that EVERY ROW IS ONE SNAPSHOT (the same 12-arm file, the same 12/12 baseline, one mutant at a time, reverted with an md5-verified restore): delete the drain (7 red / 5 green, the 5 survivors being the four named positive controls PLUS the redaction arm, which calls the recorder directly and is drain-independent by construction); delete ONLY the download capture (exactly the 5 GET-dependent arms red, the PUT arm GREEN — the conjunction is asserted per conjunct); collapse the action mapping to always report upload (exactly the 2 conjunct-distinguishing arms red, 10 green); HOIST the upload capture above the foreign-prefix refusal branch (exactly ONE arm red — the mutation guard — with 11 controls green, which is the property the DELIBERATE ABSENCE of a second `response.outcome` check at the drain exists to preserve, since a redundant guard would have let that mutation pass); stop reading kind/sensitivity off the committed row (exactly the 2 kind-asserting arms red, 10 green, with the PUT arm that asserts kind is NULL still green, proving the upload null is a real answer rather than an unwired field); and restore the raw objectKey to the failure log (exactly ONE arm red — the redaction arm — with 11 controls green). An earlier draft of this matrix mixed snapshots taken at ten, eleven and twelve arms without per-row labels, so three rows carried counts that were true when taken and stale at HEAD; it was re-run rather than re-labelled. ★ POST-REVIEW, ONE CODEX P2 ON PR #411, REAL AND FIXED IN THE SAME BRANCH: the recorder's catch branch wrote `objectKey` VERBATIM to the server log while the persisted row's copy of that value goes through sanitizeRecord. The key's SUFFIX is caller-controlled (the frozen grant schema bounds it only by length and by this org's attempt prefix), and sanitizeRecord genuinely redacts secret-shaped suffixes — measured: `whsec_<24>`, `sk-ant-…` and a postgres URL all come back ***REDACTED*** while an ordinary out.bin comes back intact — so a transient FK failure would have leaked to logs the one value the durable row refuses to keep. Fixed STRUCTURALLY rather than with a second sanitizeRecord call: the catch now logs the already-sanitized `details`, so exactly ONE sanitized objectKey exists in the module and the two sinks cannot drift. This module was the outlier; the sibling security-denial-audit.ts logs only scalars and never its details."
    },
    "E0-de27-admission-audit": {
      "epic": "E0",
      "status": "wired",
      "symbol": "recordWorkerAdmissionDenial",
      "reason": "DE-27's `audit` clause, cross-replica-admission conjunct — the FIRST crossing in the deny-path audit-wiring wave after E0-F013 Decision 1, and the fourth whose audit clause is now whole (DE-19, DE-06, DE-14, DE-27). ★ THE CLAUSE IS THE UNIT OF CLAIM AND THE WHOLE REMAINING CLAUSE HOLDS. DE-27's clause was \"cross-replica admission AND partition events are audited\", AMENDED by E0-F013 Decision 1.2(c) (founder-ruled 2026-09-11): the \"cross-replica admission\" conjunct is read WEAKLY — each admission REFUSAL is durably recorded — and the \"partition\" conjunct is DROPPED as vacuous (there is no partition detector, so no partition event exists to record). This enrolment delivers the whole remaining clause: BOTH admission refusals are recorded, and there are exactly two. Admission is already DB-serialized across replicas (pg_advisory_xact_lock over the organization for capacity at org-concurrency.ts:222-224, and the shared worker_admission_rate_limits counter both replicas increment), so the clause does NOT require — and Decision 1 no longer implies — a record naming WHICH replica decided; a whole-tree sweep for replicaId|replica_id|AOA_CONTROL_PLANE_REPLICA|controlPlaneId still returns zero hits, and the clause is satisfied without one. PRODUCTION CALLERS: this guard's countProductionCallers measures `recordWorkerAdmissionDenial` — the shared recorder BOTH deny sites funnel through. It is called directly at the over_cap deny site (server/src/services/worker-admission-rate-limit.ts:admit, on the pool handle after the shared-counter transaction has committed) and inside `drainAdmissionDenial` (server/src/services/worker-admission-denial-audit.ts), which is itself drained at THREE pool-handle sites for the capacity refusal — jobSubmissionService.submit (job-submission.ts), admitAndSubmit (job-admission-bridge.ts) and reconcileService (service-reconciler.ts), each after its `runInTenant` has rolled back on the 429. ★ WHY TWO MECHANISMS. over_cap's shared-counter transaction has already COMMITTED and returned the count when the refusal is taken, so the row is written directly on the pool handle. capacity's `admitted:false` is a plain return (which Drizzle commits), but its sole authority `admitAttemptCapacity` runs on the CALLER'S tenant transaction and every caller THROWS a 429 on the refusal, rolling that transaction back — so the row cannot live inside it. The refusal captures an INTENT into a caller-owned sink and the caller drains it on the pool handle after the transaction closes: the intent-record-then-drain pattern `worker-denial-audit.ts` established, for the reason `security-denial-audit.ts` states (\"db MUST be a pool-level handle, never the transaction that is about to reject\"). WHAT THE ROWS CLAIM, NARROWLY: one action, security.denied.worker_admission, reason `over_cap` | `capacity` distinguishing the branch. WHO — actor_type system, actor_id = organizationId, because both authorities are ORG-SCOPED and hold no finer principal at the control point (the poll limiter runs before the frozen poll authority resolves a worker; the capacity claim runs on behalf of an attempt). TENANT — organization_id always (token-attested off verifyWorkerOperationProof for over_cap; the just-inserted attempt's org for capacity), company_id null for over_cap (the limiter is org-scoped) and the submitting company for capacity. RESOURCE — worker_poll_admission/organizationId for over_cap, job_attempt/attemptId for capacity. The `security.denied.` namespace is excluded from every tenant-facing feed by notDenialNamespace() (Decision 3), so an admission refusal is not disclosed to the tenant that provoked it. WHAT IS OUT OF SCOPE, STATED NOT BURIED: the over_cap limiter's `unavailable` fail-closed return (an infra fault it already logs, not the cross-replica cap refusal), the `budget` deny beside capacity (a different crossing), and the SECOND `reason:\"capacity\"` return below the concurrency claim UPDATE (a released/terminal/gone attempt, a distinct branch the reason vocabulary keeps apart) are NOT recorded here. SCOPE: this wires ONE clause. DE-27 STAYS `partial` in the threat register and LEAVES E0-F013's audit-gap cohort, exactly as DE-06 and DE-14 did — its revocation clause is not-delivered-as-named (holds by construction, no experiment run), and the register's finding-citation contract (check-distributed-execution-foundation.mjs clause 4) refuses `delivered` for any crossing a live finding names, and E0-F013's ownership entry names DE-27. No status change is taken and none is implied. PROVEN, NOT DECLARED: server/src/__tests__/de-27-admission-audit.integration.test.ts, SEVEN arms, provoked through the REAL limiter and the REAL jobSubmissionService.submit path against real embedded PostgreSQL under the real aoa_app non-owner role. Observed 4-RED / 3-PASS against the unchanged-wiring tree (the 3 greens being setup + the two deny-PROVOKE arms, which fire the refusal but do not require a row; the 4 reds being both CLAUSE arms, the attribution arm and the anti-vacuity arm), then 7/7 green. FOUR mutants killed, one at a time, each reverted with a git-verified restore, with the two pre-existing admission suites (worker-admission-rate-limit.integration.test.ts + job-submit-capacity-admission.integration.test.ts, 9 arms) as the NAMED REGRESSION CONTROL green throughout: delete the over_cap write (3 arms red — over_cap CLAUSE, attribution, anti-vacuity — capacity + provoke green); delete the capacity intent capture (2 arms red — capacity CLAUSE, anti-vacuity); stamp a constant reason in the recorder (2 arms red — capacity CLAUSE reason≠capacity, anti-vacuity set collapses to size 1); and write on the ALLOW poll path too (2 arms red — the over_cap CLAUSE expects exactly one row and got three, and the attribution arm). Enrolled in this commit."
    },
    "E3-5-product-approval": {
      "epic": "E3",
      "status": "unwired",
      "symbol": "jobApprovalBridge",
      "reason": "JOB-010..014 parity bridge. Zero production callers; the legacy approval path still owns this. Wire when the sinks cut over (Sprint 6, MIG-005/006/007 ACTIVE)."
    },
    "E3-15-budget": {
      "epic": "E3",
      "status": "unwired",
      "symbol": "jobBudgetCostBridge",
      "reason": "Parity bridge with ZERO production callers (jobBudgetCostBridge, server/src/services/job-budget-cost-bridge.ts:150; its entrypoint priceAcceptedUsage :231 is referenced only by server/src/__tests__/job-budget-cost-parity.integration.test.ts and server/src/__tests__/job-distributed-drain.integration.test.ts). MEASURED CONSEQUENCE: a handed-off distributed run is UNBILLED. WHERE AN OPERATOR ACTUALLY READS THIS -- NOT HERE. Until 2026-09-07 this sentence read 'READ IT BEFORE ARMING AOA_DISTRIBUTED_EXECUTION_ROLLOUT', which is an instruction to a reader who is not present: this file is a CI register, and the document an operator follows to arm the dial is docs/replatform/epics/E7-coding-e2b/tickets/CLI-006-staging-canary-runbook.md ('Audience: the OPERATOR'), section 2.1 'The rollout dial (canary mode)' and section 3 step 3, which carried no budget warning at all. W16A-FIX put the warning THERE and left the measurement HERE; this entry now points at the runbook instead of standing in for it. A warning shelved where the actor never looks is the shape this programme calls a check that nothing runs. Until 2026-09-07 this entry said 'budget/cost still flow through the legacy cost-event path'. That is FALSE, and it was the standing answer to the money question printed on every green policy run; E3-F037 refuted it and REMAINS OPEN, because what is corrected here is the sentence and not the gap. MEASURED at 8075cd7a1: the repository has exactly four insert(costEvents) sites -- server/src/services/company-portability.ts:3203 (bundle import), server/src/services/costs.ts:69 (costService.createEvent), server/src/services/heartbeat.ts:2653 (inside updateRuntimeState, declared :2622) and server/src/services/one-shot-cli-budget.ts:182 (recordOneShotCliCost, extraction) -- and none of them serves a handed-off run. heartbeat's writer has exactly two call sites, :5805 (success) and :6013 (inside the outer catch opened at :5960), and BOTH sit downstream of `return; // CLI-006-SUPPRESSION-RETURN` at heartbeat.ts:5451, inside `if (shouldSuppressLegacyExecution(canaryExecutionOwner))`. A return is not a throw, so a suppressed run reaches neither: no cost_events row, no increment to agents.spentMonthlyCents or companies.spentMonthlyCents, and checkBudgetAlerts (declared heartbeat.ts:2681, called at :2676 from inside that same function) never runs. Every control that could cap or pause reads that one ledger, so all three are blind to the spend: budget_policies through getObservedCents (server/src/services/budgets.ts:21 -- used by the warning/hard-stop evaluator at :238 and by the admission gate getInvocationBlock at :345, :368, :392, :414), the per-agent budgetMonthlyCents pause (checkBudgetAlerts sets agents.status='paused'), and the crew/company hard-stop (server/src/services/crew-budget.ts:121 preflightCrewDispatch, summing cost_events by company over the calendar month at :210-217). WHAT WOULD HAVE TO CHANGE for 'wired': the accepted-usage path must call this bridge (Sprint 6 sink cutover). Until it does, the dial must not be armed for an Organization whose spend must be capped -- stated in the operator's own words at CLI-006-staging-canary-runbook.md section 2.1."
    },
    "E3-17-output": {
      "epic": "E3",
      "status": "unwired",
      "symbol": "jobOutputBridge",
      "reason": "JOB-014 output projection has zero callers; task_outputs is still written by the legacy path. Wire at sink cutover (Sprint 6)."
    },
    "E3-18-revocation": {
      "epic": "E3",
      "status": "wired",
      "symbol": "createExecutionTargetRevocationFanout",
      "reason": "PROMOTED ON EVIDENCE — DE-18. The fanout is now composed at the server composition root (server/src/index.ts, inside the config.distributedExecutionEnabled && distributedExecutionDatabases block, via the flag-gated dynamic import so it never enters the flag-off bootstrap graph) and its tick() runs inside the ALREADY-RUNNING MIG-002 convergenceTick body, under the same try/catch and the same unref'd timer — no second scheduler. It is fed the operator pool (drives the durable execution_target_revocations records), the shared jobReconciliationForCancel (the same cancel path the reaper uses), and a page-draining wrapper over the paginated listAdmittedOrganizationIds. The producer is live: POST .../workers/:workerId/revoke -> revokeExecutionTarget writes the status:'pending' rows this tick drains. Convergence BEHAVIOUR is proven end-to-end against embedded Postgres in server/src/__tests__/worker-revocation.integration.test.ts across BOTH convergence passes: (1) the LIVE-LEASE pass — every matching old-generation offered/active/revoked lease is flipped revoked, its capacity released once, its job cancelled target_revoked; and (2) the PINNED-ATTEMPT pass (DE-18 P1 fix) — a NONTERMINAL, LEASE-LESS job_attempts row pinned to the revoked target (placementTargetGeneration <= revokedGeneration) with no live offered/active/revoked lease — precisely the successor the MIG-002 reaper mints when allocateRetry copies the revoked target's placement verbatim into a new queued attempt (capacityClaimState 'held') — is terminalized to cancelled, has its held Organization slot released once, and its job handed to the SAME target_revoked cancellation, BEFORE the durable record is marked completed. Without pass (2) such a strand is invisible to the lease scan, can never lease, and pins an org slot forever (countHeldAttemptsForOrg has no status filter). Both converge under FOR UPDATE SKIP LOCKED for crash-resume idempotence, record -> completed. The 'converges a LEASE-LESS held retry successor pinned to the revoked target (MIG-002 strand)' case is red-when-removed (deleting the pinned-attempt pass drops cancellations to 0 and strands the attempt held). The wiring itself is guarded red-when-removed by server/src/__tests__/de-18-revocation-fanout-wiring.test.ts (the 'ticks the fanout inside the convergence tick' case reddens when the await revocationFanout.tick() call is deleted). The full distributed teardown over a live E2B fence rides the same E7-1 substrate that remains unbuilt."
    },
    "E4-1-leases-through-protocol": {
      "epic": "E4",
      "status": "wired",
      "symbol": "createPollLoop",
      "reason": "PROMOTED ON EVIDENCE — Sprint 5 Step 1 (CLI-006/D2). packages/worker-daemon/src/__tests__/composed-journey.component.test.ts drives composeDispatchRuntime with its REAL factories through ONE lease: createPollLoop polls, self-checks the frozen offer, and ACKs over the real lease-ack POST (fake.ackCountFor(lease)===1 for the offered lease), then hands off to a supervised run. This is 'a composed loop that took a lease in this sprint's suite' — never caller count. The traversal is the REAL protocol (real pollOnce, frozen leaseOfferV1Schema, real ackLease, a device proof the counterparty verifies INDEPENDENTLY); the counterparty is the protocol-faithful in-process control-plane double (support/fake-control-plane.ts), not real E2B. The SERVER-side leasing/placement that mints the offer is proven separately on the D1 lane (CLI-006, 40/40) + server/src/__tests__/job-leasing.integration.test.ts. Mutation-proven non-vacuous (CLI-006-D2-step1-result.md §3): M0 (positive control) reddens the whole test; M1 (skip the ACK POST) reddens ONLY this E4-1 lease case while the E4-2 supervise case stays green; M2 (remove the handoff) is the mirror — E4-1 green, E4-2 red. STRENGTHENED by Leg B Part 1 (server/src/__tests__/composed-loop-real-server.integration.test.ts): the SAME createPollLoop leases a REAL server-minted attempt over the REAL embedded-PG worker-control routes (real device proof + session; the server's activateLeaseAck flips the attempt to 'leased' + the lease to 'active' + writes a lease_ack receipt — proven ACK-specific, with a no-candidate negative control + a dist-rebuilt M1). So the counterparty is no longer only the in-process double; the server's real lease reaches the composed worker. The full DISTRIBUTED journey on REAL E2B remains E7-1 (unwired), and the credential resolve over a live fence (Leg B Part 2) is owed."
    },
    "E4-2-supervises-sandboxes": {
      "epic": "E4",
      "status": "wired",
      "symbol": "createSupervisor",
      "reason": "PROMOTED ON EVIDENCE — Sprint 5 Step 1 (CLI-006/D2). In composed-journey.component.test.ts a REAL ACK (poll-loop trackHandoff after ackLease) reaches createSupervisor, which runs the sandbox lifecycle create -> execute (tenant command INSIDE the sandbox) -> destroy (provider ops === [create,execute,destroy]) and drains a digest-valid terminal event. It also redeems the CLI-007-minted provider_key handle into spec.env (fail-closed when denied). This is an actually-supervised sandbox reached after an ACK — the exact bar this clause named. The sandbox is the per-op fake provider (support/fake-provider.ts, implements SandboxProvider); a REAL E2B sandbox is E7-1 (unwired). Mutation-proven (CLI-006-D2-step1-result.md §3): M2 (remove trackHandoff) reddens ONLY this E4-2 supervise case while the E4-1 lease case stays green; M3 (drop synthesiseRunSecrets) reddens the credential + fail-closed cases; M5 (no-op the drain) isolates the stream."
    },
    "E4-3-survives-restart": {
      "epic": "E4",
      "status": "unwired",
      "symbol": "createStartupReconciler",
      "reason": "DEFERRED (WRK-008 slice 2b §4.2), for ONE blocker: StartupReconcilerDeps.leaseCandidates (supervisor/startup-reconcile.ts:256-257) has no durable local source — the outbox persists EVENTS, not offers, so the lease-authority probe would run over [] every boot (a guard that passes because it evaluated nothing). ownershipSelector.organizationId is NOT a blocker — it is on the self-model this slice reads and the frozen schema guarantees it non-null for org/owner-scoped targets. E4-3 waits on a durable lease-candidate source (finding E4-F009)."
    },
    "E4-4-event-outbox-replay": {
      "epic": "E4",
      "status": "wired",
      "symbol": "createEventOutboxDrain",
      "reason": "Genuinely wired by WRK-008 slice 2b: composeDispatchRuntime opens the outbox store, recovers it (uploading->pending) at composition, and runtime.start() runs drain.start() at boot — independently of leasing or ACK. 'Reachable' here means reachable FROM A BOOT ROOT, not 'runs by default': dispatch is still off by construction (default-OFF flag). True without qualification."
    },
    "E5-3-patch-quarantine": {
      "epic": "E5",
      "status": "unwired",
      "symbol": "createPatchApplyService",
      "reason": "Zero callers on the DECLARED symbol createPatchApplyService, a server-side DAT-003 service. CORRECTED 2026-09-03: the previous reason reasoned about the SIBLING runOrphanQuarantine (whose producer startup-reconcile is itself uncalled) and concluded the clause is 'blocked on E4 wiring'. That is a substitution, not an addition — the checker reads the caller count of createPatchApplyService, which no E4 daemon ticket touches, so wiring the startup reconciler (WRK-013) flips E4-3-survives-restart and does NOT flip this clause. The GO-BOOK's WRK-013 row inherited the same error and is corrected in the same change."
    },
    "E5-2-fenced-object-commit-worker-half": {
      "epic": "E5",
      "status": "unwired",
      "symbol": "createArtifactExportSequencer",
      "reason": "DAT-009 slice 3, slice a. The E5 gate says fenced object commit passes in D1, and its SERVER half does: mint (artifact-transfer-grant.ts), commit (artifact-commit.ts) and a real presigned PUT are live-proven on the D1 lane against real MinIO (DAT-002-live-minio-result.md, d1-merge-train 31885553697, 13/13). This clause is deliberately named for the WORKER half, which is the half that has never run: nothing in production sequences digest -> mint upload grant -> export -> commit. Do NOT read this entry as saying the commit path is unbuilt - that substitution is the exact error E5-3-patch-quarantine had to be corrected for. createArtifactExportSequencer has ZERO production callers by construction: its only consumers are its own tests until DAT-009 slice 3c adds the supervisor hook (SupervisorDeps.resolveExportArtifacts) and 3d composes it in dispatch-runtime.ts, following the createStagedInputResolver precedent at E7-1-staged-input-grant. Promote then, with evidence. Note that composing it is NOT sufficient for a byte to move. CORRECTED 2026-09-06 (W4U1): the previous sentence asserted that BOTH shipped providers were still at the provider-wire value, citing e2b-provider.ts:178 - a value AND a line that PR #353 invalidated, and that nothing read. Measured at this tip: packages/sandbox-e2b-provider/src/e2b-provider.ts:246 declares artifactExportMode = grant_upload and implements export for real, while packages/provider-wire/src/driver.ts:83 still declares artifactExportMode = none and its digestArtifact (:182-184) and exportArtifact (:185-192) throw UnsupportedProviderOperation UNCONDITIONALLY, never reading the property at all. READ THIS AS A CORRECTION TO THE REGISTER, NOT AS THE PRECONDITION BEING SATISFIED: the networked/container lane still cannot export, slice 3e still owes the wire route, and NOTHING yet produces the ArtifactExportRequest[] the sequencer would consume - that producer is CLI-008 Unit F. The two VALUES above are machine-checked against source by providerCapabilityClaims below; the line numbers deliberately are not (scripts/lib/gate-clause-wiring.mjs says why). This clause therefore does NOT gate, and must not be read as gating, the E7-1 capabilityProven counter, which filters kind=workspace_patch (countProducedOutputs arm 1 in server/src/services/e7-distributed-run-verifier-store.ts). CITATION DE-PINNED 2026-09-08 (W21D): this read `e7-distributed-run-verifier-store.ts:207`, and W21B/W21C moved that predicate ~230 lines - `:207` is now a `getAttemptTerminalReceipt` parameter, so the pin pointed at the wrong function. This is exactly the rot the DOES NOT clause in scripts/lib/gate-clause-wiring.mjs predicts for line pins; the remedy applied here is the one that register recommends by example - anchor on the SYMBOL, which does not move on unrelated edits - NOT a new positional guard.",
      "providerCapabilityClaims": [
        {
          "file": "packages/sandbox-e2b-provider/src/e2b-provider.ts",
          "property": "artifactExportMode",
          "expect": "grant_upload"
        },
        {
          "file": "packages/provider-wire/src/driver.ts",
          "property": "artifactExportMode",
          "expect": "none"
        }
      ]
    },
    "E5-5-redaction": {
      "epic": "E5",
      "status": "wired",
      "symbol": "synthesiseRunSecrets",
      "reason": "DAT-008 slice 5 (go-book Sprint 4) gives redaction a real INPUT and a production caller. The old symbol createFenceAwareEgressProxy was the DAT-005 EGRESS proxy (Direction B), which DAT-008's Direction A (env + sandbox_local_only) never uses; re-pointed to the worker function that redeems the handle and returns the values that become per-run canaries. synthesiseRunSecrets is referenced by composeDispatchRuntime (dispatch-runtime.ts) behind the default-OFF flag, and the redaction is PROVEN by a planted-leak test on BOTH streams (supervisor-secret-materialization.test.ts: a seeded secret in a lifecycle log AND in a fence-close network_denied is scrubbed to the marker; an unseeded control leaks it verbatim). What remains for Sprint 5 is a real sandbox authenticating over live E2B (parent DAT-008 design §9 limit 3) — the wiring checker reads caller count, not this field, so that residual lives in the test evidence."
    },
    "E7-1-coding-journey": {
      "epic": "E7",
      "status": "unwired",
      "symbol": "E2bSandboxProvider",
      "expectedReferences": 4,
      "reason": "TWO one-file construction seams now name E2bSandboxProvider, 2 references each (a ProviderModule interface property + a `new mod.E2bSandboxProvider(...)` construction), 4 total. (a) DEP-010 (Sprint 2): the DESKTOP root packages/worker-keystore/src/bin/sandbox-provider.ts. (b) DEP-012 Slice 3 · Wave β2: the ADAPTER-MANAGER host root packages/adapter-manager/src/bin/adapter-manager.ts, which resolves the provider over the real transport and hands it to createProviderServer. STILL UNWIRED in a shipped boot: (a) requires AOA_WORKER_SANDBOX_PROVIDER=e2b + a composed supervisor loop (deferred); (b) DEP-012 Slice 4+5 added the adapter-manager Dockerfile (docker/adapter-manager) and the staging compose now injects the AM public key + boot envs AND the matched CP mint key, but the image is NOT built/pushed and the staging compose is render-only in CI (operator/C0 builds + `docker compose up`), so nothing starts this bin in a shipped CI boot; the through-the-daemon consumer is DEP-011. The bin fail-closes without the CP public key (β2 §β2.2.1), the matched-pair keypair is smoke-checked at C0 by `pnpm verify:cp-am-keypair` before the canary, and the real E2B connect runs only in the operator-dispatched keyed lane. Promote when the operator builds+deploys the adapter-manager image and DEP-011 wires the daemon consumer. The number is typed out, not computed, so a later reference still fires unwired_but_now_has_caller."
    },
    "E8-1-sandbox-local-browser": {
      "epic": "E8",
      "status": "unwired",
      "symbol": "runBrowserSession",
      "reason": "packages/browser-runtime has zero importers and is in no dependency list. The 1 known reference is runner.ts calling it INSIDE the package; runner.ts is the in-guest entrypoint and nothing stages it, so the capability is unreachable despite the non-zero count. Blocked on dispatch (Sprint 3) then BRW-004+ (Sprint 7).",
      "expectedReferences": 1
    },
    "E10-2-legacy-reconciliation": {
      "epic": "E10",
      "status": "wired",
      "symbol": "reconcileOrganizationLegacyResources",
      "reason": "MIG-010 Unit 2.3 (E10-F002). Driven by server/src/cli/reconcile-legacy-resources.ts, exposed as `pnpm reconcile:legacy-resources`. Declared `wired` deliberately and enrolled here for the CLASS, not just the instance: E10-F002 was exactly this symbol's predecessor (reconcileCompanyLegacyResources) plus its store having ZERO non-test callers, and nothing mechanical noticed for the life of MIG-008 -- the crosswalk was never written and canary-preflight answered reconciliation_incomplete for every organization, forever. The guard now fails if that call site is removed. PROVEN TO BITE, not merely declared: renaming the CLI's call reddens check-gate-clause-wiring.mjs with exit 1 (claimed_wired_but_no_caller) and restoring it returns exit 0. SCOPE, STATED HONESTLY: `wired` here means a production, non-test, non-comment caller exists -- which is the whole of what this guard measures. It does NOT mean the canary opens. Unit 2.3 closes E10-F002 only; a lease created after the pass still re-closes the gate (E7-F004) and there is no watermark or reconciliation_stale until Unit 2.4."
    },
    "E10-2-reconciliation-watermark": {
      "epic": "E10",
      "status": "wired",
      "symbol": "recordCompletedPass",
      "reason": "MIG-010 Unit 2.4a. The durable marker of a COMPLETED reconciliation pass (legacy_reconciliation_passes, migration 0269) is what Unit 2.4b's canary gate narrows its lease inventory to; without a marker WRITE the gate reads no evidence and refuses reconciliation_stale forever. Enrolled here for the CLASS, exactly as E10-2-legacy-reconciliation was: E10-F002's whole shape was a capability whose production path had zero callers, unnoticed for the life of MIG-008, and a marker writer is the same shape one layer down. `wired` means a production, non-test caller exists -- reconcileOrganizationLegacyResources calls it once per company, after every record for that company is on disk. It does NOT mean the canary opens: nothing READS the marker until Unit 2.4b."
    },
    "E10-1-drain": {
      "epic": "E10",
      "status": "unwired",
      "symbol": "createDistributedExecutionDrain",
      "reason": "MIG-009 FIXED the two correctness defects and proved them at embedded-PG (server/src/__tests__/job-distributed-drain.integration.test.ts): (1) the rollback-safety grain is now per-Company (was assertRollbackSafe(organizationId) against a Company-keyed gate — an interface lie that fails CLOSED against the real bridge; the drain now enumerates every Company under the org via listOrganizationCompanyIds and asserts each, so a pending authoritative-cost receipt on ANY Company skips the whole org, closing the sibling-Company fail-open); (2) the missing listActiveAttempts SQL now exists (server/src/services/job-distributed-drain-store.ts — tenant-scoped runInTenant read, notInArray(TERMINAL), selectDistinct by job). The untested drain statuses (cancelled + no_active_lease) are covered. The drain is CORRECT WHEN WIRED. It stays UNWIRED (zero production callers by design): promoting to wired needs an honest drainAll INVOCATION — a real operator teardown / kill-switch write path — which is REL-005 scope (boot/SIGTERM/sweeper are the WRONG triggers; they'd cancel in-flight work on every restart). Composing createDistributedExecutionDrain in index.ts without a drainAll trigger would flip the caller count to >=1 and force a vacuous 'wired', the exact anti-pattern this register catches. Count stays 0 until REL-005 lands the trigger."
    },
    "E11-5-provider-kill-switch": {
      "epic": "E11",
      "status": "wired",
      "symbol": "evaluateKillSwitches",
      "reason": "Genuinely wired, by exactly ONE production caller: server/src/services/job-leasing.ts (:49 import, :720 call) on the real poll path. `node scripts/check-gate-clause-wiring.mjs --counts` measures evaluateKillSwitches (server/src/services/execution-kill-switches.ts:230) at 1. CORRECTED 2026-09-07 (W16A): this entry previously added 'and by the warm-sandbox reaper'. MEASURED FALSE, and not merely the wrong name -- server/src/services/warm-sandbox-reaper.ts:195 calls killedProviders, a DIFFERENT function in the same module, whose adjacent comment at :190-192 (matching its own docstring at :206) states the DELIBERATELY INVERSE polarity: \"Fail-OPEN, inverted from leasing: `killedProviders` returns the empty set for an absent, malformed or unreadable document, and a read that THROWS must not be able to trigger a fleet-wide teardown\" -- where evaluateKillSwitches refuses an unreadable document (execution-kill-switch-policy.ts:13). The reaper also acts only on entries carrying reclaim:true, so it is not a consumer of the kill VERDICT at all. A reader auditing kill-switch coverage on the old sentence inferred a fail-closed property the reaper does not have and by design never had. E11-F003 filed that defect and is RESOLVED by this correction. The four other PRODUCTION files that name evaluateKillSwitches (packages/db/src/schema/instance_settings.ts:18, execution-kill-switch-policy.ts:13 and :36, execution-kill-switches.ts:206, execution-target-resolver.ts:62 -- five mentions) are COMMENTS, not callers. 'PRODUCTION' is load-bearing and was added 2026-09-07 (W16A-FIX): `grep -rn evaluateKillSwitches server packages ui --include=*.ts | grep -v node_modules` returns 67 lines across 11 files -- 5 production and 6 under __tests__ -- so an unqualified 'four other files' is false as a repository-wide count even though every non-caller mention it enumerates is correct. Residual, recorded in the wave doc and unchanged by this correction: no write path (throwing it means hand-executed SQL) and it is instance-wide per provider with no Organization or sink dimension - REL-005 scope."
    },
    "E7-1-staged-input-write": {
      "epic": "E7",
      "status": "wired",
      "symbol": "stageJobInputFiles",
      "reason": "CLI-008 Unit B. `JobArtifactsRepository.insert` had ZERO callers — production OR test — and is the ONLY unguarded write on the inbound artifact path; `stageJobInputFiles` is its production consumer. Referenced from the distributed composition root (server/src/index.ts, `RunExecutionOwnerDeps.stageJobInput`) and invoked by `resolveExecutionOwner` BETWEEN the convert and the placement — the job must exist (the job_artifacts composite FK) and the attempt must not be leasable yet (a worker that leased first would find nothing committed). ★ STATED PLAINLY, because `wired` here means REACHABLE FROM A BOOT ROOT and not 'runs today' (the same sense E4-4 records): the seam currently passes NO files, because Unit B is the CHANNEL and Units C (MCP config) and D (instructions bundle) supply what rides it. So no production run stages a file yet and `capabilityProven` is unmoved. No stub assembler was added to make the count look better — a function that returns [] by construction is the vacuously-true clause this register exists to prevent. The channel ITSELF is proven end to end with a real bundle in server/src/__tests__/cli-008-unit-b-staging-channel.integration.test.ts: a control-plane-authored file is inside the sandbox with the right bytes at the instant the tenant command runs, over embedded PostgreSQL, the real leasing poll, the real artifact_transfer_grant download branch on a real live fence, the real E2B driver and the mock transport's filesystem. Mutation-proven per link (Task 5 Step 2): skipping this insert reds job-input-staging.integration (3/6) while the resolver and provider link tests stay green. The provider half (`SandboxProvider.stageFiles`) is deliberately NOT given its own clause: the symbol is an interface method with references on every implementer, so its count is a weak signal — stage-files.test.ts is what pins it."
    },
    "E7-1-staged-input-grant": {
      "epic": "E7",
      "status": "wired",
      "symbol": "createStagedInputResolver",
      "reason": "CLI-008 Unit B. `ControlPlaneClient.artifactTransferGrant` had exactly ONE caller in the repo — a unit test (worker-daemon/src/__tests__/artifact-commit-client.test.ts:80). `createStagedInputResolver` is its production consumer: it reads the control plane's staged-input pointer off the frozen envelope's extensions[] and mints one short-lived download grant per file. Referenced once, by `composeDispatchRuntime` (packages/worker-daemon/src/lifecycle/dispatch-runtime.ts), which passes it to the supervisor as `resolveStagedFiles` — the same shape and the same composition point as `materializeRunSecrets`. Reachable from the dispatch boot root; like every other symbol on that root it is behind the default-OFF distributed-execution flag. Grants out, never bytes: nothing here fetches an object, so no payload crosses the dependency-pinned daemon. A run whose envelope carries no pointer resolves to [] and makes no request at all, which is every production run today. Mutation-proven (Task 5 Step 2): skipping the grant mint reds staged-input-resolver.test.ts (7/9) while the control-plane write and the provider link tests stay green — that test was ADDED because the mutation originally reddened only the end-to-end suite, which is the 'proven only at its ends' hole."
    },
    "E5-6-denied-egress": {
      "epic": "E5",
      "status": "unwired",
      "symbol": "createFenceAwareEgressProxy",
      "reason": "E5's exit gate names 'denied egress' as its clause 6; the audit (epics/E5-workspaces-secrets/qa/2026-08-24-d0-e5-exit-gate-audit-a1.md) numbers the same seven, and its clauses 2/3/5 are this register's E5-2/E5-3/E5-5, so 6 is the free and correct id. The production symbol for that clause is createFenceAwareEgressProxy (server/src/services/egress-proxy.ts:146) -- the sole consumer of classifyEgressDestination and the only code in the repo that would REFUSE a destination. It has ZERO production callers: egress-proxy.ts is imported by exactly one file in the tree, the integration test server/src/__tests__/egress-proxy.integration.test.ts, and two production comments assert the same (routes/worker-control.ts:160, services/execution-secret-resolve.ts:7). WHY IT WAS MISSING: this clause was carried by E5-5 until DAT-008 slice 5 legitimately RE-POINTED that entry's symbol to synthesiseRunSecrets (Direction A never uses the egress proxy, so the old pairing was wrong). The re-point was correct and the side effect was not: the egress half stopped being DECLARED at all, so from that commit until this entry the register reported nothing about it on green runs, while the library header still lists createFenceAwareEgressProxy among the 17 unprovable clauses the register exists to surface. Re-enrolled honestly rather than left silent. WHAT WOULD HAVE TO CHANGE for 'wired': some production path must call this proxy. CORRECTED 2026-09-07 (W16A): this entry previously said 'BRW-004 slice (f) is the only chartered candidate'. MEASURED FALSE. RE-DERIVED FROM THE CHARTERS 2026-09-07 (W16A-FIX, after Codex on PR #380 and an adversarial reviewer both objected to an earlier 'THREE chartered tickets' in this same sentence): TWO tickets were CHARTERED over this capability, and a THIRD was only NOMINATED as their successor and never took it. All three have result docs on disk. CHARTERED (1): DAT-005 BUILT the symbol and booked itself COMPLETE (DAT-005-result.md:3) with zero production callers -- its own result calls the live outbound channel 'the inert E4-D12 seam'. CHARTERED (2): DSK-002's charter required the same capability -- DSK-002-design.md:15-17 outcome 'mediate device-local handles through the DAT-004 broker plus a fence-aware egress path', acceptance clause (4) at :25 'The sandbox cannot read OS credential storage or bypass the broker/proxy', and D5 at :198-200 'performing an egress through the broker' -- and it SHIPPED having explicitly DECLINED it (DSK-002-result.md:161-163: sandbox.filtered_egress 'is claimed by no mechanism ... claiming it now is the exact over-report D4 forbids'), handing the device-side residual on to DSK-003 (:168-179, 'belongs with DSK-003'). NOMINATED, NEVER CHARTERED (3): DSK-003's own charter carries NO egress requirement -- its Outcome (DSK-003-design.md:14-16) is a least-privilege desktop background host with signed installers, and none of its five acceptance clauses (:20-24) names egress, a proxy or a broker. It was named by DSK-002's RESULT (:168-179, 'belongs with DSK-003'), which is a nomination and not a charter, and it shipped without taking it: `grep -ni egress` over BOTH DSK-003 documents returns ZERO hits and DSK-003 has a result doc; both DSK-004 documents are zero too. COUNTING DSK-003 AS CHARTERED WOULD BE THE SAME REGISTER-ACCURACY DEFECT THIS SENTENCE EXISTS TO CORRECT, which is why the count is stated as two-plus-a-nominee rather than three. So the capability has been declined once explicitly by a chartered owner and dropped once silently by a nominated successor, and it is not browser-scoped work: this file's own header calls it 'the FENCE-AWARE egress proxy (server-side, inert-until-wired)' and its step 2 is broker.resolve(), the DAT-004 broker DSK-002's outcome names. BRW-004 slice (f) is the only REMAINING named candidate, and it is a DEFERRED slice of a ticket already at gate review rather than an unstarted one: BRW-004-result.md covers slices (a)-(d) only, :15 and :99 record (f)-(h) not attempted, and :366 books the acceptance condition 'allowed domains ... are enforced' as `deferred`. It is browser-scoped, while the org/crew sandbox egress path that E8-F003 measured has no chartered owner at all. E5-F001 filed the 'only' error and is RESOLVED by this correction; the security substance remains E8-F003's and is not duplicated here. DO NOT read the D1 grade as evidence for this symbol: the audit grades clause 6 proven_in_d1 on tests/d1/e6f-08-egress-isolation.test.mjs, which proves denial by docker-compose `internal: true` network segmentation around a container ANALOG (worker-a plus fake-provider) and never touches this symbol or the real provider -- and E8-F003 measured the real E2B provider REACHING the identical targets (169.254.169.254 answered HTTP 401; a non-allowlisted public host returned 200)."
    },
    "E8-w10c-internal-range-deny-set": {
      "epic": "E8",
      "status": "wired",
      "symbol": "INTERNAL_RANGE_DENY_CIDRS",
      "reason": "W13 PROMOTED THIS FROM `unwired` TO `wired`, and the promotion condition is the one this entry already named. server/src/services/w10c-internal-range-deny-set.ts is the mechanically-derived CIDR cover of isPrivateIP (outbound-url-guard.ts). Its ORIGINAL consumer -- applying it to the E2B provider-layer network.denyOut body -- was measured out of existence on 2026-09-07 (E8-F008, workflow run 34085130892): the tier accepts a deny set, validates it server-side, stores it, echoes it back verbatim from getInfo(), and routes the denied traffic anyway, on both Sandbox.create and updateNetwork. That consumer is still dead and is NOT what wires this. WHAT WIRES IT: server/src/services/mcp-connector-oauth.ts builds its two node:net BlockList SSRF tables from INTERNAL_RANGE_DENY_CIDRS at module load, replacing the hand-typed range lists that had diverged from isPrivateIP by one IPv4 /24 (192.88.99.0/24) and eight IPv6 classes including both ends of the 6to4 tunnel (192.88.99.0/24 + 2002::/16) and the NAT64 well-known prefix 64:ff9b::/96 -- the divergence E8-F009 measured. That is verbatim the fourth promotion condition the previous version of this entry listed: \"fixing the mcp-connector-oauth divergence against a derived source of truth\". THE REFERENCE IS REAL AND IN PRODUCTION: the symbol is read in a module-scope for-loop in mcp-connector-oauth.ts (not a type position, not a comment, not a re-export), on a path that runs for every OAuth-bearing MCP connector; the remaining reference is still the intra-module default parameter in isCoveredByDenySet's signature. WHAT THIS ENTRY DOES NOT CLAIM: it claims nothing about SANDBOX egress. Nothing in this tree filters sandbox egress at any layer (E8-F003 section 8 census) and DE-08 reads not-delivered; adopting this set into the managed-E2B network body at the measured tier stays REFUTED, not pending. The scope of the wiring is one in-process control-plane SSRF filter."
    },
    "E9-5-service-generation-rollout": {
      "epic": "E9",
      "status": "wired",
      "symbol": "rollServiceGeneration",
      "reason": "SVC-005a. E9's exit gate for SVC-005 opens with 'no two generations may perform external effects simultaneously', and this row tracks the WRITER that makes a second generation possible at all -- NOT the full clause, which is NOT claimed (see the last paragraph). WHAT WAS NOT TRUE BEFORE: services.generation HAD NO WRITER. Three shipped records said so and all three were right -- SVC-002-design.md ('SVC-002 reads generation under a row lock and never bumps it; services.generation still has no writer after this ticket'), SVC-007a's updateServiceDesiredState docstring (which scoped the bump out to SVC-005 by name), and the DE-12 row of docs/architecture/distributed-execution-threat-controls.json ('services.generation HAS NO WRITER ... so no generation rollover can be performed and the fence has nothing to refuse'), on which E0-F013's founder ruling dropped DE-12's audit conjunct as VACUOUS. PRODUCTION CALLERS, measured with this guard's own countProductionCallers at head, base vs head: rollServiceGeneration 0 -> 1; bumpServiceGeneration (the repository write) 0 -> 3; listUnwitnessedGenerationPredecessors (the fence read) 0 -> 3; findBlockingPredecessor (the pure classifier) 0 -> 1. Every symbol is ABSENT at base c27feeea8 -- git grep returns zero files for each -- so the base figures are definitive rather than inferred. THE MEASURED FIGURES ARE STATED RATHER THAN THE INTUITIVE ONES, for SVC-002's reason at this same register: the guard skips only lines matching (export)?(async)?(function|class|const|let|interface|type) <symbol>, so a TypeScript interface METHOD declaration and its object-literal implementation each count as a reference. bumpServiceGeneration's 3 is therefore one interface declaration (packages/db/src/repositories/tenant/job-control.ts), one implementation in the same file, and ONE real call site -- in rollServiceGenerationWithinTenant. listUnwitnessedGenerationPredecessors's 3 decomposes identically, its one real call site being step 4b of reconcileServiceWithinTenant. THE COMPOSITION ROOT is server/src/routes/job-control.ts -- the POST .../services/:serviceId/generation handler, which is the single production call of rollServiceGeneration. jobControlRoutes is mounted by createApp INSIDE the opts.distributedExecutionEnabled block over the non-owner aoa_app pool, which is why the roll needs no flag check of its own: flag-off, this router is never built. Authority is assertOrgAdmin (execution_target:manage, org owner/admin), run FIRST so an unauthorized caller cannot tell a malformed definition from a valid one. It is the SAME router SVC-007a's create and desired-state controls are mounted on, so the roll is reachable by exactly the operator who can already create and stop a service, and by nobody else. THE FENCE IS SEPARATELY ARMED, and this matters because a rollout writer whose fence nothing consults would be the bump alone. findBlockingPredecessor's single production caller is step 4b of reconcileServiceWithinTenant, inside the SAME reconcile pass and the SAME transaction that SVC-002's composition root already drives on a nextDelayMs timer (createServiceReconciler, E9-1-service-reconciler above). So the refusal runs on every placement pass of every admitted organization, not only when someone calls the roll. WHAT IS PROVEN AND WHAT IS NOT. PROVEN over real embedded PostgreSQL in server/src/__tests__/service-generation-rollout.integration.test.ts (14 cases): the bump plus its immutable generation row commit together (R-T1/R-T2); the roll drains a live instance through the SAME requestCancellation({graceful:true}) + attempt-terminal backstop composition SVC-007a's stop uses, and terminalized_by records WHO ended it (R-T3); a clock-condemned predecessor of an OLD generation REFUSES the new placement (R-T4) and the refusal CLEARS when the old attempt goes terminal (R-T5); a SAME-generation replacement is unaffected, so SVC-003b's shipped behaviour is untouched (R-T6); a worker-witnessed predecessor does not stall (R-T7/R-T7b); and SVC-003a's stale-generation refusal still refuses after a real bump, in both directions (R-T8). ★★★ THE CLAUSE IS NOT CLAIMED, AND THIS ROW MUST NOT BE READ AS CLAIMING IT. What SVC-005a delivers is 'no two generations are PLACED while the older one is un-drained or unwitnessed'. It does NOT deliver 'no two generations perform external effects simultaneously': a closed fence stops the old worker WRITING, not its PROCESS, and no control-plane fact can prove a remote process stopped -- E9-F007 section 3 says so explicitly and SVC-005a does not overturn it. The residual is filed as E9-F012. SVC-005 also stays OPEN on its other named conjuncts: operator pause/resume beyond what SVC-007a's desired-state control already does, worker DRAIN (E9-F008 records that the frozen drain and graceful_stop command kinds have ZERO producers and that queueGovernedControlCommand is narrowed at the TYPE level against them), replace-BEFORE-stop (structurally unreachable under service_instances_live_service_uq, and stated rather than silently dropped), hard runtime/spend limits, and stuck-stop force-kill. E9's exit gate is NOT moved and is NOT claimed."
    }
  }
}
