#!/bin/sh
# POSIX shim: lets Commander cli-mode resolve `codex` from PATH to the
# deterministic fake CLI (see fake-codex.mjs). Marked executable in git
# (`git update-index --chmod=+x`) so `which codex` finds it on Linux CI.
exec node "$(dirname "$0")/fake-codex.mjs" "$@"
