# E3 — Job control: M1a candidate reachability ledger — FILLED AT CANDIDATE

**Measured at:** **CANDIDATE `7be35ae6b7719877e61f54ab552de84de8491e7d`** (filled 2026-09-23 UTC by the `M1a` QA owner, a distinct
review session, per the README's *Filling a ledger at candidate freeze*). Every caller count in the
`At candidate` column was re-measured at the candidate with `countProductionCallers`
(`scripts/check-gate-clause-wiring.mjs`); no tip value was copied forward. Register statuses are
`scripts/gate-clause-wiring.json` at the candidate, where the guard reports
**`OK (28 wired clause(s), 6 declared dormant, 2 provider-capability claim(s) matched to source)`** —
at the tip it reported 22 wired / 12 dormant.
**Candidate:** `7be35ae6b7719877e61f54ab552de84de8491e7d`
**Campaign evidence this ledger cites** — both records under `../qa/`:
`M1-D1-SPINE` = run `35912752449`, jobs `m1-spine` + `m1-fault-matrix`, at `8c01f4e94f8e25d7abaf524e8b266b809c67b8cb`
(a **tree-equivalent** revision: every product subtree hash is identical to the candidate's; the sole
delta is one added docs file). `M1a-D2-MECHANISM` = run `35920425288`, job `shipped-boot`, at the
candidate exactly.
★ **Both gate records are `a3` and both carry `Result: fail`** —
`../qa/2026-09-23-m1-d1-spine-m1a-candidate-7be35ae6b771-a3.md` and
`../qa/2026-09-23-m1a-d2-mechanism-m1a-candidate-7be35ae6b771-a3.md`.
★★★ **The `M1a-D2-MECHANISM` profile of `tests/d1/fault-matrix.json` declares 23 cases and ALL 23 are
`evidence: "pending"` at the candidate**, and `.github/workflows/m1-shipped-boot.yml` contains no
fault-matrix step, so the keyed run fired none of them. Wherever a row below says a clause is
certified by the spine, that is not shorthand for "and also by the mechanism lane".
**Exit gate** (`epics/E3-job-control/README.md`): submit, enroll, admission, assignment, product
approval, runtime decision, lease, ACK, renew, fence, event, completion, cancellation, retry, budget,
cost, output/run projection, revocation, and operator controls pass in D1 with source-specific provenance.
**Columns:** see [README](./README.md).

★ **Column meanings, in brief.** The `Certified only by M1-D1-SPINE` column is `TO MEASURE AT
CANDIDATE FREEZE` on every row, and it is not repeated in the table. Tenant cells marked
"kernel RLS" rest on the `*_rls` migrations for that table. Tenant cells marked "query predicate"
cover legacy tables with no RLS (README → *Measurements that apply to every ledger*).

★★★ **READING THIS FILLED LEDGER.** The `@tip` columns are kept **verbatim** as the S0-7b tip
measurement and are not rewritten; every `TO MEASURE AT CANDIDATE FREEZE` still visible inside them
is answered in the final **`At candidate`** column, which is the candidate measurement. The
skeleton's sentence *"`Certified only by M1-D1-SPINE` is `TO MEASURE AT CANDIDATE FREEZE` on every
row"* is now discharged: that judgement is folded into each `At candidate` cell, which names the
lane (and the fault-matrix case id) that certifies the row, or says no lane does.

| # | Mechanism | Register key · status @tip | Symbol · file | Present | Callers @tip (where) | Production-reachable @tip | Tenant isolation (F10) @tip | M1 tickets that change this row | At candidate |
|---|---|---|---|---|---|---|---|---|---|
| E3-1 | submit | no register entry | `jobSubmissionService` · `server/src/services/job-submission.ts`; `submitJobWithinTenant` | yes | 1 (`routes/job-control.ts`); `submitJobWithinTenant` 3 (`job-admission-bridge.ts`, `job-submission.ts`, `service-reconciler.ts`) | Chain read: `jobControlRoutes` is mounted in `createApp`'s distributed block. **Reachable behind `AOA_DISTRIBUTED_EXECUTION_ENABLED`.** | `jobs` has kernel RLS (`0211`). Submission runs inside `runInTenant`. `TO MEASURE AT CANDIDATE FREEZE`: the hostile cross-tenant submit case | — | **1 caller** (`routes/job-control.ts`); `submitJobWithinTenant` **3**. Unchanged. Reachable behind the flag. **Certified by `M1-D1-SPINE` only** — `d1.tenant.journey.A`/`.B` submit through the real route; the mechanism lane submits through heartbeat, not this route. |
| E3-2 | enroll | no register entry | `createWorkerEnrollmentService` · `server/src/services/worker-enrollment.ts` | yes | 1 (`routes/worker-control.ts`) | `workerControlRoutes` is mounted in `createApp`'s distributed block. **Reachable behind the flag.** | Enrollment tables have RLS (`0221_worker_enrollment_rls.sql`) | — | **1 caller**. Unchanged. Enrolment exercised on BOTH lanes: `workers.json` shows all three keyed workers `status: enrolled`, each on its own Organization's target. |
| E3-3 | admission | no register entry | `jobAdmissionBridge` · `server/src/services/job-admission-bridge.ts` | yes | 2 (`server/src/index.ts`, one site) | Composed with `createHeartbeatDistributedRolloutHook` in the `index.ts` flag block. **Reachable for Organizations enabled in `AOA_DISTRIBUTED_EXECUTION_ROLLOUT`.** | **Per-Organization gate: present** (the rollout source). Admission also drives legacy `issues` through `runInTenant`'s raw `tx`, where isolation is a query predicate. | — | **2 callers**. Unchanged. **Exercised on the mechanism lane**: `journey.json` `rolloutResolution` logs `canary` for a/b and `off` for c, which is the admission hook resolving per Organization. |
| E3-4 | assignment / placement | no register entry | `createJobPlacementService`, `decideJobPlacement` · `server/src/services/job-placement.ts` | yes | 3 (`index.ts`, `job-placement.ts`); `decideJobPlacement` 1 (`job-placement-transaction.ts`) | Composed in the `index.ts` flag block. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` (target scope/owner binding against another Organization's target) | — | **3 callers**; `decideJobPlacement` **1**. Unchanged. Mechanism lane: `placement_disposition: "selected"`, `placement_reason_code: "target_selected"` for both enabled tenants. |
| E3-5 | product approval | `E3-5-product-approval` · **unwired** | `jobApprovalBridge` · `server/src/services/job-approval-bridge.ts` | yes | **0** | **Not reachable.** The register says the legacy approval path still owns it. | n/a while unwired | none in M1 §4 | **0 callers**, register still **unwired**. Unchanged and **not reachable**. Outside `M1a`'s required set; no campaign case touches it. |
| E3-6 | runtime decision | no register entry | `TO MEASURE AT CANDIDATE FREEZE` (not identified at the tip) | — | — | — | — | — | Still not identified as a distinct symbol. **Nothing measured.** No campaign case names a "runtime decision" clause, so this row is uncertified by either gate. |
| E3-7 | lease | no register entry | `createJobLeasingService` · `server/src/services/job-leasing.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | Leases have kernel RLS (`0211`, `0228_job_leasing_rls.sql`). F10 case *"A cannot lease B's jobs"*: `TO MEASURE AT CANDIDATE FREEZE` | — | **1 caller**. Unchanged. **Certified by `M1-D1-SPINE` only**: `d1.tenant.cross.lease` — hostile renew `409 stale_fence`, own renew `200 renewed`. The mechanism lane took 1 lease per tenant (`leaseCount: 1`) but ran **no** hostile lease case (`d2m.tenant.cross.lease` is `pending`). |
| E3-8 | ACK | no register entry | `createJobControlAckService` · `server/src/services/job-control-ack.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | `WRK-013` (writes the lease-candidate store on ACK) | **1 caller**. Unchanged. `WRK-013` now writes the lease-candidate store just before ACK (`poll-loop.ts` `handleOffer` → `recordCandidate`). ACK exercised on both lanes. |
| E3-9 | renew + fence | no register entry | `createJobLeaseRenewalService` · `server/src/services/job-fencing.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | — | **1 caller**. Unchanged. **`M1-D1-SPINE` only**: fence behaviour is `d1.tenant.cross.events` (`hostileAck` `409 stale_fence`) and `d1.fault.link_cut.worker_to_control_plane` (`lease_reclaimed_and_late_ack_refused`). |
| E3-10 | event ingest | no register entry | `createJobEventIngestService` · `server/src/services/job-events.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `job_events` has RLS (`0235_job_events_rls.sql`). F10 case *"A cannot see B's events"*: `TO MEASURE AT CANDIDATE FREEZE` | `JOB-016` (the `E3-D-ACC` seam sits in or beside `acceptEvent`) | **1 caller**. Unchanged, but the ingest now carries the `E3-D-ACC` seam. Both lanes ingest real events (mechanism: `attemptStartedEvents: 1`, `terminalEvents: 1` per enabled tenant). |
| E3-11 | completion | no register entry | `resolveAttemptTerminalSignal` · `server/src/services/job-events.ts` | yes | 1 (`job-events.ts`) | Reached through event ingest (E3-10). | `TO MEASURE AT CANDIDATE FREEZE` | `JOB-017` (retire or rework `projectTerminalWinner`) | **1 caller**. Unchanged. Mechanism lane: one `terminal` event per enabled tenant, `exit_code "0"`, `projectionReceiptApplied: true`. |
| E3-12 | cancellation | no register entry | `setDistributedCancellationPort` · `server/src/services/distributed-cancellation-port.ts`; `dispatchCancel` | yes | 2 (`index.ts`); `dispatchCancel` 2 (`heartbeat.ts`, `issues.ts`) | Registered in the `index.ts` flag block (CLI-006). **Reachable behind the flag.** | The Organization is resolved at registration (comment at the registration site). F10 case *"A cannot cancel B's jobs"*: `TO MEASURE AT CANDIDATE FREEZE` | `DEP-018` (cancellation in every profile) | **2 callers**; `dispatchCancel` **2**. Unchanged. **`M1-D1-SPINE` only, and this is a gate gap**: `d1.cancel.unleased_attempt` + `d1.cancel.leased_attempt` fired on the spine, but `d2m.cancellation.leased_attempt` is `pending` — the mechanism gate's own cancellation clause is **uncertified**. |
| E3-13 | retry | no register entry | `TO MEASURE AT CANDIDATE FREEZE` (not identified at the tip) | — | — | — | — | — | Still not identified as a distinct symbol. Retry is observed indirectly by `d1.reconcile.expired_lease_reaped` (`single_winner_retry_minted`) on the spine. **Mechanism lane: nothing.** |
| E3-14 | budget + cost | `E3-15-budget` · **unwired** | `jobBudgetCostBridge` · `server/src/services/job-budget-cost-bridge.ts`; `resolveAuthoritativeRate` | yes | **0**; `resolveAuthoritativeRate` 2 (inside the bridge only) | **Not reachable.** `E3-F037` (HIGH) is open. There is also no usage producer: the supervisor built by `composeDispatchRuntime` receives no `observeRun` (see E4-U). | ★ `cost_events` is a **legacy table with no RLS**. Isolation there is a query predicate. F10 case *"A cannot see B's cost rows"* must be exercised directly: `TO MEASURE AT CANDIDATE FREEZE` | **`JOB-016`** (seam + price at ingest), **`WRK-018`** (usage producer, for closure) | ★ **`jobBudgetCostBridge` 0 → 1 caller** (`JOB-016`), and `resolveAuthoritativeRate` **2**. The register key `E3-15-budget` **stays `unwired` deliberately** — its reason says the D1 units are canned by the reference provider and promotion waits on the `WRK-018` keyed acceptance. **Spine: a priced row EXISTS** — `d1.tenant.journey.A`/`.B` each `costRows: 1`, `legacyTables.cost_events` `ownCents: 83`, `foreign: 0`. ★★★ **Mechanism lane: `costEventsForRun: 0` and `costUsd: null` on both tenants — NO priced row is proven on real E2B.** Real usage WAS parsed there (8/618 and 8/597 tokens, `cachedInputTokens` non-zero), so the producer works; the charge is not shown. |
| E3-15 | output / run projection | `E3-17-output` · **unwired** | `jobOutputBridge` · `server/src/services/job-output-bridge.ts` | yes | **0** | **Not reachable.** | `task_outputs` and `artifacts` are legacy tables with no RLS. Isolation there is a query predicate. | **`JOB-017`**; `CLI-014` (M1b) | `jobOutputBridge` factory **still 0 callers**; the register key `E3-17-output` is now **`wired`** on a DIFFERENT symbol, `resolveAcceptedOutputProjector`, and `projectAcceptedOutputCore` has **2 callers**. **Arm 2 still reads 0 on every real run** (`E7-F018`, HIGH, open): both lanes show `taskOutputs: 0`. Output is `M1b`. |
| E3-16 | activity-audit parity | `E3-audit-parity-bridge` · **unwired** | `jobAuditBridge` · `server/src/services/job-audit-bridge.ts` | yes | **0** | **Not reachable.** | `activity_log` is a legacy table with no RLS. | **`JOB-017`** | `jobAuditBridge` factory **still 0 callers**; `E3-audit-parity-bridge` is now **`wired`** on `createAcceptedActivityAuditProjector`, and `recordAcceptedActivity` has **2 callers**. **Spine only**: `d1.tenant.journey.*` show `activity: ["job.attempt_started","job.attempt_terminal"]` and `activity_log` `own 2 / foreign 0`. ★★★ **Mechanism lane: `signals.audit.jobSubmitted` and `signals.audit.securityDenials` are both `[]` — the operator-visible audit signal the gate text names is EMPTY there.** |
| E3-17 | revocation | `E3-18-revocation` · **wired** | `createExecutionTargetRevocationFanout` · `server/src/services/execution-target-revocation-fanout.ts` | yes | 2 (`index.ts`, one site) | **Reachable behind the flag** (guard-checked). | `execution_target_revocations` has RLS (`0239`) | — | **2 callers**. Unchanged, `wired`. No campaign case exercises revocation on either lane. |
| E3-18 | operator controls | no register entry | `createJobOperationsService` · `server/src/services/job-operations.ts` (`drainJob`, `revokeWorker`, …) | yes | 1 (`routes/job-control.ts`) | Job-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` (an operator in A acting on B) | `MIG-009` (fleet drain trigger, E10) | **1 caller**. Unchanged. `MIG-009` added `runDistributedExecutionDrainTrigger` as `createDistributedExecutionDrain`'s first production caller (**1**; `E10-1-drain` now `wired`). ★★★ **The rollback rehearsal belongs in the `M1-D1-SPINE` QA RECORD itself** (M1 execution plan §6: *"including the rollback rehearsal via the `MIG-009` CLI, attributed to the rollback owner"*), **not in the handoff** — a handoff cannot add campaign evidence to an immutable QA attempt. It is **absent** from the candidate campaign, which is one of the two reasons the spine record carries `Result: fail`. The operator entrypoint exists: `pnpm drain:distributed-execution --operator <who>`. |
| E3-19 | sweeper / reconciliation / ready scheduler / outbox (supporting) | no register entry | `createJobControlSweeper`, `createJobReconciliationService`, `createJobReadyScheduler`, `createJobOutboxWorker` | yes | 2 / 4 / 2 / 2 (`index.ts`; reconciliation also in `worker-control.ts` and `job-operations.ts`) | Composed in the `index.ts` flag blocks. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | — | Sweeper **2**, reconciliation **4**, ready scheduler **2**, outbox worker **2**. Unchanged. Spine: `d1.reconcile.expired_lease_reaped`, `d1.restart.control_plane_process` and `d1.cleanup.orphan_object_swept` all fired with the suppression control. **Mechanism lane: `d2m.reconcile.daemon_restart_with_live_lease` is `pending`.** |
