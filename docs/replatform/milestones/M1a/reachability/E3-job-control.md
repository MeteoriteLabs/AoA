# E3 — Job control: M1a candidate reachability ledger (SKELETON)

**Measured at:** program tip `b71f0dd539fe713c776f3935932be33af1a24fae` (not a candidate)
**Candidate:** `TO MEASURE AT CANDIDATE FREEZE`
**Exit gate** (`epics/E3-job-control/README.md`): submit, enroll, admission, assignment, product
approval, runtime decision, lease, ACK, renew, fence, event, completion, cancellation, retry, budget,
cost, output/run projection, revocation, and operator controls pass in D1 with source-specific provenance.
**Columns:** see [README](./README.md).

★ **Column meanings, in brief.** The `Certified only by M1-D1-SPINE` column is `TO MEASURE AT
CANDIDATE FREEZE` on every row, and it is not repeated in the table. Tenant cells marked
"kernel RLS" rest on the `*_rls` migrations for that table. Tenant cells marked "query predicate"
cover legacy tables with no RLS (README → *Measurements that apply to every ledger*).

| # | Mechanism | Register key · status @tip | Symbol · file | Present | Callers @tip (where) | Production-reachable @tip | Tenant isolation (F10) @tip | M1 tickets that change this row | At candidate |
|---|---|---|---|---|---|---|---|---|---|
| E3-1 | submit | no register entry | `jobSubmissionService` · `server/src/services/job-submission.ts`; `submitJobWithinTenant` | yes | 1 (`routes/job-control.ts`); `submitJobWithinTenant` 3 (`job-admission-bridge.ts`, `job-submission.ts`, `service-reconciler.ts`) | Chain read: `jobControlRoutes` is mounted in `createApp`'s distributed block. **Reachable behind `AOA_DISTRIBUTED_EXECUTION_ENABLED`.** | `jobs` has kernel RLS (`0211`). Submission runs inside `runInTenant`. `TO MEASURE AT CANDIDATE FREEZE`: the hostile cross-tenant submit case | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-2 | enroll | no register entry | `createWorkerEnrollmentService` · `server/src/services/worker-enrollment.ts` | yes | 1 (`routes/worker-control.ts`) | `workerControlRoutes` is mounted in `createApp`'s distributed block. **Reachable behind the flag.** | Enrollment tables have RLS (`0221_worker_enrollment_rls.sql`) | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-3 | admission | no register entry | `jobAdmissionBridge` · `server/src/services/job-admission-bridge.ts` | yes | 2 (`server/src/index.ts`, one site) | Composed with `createHeartbeatDistributedRolloutHook` in the `index.ts` flag block. **Reachable for Organizations enabled in `AOA_DISTRIBUTED_EXECUTION_ROLLOUT`.** | **Per-Organization gate: present** (the rollout source). Admission also drives legacy `issues` through `runInTenant`'s raw `tx`, where isolation is a query predicate. | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-4 | assignment / placement | no register entry | `createJobPlacementService`, `decideJobPlacement` · `server/src/services/job-placement.ts` | yes | 3 (`index.ts`, `job-placement.ts`); `decideJobPlacement` 1 (`job-placement-transaction.ts`) | Composed in the `index.ts` flag block. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` (target scope/owner binding against another Organization's target) | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-5 | product approval | `E3-5-product-approval` · **unwired** | `jobApprovalBridge` · `server/src/services/job-approval-bridge.ts` | yes | **0** | **Not reachable.** The register says the legacy approval path still owns it. | n/a while unwired | none in M1 §4 | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-6 | runtime decision | no register entry | `TO MEASURE AT CANDIDATE FREEZE` (not identified at the tip) | — | — | — | — | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-7 | lease | no register entry | `createJobLeasingService` · `server/src/services/job-leasing.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | Leases have kernel RLS (`0211`, `0228_job_leasing_rls.sql`). F10 case *"A cannot lease B's jobs"*: `TO MEASURE AT CANDIDATE FREEZE` | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-8 | ACK | no register entry | `createJobControlAckService` · `server/src/services/job-control-ack.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | `WRK-013` (writes the lease-candidate store on ACK) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-9 | renew + fence | no register entry | `createJobLeaseRenewalService` · `server/src/services/job-fencing.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-10 | event ingest | no register entry | `createJobEventIngestService` · `server/src/services/job-events.ts` | yes | 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `job_events` has RLS (`0235_job_events_rls.sql`). F10 case *"A cannot see B's events"*: `TO MEASURE AT CANDIDATE FREEZE` | `JOB-016` (the `E3-D-ACC` seam sits in or beside `acceptEvent`) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-11 | completion | no register entry | `resolveAttemptTerminalSignal` · `server/src/services/job-events.ts` | yes | 1 (`job-events.ts`) | Reached through event ingest (E3-10). | `TO MEASURE AT CANDIDATE FREEZE` | `JOB-017` (retire or rework `projectTerminalWinner`) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-12 | cancellation | no register entry | `setDistributedCancellationPort` · `server/src/services/distributed-cancellation-port.ts`; `dispatchCancel` | yes | 2 (`index.ts`); `dispatchCancel` 2 (`heartbeat.ts`, `issues.ts`) | Registered in the `index.ts` flag block (CLI-006). **Reachable behind the flag.** | The Organization is resolved at registration (comment at the registration site). F10 case *"A cannot cancel B's jobs"*: `TO MEASURE AT CANDIDATE FREEZE` | `DEP-018` (cancellation in every profile) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-13 | retry | no register entry | `TO MEASURE AT CANDIDATE FREEZE` (not identified at the tip) | — | — | — | — | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-14 | budget + cost | `E3-15-budget` · **unwired** | `jobBudgetCostBridge` · `server/src/services/job-budget-cost-bridge.ts`; `resolveAuthoritativeRate` | yes | **0**; `resolveAuthoritativeRate` 2 (inside the bridge only) | **Not reachable.** `E3-F037` (HIGH) is open. There is also no usage producer: the supervisor built by `composeDispatchRuntime` receives no `observeRun` (see E4-U). | ★ `cost_events` is a **legacy table with no RLS**. Isolation there is a query predicate. F10 case *"A cannot see B's cost rows"* must be exercised directly: `TO MEASURE AT CANDIDATE FREEZE` | **`JOB-016`** (seam + price at ingest), **`WRK-018`** (usage producer, for closure) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-15 | output / run projection | `E3-17-output` · **unwired** | `jobOutputBridge` · `server/src/services/job-output-bridge.ts` | yes | **0** | **Not reachable.** | `task_outputs` and `artifacts` are legacy tables with no RLS. Isolation there is a query predicate. | **`JOB-017`**; `CLI-014` (M1b) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-16 | activity-audit parity | `E3-audit-parity-bridge` · **unwired** | `jobAuditBridge` · `server/src/services/job-audit-bridge.ts` | yes | **0** | **Not reachable.** | `activity_log` is a legacy table with no RLS. | **`JOB-017`** | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-17 | revocation | `E3-18-revocation` · **wired** | `createExecutionTargetRevocationFanout` · `server/src/services/execution-target-revocation-fanout.ts` | yes | 2 (`index.ts`, one site) | **Reachable behind the flag** (guard-checked). | `execution_target_revocations` has RLS (`0239`) | — | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-18 | operator controls | no register entry | `createJobOperationsService` · `server/src/services/job-operations.ts` (`drainJob`, `revokeWorker`, …) | yes | 1 (`routes/job-control.ts`) | Job-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` (an operator in A acting on B) | `MIG-009` (fleet drain trigger, E10) | `TO MEASURE AT CANDIDATE FREEZE` |
| E3-19 | sweeper / reconciliation / ready scheduler / outbox (supporting) | no register entry | `createJobControlSweeper`, `createJobReconciliationService`, `createJobReadyScheduler`, `createJobOutboxWorker` | yes | 2 / 4 / 2 / 2 (`index.ts`; reconciliation also in `worker-control.ts` and `job-operations.ts`) | Composed in the `index.ts` flag blocks. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | — | `TO MEASURE AT CANDIDATE FREEZE` |
