# E5 — Workspaces and secrets: M1a candidate reachability ledger — FILLED AT CANDIDATE

**Measured at:** **CANDIDATE `7be35ae6b7719877e61f54ab552de84de8491e7d`** (filled 2026-09-23 UTC by the `M1a` QA owner, a distinct
review session, per the README's *Filling a ledger at candidate freeze*). Every caller count in the
`At candidate` column was re-measured at the candidate with `countProductionCallers`
(`scripts/check-gate-clause-wiring.mjs`); no tip value was copied forward. Register statuses are
`scripts/gate-clause-wiring.json` at the candidate, where the guard reports
**`OK (28 wired clause(s), 6 declared dormant, 2 provider-capability claim(s) matched to source)`** —
at the tip it reported 22 wired / 12 dormant.
**Candidate:** `7be35ae6b7719877e61f54ab552de84de8491e7d`
**Campaign evidence this ledger cites** — both records under `../qa/`:
`M1-D1-SPINE` = run `35912752449`, jobs `m1-spine` + `m1-fault-matrix`, at `8c01f4e94f8e25d7abaf524e8b266b809c67b8cb`
(a **tree-equivalent** revision: every product subtree hash is identical to the candidate's; the sole
delta is one added docs file). `M1a-D2-MECHANISM` = run `35920425288`, job `shipped-boot`, at the
candidate exactly.
★ **BOTH gate records carry `Result: fail`** — spine `a15`, mechanism `a18` —
`../qa/2026-09-24-m1-d1-spine-m1a-candidate-7be35ae6b771-a15.md` and
`../qa/2026-09-24-m1a-d2-mechanism-m1a-candidate-7be35ae6b771-a18.md`.
★★★ **The `M1a-D2-MECHANISM` profile of `tests/d1/fault-matrix.json` declares 23 cases and ALL 23 are
`evidence: "pending"` at the candidate**, and `.github/workflows/m1-shipped-boot.yml` contains no
fault-matrix step, so the keyed run fired none of them. Wherever a row below says a clause is
certified by the spine, that is not shorthand for "and also by the mechanism lane".
**Exit gate** (`epics/E5-workspaces-secrets/README.md`): immutable workspace staging, fenced object
commit, patch conflict quarantine, lease-scoped secrets, redaction, denied egress, and the brokered
internal tool surface (DAT-007) pass in D1.
**Columns:** see [README](./README.md). `Certified only by M1-D1-SPINE` is `TO MEASURE AT
CANDIDATE FREEZE` on every row, and it is not repeated in the table.

★ **This ledger does not replace the E5 seven-clause audit.** The clause audit is
`epics/E5-workspaces-secrets/qa/2026-08-24-d0-e5-exit-gate-audit-a1.md`. That `a1` record is
grandfathered as rewritten and is `historical_only`, so its observations are input only. `a2` attests
the `M1a` candidate (matrix frozen by S0-6, `E5-A2-MATRIX`). This ledger records only whether each
mechanism is **reachable**. Whether each clause **passes** is `a2`'s job.

★ **`M1a` entry defers exactly `tools` and `output`** (scope-triage → *Entry criteria*). Rows E5-7
(tool surface) and the output half of E5-2 are therefore `M1b` scope. They are kept here because a
ledger that drops rows cannot show what was deferred.

★★★ **READING THIS FILLED LEDGER.** The `@tip` columns are kept **verbatim** as the S0-7b tip
measurement and are not rewritten; every `TO MEASURE AT CANDIDATE FREEZE` still visible inside them
is answered in the final **`At candidate`** column, which is the candidate measurement. The
skeleton's sentence *"`Certified only by M1-D1-SPINE` is `TO MEASURE AT CANDIDATE FREEZE` on every
row"* is now discharged: that judgement is folded into each `At candidate` cell, which names the
lane (and the fault-matrix case id) that certifies the row, or says no lane does.

| # | Mechanism | Register key · status @tip | Symbol · file | Present | Callers @tip (where) | Production-reachable @tip | Tenant isolation (F10) @tip | M1 tickets that change this row | At candidate |
|---|---|---|---|---|---|---|---|---|---|
| E5-1 | immutable workspace staging (control-plane write) | `E7-1-staged-input-write` · **wired** (an E7 key; the mechanism is E5 staging) | `stageJobInputFiles` · `server/src/services/job-input-staging.ts` | yes | 2 (`server/src/index.ts`, one site: `RunExecutionOwnerDeps.stageJobInput`) | **Reachable behind `AOA_DISTRIBUTED_EXECUTION_ENABLED`** (guard-checked). It is invoked between convert and placement. | Called with `appDb` plus an explicit `organizationId`. `job_artifacts` has kernel RLS (`0211`). F10 case *"A cannot read B's staged inputs"*: `TO MEASURE AT CANDIDATE FREEZE` | `DEP-017` (live env-absence probe on this path, F9) | **2 callers**. Unchanged, `wired`. Staged input is exercised on the mechanism lane (the journey stages input; `M1a` entry requires workspace staging). **Cross-tenant staged-input denial is `M1-D1-SPINE` only**: `d1.tenant.cross.staged_inputs` hostile `409 stale_fence` / own `200 upload_granted`; `d2m.tenant.cross.staged_inputs` is `pending`. |
| E5-1w | immutable workspace staging (worker download grant) | `E7-1-staged-input-grant` · **wired** | `createStagedInputResolver` · `packages/worker-daemon/src/lease/staged-input.ts` | yes | 1 (`lifecycle/dispatch-runtime.ts`) | **Reachable with worker dispatch enabled** (guard-checked). | Server-minted grant per file. `TO MEASURE AT CANDIDATE FREEZE`: B's pointer is refused to A | `DEP-017` | **1 caller**. Unchanged, `wired`. Reached on the keyed lane from a real deployed worker. |
| E5-2s | fenced object commit (server half) | no register entry (the register reason says it is live-proven on D1) | `createArtifactCommitService` · `server/src/services/artifact-commit.ts`; `createArtifactTransferGrantService` · `server/src/services/artifact-transfer-grant.ts` | yes | 1 / 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `TO MEASURE AT CANDIDATE FREEZE` | — | **1 / 1 caller**. Unchanged. **Spine only**: `d1.tenant.cross.outputs` hostile `409 stale_fence` / own `200 committed`, plus `d1.fault.object_store.truncated_upload` → `fenced_commit_refuses_unverifiable_object`. Mechanism lane: nothing. |
| E5-2w | fenced object commit (worker half) | `E5-2-fenced-object-commit-worker-half` · **unwired** | `createArtifactExportSequencer` · `packages/worker-daemon/src/lease/artifact-export.ts` | yes | **0** | **Not reachable.** | n/a while unwired | `DAT-009-3c`, **`DAT-009-3d`** (→ `wired`), `DAT-009-3e` (M1b) | ★ **`createArtifactExportSequencer` 0 → 1 caller**; `E5-2-fenced-object-commit-worker-half` is now **`wired`** (`DAT-009-3d`). No campaign case exercises it on either lane. |
| E5-2r | result commit (worker) | `E5-result-commit-worker` · **unwired** | `createResultCommitter` · `packages/worker-daemon/src/patch/result-commit.ts` | yes | **0** | **Not reachable.** | n/a | none named in M1 §4 | **0 callers**, register still **unwired**. Unchanged. This is one half of why `capabilityProven` is `false` on every keyed tenant. |
| E5-3 | patch conflict quarantine | `E5-3-patch-quarantine` · **unwired** | `createPatchApplyService` · `server/src/services/patch-apply.ts` | yes | **0** | **Not reachable.** | n/a | none named in M1 §4 | **0 callers**, register still **unwired**. Unchanged. Not in `M1a`'s required set. |
| E5-4 | lease-scoped secrets | no register entry | `createSecretBrokerService` · `server/src/services/secret-broker.ts`; `decideExecutionSecretHandle`; `admitSandboxLocalResolution` | yes | 2 (`routes/worker-control.ts`, `services/egress-proxy.ts`) / 1 (`execution-secret-handle-mint-runner.ts`) / 1 (`routes/worker-control.ts`) | Worker-control mount. **Reachable behind the flag.** | `provider_credentials`: `aoa_app` holds table-level `SELECT` (`0259`) with **no RLS**. Scoping is by the locked lease's `company_id` in the query (per the `0259` header). F10 case *"A cannot see B's secrets"*: `TO MEASURE AT CANDIDATE FREEZE` | `DEP-018` (the credential cases are in `M1-D2-CODING`) | **2 / 1 / 1 callers**. Unchanged. ★ **Secret redemption is proven on the mechanism lane**: `reconcile-and-preflight.json` records `credentialAuthority: "company_api_key"` for all three Organizations, and each enabled tenant's env probe lists `redeemedNames: ["ANTHROPIC_API_KEY"]`. Cross-tenant secret denial is **spine only, and partly uncontrolled even there**: `d1.tenant.cross.secrets` classifies on the RLS row read (`foreignScopedRows 0 / ownScopedRows 1`) while its own detail states the fenced ROUTE arm *"carries no control"* because owner and attacker are indistinguishable; `d2m.tenant.cross.secrets` and `d2m.credential.production_reader_company_predicate` are both `pending`. |
| E5-5 | redaction | `E5-5-redaction` · **wired** | `synthesiseRunSecrets` · `packages/worker-daemon/src/lease/secret-redemption.ts` | yes | 1 (`lifecycle/dispatch-runtime.ts`) | **Reachable with worker dispatch enabled** (guard-checked). Per-run canaries are seeded per lease. | Per-run, per-lease canaries. `TO MEASURE AT CANDIDATE FREEZE` | `WRK-018` (the new stdout channel must pass these canaries, H-04) | **1 caller**. Unchanged, `wired`. ★ The new `WRK-018` stdout channel passed the per-run canaries in practice: the keyed job's step *"Scan the evidence AND the job log for job secrets and key material"* succeeded, and no credential class was `present` in either env probe. |
| E5-6 | denied egress | `E5-6-denied-egress` · **unwired** | `createFenceAwareEgressProxy` · `server/src/services/egress-proxy.ts` | yes | **0** | **Not reachable.** The DE-08 managed-shared residual stands: `M1a` observes the dormant-egress residual and makes **no egress-enforcement claim** (exit criterion 5). | n/a | none (the residual is accepted) | **0 callers**, register still **unwired**. ★★★ **And the mechanism lane MEASURED the residual rather than assuming it**: `de08MetadataResidual` on BOTH enabled tenants records `target 169.254.169.254`, **`reachable: true`**, `httpStatus 401`. That is live evidence that H-06's metadata clause is **not** met. `M1a` makes no egress-enforcement claim (criterion 5) and **neither gate record marks H-06 passed**. |
| E5-7 | brokered internal tool surface (DAT-007) | no register entry | `createDistributedRunCurrencyResolver` · `server/src/mcp/distributed-run-currency-resolver.ts` | yes | 1 (`server/src/mcp/server.ts`, in `mcpServerRoutes`) | The resolver is built in `mcpServerRoutes`, which `createApp` mounts **outside** the distributed block. The currency gate is armed by `readDistributedExecutionDeploymentFlag(process.env)`, read once at construction. The **tool surface itself** is armed per run by `readDistributedToolSurfaceFlag(process.env)` in `heartbeatService`, which is **deployment-wide**. **`M1b` scope.** | ★ Two measured facts. (1) The tool-surface switch has **no per-Organization dimension**, so enabling it for one tenant enables it for all (ruling F10). (2) The resolver queries `job_attempts`, `leases`, `execution_targets` and `heartbeat_runs` through the `db` handle passed to `mcpServerRoutes`, not through `runInTenant`. **Its SQL has no predicate on the request's `companyId`**: its only `WHERE` is `eq(heartbeatRuns.id, signedRunId)`, a primary-key probe on the signed run id (`createDistributedRunCurrencyResolver`). The wrong-tenant denial rests on two application predicates: `classifyRunCurrency` (`server/src/mcp/distributed-run-currency.ts`) compares `snapshot.runCompanyId !== companyId` in JavaScript after the read, and the attempt `leftJoin` carries `eq(jobAttempts.companyId, heartbeatRuns.companyId)`. Whether that handle is subject to RLS: `TO MEASURE AT CANDIDATE FREEZE`; `DAT-007-S3` answered it from source plus the boot-enforced `assertPrimaryDbBypassesRls` invariant (the owner pool, RLS bypassed), not from a captured candidate boot. ★ *Corrected 2026-09-21 (M1 corrections batch 1), verified at source; see `epics/E5-workspaces-secrets/tickets/DAT-007-S3-result.md` (S0-8 section). Superseded text: "not through `runInTenant`, with a `companyId` predicate."* | **`DAT-007-S3`** (`M1a`: Tier-3 deny cases + `signedRunId` pin), **`CLI-016`** (`M1b`: per-Organization tool surface) | **1 caller**. Unchanged. `DAT-007-S3-result.md` is `Status: complete` at the candidate. The tool surface is **disarmed on every replica** on both lanes — `tenant-set-and-flags.json` shows `toolSurfaceEnabled: null` / `mustBeOff: "off"` on all four control-plane replica readings, and the spine profile shows `toolSurfaceArmed: false` with `organizationToolSurface` false for all three Organizations. `d2m.tenant.cross.tool_calls` is `pending` **by design** (`M1b`/`CLI-016`). ★★★ **So it needs a gate-owner DISPOSITION, not a runner**: running it would require arming the surface, which would violate the `M1a` freeze checklist. Of the 23 declared `M1a-D2-MECHANISM` cases, **22 need a runner and this one needs a reallocation or a reviewed `pending` disposition.** The switch still has no per-Organization dimension. |
