# E6 — Deployment and test harness: M1a candidate reachability ledger — FILLED AT CANDIDATE

**Measured at:** **CANDIDATE `7be35ae6b7719877e61f54ab552de84de8491e7d`** (filled 2026-09-23 UTC by the `M1a` QA owner, a distinct
review session, per the README's *Filling a ledger at candidate freeze*). Every caller count in the
`At candidate` column was re-measured at the candidate with `countProductionCallers`
(`scripts/check-gate-clause-wiring.mjs`); no tip value was copied forward. Register statuses are
`scripts/gate-clause-wiring.json` at the candidate, where the guard reports
**`OK (28 wired clause(s), 6 declared dormant, 2 provider-capability claim(s) matched to source)`** —
at the tip it reported 22 wired / 12 dormant.
**Candidate:** `7be35ae6b7719877e61f54ab552de84de8491e7d`
**Campaign evidence this ledger cites** — both records under `../qa/`:
`M1-D1-SPINE` = run `35912752449`, jobs `m1-spine` + `m1-fault-matrix`, at `8c01f4e94f8e25d7abaf524e8b266b809c67b8cb`
(a **tree-equivalent** revision: every product subtree hash is identical to the candidate's; the sole
delta is one added docs file). `M1a-D2-MECHANISM` = run `35920425288`, job `shipped-boot`, at the
candidate exactly.
★ **BOTH gate records carry `Result: fail`** — spine `a7`, mechanism `a8` —
`../qa/2026-09-23-m1-d1-spine-m1a-candidate-7be35ae6b771-a7.md` and
`../qa/2026-09-23-m1a-d2-mechanism-m1a-candidate-7be35ae6b771-a8.md`.
★★★ **The `M1a-D2-MECHANISM` profile of `tests/d1/fault-matrix.json` declares 23 cases and ALL 23 are
`evidence: "pending"` at the candidate**, and `.github/workflows/m1-shipped-boot.yml` contains no
fault-matrix step, so the keyed run fired none of them. Wherever a row below says a clause is
certified by the spine, that is not shorthand for "and also by the mechanism lane".
**Exit gate** (`epics/E6-deployment-test-harness/README.md`): full D1 isolated topology,
fake/reference provider isolation suite, MinIO, Toxiproxy, CI evidence, staging manifests,
two-replica HA, and distributed telemetry pass. Real E2B conformance is the CLI-001/D2 gate, not an
E6 prerequisite.
**Columns:** see [README](./README.md). `Certified only by M1-D1-SPINE` is `TO MEASURE AT
CANDIDATE FREEZE` on every row, and it is not repeated in the table.

★ **E6 has no `gate-clause-wiring` entries.** Its mechanisms are harness and deployment, not
production symbols. For E6, **"production-reachable" means exercised by a CI lane on the program
branch without an operator dispatch.** An operator-dispatched workflow is recorded as such, and
ruling F3 excludes it from "shipped CI boot".

★ **The last D1 lane run at the tip is not tip evidence.** The last `d1-merge-train` success on
`docs/replatform-program` is run `35504786263`, job `d1-merge-train`, at `52626d80e9fc`. That SHA
is an ancestor of the tip, but **19 non-doc files differ** between the two
(`git diff --stat 52626d80e9f <tip> -- ':!docs'`). The lane is path-triggered (`on.push.paths`),
so it did not re-run. The runs before it, `35238458091` (2026-09-17) and `35017820850`
(2026-09-15), **failed**.

★★★ **READING THIS FILLED LEDGER.** The `@tip` columns are kept **verbatim** as the S0-7b tip
measurement and are not rewritten; every `TO MEASURE AT CANDIDATE FREEZE` still visible inside them
is answered in the final **`At candidate`** column, which is the candidate measurement. The
skeleton's sentence *"`Certified only by M1-D1-SPINE` is `TO MEASURE AT CANDIDATE FREEZE` on every
row"* is now discharged: that judgement is folded into each `At candidate` cell, which names the
lane (and the fault-matrix case id) that certifies the row, or says no lane does.

| # | Mechanism | Symbol / artefact | Present @tip | Exercised by a non-dispatch CI lane @tip | Tenant isolation (F10) @tip | M1 tickets that change this row | At candidate |
|---|---|---|---|---|---|---|---|
| E6-1 | full D1 isolated topology | `docker-compose.d1.yml`, services `postgres`, `minio`, `toxiproxy`, `migrate`, `control-plane`, `control-plane-b`, `worker-a`, `worker-b`, `fake-provider`, `test-runner` | yes | `d1-merge-train.yml` (push to `main` / `docs/replatform-program`, **path-filtered**). The last success is at `52626d80e9fc` (see above). | ★ **Single-Organization at the tip.** `AOA_DISTRIBUTED_EXECUTION_ROLLOUT` is not set in `docker-compose.d1.yml`. `worker-a.profile.json` is platform-scoped (`organizationId: null`), and `worker-b.profile.json` is bound to one Organization. **The F10 topology (≥2 enabled Organizations plus 1 control) does not exist in D1.** | **`DEP-016`** (the `m1-spine` one-worker profile, evidence retained on pass), **`DEP-018`** (F10 tenant matrix) | ★ **The F10 topology now exists, on BOTH lanes.** Spine (`DEP-016`/`DEP-019`, run `35912752449`): two enabled Organizations plus one control, `executor: "worker"`, with a red control *"with the deployed worker NOT the executor, the claim MUST NOT stand"*. Mechanism (run `35920425288`): three Organizations, three workers, three targets (`tenants.json`, `workers.json`, `targets.json`), rollout `canary/canary/off`. |
| E6-2 | fake/reference provider isolation suite | `docker/d1/fake-provider-entry.mjs`, `fake-provider.Dockerfile`; `packages/sandbox-fake-provider` | yes | through E6-1 | `TO MEASURE AT CANDIDATE FREEZE` (cross-resource denial, DEP-008) | — | Exercised through E6-1 on the spine. The mechanism lane uses **real E2B**, not the reference provider: `E2B_SANDBOX_ID_SHAPE` (`scripts/lib/m1-shipped-boot.mjs`) rejects every test-double id shape, and both observed ids pass it. |
| E6-3 | MinIO | compose service `minio` (`docker/d1/minio-init.sh`) | yes | through E6-1 | Object keys and prefixes per Organization: `TO MEASURE AT CANDIDATE FREEZE` | — | MinIO booted on both lanes. Mechanism lane: `presign-probe.txt` reads `PRESIGN_PROBE_OK status=200` from the adapter-manager's seat. Per-Organization key separation is **not** separately asserted on either lane. |
| E6-4 | Toxiproxy | compose service `toxiproxy` (`docker/d1/toxiproxy.json`) | yes | through E6-1 | n/a | **`DEP-018`** (a fault matrix per profile, with each injection shown to fire) | Toxiproxy exercised on the spine only: `d1.fault.link_cut.worker_to_control_plane` and `d1.fault.link_cut.control_plane_to_postgres` both fired. ★ Both report the **same** `observedClassification` in the suppressed-injection arm, so for those two the suppression control discriminates through `injectionFired` alone, not through the observed outcome. |
| E6-5 | CI evidence | `ci-required` aggregator (`.github/workflows/pr.yml`); the DEP-013 verdict consumer; D1 evidence retention | yes | `pr.yml` on every PR. The M1 plan §1.3 states that D1 keeps evidence **only on failure** (not re-measured here). | n/a | **`DEP-016`** (retain on pass) | Evidence is now retained **on pass** on both lanes (`DEP-016`): the spine uploaded a `passing` bundle before any control mutated the stack, and the keyed lane uploaded 22 files on success. |
| E6-6 | staging manifests | `scripts/check-staging-manifest.mjs` (the `policy` job in `pr.yml`) | yes | `policy` job on every PR | `checkDispatchDefaultOff` (`staging-manifest-invariants.mjs`) rejects the worker's `AOA_WORKER_PROVIDER_URL` (M1 plan §1.1, not re-measured here) | **`DEP-015`** (an amendment scoped to the CI-boot overlay only) | ★ The `DEP-015` overlay amendment is live and **scoped**: `manifest-check-pre-boot.txt` and `manifest-check-post-rollout.txt` both read *"rendered shipped boot satisfies DEP-015 (scoped), and the unscoped default-off check reds it (6 violation(s)) — the admission is scoped"*. The unscoped `checkDispatchDefaultOff` still reds, which is the positive control. |
| E6-7 | two-replica control-plane HA | compose service `control-plane-b` (DEP-009) | yes | through E6-1 | n/a | — (the M1 spine is one control plane) | Two control-plane replicas booted on the mechanism lane (`control-plane` and `control-plane-b`; `tenant-set-and-flags.json` asserts the tenant set on **both**, rendered and running). No HA failover case runs on either lane. |
| E6-8 | distributed telemetry | DEP-007 baseline | `TO MEASURE AT CANDIDATE FREEZE` | `TO MEASURE AT CANDIDATE FREEZE` | per-Organization attribution: `TO MEASURE AT CANDIDATE FREEZE` | — | **Still not measured.** Neither campaign records a per-Organization telemetry assertion. Uncertified by both gates. |
| E6-9 | adapter-manager image and boot | `packages/adapter-manager`; `docker/adapter-manager/` | source yes | ★ **No.** `docker-compose.d1.yml` and `docker/images/build.sh` contain **zero** occurrences of `adapter-manager` (measured with `grep -c`). Only the operator-dispatched `deploy-replatform-campaign.yml` deploys it (M1 plan §1.1). | Worker→adapter-manager mTLS is not built (M1 plan §1.1, not re-measured here) | **`DEP-014`** (image built and pushed by CI; the D1 train builds it) | ★ **Built and booted.** `candidate.json` records `localhost/aoa/adapter-manager:7be35ae6b…`, digest `sha256:bba7540e4b34a25e0b5ba564b5c3ac363978bd4e4ba89ea5e0af8232d35f70ff`, built in-job from the candidate; `logs-adapter-manager.txt` is retained. `DEP-014-result.md` is `Status: complete`. **Worker→adapter-manager mTLS is still not built** — a named residual (M1 plan §8). |
| E6-10 | shipped CI boot (ruling F3) | — | **no** | **no.** No workflow builds the control-plane, worker and adapter-manager images together and runs the journey in the boot they form. | must boot the F10 three-Organization topology | **`DEP-015`** | ★ **Exists and ran.** `.github/workflows/m1-shipped-boot.yml`, `workflow_dispatch` only, bound to a named candidate (steps *"Validate the named candidate"* and *"Bind the run to the candidate"*), building all three images from source and generating an ed25519 control-plane keypair in-job (`candidate.json` `keypair.generatedInJob: true`, `signVerifyProbe: "pass"`). This satisfies ruling F3. `DEP-015-result.md` is **`Status: complete`** at the candidate (set by the distinct reviewer of attempt 2, re-affirmed at attempt 3). ★ *Corrected 2026-09-23: this cell first read `gate_review`, which mistook the record’s quoted, deliberately preserved ORIGINAL status line for its live one.* |
| E6-11 | live env-absence probe (criterion 5, F9) | — | **no** | **no** | a planted canary must turn the probe red for **each** tenant | **`DEP-017`** | ★ **Built and fired, with its positive control red.** Per enabled tenant: `verdict: "absent"`, **18** classes checked, `present: []`, and `plantedControl.red: true` with `DATABASE_URL`→`datastore_credential`, `OPENAI_API_KEY`→`cross_tenant_credential`, `ANTHROPIC_API_KEY`→`provider_credential_value_mismatch`, each `satisfied: true`. ★ The participating Organization's own redeemed key IS present (`allowedPresent: ["ANTHROPIC_API_KEY"]`) — that is the accepted DE-08 managed-shared model, not a leak. **No probe ran for the control tenant** (`envProbe: null`), which is structural: no sandbox is created. ★ **And this is not the only lane that observes criterion 5**: `M1-D1-SPINE` observes it for its ONE worker-driven enabled tenant inside the reference sandbox, with `scope: "stage_in_env_only"`, and records its other enabled tenant `observed: false` under a two-directional tripwire (`evaluateSpineEnvProbe` / `evaluateEnvProbeObservability`, `scripts/lib/m1-spine-assertions.mjs`) — `E6-D002` and M1 plan §6. *Corrected 2026-09-23 after review.* `DEP-017-result.md` is `Status: gate_review` at the candidate. |
| E6-12 | real E2B lane | `.github/workflows/keyed-e2b-*.yml` | yes | **no, not as a CI boot.** All seven `keyed-e2b-*.yml` workflows have `workflow_dispatch` plus a `push` trigger that is path-filtered to a dedicated trigger file (for example `.github/keyed-e2b-trigger`) or to the workflow file itself. Some are also filtered to specific branches. None runs on an ordinary program commit. Each run is a deliberate, spend-bearing action that requires an F8 named-list authorization. None boots the control plane, worker and adapter-manager together (E6-10). | `TO MEASURE AT CANDIDATE FREEZE` | `DEP-015`, `E7-1-JOURNEY-ARM` (F8 list) | ★ The keyed lane is no longer only the seven `keyed-e2b-*.yml` workflows: `m1-shipped-boot.yml` boots control plane, three workers and the adapter-manager together and runs the journey on real E2B (run `35920425288`, `mode: keyed`; a keyless rehearsal of the same candidate is `35919374111`). ★★★ **It still fires none of the 23 declared `M1a-D2-MECHANISM` fault-matrix cases.** |
