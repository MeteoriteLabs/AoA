# Handoff — M0, record and lane health

**Date (UTC):** `2026-09-21`
**Milestone:** `M0`
**Record path:** `docs/replatform/milestones/M0/handoffs/2026-09-21-M0-0c6ad7c13535-a1.md`
**Gate slug:** `m0`
**Reviewed revision:** `0c6ad7c13535fc71d8a6d9848ca1c137701fa52b`
**Attempt:** `1`
**Supersedes:** `none`
**Decision:** `pass`
**Gate owner role:** `Integration Gate Owner`
**Gate owner identity:** `founder (gate owner)`

> ★★★ **NON-PROMOTING.** This handoff records the gate owner's decision on milestone `M0` at one
> exact revision. It **flips no epic's status** — epic completion requires that epic's own normative
> gate, and only the Integration Gate Owner changes one. It does not use `epic-completion` in its
> name, and nothing in it may be read as completing E3, E4, E5, E6, E7 or E10.

> This file is immutable from its first commit. A correction, rerun, changed decision or changed
> revision creates a higher attempt and links this path through `Supersedes`.

**Prepared by:** the M0 execution session (Claude Opus 5) — the **evidence** is the preparer's; the
**Decision** above is the gate owner's alone.

**Decision recorded:** `pass`, by the founder as gate owner, 2026-09-21. The founder reviewed the
execution session's report together with the planning session, which checked its claims at source,
and the decision was relayed from the planning session to the execution session.

---

## Why there is no QA record

`M0` names **no gate** — its row in `epic-regrooming/scope-triage.md` → *The milestone sequence*
reads *"(no gate — entry criteria for M1a)"*. Exit criterion 8 is scoped by the **naming relation**
(QA records for *each partial gate that milestone names*), so `M0` owes **zero** milestone QA
records. `EVID-04` (`test-gates.md`) constrains only the **`qa/`** record path, so a handoff under
`milestones/M0/handoffs/` conforms today without any gate amendment. `EVID-04` remains blocking for
`M1a` and `M2-RTF`.

★ **No guard validates this record's filename.** `check-evidence-immutability.mjs` and
`check-distributed-execution-foundation.mjs` both walk `milestones/<m>/{qa,handoffs}/` and enforce
immutability from first commit; neither checks the `<date>-<slug>-<sha12>-a<attempt>` shape. The
name was checked by hand against `milestones/README.md`.

## Included ticket results

All eight were approved by a reviewer **distinct** from the implementing session (PR #527, merged
`9793e48d6`). Blob SHAs are read at the reviewed revision.

| Ticket | Ticket-result path | Ticket-result Git blob SHA | Reviewed implementation SHA | Latest review disposition |
|---|---|---|---|---|
| `MIG-009-B1` | `docs/replatform/epics/E10-desktop-migration-realtime/tickets/MIG-009-B1-result.md` | `241fb3f7670c425b188f12015b518a85e912c18f` | `bbfc7cfbc0e0a58e86c9bd0c50af56960bd24b6e` | `approved` |
| `MIG-010` | `docs/replatform/epics/E10-desktop-migration-realtime/tickets/MIG-010-result.md` | `782cadbd059371ced0a9824459a843f508620962` | `6f9031220bd2a20a6485b83a5b2b74cf6b5782d0` | `approved` |
| `WRK-017-B1` | `docs/replatform/epics/E4-worker-daemon/tickets/WRK-017-B1-result.md` | `296990960b29ac3c528245a4294334a643d3808d` | `1ed64453994ab739c4553e8ddda1b4f799919f7e` | `approved` |
| `DAT-008-A1` | `docs/replatform/epics/E5-workspaces-secrets/tickets/DAT-008-A1-result.md` | `0177558c74e5001aa11b777caf8c8f4afa29529b` | `5f3b47556d0df152db0d53d76c304861f30ffd37` | `approved` |
| `DAT-011-B1` | `docs/replatform/epics/E5-workspaces-secrets/tickets/DAT-011-B1-result.md` | `c705a453a1da782ad4fd393dc197766fd2bd7549` | `51d753d15379e9ee7ab8da37d12f681f9415f46f` | `approved` |
| `TRACK-001-B1` | `docs/replatform/epics/E5-workspaces-secrets/tickets/TRACK-001-B1-result.md` | `178ec785ebf361484e26dd057a6761c6c79ae57e` | `5f3b47556d0df152db0d53d76c304861f30ffd37` | `approved` |
| `DEP-008-B1` | `docs/replatform/epics/E6-deployment-test-harness/tickets/DEP-008-B1-result.md` | `90466d84bfcbd82ab0dba47f64de3cb41b7d4ec1` | `5f3b47556d0df152db0d53d76c304861f30ffd37` | `approved` |
| `TRACK-002-B1` | `docs/replatform/epics/E6-deployment-test-harness/tickets/TRACK-002-B1-result.md` | `5261d6a3727fa3caa6e6ab91d6a0956e521f5549` | `5f3b47556d0df152db0d53d76c304861f30ffd37` | `approved` |

★ `DAT-008-A1` is the **disposition-A** correction, listed here because it carries a result record;
the other seven are disposition-B currency records. `DEP-013` discharges through the consumer clause,
and `MIG-010` through its own result, as `scope-triage.md` specifies — together all eight B members.

## QA evidence

| QA record | QA revision | Lane | Attempt | Result |
|---|---|---|---:|---|
| none — `M0` names no gate (see above) | — | — | — | — |

## Threshold decision — every M0 exit clause

Each clause is quoted from `scope-triage.md` → `M0` → **Exit**, and each was measured at the
reviewed revision `0c6ad7c13535`, not carried from an earlier state.

| Requirement | Class | Required | Observed | Evidence | Decision |
|---|---|---|---|---|---|
| **M0-1** `d1-merge-train` and every keyed lane | REQUIRED | green, or explicitly quarantined with an owner | **all green.** Latest completed run on `docs/replatform-program`: `d1-merge-train` `35504786263` success; `keyed-e2b-conformance` `33788025048`; `keyed-e2b-cdp-probe` `32634043999`; `keyed-e2b-egress-constraint-probe` `33878657069`; `keyed-e2b-unit-d` `35561906027` (**6/6**, key present — the lane M0 repaired); `keyed-e2b-w7u1-output-probe` `34087197668`; `keyed-e2b-w10b-egress-enforcement-probe` `34085130892`; `keyed-e2b-dat-009-export` `34341130645` — all `success`. And the consumer reports **no finding on any of them** at this revision (row M0-2), i.e. none owes an uncovered commit. | runs above; consumer run `35574256850` | `pass` |
| **M0-2** the DEP-013 consumer | REQUIRED | zero unowned findings; a `main`-only stream is satisfied by being recorded **blocked on the integration checkpoint, with a named owner** | **`Findings: 1 · Unowned: 0`.** The one finding is `cross-platform-weekly.yml@main`, `not_success`, **owned by `founder (gate owner)`, blocked on the program integration checkpoint (M5)**. Published by run `35574256850`, job `reconcile` [`106252416866`], step *"Reconcile workflow verdicts and publish the consumed artifact"* → `success`; read back from issue #358 (`"unownedCount":0`, `"runId":"35574256850"`). | issue #358; `scripts/workflow-verdict-manifest.json` → `cross-platform-weekly.yml@main.blocked` | `pass` |
| **M0-3** disposition-A record corrections | REQUIRED | landed | **landed.** All 20 A tickets audited with a verdict each; four epic README range claims narrowed (E3, E4, E5 via `DAT-008-A1`, E6); `E5-F004` and `E6-F022` filed and declared. | `docs/replatform/qa/2026-09-21-disposition-a-audit.md`; `DAT-008-A1` above | `pass` |
| **M0-4** the `E7-F007` successor | REQUIRED | filed (**D-10**), so `MIG-010` can carry a result | **filed.** `MIG-011` has a design file and a `#### MIG-011` program node; `scripts/finding-ownership.json` → `E7-F007` `owned` → `MIG-011`. Proven by positive control: reverting the re-point reds the ownership guard with `owner_ticket_already_complete` + `successor_missing`. | `tickets/MIG-011-design.md`; `MIG-010-result.md` §3.1 | `pass` |
| **M0-5** every disposition-B ticket | REQUIRED | an approved, candidate-current result — `TRACK-001`, `TRACK-002`, `DAT-011`, `DEP-008`, `WRK-017`, `MIG-009`'s evidence-currency record, and `MIG-010` | **all `complete` / `approved`** by a distinct reviewer — see *Included ticket results*. | PR #527 | `pass` |

★ **The guard state at this revision:** the 38 pure-node `pr.yml` guards plus
`check-evidence-immutability.mjs --base origin/docs/replatform-program` are green.

## Decisions and findings

**Rulings taken during M0** (founder, 2026-09-20/21): keyed E2B spend authorised for the
`keyed-e2b-unit-d` repair; `cross-platform-weekly` repaired inside M0; M0 Step 0 filed the untasked
B records; the `CLI-008` link-scoped ids renumbered to `CLI-010`…`CLI-016`, re-pointing only the two
findings that genuinely map; criterion 3 by per-ticket audit of all 20; criterion 2 amended to
*blocked on the integration checkpoint, with a named owner*; `E6-F023` option 3; the DEP-013
consumer given a real `blocked` state rather than `not-watched`; the checkpoint-blocked stream owned
by the founder.

**Findings open at this revision that a reader of this handoff should know about** (none is an M0
exit item):

| Finding | Severity | What |
|---|---|---|
| `E0-F019` | LOW | `check-finding-ownership` never reads a finding's prose `Owner:` line — its fix is a gate-owner choice |
| `E5-F003` | MEDIUM | the approved-workspace-root check compares lexically while git resolves symlinks; fails closed |
| `E5-F004` | LOW / MEDIUM | `DAT-008` slice 6 mint-side only; placement-side residual |
| `E5-F005` | LOW | a family of two real-process runtime-service races on Windows |
| `E3-F039` | LOW | JOB-003's deadline case is a proven macOS flake |
| `E6-F022` | LOW | E6's range claim asserted `DEP-011` shipped; it has no result doc |

**Resolved in M0:** `E7-F037` (the keyed lane's red was misattributed), `E6-F023` (the weekly lane's
false green — option 3, verified against four controls).

## Known follow-ups — NOT exit items

These were raised in the execution report. The gate owner decided `pass` with each one carrying the
disposition below. **None of them is an M0 exit clause, and none is filed by this handoff.** The
planning session owns filing the plan items.

| # | Follow-up | Disposition |
|---|---|---|
| 1 | **No guard enforces `E6-F023`'s new workflow shape.** The only `continue-on-error` assertions in any guard read `verdict-reconcile.yml` and `pr.yml`. Re-adding the flag to `verify-cross-platform`, `e2e-cross-platform` or the `Install Playwright` step would silently restore the false green while every guard stays green. | **`M1a` Step 0** — filed into the plan by the planning session. |
| 2 | **Controls 3 and 4 were folded into one run**, as permitted. That run's `failure` has two causes at once (`verify` and `e2e` both failed), so it does not *isolate* `verify`'s contribution to the run verdict. `verify`'s own job failure was observed. That `verify` alone would fail the run is inferred from the parsed YAML plus controls 1 and 2, which show failing advisory shards leave the run `success`. | **Accepted as-is.** |
| 3 | **The M5 block must come off.** Criterion 2 is met by the block on `cross-platform-weekly.yml@main`, **not** by that lane being healthy. `@main` still runs the old all-advisory workflow until the program branch reaches `main` at M5, so a green scheduled run there still means nothing. The manifest's `reason` says the block comes off at M5, but nothing enforces its removal. | **Onto `M5`'s exit list** — filed into the plan by the planning session. |
| 4 | **No guard validates milestone record filenames.** `check-evidence-immutability.mjs` and `check-distributed-execution-foundation.mjs` both walk `milestones/<m>/{qa,handoffs}/` and enforce immutability only; neither checks the `<date>-<slug>-<sha12>-a<attempt>` shape. This record's name was checked by hand against `milestones/README.md`. | **A finding only** — not a plan item. It is **not filed by this handoff**; this row records the disposition, not a finding id. |

## Compatibility and rollout

No schema, protocol or migration change in M0's final PRs. The DEP-013 marker gains an additive
`unownedCount` key, which nothing asserts the key set of. The `cross-platform-weekly` change takes
effect on `docs/replatform-program` now and on `main` only at M5. Rollback: revert the merge commit
of PR #528 (`0c6ad7c13535`) or PR #525 (`5f3b47556`); both are documentation-, test- and CI-only apart
from the consumer change, which is a tooling script.

## Next unblocked work

`M1a` (*the spine*). Its Step 0 files the four tickets its required-result set marks `TO FILE`:
`WRK-013`; `DEP-011`'s remaining deploy half (scope not yet established); and the three parity-bridge
consumers plus the `observeRun` usage producer (D-8). `EVID-04` must be amended before any `M1a`
milestone QA record can be filed — a gate-owner action.
