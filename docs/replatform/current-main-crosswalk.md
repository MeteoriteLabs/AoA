# Current-main Execution Crosswalk

**Observed current-main baseline:** `003492988269a91eadfadb352bff7f413fa61adb` (includes PR #320 Cloud Execution Isolation at `861134f6e5d8d95de2484a44fadc5818062b2049` and the subsequent explicit migration-0188 deployment marker flow)

**Execution freeze:** This program executes against a **frozen main revision**. Before E0 begins, main is frozen and its exact SHA is recorded here (superseding the observed baseline above if main has advanced). Every gate, crosswalk-row closure, and ticket verifies against that pinned SHA fetched from `origin/main` — never a local checkout, which may lag origin and lack the characterized code. Advancing the frozen SHA is a Migration-Custodian decision recorded with its rationale, and it re-opens any crosswalk row whose characterized code changed.

**Purpose:** Freeze the execution paths that exist before the distributed worker re-platform and assign every path an explicit bridge, cutover, disablement, drain, rollback, and evidence owner. This is an observed migration inventory, not a new architecture authority. Locked decisions and [`program-design.md`](program-design.md) remain controlling.

## Reading rule

No ticket may replace a row by inference. Its owning migration ticket must characterize the observed path on the frozen baseline, implement the named disposition, prove that shadow mode causes no effects, identify active-work handling, and retain rollback and hard-negative evidence. Until that ticket passes, the current owner remains authoritative. There is never a permanent dual writer or a silent host fallback in `cloud_auth`.

## Execution and lifecycle sinks

| Row ID | Current sink | Observed current authority and implementation | Target disposition | Owning ticket IDs | Required cutover and evidence |
|---|---|---|---|---|---|
| CM-001 | Decision #117 target registry and route-by-credential | `execution_targets`, `environments.execution_target_id`, the execution-target resolver, and the gVisor/dedicated/local-host/provider-sandbox seams currently select an execution owner and credential-compatible target before PR #320 dispatch. | Make JOB-009 the only placement authority, mapping every legacy target/profile/credential/locality/fallback rule or explicitly blocking it; retain the current owner for active attempts. | JOB-009, MIG-001, MIG-002, MIG-008 | Complete mapping table; non-effecting shadow decision comparison; atomic per-Organization/workload ownership flip; active-owner retention and drain; unmapped/unsafe/implicit fallback denial; rollback to one scheduler; no double lease or provider resource. |
| CM-002 | Organization heartbeat/agent run | The existing heartbeat/run services resolve the execution target, acquire an environment lease, stage a workspace, and broker `cloud_auth` execution into E2B with no host fallback. Current policy may use ephemeral leases or warm per-agent reuse, including the software-development default/override; prose that describes all Organization runs as ephemeral is stale. | Bridge the domain run into one distributed job; preserve the domain run as product authority and make the job path the sole executor only after per-Organization/workload cutover. | CLI-005, JOB-010, JOB-011, JOB-012, JOB-013, JOB-014, MIG-002, MIG-008 | Envelope/provenance and policy shadow comparison; atomic checkout/assignee/approval/budget/cost/audit/output parity; one executor; warm and ephemeral lease drain; rollback creates no second attempt; self-hosted legacy path remains available for non-migrated tenants. |
| CM-003 | Crew run | Crew dispatch currently uses the same cloud broker/E2B boundary but has its own trigger, roster, run, completion, cost, and audit behavior; cloud runs are ephemeral. | Add a crew-specific bridge and cutover instead of assuming heartbeat parity. | MIG-006, JOB-010, JOB-011, JOB-012, JOB-013, JOB-014, MIG-002 | Characterize roster/team identity and trigger; source union round trip; completion/cost/activity/output parity; non-effecting shadow; active-run drain/cancel; no duplicate crew dispatch; self-hosted preservation. |
| CM-004 | Commander turn and warm conversation sandbox | Decision #120 and `environment_leases.commanderConversationId` govern a warm paused/resumed per-conversation E2B lease in `cloud_auth`; the instance flag may choose ephemeral-per-turn behavior and the warm reaper bounds idle leases. | Migrate turns and warm-lease ownership to distributed jobs without destroying continuity or allowing the old and new owners to resume the same conversation resource. | MIG-005, JOB-010, JOB-011, JOB-012, JOB-013, JOB-014, MIG-008 | Conversation/turn provenance; warm versus ephemeral equivalence; compare-only shadow; atomic new-turn cutover; in-flight turn and paused-lease drain; old-owner fence denial; reaper/TTL parity; rollback to one owner; zero leaked sandboxes. |
| CM-005 | One-shot extraction | The discussion path still performs a host-`PATH` CLI presence probe before the cloud branch, even though `cloud_auth` then resolves the Company's model-provider key and calls the sandbox provider runtime directly rather than MCP. Extract-then-scope may pre-acquire one batch sandbox handle, reuse it across bounded entries, and release it separately. The operation is not necessarily backed by an issue run. | Remove the stale host-CLI gate from `cloud_auth`; admit against the configured sandbox adapter/provider instead. Submit an explicit `one_shot/extraction` source while preserving claim/idempotency and bounded batch reuse/release. | MIG-007, MIG-008, JOB-010, JOB-011, JOB-012, JOB-013, JOB-014 | Cloud extraction succeeds without a host CLI when Company/provider/sandbox policy is valid; atomic claim/replay parity; typed actor; Company-key audience and containment; batch handle acquire/reuse/release and partial-failure cleanup; cost/audit/result parity; effect-free shadow; drain/cancel; no host fallback. |
| CM-006 | One-shot compaction | The current compaction caller uses the same direct one-shot sandbox/provider path in `cloud_auth` and host-direct execution in self-hosted modes. | Submit an explicit `one_shot/compaction` execution source. | MIG-007, JOB-010, JOB-011, JOB-012, JOB-013, JOB-014 | Input/output hash parity; actor and company scope; credential containment; shadow without model/provider effects; bounded retry; active-operation drain; rollback and self-hosted preservation. |
| CM-007 | Readiness/auth probe | The current one-shot path sandboxes supported `claude_local` and `codex_local` readiness/auth probes in `cloud_auth`; unsupported adapters remain explicit fail-closed. A probe may have no issue or domain run and does not use the MCP broker. | Submit an explicit `one_shot/readiness_probe` execution source for the supported matrix; preserve explicit denial for unsupported adapters. | MIG-007, JOB-010, JOB-011, JOB-012, JOB-013, JOB-014 | Provenance without fabricated `runId`/`issueId`; supported/unsupported adapter matrix; Company-key audience and zero-secret evidence; timeout/cleanup; no host fallback; clear rollback behavior. |
| CM-008 | Workspace staging, output, patch, artifact, and preview host | Current environment/workspace services stage files through `sandbox-provider-runtime.ts`, persist execution-workspace/environment-lease references, read results, and resolve provider preview hosts. | Move bytes through immutable manifests and fenced object operations; keep Git/object-store/control-plane authority and preserve current user-visible output semantics. | DAT-001, DAT-002, DAT-003, CLI-002, CLI-003, JOB-013, JOB-014, MIG-005, MIG-006, MIG-007 | Base/hash and path equivalence; transactional audit and explicit output/run-summary projection; preview authorization; stale output only through quarantine; no cross-tenant/provider-host leakage; active-work drain; rollback never auto-applies orphan output. |
| CM-009 | MCP/connector broker, stdio connectors, and run-JWT callback | PR #317's control-plane broker remains OAuth/token authority. PR #320 resolves allowed HTTP/OAuth connector material and run-JWT callback authority on the host, stages scoped values into Organization/crew/Commander sandboxes, and executes verified stdio connectors inside E2B. Warm resume must not retain revoked or rotated authority. | Keep refresh/revocation and HTTP broker authority in the control plane; send only lease/fence-scoped opaque handles/materialization grants, run stdio connectors inside the job sandbox, and re-resolve on every new lease/resume. Never create a second OAuth store. | DAT-004, DAT-005, CLI-002, CLI-005, MIG-005, MIG-006, MIG-007 | Connector allowlist and Company/actor scope; token/refresh/run-JWT redaction; rotation/revocation and warm-resume re-resolution; stdio child containment; broker reachability/audience; stale-fence denial; rollback and self-hosted continuity. |
| CM-010 | E2B provider lifecycle | `server/src/services/sandbox-provider-runtime.ts` currently creates, connects/resumes, executes, stages files, resolves preview hosts, pauses, kills, and destroys E2B resources. Provider operations and credentials therefore still live on the current server-side execution path. | Move provider lifecycle authority behind the separately deployed worker adapter and its monotonic cleanup authority. | CLI-001, CLI-004, MIG-008, DEP-006, DEP-008 | Current/new provider-contract comparison; pinned template/policy/limits; provider credential absent from control plane and tenant sandbox after cutover; lost-response/idempotent cleanup; kill switch; old credential denial; final zero-resource reconciliation. |
| CM-011 | Environment leases and in-flight resources | `packages/db/src/schema/environment_leases.ts` records Company, environment, workspace, issue/run/agent/Commander identities, provider lease, active/paused/released state, timestamps, and cleanup metadata. Startup/scheduled reapers clean orphaned and warm resources. | Reconcile legacy lease rows and resources into the new job/attempt/lease/resource model or terminal migration records; do not reinterpret a live legacy fence as a distributed fence. | MIG-008, CLI-004, JOB-006 | Inventory by status/provider/owner; immutable mapping record; pause/resume and reaper behavior; atomic stop-new-work; bounded drain; resource-label reconciliation; rollback only before ownership transfer; zero live unmapped resource at closure. |
| CM-012 | Provider-control credential location | PR #320 reads E2B configuration/API authority in the current provider runtime. The re-platform contract requires provider-control credentials only in the worker/provider-management boundary. | Rotate/move the credential during provider cutover; never serialize it into protocol, tenant env, artifacts, logs, support bundles, or the hosted control-plane process. | DEP-006, CLI-001, MIG-008, REL-004 | New/old key overlap and cutoff; old-key denial; current cleanup authority survives rotation; control-plane/sandbox/env/metadata scans; revocation and provider-kill rehearsal. |
| CM-013 | Tenant model-provider credential resolution and sandbox materialization | Heartbeat, crew, Commander, and one-shot paths resolve Company model-provider credentials and adapter configuration on the control-plane host, then inject or stage scoped values for CLI execution inside E2B. This is distinct from MCP OAuth tokens and the E2B provider-control credential. | Keep encrypted model-credential resolution/rotation authority in the control plane, but issue only job/attempt/lease/fence/Company/principal-scoped sandbox materialization and egress authority; re-resolve on every new lease and warm resume. Never place the value in common protocol, prompt, argv, logs, artifacts, or evidence. | DAT-004, DAT-005, CLI-002, CLI-005, MIG-005, MIG-006, MIG-007 | Adapter/provider/audience matrix; direct-materialization and egress boundary; wrong Company/principal/target denial; rotation/revocation and warm-resume re-resolution; secret-canary scans; active-work drain; self-hosted preservation; rollback without a second credential store or stale grant. |
| CM-014 | Decision #117 per-Organization concurrency clamps | The current cloud path combines per-agent slots with `ORG_MAX_CONCURRENT_RUNS_*` per-Organization limits, atomically claims capacity, queues excess work, wakes queued work on release, and must release failed or terminal claims once. | Make JOB-007 the single durable quota/capacity authority and JOB-010 preserve admission semantics; cut over the claim/release/wakeup owner with the workload bridge rather than stacking a second counter. | JOB-007, JOB-010, CLI-005, MIG-002 | `min(perAgentSlots, perOrgSlots)` parity; concurrent atomic claim; queued wakeup; failed-submit, cancel, terminal, and retry release; tenant/workload cutover and rollback; no over-cap scheduling, leak, or double release. |
| CM-015 | Explicit migration-0188 snapshot and `cloud_auth` flip marker | Current `deploy-testing.yml` requires an operator-selected `record_0188_marker` path for the first populated single-tenant→`cloud_auth` flip. It takes a pre-migration `pg_dump`, writes the durable `instance_settings.general.migrationSnapshots` marker, verifies `0188`, and stops before deployment on failure. | DEP-003 preserves the explicit privileged migration/readiness boundary; MIG-002 may replace the workflow seam only with an audited equivalent that pins the candidate revision, creates and validates a restorable snapshot before the one-way marker, is idempotent, and never silently auto-bypasses the gate. A dry-run/shadow may inspect eligibility and snapshot metadata but cannot write the marker or transfer execution ownership. Active legacy work stays legacy-owned until the deployment/cutover drain succeeds. CM-015 closes on the pre-cutover snapshot/marker/cutover contract; REL-003 later rehearses full post-cutover disaster recovery as release evidence and is not a prerequisite to this row or MIG-002. Marker deletion is never a rollback. | DEP-003, MIG-002 | Empty/populated and first/repeated flip; explicit operator identity; no-opt-in fail-closed; snapshot failure writes no marker; marker/write/verification failure stops deploy; snapshot checksum plus isolated pre-cutover restore validation; active-work drain; exact candidate SHA; dry-run causes no marker/cutover; no dual execution owner; audited idempotent rerun. REL-003 separately proves the full release restore. |

## Cloud plugin and host-extension sinks

The frozen baseline contains a documentation/code mismatch that is a release blocker, not a migration option. Decision #103's cloud amendment and `docs/guides/cloud-plugin-execution.md` say `cloud_auth` executes no plugin workers or runtime surfaces. The PR #320 code still composes the plugin worker manager, lifecycle, loader, tools, jobs, bridge, stream, webhooks, UI and install routes in the host process; `cloud-plugin-execution.ts` explicitly allowlists `worker-fork`, `worker-manager`, `lifecycle`, and `loader` on cloud while blocking only selected sinks. FND-006 and FND-008 must make the locked rule true before E0 can pass.

| Row ID | Current sink family | Observed current composition | Required disposition | Owner and evidence |
|---|---|---|---|---|
| CP-001 | Typed plugin process gates | `cloud-plugin-execution.ts` defines `worker-manager`, `worker-fork`, `lifecycle`, `loader`, `loader-import`, and `ui-static`; the first four are allowlisted in cloud and `AOA_PLUGIN_WORKER_PROCESS=1` bypasses most remaining checks if present in the parent environment. | Block all six sinks in the `cloud_auth` parent; reject/ignore the child marker as parent authority; preserve self-hosted behavior. | FND-006: six-sink matrix, parent-marker bypass negative, process/I/O/import/static sentinels, and self-hosted positives. |
| CP-002 | Plugin subsystem composition and startup | `server/src/app.ts` unconditionally composes worker manager, lifecycle, loader, jobs, tools, event/stream buses, routes, and host services. | In `cloud_auth`, do not construct or start effectful plugin runtime components; reconcile stale ready rows to blocked metadata-only state. | FND-006: real `createApp()`/startup tests, zero child/process/background activation, and source-boundary mutation tests. |
| CP-003 | Plugin tools, jobs, webhooks, bridge/RPC, streams/events | Routes and MCP host dispatch can reach the composed worker manager, job scheduler/coordinator/store, tool dispatcher, bridge, webhooks, and event/stream buses. | Preserve Decision #103's stable cloud denial contracts while preventing every runtime effect: registered HTTP surfaces return the exact documented 503 envelope, persisted rows use the existing `statusReasonCode`, and marketplace denials retain `errorCode`/`errorDocs`; non-HTTP dispatchers fail with their typed equivalent before execution. | FND-008: enumerate registrations; assert exact denial envelopes/codes and persisted status; exercise scheduled/webhook/tool/job/MCP calls; prove zero dispatch/spawn/effect. |
| CP-004 | Plugin install/upgrade/import and UI | Plugin lifecycle/loader/marketplace paths can reach package I/O or executable manifest operations; selected loader-import and UI-static sinks are already blocked. | Reject every plugin install/reinstall/upgrade/uninstall/import and browser-code route before I/O/import/static effects; metadata-only reads use persisted validated data. | FND-008: package/I/O/import/UI negative matrix, cached/stale row cases, and unchanged self-hosted positives. |
| CP-005 | External adapters | External-adapter install/reinstall/uninstall, server import/reload, and UI-parser paths already fail closed in `cloud_auth` before package I/O or execution. | Preserve and verify the existing exclusion; do not conflate it with the live plugin-worker mismatch or weaken it while changing composition. | FND-008: existing route/service negatives, source-boundary checks, and self-hosted positives. |

A separately isolated cloud plugin worker is out of scope. Enabling it later requires a new locked architecture decision plus OS and network-isolation, tenant-boundary, secret, provider, cleanup, and release-gate evidence; an agent E2B sandbox is not that approval.

## Closure rule

The Migration Custodian maintains this crosswalk. A row closes only when its named ticket result and immutable QA/handoff evidence identify the exact revision, record the before/after owner, show shadow/cutover/drain/rollback behavior, and demonstrate the required hard negatives. E0 cannot pass until CP-001 through CP-005 are closed. MIG-002 may complete only after CM-001 through CM-015 are closed or an owning ticket records an approved fail-closed disablement for a genuinely unsupported source; an exposure flag is not closure. REL-005 may begin only after all mandatory coding, browser, and service rows are closed and their D2–D4 gates pass.
