# E3 Recovery Sheet — Durable Job Control

## Proposed disposition

Treat shipped job-control mechanisms as retained implementation, not epic completion. Recover canonical ticket approval, run an exact-candidate integrated D1 campaign, and then seek an E3 completion handoff.

## Present evidence

The epic-local README says `complete` and reports all fifteen job-control tickets shipped. The formal epic index still says `in_progress`. No policy-compliant epic-completion QA record and same-candidate completion handoff are present in the E3 epic folder.

## Record integrity

Historical result documents use mixed statuses and formats. They may be adopted only at the clauses they prove; noncanonical or frozen inaccurate records require findings plus successor/reconciliation results.

## Retained scope

Submission, admission, authoritative placement, leasing/ACK, renew/fence, events, terminalization, cancellation/retry, budgets/cost, audit, output/run projection, revocation, and operator controls remain in the first-milestone dependency set.

## Deferred scope

Parity and cutover for Commander, crew, one-shot, browser, and service sources remains later-phase work unless directly needed by the `task_run` milestone.

## Remaining implementation

Close every first-milestone production-reachability gap in budget/cost, audit, and output projection; reconcile any shipped-but-dormant bridge; and retain later-source parity as explicit deferred work.

## Grooming actions

Create a canonical ticket ledger map, pin each retained result blob, assign successor tickets for noncanonical evidence, and map each exit-gate clause to a production caller plus focused test.

## Candidate QA

Run the full `task_run` lifecycle in D1 on the frozen candidate, including hostile tenant/source inputs, placement ceilings, lease races, cancellation, retries, budget/cost, audit, output projection, revocation, and operator controls.

## Completion handoff

After a committed passing E3 QA record, issue a separate completion handoff that pins every approved result/successor and the exact candidate revision.

## Dependencies / reopen triggers

Depends on adopted E1/E2 and the recovered E6 D1 topology. Reopen for protocol, tenant, placement, lease/fence, parity, or required production-caller changes.

## Gate-review readiness

**Can enter gate review now? No.** Ticket-ledger adoption, production-reachability closure, and exact-candidate E3 QA are still required.
