# E3 Recovery Sheet — Durable Job Control

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E3 README](../../epics/E3-job-control/README.md), [findings](../../epics/E3-job-control/findings.md), and [latest numbered result](../../epics/E3-job-control/tickets/JOB-015-result.md).

## Proposed disposition

Treat shipped job-control mechanisms as retained implementation, not epic completion. Recover canonical ticket approval and exercise the milestone subset through `M1-D1-SPINE`; do not seek E3 completion until its normative full-D1 gate passes or is separately amended.

## Present evidence

The epic-local README says `complete` and reports all fifteen job-control tickets shipped. The formal epic index still says `in_progress`. No policy-compliant epic-completion QA record and same-candidate completion handoff are present in the E3 epic folder.

The refreshed origin tip adds durable transactional audit for operator job submission and drain, and closes the gate-clause census by registering `jobAuditBridge`. That bridge is still correctly declared `unwired` with zero production callers; registering dormancy makes the gap visible but does not close audit parity or an E3 gate.

## Record integrity

Historical result documents use mixed statuses and formats. They may be adopted only at the clauses they prove; noncanonical or frozen inaccurate records require findings plus successor/reconciliation results.

## Retained scope

Submission, admission, authoritative placement, leasing/ACK, renew/fence, events, terminalization, cancellation/retry, budgets/cost, audit, output/run projection, revocation, and operator controls remain in the first-milestone dependency set.

## Deferred scope

Parity and cutover for Commander, crew, one-shot, browser, and service sources remains later-phase work unless directly needed by the `task_run` milestone.

## Remaining implementation

Close the remaining first-milestone production-reachability gaps in budget/cost, task-run audit parity, and output projection; reconcile every shipped-but-dormant bridge; and retain later-source parity as explicit deferred work. Adopt the new operator submit/drain audit only for the clauses it actually proves.

## Grooming actions

Create a canonical ticket ledger map, pin each retained result blob, assign successor tickets for noncanonical evidence, and map each exit-gate clause to a production caller plus focused test.

## Candidate QA

Run the included `task_run` lifecycle under `M1-D1-SPINE` on the frozen candidate, including hostile tenant/source inputs, placement ceilings, lease races, cancellation, retries, budget/cost, audit, output projection, revocation, and operator controls. Record the full-D1 clauses and topology not certified.

## Completion handoff

After a passing `M1-D1-SPINE` record, issue only the named non-promoting milestone/dependency handoff. A separate E3 completion handoff waits for the normative E3/full-D1 campaign.

## Dependencies / reopen triggers

Depends on adopted E1/E2 and the E6-supported `M1-D1-SPINE` topology. Reopen for protocol, tenant, placement, lease/fence, parity, or required production-caller changes.

## Gate-review readiness

**Can enter gate review now? No.** Ticket-ledger adoption, production-reachability closure, and exact-candidate E3 QA are still required.
