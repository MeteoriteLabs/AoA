# E11 Recovery Sheet — Hardening and Private-beta Release

## Proposed disposition

Place the full E11 release epic after the first milestone. Retain completed hardening slices as inputs, but do not let them imply release readiness or a private-beta pass.

## Present evidence

The epic remains `backlog`. Foundation-gate, release-test, signing/kill, and disaster-recovery implementation slices exist, while broader adversarial, load/SLO, live restore, and private-beta campaigns remain incomplete or dependency-blocked.

## Record integrity

Noncanonical slice results and inert traceability nodes are not ticket or epic passes. Historical buildable-core evidence must be separated from live operational rehearsal evidence.

## Retained scope

Cross-tenant/secret adversarial testing, load/fairness/SLO, disaster recovery, signed artifacts/SBOM/vulnerability and kill gates, and the full private-beta evidence pack remain retained.

## Deferred scope

The full E11 gate, external design-partner campaign, all-workload D6 matrix, live DR rehearsal, and production signing-root evidence are after the internal coding milestone.

## Remaining implementation

Close dependencies from E8/E9/E10, complete real operator restore/rehearsal and kill/write operations, run workload-specific security/load coverage, and assemble the exact-candidate release evidence pack.

## Grooming actions

Separate implemented core from operator/live evidence, reconcile every release result, keep inert nonnumeric traceability outside ticket counts, and define later milestone owners for signing, DR, incident, and beta operations.

## Candidate QA

The later release candidate must run the named adversarial, load/fairness, restore/recovery, signature/vulnerability/kill, and D6 campaigns on one exact advertised matrix. First-milestone QA is not a substitute.

## Completion handoff

Require a fresh passing E11 release QA record and same-candidate completion handoff after all mandatory workload and advertised-target joins pass.

## Dependencies / reopen triggers

Depends on E7–E10 gates appropriate to the advertised matrix. Reopen for any candidate, dependency, signing root, provider/target matrix, recovery procedure, or beta-participant change.

## Gate-review readiness

**Can enter gate review now? No.** The full release dependencies and campaigns are incomplete, and E11 is intentionally after the first milestone.
