# E9 Recovery Sheet — Long-running Service Agents

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E9 README](../../epics/E9-service-agents/README.md), [findings](../../epics/E9-service-agents/findings.md), [SVC-007b result](../../epics/E9-service-agents/tickets/SVC-007b-result.md), [SVC-009 design](../../epics/E9-service-agents/tickets/SVC-009-design.md), and the [leased/supervised integration proof](../../../../server/src/__tests__/service-leased-supervised.integration.test.ts).

## Proposed disposition

Move full E9 after the first milestone. Preserve the implemented service control-plane and daemon slices, open findings, and decisions for a later service milestone.

## Present evidence

The epic remains `backlog`. Schema, reconciler, routes, health projection, and service-supervisor slices exist. Revision `3bdca18da` adds the first integration proof that creates, reconciles, leases, ACKs, and supervises a service job through the real leasing and supervisor paths. The proof is deliberately keyless and CI-runnable: it uses an in-process `E2bSandboxProvider` with mock transport, manually seeds the placement columns owned by JOB-009, does not exercise server-side `service_instances.status` projection, and does not run beyond the 240-second effect-authority boundary. It is a valuable mechanism bridge, not production-real service capability or D4 evidence; the 72-hour D4 campaign has not run.

## Record integrity

Partial and letter-suffixed slice results are historical evidence, not canonical completion of their parent ticket. Their stated limitations and open findings remain binding during adoption.

## Retained scope

Desired state, immutable generations, placement, health, restart/backoff, checkpoint recovery, graceful stop/drain, budgets/TTL, management evidence, and D4 continuity remain retained.

## Deferred scope

The entire service capability and D4/72-hour campaign are deferred from the coding milestone. Service remains required before a broader private-beta claim that advertises it.

## Remaining implementation

Finish `SVC-009` worker-side renewal-capability consumption and the E2B TTL extension, then prove a service run beyond 240 seconds on real infrastructure. Complete composed placement without the test seed, server-side health/status projection, generation rollout, `graceful_stop`, checkpoint, budget/TTL and split-brain controls. The real `drain` producer and durable operator submit/drain audit have landed; the remaining worker-revoke audit gap needs an org-scoped sink or an explicit accept-the-gap decision before D4.

## Grooming actions

Map each shipped slice to its parent acceptance clause, create canonical successors where needed, assign every open finding, and define the later candidate’s continuity and failure schedule.

## Candidate QA

Run the later D4 campaign for 72 wall-clock hours across control-plane/worker restarts, partition, E2B pause/resume or replacement, drain, generation rollout, checkpoint restore, and budget/TTL stop.

## Completion handoff

Require a fresh E9 passing QA record and same-candidate completion handoff. No first-milestone completion decision is proposed.

## Dependencies / reopen triggers

Depends on completed E7 capability and relevant E10 realtime/migration inputs. Reopen retained slices for lease/fence/service-definition changes or when the later milestone is approved.

## Gate-review readiness

**Can enter gate review now? No.** A narrow keyless CI mechanism bridge now exists, but real-infrastructure service dispatch, the beyond-240-second authority proof, composed projection/continuity evidence, and D4 are absent. E9 is intentionally deferred.
