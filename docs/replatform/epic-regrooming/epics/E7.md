# E7 Recovery Sheet — Coding/CLI on E2B

## Proposed disposition

Make E7 the first-milestone product epic, but keep it below gate review until a useful `task_run` coding capability—not only the transport/execution mechanism—is proven on the exact candidate.

## Present evidence

The September 18 real-E2B run corroborated the distributed mechanism with `ok=true`, while its verifier deliberately reported `capabilityProven=false`. It did not prove the approved tool surface, workspace/output return, or a complete D2 capability campaign.

## Record integrity

Existing results and findings contain explicit qualifications and later corrections. Preserve those qualifications; do not quote the mechanism PASS without the capability verdict or promote manual staging evidence into a CI/epic gate.

## Retained scope

Real-E2B provider behavior, workspace staging, supported coding adapters, logs/cancellation/usage, cleanup reconciliation, existing-run bridge, terminal projection, tools, and attributable output remain first-milestone scope.

## Deferred scope

Browser, service, desktop, mobility, Commander, crew, one-shot, HA, autoscaling, and external beta remain later. Additional adapters beyond the approved first-milestone set retain their explicit dispositions.

## Remaining implementation

Finish and production-wire the approved sandbox tool surface, useful workspace/output path, audit/cost/return sinks, and any still-dormant gate prerequisite. Keep the E2B egress limitation disclosed and credential taxonomy enforced.

## Grooming actions

Reconcile the E7 ticket/slice records, define the exact supported adapter and output contract, map every capability-verdict input to a non-forgeable production producer, and freeze the one-Organization enablement/rollback plan.

## Candidate QA

Run a fresh D2 campaign on the frozen candidate covering real E2B isolation, approved tool use, workspace input, attributable reviewable output, cancellation, usage, provider outage/timeout, stale fences, secret canaries, artifact integrity, and every cleanup path.

## Completion handoff

After a committed passing D2 E7 record with useful capability evidence, issue a separate exact-candidate completion handoff. The September 18 mechanism run remains retained supporting evidence only.

## Dependencies / reopen triggers

Depends on recovered E3–E6 and the required narrow E10 reconciliation inputs. Reopen for adapter/tool/output changes, E2B provider/template/policy drift, credential taxonomy changes, a false-positive capability verdict, or failed cleanup.

## Gate-review readiness

**Can enter gate review now? No.** `capabilityProven=false`, and the fresh exact-candidate D2 completion evidence does not yet exist.
