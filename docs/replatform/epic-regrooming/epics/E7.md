# E7 Recovery Sheet — Coding/CLI on E2B

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E7 README](../../epics/E7-coding-e2b/README.md), [findings](../../epics/E7-coding-e2b/findings.md), [CLI-006 result](../../epics/E7-coding-e2b/tickets/CLI-006-result.md), and [September 18 mechanism handoff](../../HANDOFF-2026-09-18-post-e7-1-sink-cutover.md).

## Proposed disposition

Make E7 the first-milestone product focus, but use `M1-D2-CODING` only to approve the internal alpha. Keep E7 below completion review until useful capability and the normative full-D2/H-06 gate pass or are separately amended.

## Present evidence

The September 18 real-E2B run corroborated the distributed mechanism with `ok=true`, while its verifier deliberately reported `capabilityProven=false`. It did not prove the approved tool surface, workspace/output return, `M1-D2-CODING`, or a complete normative D2 campaign.

CLI-008 Unit C slices 1–4 now stage the AoA MCP configuration, mint/resolve the lease-scoped `run_jwt`, and connect both behind `AOA_DISTRIBUTED_TOOL_SURFACE_ENABLED`. The flag defaults off and is enabled in no CI workflow; the currency gate remains inert and the keyed E2B proof is still outstanding. This is meaningful implementation progress, not tool-reach or useful-capability evidence.

## Record integrity

Existing results and findings contain explicit qualifications and later corrections. Preserve those qualifications; do not quote the mechanism PASS without the capability verdict or promote manual staging evidence into a CI/epic gate.

## Retained scope

Real-E2B provider behavior, workspace staging, supported coding adapters, logs/cancellation/usage, cleanup reconciliation, existing-run bridge, terminal projection, tools, and attributable output remain first-milestone scope.

## Deferred scope

Browser, service, desktop, mobility, Commander, crew, one-shot, HA, autoscaling, and external beta remain later. Additional adapters beyond the approved first-milestone set retain their explicit dispositions.

## Remaining implementation

Complete the keyed live proof and candidate-specific enablement of the approved sandbox tool surface; finish the useful workspace/output path, audit/cost/return sinks, and every still-dormant gate prerequisite. Keep the E2B egress limitation disclosed and credential taxonomy enforced.

## Grooming actions

Reconcile the E7 ticket/slice records, define the exact supported adapter and output contract, map every capability-verdict input to a non-forgeable production producer, and freeze the one-Organization enablement/rollback plan.

## Candidate QA

Run `M1-D2-CODING` on the frozen candidate covering real E2B execution, approved tool use, workspace input, attributable reviewable output, cancellation, usage, provider outage/timeout, stale fences, secret canaries, artifact integrity, and every included cleanup path. Record the accepted DE-08 residual, state H-06 is not passed, and enumerate the normative D2 counts/schedule not certified.

## Completion handoff

After a passing `M1-D2-CODING` record with useful capability evidence, issue only the exact-candidate non-promoting milestone handoff. The September 18 mechanism run remains supporting evidence; E7 completion waits for full D2/H-06 or an approved normative amendment.

## Dependencies / reopen triggers

Depends on recovered E3–E6 and the required narrow E10 reconciliation inputs. Reopen for adapter/tool/output changes, E2B provider/template/policy drift, credential taxonomy changes, a false-positive capability verdict, or failed cleanup.

## Gate-review readiness

**Can enter gate review now? No.** `capabilityProven=false`; neither `M1-D2-CODING` nor the normative full-D2 completion evidence exists yet.
