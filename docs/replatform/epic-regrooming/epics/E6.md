# E6 Recovery Sheet — Deployment and Distributed Test Harness

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E6 README](../../epics/E6-deployment-test-harness/README.md), [findings](../../epics/E6-deployment-test-harness/findings.md), and [E6-D1-FOUNDATION campaign](../../epics/E6-deployment-test-harness/qa/2026-08-14-e6-d1-foundation-campaign-pass-85599b192.md).

## Proposed disposition

Retain the foundation and later harness mechanisms, but do not treat the epic-local `complete` label as policy-compliant completion. Reconcile results and run `M1-D1-SPINE` for the internal alpha; run the full normative E6/D1 gate separately before E6 completion.

## Present evidence

The epic-local README reports DEP-000 through DEP-013 (14 tickets) shipped and a passing named `E6-D1-FOUNDATION` partial gate. The formal index remains `in_progress`. Existing QA files cover that partial gate, but the epic folder has no same-candidate epic-completion handoff.

## Record integrity

Historical partial-gate evidence remains valid at its named scope. Mixed ticket/slice statuses require independent adoption or successor results; the partial gate must not be promoted into an epic pass.

## Retained scope

Split images, D1 topology, migration/readiness behavior, CI harnesses, network/clock faults, staging contract, observability, provider conformance, adapter-manager/worker transport, and operational tracking remain retained.

## Deferred scope

Two-control-plane HA and replicated/autoscaled worker assurance are outside the first milestone. The second worker required by D1-00 is also outside the proposed one-worker M1 topology, so M1 cannot pass full D1 or complete E6. Real E2B provider evidence remains in the coding partial gate rather than being backfilled into E6.

## Remaining implementation

Close the milestone’s declared-open harness gaps, prove the one-control-plane/one-worker topology boots the production path, and complete the required tracking/assurance items without claiming deferred HA.

## Grooming actions

Separate partial-gate adoption from epic completion, reconcile each result’s canonical state, declare the exact first-milestone topology, and map all current checker open gaps to owners or deliberate later-phase deferrals.

## Candidate QA

Run the frozen candidate through `M1-D1-SPINE`: one control plane, one separately deployed worker, migration/readiness, fake/reference provider, included fault injection, object storage, telemetry, production boot roots, and cleanup/recovery. The result must list D1-00’s missing second worker, the unrun full-D1 clauses, and H-06 as uncertified under the accepted DE-08 residual.

## Completion handoff

After `M1-D1-SPINE` passes, issue only the non-promoting milestone/dependency handoff. The old `E6-D1-FOUNDATION` record remains a retained partial-gate artifact. E6 completion still requires full D1/H-06 or a separately approved normative amendment.

## Dependencies / reopen triggers

Depends on current E2–E5 inputs. Reopen for topology/image/migration/provider-wire changes, a checker open gap becoming relevant, a boot root becoming dormant, or a candidate harness failure.

## Gate-review readiness

**Can enter gate review now? No.** Result reconciliation and `M1-D1-SPINE` can prepare the internal alpha, but they cannot place E6 in completion review.
