# E10 Recovery Sheet — Desktop, Migration, and Realtime

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E10 README](../../epics/E10-desktop-migration-realtime/README.md), [findings](../../epics/E10-desktop-migration-realtime/findings.md), [MIG-003 result](../../epics/E10-desktop-migration-realtime/tickets/MIG-003-result.md), and [MIG-008 result](../../epics/E10-desktop-migration-realtime/tickets/MIG-008-result.md).

## Proposed disposition

Split E10 by milestone. Retain only the narrow legacy-resource/reconciliation and continuity prerequisites consumed by the coding milestone; place installed desktop, mobility, and most source cutovers after it.

## Present evidence

The formal epic status is `backlog`. Durable-realtime, desktop, migration-dial, reconciliation, drain, and source-cutover slices exist in multiple E10 workspaces, but there is no full E10 completion QA/handoff.

## Record integrity

Results are slice-specific and sometimes grouped across ticket IDs. Adopt only exact clauses with pinned blobs; use successor/reconciliation results instead of editing frozen slice histories.

## Retained scope

The first milestone retains the exact reconciliation/preflight work required to avoid dual authority on its `task_run` path and the already-built realtime behavior actually consumed by its UI/terminal projection.

## Deferred scope

Installed desktop host/distribution, cross-target mobility, Commander/crew/one-shot cutovers, remaining current-path migration, two-control-plane realtime, and replicated fleet behavior move to later phases.

## Remaining implementation

For the first milestone, close stale/unresolved reconciliation inputs and prove there is one authority for the enabled coding path. Later milestones retain the full desktop lifecycle, source cutovers, drain, rollback, and handoff work.

## Grooming actions

Create separate milestone maps for realtime, current-path migration, desktop, and mobility; reconcile grouped slice results; and ensure deferred flags/routes remain hard off with negative evidence where relevant.

## Candidate QA

The coding candidate needs focused reconciliation, no-dual-scheduling, catch-up/duplicate/gap, rollback-safety, and disabled-surface checks as inputs to the named M1 partial gates at its exact revision. Later candidates run their own desktop, mobility, cutover, and HA campaigns.

## Completion handoff

Do not issue a full E10 completion handoff for the first milestone. Use narrowly named partial-gate handoffs where a dependency truly passes, then require a later full E10 exact-candidate completion handoff.

## Dependencies / reopen triggers

Depends on the source and workload being migrated. Reopen for routing/authority changes, a new advertised target, desktop/mobility enablement, realtime topology changes, or reconciliation drift.

## Gate-review readiness

**Can enter gate review now? No.** Only narrow partial gates may be prepared; most E10 scope is later and the full epic gate is not assembled.
