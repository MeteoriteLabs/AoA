# E4 Recovery Sheet — Worker Daemon

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E4 README](../../epics/E4-worker-daemon/README.md), [findings](../../epics/E4-worker-daemon/findings.md), and [WRK-017 result](../../epics/E4-worker-daemon/tickets/WRK-017-result.md).

## Proposed disposition

Retain the shipped worker mechanisms but withdraw the unsupported epic-complete inference. Recover canonical results and run the exact candidate through the real composed worker path before completion review.

## Present evidence

The epic-local README says `complete` and reports seventeen shipped worker tickets, while the formal index remains `in_progress`. The folder has no epic-completion QA/handoff pair satisfying the current policy.

## Record integrity

Many results are slice-oriented or carry noncanonical labels. Preserve them; adopt narrow evidence through independent review and use successor/reconciliation results where the old record cannot be made canonical without rewriting history.

## Retained scope

Separate worker packaging, identity/session, poll/offer/ACK, supervision, renewal/fencing, encrypted outbox, restart recovery, provider cleanup, composed dispatch, and enrollment remain required inputs.

## Deferred scope

Replicated/autoscaled worker identity and later workload-specific daemon behavior remain later phases unless selected for a future expansion.

## Remaining implementation

Prove the shipped production boot root constructs the required provider/session/self-model/dispatch chain, close any still-dormant or unowned link, and finish the first-milestone enrollment/dispatch assurance.

## Grooming actions

Normalize the acceptance map outside historical files, identify the exact supported one-worker profile, and assign successors for slice records that do not constitute canonical ticket completion.

## Candidate QA

Under `M1-D1-SPINE` and `M1-D2-CODING` on the frozen candidate, enroll one separately deployed worker, lease real work, supervise and cancel the process tree, renew/fence, restart with outbox replay, recover or quarantine orphans, and clean provider resources. The record must identify the second-worker and other normative D1/D2 coverage not run.

## Completion handoff

Commit the worker evidence into the named M1 partial-gate records and use only the non-promoting milestone handoff. E4 completion waits for its normative exit-gate campaign.

## Dependencies / reopen triggers

Depends on E1, E3, and the recovered E6 topology. Reopen for identity/session/profile changes, provider transport changes, daemon boot-root changes, or a failed restart/fence/cleanup campaign.

## Gate-review readiness

**Can enter gate review now? No.** The result adoption and exact-candidate composed-worker campaign are missing.
