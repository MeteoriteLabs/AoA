# E5 Recovery Sheet — Workspaces, Artifacts, Secrets, and Network Policy

> **Proposal only — non-authoritative pending review.** See the [proposal index](../README.md), [scope triage](../scope-triage.md), and [QA/handoff recovery](../qa-handoff-recovery.md). Source evidence: [E5 README](../../epics/E5-workspaces-secrets/README.md), [findings](../../epics/E5-workspaces-secrets/findings.md), and [exit-gate audit a1](../../epics/E5-workspaces-secrets/qa/2026-08-24-d0-e5-exit-gate-audit-a1.md).

## Proposed disposition

Keep all shipped slices, but hold E5 below completion. Supersede the breached a1 audit with a fresh a2 exact-candidate audit and close the milestone subset through the named M1 partial gates without calling that full D1/D2 or E5 completion.

## Present evidence

The epic README says its tickets are complete but the exit gate is not met. The last formal audit reports 2 of 7 clauses passing in D1, 4 `proven_weakly`, and 1 `not_proven`. Substantial DAT-007 implementation has landed after that audit, so the a1 matrix is useful history but not current proof.

## Record integrity

The a1 audit was modified in a later commit, violating write-once QA policy. Preserve it and create a2 with `Supersedes`; never repair a1 in place.

## Retained scope

Immutable workspace staging, fenced object commit, patch/conflict quarantine, lease-scoped secrets, redaction, the qualified network-policy boundary, and the brokered internal-tool surface remain required for the coding milestone.

## Deferred scope

Local-folder/desktop admission, later workspace retention/reconciliation expansions, warm-resume behavior without a shipped mechanism, and non-coding workload variants remain later-phase scope.

## Remaining implementation

Wire and prove each weak/unproven first-milestone clause through a production caller. Finish the tool surface, useful output/workspace path, secret currency, and honest network-policy qualification needed by E7.

## Grooming actions

Open the immutable-record finding; close the M1 implementation/build gaps; make focused acceptance green; freeze the a2 seven-row matrix, commands, exact topology, QA owner, and decision owner; and attach post-audit evidence without claiming it was in a1.

## Candidate QA

After the implementation gaps, focused acceptance, and audit plan are complete, run `M1-D1-SPINE` and `M1a-D2-MECHANISM` on the exact frozen `M1a` candidate, and `M1-D2-CODING` on the `M1b` candidate. ★ *Corrected 2026-09-20 (third round): this said “the exact shared candidate”, singular, which the M1a/M1b split makes untrue — the mechanism campaigns and the capability campaign now run on different frozen candidates, and a2 consumes whichever records exist when it is committed.* Then commit a2 as a consumer of those campaign records, covering workspace bytes and hashes, stale fences at each transfer phase, quarantine/non-promotion, secret scope/rotation/revocation, redaction canaries, tool authorization, and the documented DE-08 residual. Record H-06 as not certified for full D1/D2; do not require a2 to pass before the campaigns it audits begin.

## Completion handoff

An a2 milestone result may support only the non-promoting M1 handoff. An E5 completion handoff requires the full seven-clause normative exit gate, including the current network-boundary requirement or an approved amendment.

## Dependencies / reopen triggers

Depends on recovered E3/E4/E6. Reopen for storage/provider changes, secret materialization changes, new tool/credential classes, a production caller becoming dormant, or any candidate clause falling below `pass`.

## Gate-review readiness

**Can enter gate review now? No.** First close the M1 implementation/build gaps, make focused acceptance green, and freeze the a2 audit plan. The a2 result intentionally follows and consumes the exact M1 campaigns; the breached a1 cannot be the completion record.
