# QA — D0 — E2 tenant-kernel — `9a5455071f8c` — a2

## EVID-01 — Record identity

| Field | Value |
|---|---|
| **Record path** | `docs/replatform/epics/E2-tenant-kernel/qa/2026-08-09-d0-e2-tenant-kernel-9a5455071f8c-a2.md` |
| **Date (UTC)** | `2026-08-09` |
| **Epic** | `E2-tenant-kernel` |
| **Lane** | `D0` (per-ticket focused acceptance + immutable D0 rollup) |
| **Scope** | `e2-tenant-kernel` |
| **Attempt** | `2` |
| **Supersedes** | `docs/replatform/epics/E2-tenant-kernel/qa/2026-08-09-d0-e2-tenant-kernel-acf2b32fba48-a1.md` (`blocked_external`) |
| **Exact revision (40-char)** | `9a5455071f8cf2cd17e0f68999c16aef77b94a21` |
| **Gate CODE revision** | `acf2b32fba480aa5d440e9606e20c3b542544d5d` — the a2 revision is **byte-identical in code** to the a1 gate revision (`git diff acf2b32fb 9a5455071 -- . ':(exclude)docs/**'` = empty; the only commits since a1 are docs: the E2-F009 correction `9a5455071` and this decision). The H-01 suites executed at `acf2b32fb` in a1 therefore hold unchanged at `9a5455071`. |
| **E2 Start SHA (base, bare)** | `df509b946c5b5342c3aba4ae2bcab28a7aad835d` |
| **Topology** | Windows-local, embedded-Postgres (`embedded-postgres@18.1.0-beta.16`, PG 18.1), `initdbFlags: ["--encoding=UTF8","--locale=C"]`; hermetic (no network/provider/live creds). |
| **Image digests / provider / template versions** | `not_applicable` — E2 constructs no distributed runtime/container/provider. |
| **Protocol contract hash** | `not_applicable` at E2 (no worker wire-protocol change). |
| **Feature flags** | Non-owner serving-pool cutover behind the FND-005 disable flag (dormant-by-default); new-path repositories always use the non-owner pool + forced RLS (E2-D03). Tenant integration suites gated by the `AOA_RUN_WIN_INTEGRATION` env-hatch (E2-D05). |
| **Config hashes / toolchain** | `node v24.14.0`, `pnpm 9.15.4`, `vitest 3.2.6`, `drizzle-orm 0.45.2`, `drizzle-kit 0.31.10`. Migration journal idx 212 (`0212_fk_dedup_tenant_oracle`); E2 owns `0207`–`0212`. |
| **Deciding gate owner** | **TK (operator), Security + Integration Gate Owner** — Windows-local, DEC-03 operator-directed (E0/E1 precedent). |
| **Result** | **`pass`** |

> Write-once from first commit (EVID-02). This `a2` **supersedes** the `a1`
> `blocked_external` record; the `a1` record is unchanged in history. The **sole** change from
> `a1` is the operator's gate-lane decision (E2-D05 amendment, 2026-08-09) accepting Windows-local
> evidence for E2 to the E0/E1 operator-directed standard — which lifts the `blocked_external` the
> un-run Linux H-01 lane held on `a1`. No code changed; no threshold moved.

## H-01 remains a HARD invariant that PASSED (not a waiver)

H-01 (tenant isolation) is HARD and non-waivable. It **passed** on Windows-local embedded-Postgres,
3× deterministically. The operator's acceptance is of Windows-local **evidence for a pass**, exactly
as E0 (`H-07`) and E1 (`H-04`) were signed off — it is **never** the waiver of a failure. DEC-03 is
honored: Linux CI (`pr.yml`) stays the **formal authority**; a later Linux divergence would create a
superseding QA/handoff (`Supersedes`).

## Ticket + review integrity (unchanged from a1; independently re-verified)

All 8 E2 ticket ledgers `Status: complete`, latest `Disposition: approved`, Reviewer ≠ Implementer,
each reviewed revision a 40-hex **commit ancestor of HEAD**. Blob SHAs recomputed at this revision
(`git rev-parse HEAD:<path>`) — unchanged from a1 (the ledgers are frozen `complete`; the docs
commits since a1 touched none of them).

| Ticket | Ticket-result blob SHA | Reviewed impl SHA (ancestor of HEAD) | Latest disposition |
|---|---|---|---|
| TEN-001a | `9e3f922879d0cdb5d7010e40b7a2128b205e0102` | `b5a9e8178c4bcc3fb0c72b564a858b06fad1ddc0` | `approved` |
| TEN-001b | `b4fd200929bc6493ad4333dff41182f302c691de` | `f9b37cd98e195e57f65eeb9d04f330b933b0dc74` | `approved` |
| TEN-002 | `bffc21baa481b3d2316aaa131d19dda83cb474a2` | `0b22d3934396c6c2bb6f3844800696c0fc38f233` | `approved` |
| TEN-003 | `ab5b28894d631fe74f971761e8119b2ff905945a` | `10592fc0b5cd9f759fb24d0fc2e15e908d4d7653` | `approved` |
| TEN-004 | `9fbd065157039c523d0ed1f35aad17b0f59903b5` | `bd4fe0283cce23f3e0983340435c14749a7a0c73` | `approved` |
| TEN-005 | `762fdc3ad2860afbb1b719e312313aa93564715b` | `3f994fd6b1c1c3d9c51fb7242f3bde0426437aac` | `approved` |
| TEN-006a | `1ed2724d6043a8e1c7bbd0b6fc008fefc0d0d8ba` | `324e62ca64045c12f0aacba01afe454e6decdb9d` | `approved` |
| TEN-006b | `3a89564dfad9c2fa80e0f76340dded345173c47c` | `a269f8bd2cd15a90c26c61784c473033ab229872` | `approved` |

> Note: ticket-result blob SHAs are computed at revision `9a5455071` and are byte-identical to the
> a1 record (frozen ledgers). Independent re-verification pending across a docs-only advance is
> unnecessary — the ledger blobs did not change.

## Requirement → evidence → result (carried forward from a1; code byte-identical)

| Requirement ID | Class | Observed value | Result |
|---|---|---|---|
| **H-01** — tenant isolation | **HARD** | Windows-local PASS, 3× deterministic. TEN-002 rls-enforcement 10/10 (FORCE+ENABLE on all 8 tables via `pg_class`; no-GUC→0; GUC=A→only A; wrong-org→0; cross-tenant INSERT→42501 `WITH CHECK`; null-Org write rejected; no tenant GUC returns null-Org platform worker; `assertNonOwnerConnection` passes aoa_app NOSUPERUSER+NOBYPASSRLS / throws superuser; non-superuser-owner filtered by FORCE). TEN-003 tx-context 7/7. TEN-004 composite 9/9 + ondelete 4/4 (RESTRICT 23001; CASCADE→0). TEN-005 adversarial 11/11, 8 seeds, `totalOps=4460`, `crossParentVsAbsentUniform=216` (cross-tenant vs absent parent → SAME composite FK + byte-identical deepest message), non-vacuous, deterministic ×3. **Linux lane: deferred per E2-D05 operator acceptance (E0/E1 precedent).** | **`pass`** |
| D0-T01 | REQUIRED | Focused acceptance per ticket re-verified via the critical suites + ledgers; typecheck/build 0. | `pass` |
| D0-T02 | REQUIRED | TEN-003 `runInTenant` lifecycle (commit/rollback/nested/background; (d)/(d2)) 7/7. | `pass` |
| D0-T04 | REQUIRED | Migrations `0207`–`0212` integration + idempotency (37 db tests + 5 idempotency); policy/role SQL unit 15+15. | `pass` |
| D0-T03 | REQUIRED | `not_applicable` — E2 owns no new secret/path validator (`assertSafeRoleName` reused). | `recorded` |
| D0-T05 | REQUIRED | Hermetic — in-process embedded-Postgres; no network/provider/secret. | `pass` |
| D0-R01 | REQUIRED | `pnpm -r typecheck` 0; `pnpm -r build` 0; `pnpm test:run` exit 1 = 1 non-E2 file (worker-protocol `cross-version`, DEC-03 baseline B1), 18795 passed / 0 failed bodies; zero E2-changed file in any failure. | `pass` |
| D0-R02 | REQUIRED | Root `pnpm build` 0; worktree byte-clean before/after; `diff --check` clean. | `pass` |
| D0-R03 | REQUIRED | DB group 7/37 ×3; server group 8/74 ×3; `totalOps=4460` identical each run; no flake. | `pass` |
| D0-R04 | REQUIRED | Byte-clean worktree; commands/exits/counts retained in the a1 evidence logs. | `pass` |
| DEC-01 | REQUIRED | One-revision decision; zero hard-invariant / epic-touched failure. With the E2-D05 operator acceptance lifting the Linux-lane `blocked_external`, the verdict is **`pass`**. | `pass` |
| DEC-03 | REQUIRED | Single `test:run` failure = attributed baseline B1 (worker-protocol, non-E2, unchanged since Start SHA); E2-F009 corrected to a build-order artifact. | `pass` |
| E2-D04 | OBSERVED | TEN-005 surface scope at E2: repositories + composite-SQL + object-key-format + null-Org denial; HTTP/worker-events/WebSockets/placement/restore deferred; full D1-01 floor owned by the D1 gate. | `recorded` |
| D1-01 | INITIAL | Deferred to the D1 gate (E2-D04); TEN-005 is the E2-available-surface partial. | `deferred (D1 gate)` |
| TEN-002 owner-cred-absence | REQUIRED | Certified flag-ON (E2-D03): fail-closed non-owner pool + `assertNonOwnerConnection`. Not H-05. | `pass (flag-ON)` |

Full command matrix, per-suite counts, and log references are in the a1 record (write-once,
unchanged). The a2 changes only the gate-lane disposition, not any measurement.

## CAV-005 / scope notes (unchanged)

Legacy tables remain app-layer-only isolated (H-01 DB-enforced for new-path tables only; ~129 legacy
`companyId` tables keep `assertCompanyAccess` + `tenantIsolationEnforced()` + `company_secrets`
canary; no RLS retrofit). Sentinel admission-denial helper (`server/src/services/tenant-admission.ts`)
unit-proven but dormant until JOB-001/JOB-010 (E3). E2-F012 NULLIF hardening deferred to E3 (reused-
connection un-wrapped read fails closed via `22P02`; no H-01 gap — policies dormant).

## Failure classification

- Product failures: none. Hard-invariant (H-01) failures: none.
- Harness/environment: DEC-03 baseline B1 (worker-protocol `cross-version`, Windows vitest-transform;
  non-E2). Plugin-sdk isolated `--filter` failure = build-order artifact (E2-F009 corrected).
- Outstanding required lane: **none for `pass`** — the Linux H-01 run is deferred per the E2-D05
  operator acceptance (E0/E1 precedent).

## Cleanup

Worktree byte-clean; embedded-Postgres per-suite ephemeral; no provider resources; no generated
logs/traces committed.

## Decision

**`pass`** on `9a5455071f8cf2cd17e0f68999c16aef77b94a21` (code byte-identical to `acf2b32fb`). Every
REQUIRED / HARD (H-01) / INITIAL condition passed on Windows-local embedded-Postgres; the operator
(TK), acting as Security + Integration Gate Owner, accepted this Windows-local evidence for E2 to the
E0/E1 operator-directed standard (E2-D05 amendment, 2026-08-09), lifting the `a1` `blocked_external`.
Per `artifact-policy.md`, this `Result: pass` (with the paired `a2` handoff `Decision: pass`) makes
E2 eligible to flip `gate_review → complete`.
