# QA — D0 — E2 tenant-kernel corrective prerequisite — `21335854f` — a5

## Record identity

| Field | Value |
|---|---|
| Date (UTC) | `2026-08-10` |
| Scope | Prerequisite P1 fix round 2: remaining bounded-role findings only |
| Supersedes | a4 |
| RED revision | `cdeb9caaa51c2d04da57e396bf8f19ae391d7e4a` |
| Candidate code revision | `21335854f1fe33773b1ef70b4b5da9bc8f618f3f` |
| Topology | `operator-directed windows-local`, embedded PostgreSQL 18.1 |
| Formal authority | Linux CI under DEC-03 |
| Reviewed revision | `7843b86e25eb1ff9c520308aef7f123fec6997a7` |
| Gate owner | `Codex distinct reviewer (/root/e2_role_correction_review)` |
| Result | **`pass`** |

The candidate observations below are implementer evidence; the independent decision
and rerun evidence are recorded in the final section. This QA passes the corrective
E2 prerequisite only and does not itself implement an E3 ticket.

## Candidate observations

| Requirement | Observation | Candidate result |
|---|---|---|
| Authenticated identity | Direct-pool and real-server startup reject owner/superuser URLs masked as either bounded role by startup options. Valid direct role sessions remain exact and non-superuser after `SET ROLE NONE`. | `observed_green` |
| Complete effective authority | Table and column audits cover relkinds `r,p,v,m,f`; an operator-granted view over `company_secrets` aborts startup before health. | `observed_green` |
| Heartbeat target resolver | The real pinned-target resolver runs under `aoa_app` with seven named target columns; token, owner, capabilities, and liveness columns remain denied. Operator authority is unchanged. | `observed_green` |
| Focused acceptance | Root Vitest invocation with `AOA_RUN_WIN_INTEGRATION=1`: 5 files, 56/56 tests passed in 71.22 s. | `observed_green` |
| Migration | Additive custom migration 0215; applied migrations 0213/0214 untouched. Migration idempotency passes 5/5. | `observed_green` |
| Typecheck/build | Affected DB/server and recursive workspace typechecks pass; affected DB/server and production workspace builds pass. | `observed_green` |
| Repository tests | `pnpm test:run` completes without timeout: 2005 files and 18,803 tests pass; the sole failed suite is the independently reproduced Windows worker-protocol transform/collection SyntaxError at `cross-version.test.ts:12`, before its test bodies run. | `observed_with_baseline` |

## Independent review decision

**Reviewer identity:** `Codex distinct reviewer (/root/e2_role_correction_review)`

**Reviewed revision:** `7843b86e25eb1ff9c520308aef7f123fec6997a7`

**Decision:** `pass`

The reviewer reran the masked-owner direct and real-child cases (`4/4`), adversarial
secret-view case (`1/1`), actual heartbeat pinned-target resolver (`1/1`), full P1
focused matrix (`56/56`), migration idempotency (`5/5`), and affected DB/server
typecheck/build; all passed. A clean exact-role flag-on gate passed, and supplemental
materialized-view and foreign-table authority drift probes were rejected. The grant
remains the exact seven-column resolver projection and does not expose token, owner,
capabilities, or liveness columns to `aoa_app`; operator authority is unchanged.

**Residuals:** the isolated worker-protocol Vitest lane retains the known Windows
transform/collection `SyntaxError` at `cross-version.test.ts:12`, with zero tests
collected; both imported JavaScript files pass `node --check`, and E1 is untouched.
Linux CI remains formal DEC-03 authority. JOB-002 continues to own future operator
write/enrollment/proof/revocation authority.
