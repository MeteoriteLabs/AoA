# Handoff — E2 serving-role correction (prerequisite P1), superseding a5

**Date (UTC):** `2026-09-21`
**Epic:** `E2-tenant-kernel`
**Record path:** `docs/replatform/epics/E2-tenant-kernel/handoffs/2026-09-21-epic-completion-589854ddb019-a6.md`
**Gate slug:** `epic-completion` (the same slug as `a5`; the scope is the P1 corrective gate, not the E2 epic gate, whose completion is `a2`)
**Reviewed revision:** `589854ddb01918f60e0e607bb88f9ec41ee8d578`
**Attempt:** `6`
**Supersedes:** `docs/replatform/epics/E2-tenant-kernel/handoffs/2026-08-10-epic-completion-21335854f-a5.md`
**Decision:** `fail`
**Gate owner role:** `Integration Gate Owner`
**Gate owner identity:** `the M1 planning session, under founder delegation (M1 ruling F2, scope-triage.md "M1 rulings")`

> This file is immutable from its first commit. A correction, rerun, changed decision, or changed revision creates a higher attempt and links this path through `Supersedes`.

## Included ticket results

| Ticket | Ticket-result path | Ticket-result Git blob SHA | Reviewed implementation SHA | Latest review disposition |
|---|---|---|---|---|
| `P1` (E2 serving-role correction) | `docs/replatform/epics/E3-job-control/prerequisites/E2-serving-role-correction-result.md` | `0a4949615fa1ff097e24ed6802f98ee3a747bcb8` | `21335854f1fe33773b1ef70b4b5da9bc8f618f3f` | `approved` (review attempt 3, reviewed `7843b86e25eb…`) |

## QA evidence

| QA record | QA revision | Lane | Attempt | Result |
|---|---|---|---:|---|
| `docs/replatform/epics/E2-tenant-kernel/qa/2026-09-21-d0-e2-tenant-kernel-589854ddb019-a6.md` | `589854ddb01918f60e0e607bb88f9ec41ee8d578` | `D0` | `6` | `fail` |

The QA session was distinct from the gate owner. It filed the QA record, and the gate owner filed this handoff.

## Threshold decision

**`fail`**, and it has to be. The `a6` QA record re-measured `a5`'s claims against the tree that
CI run `35572455312` tested (merge commit `589854ddb019`, whose tree is byte-identical to program
tip `0c6ad7c13535`). Two P1 clauses do **not** hold on that tree:
- `P1-RESOLVER-EXACT`, the exact seven-column grant;
- the operator-posture half of `P1-OPERATOR-UNCHANGED`.

A `pass` here would certify a grant set production no longer has.

## Why they fail: superseded by design, not a regression

The gate owner verified at source that the widening is deliberate. It comes from later reviewed
tickets that are complete in their own right:
- **`JOB-002`** (`packages/db/src/migrations/0221_worker_enrollment_rls.sql`, `JOB-002-result.md`) grants
  `aoa_app`:
  - column `SELECT` on `execution_targets` including `owner_user_id` and `capabilities`;
  - column `UPDATE` on `worker_token_hash`, `device_generation`, `status`, `last_seen_at`,
    `updated_at`.

  It also grants `aoa_operator` DML on `workers`.
- **`JOB-009`** (`0224_job_placement_authority_grants.sql`, `JOB-009-result.md`) adds column `SELECT`
  for placement authority.

The current grant set is what `PLAN_DERIVED_ACL_MATRIX` records, and it is certified by those
tickets' results, not by P1. **P1's completion at its accepted revision `21335854f1fe` is
unaffected.** This attempt's job was to repair `a5`'s provenance: `a5` was rewritten in
`7843b86e2`→`6b1af52a4`, which breached its immutability. It does that, and it records honestly
that P1's wording is historical. The `a6` QA record states that **neither failure touches H-01**: row
visibility on `execution_targets` is still owned by forced RLS. That is its reasoning about the
failures, not a separate H-01 measurement, and it is quoted here as such.

## Decisions and findings

- **This attempt satisfies the M1 entry bullet** "E0–E2 historical completion evidence has passed
  a current dependency/delta review, including superseding records for any immutable-record
  breach". The combined evidence is:
  - the delta review (`docs/replatform/milestones/M1a/2026-09-21-e0-e2-delta-review-b71f0dd539fe.md`);
  - this superseding pair;
  - the disposition above (superseded by design, with the successor tickets named).

  The bullet asks for a delta review and a superseding record, not a `pass`. What M1 relies on is
  tenant isolation on the *current* tree, and M1 proves that itself through ruling F10's tenant
  matrix (`DEP-018`), not through P1's historical wording. This is recorded in `scope-triage.md`.
- **QA F-2 becomes finding `E2-F016`.** The `e2-serving-role-correction` integration suite
  re-applies `0214` in setup, which restores the E2-era grant set before asserting. It is green
  against a database state production never has.
- No further attempt is planned. A carry-forward pinned at `21335854f1fe` would re-prove what
  nobody disputes (P1 held at its revision) and would add nothing about the current tree.

## Compatibility and rollout

No code, schema, or flag change. This record changes no epic's status.

## Next unblocked work

The M1 campaign entry no longer waits on E0–E2 evidence. The remaining M1 entry items are tracked
in `docs/replatform/qa/2026-09-21-m1-execution-plan.md`.
