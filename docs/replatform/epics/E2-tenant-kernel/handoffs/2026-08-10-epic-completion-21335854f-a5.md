# Handoff — E2 tenant-kernel corrective prerequisite fix-round-2 candidate

**Date (UTC):** `2026-08-10`

**Candidate code revision:** `21335854f1fe33773b1ef70b4b5da9bc8f618f3f`

**Supersedes:** a4 only if the distinct reviewer accepts this candidate

**Decision:** `awaiting_review`

**Gate owner:** `TBD — distinct reviewer`

This handoff is intentionally non-passing. It neither completes prerequisite P1 nor
authorizes JOB-001, JOB-002, or any other E3 work.

## Candidate correction

- Startup binds both `session_user` and `current_user` to the exact expected bounded
  role. Masked privileged URLs fail before serving; an accepted login cannot recover
  broader authority with `SET ROLE NONE`.
- Exact effective-authority audits cover ordinary and partitioned tables, views,
  materialized views, and foreign tables with consistent table/column checks. An
  adversarial view over `company_secrets` fails closed.
- Additive custom migration 0215 grants `aoa_app` only the seven target columns read
  by the current heartbeat pinned-target resolver. Token, owner, capabilities, and
  liveness columns remain denied, and operator authority does not change.
- Focused embedded-Postgres acceptance is implementer-observed green at 56/56;
  migration idempotency is 5/5; affected/full typecheck and build pass. The full
  repository test run completes with 2005 files/18,803 tests passing and only the
  independently reproduced Windows worker-protocol collection baseline failing.

## Evidence paths

- Result ledger: `docs/replatform/epics/E3-job-control/prerequisites/E2-serving-role-correction-result.md`
- QA candidate: `docs/replatform/epics/E2-tenant-kernel/qa/2026-08-10-d0-e2-tenant-kernel-21335854f-a5.md`
- Implementation report: `.superpowers/sdd/implementation-plan/prereq-p1-e2-role-correction-report.md`

## Reviewer action required

Review the exact docs revision, independently validate the masked-identity,
table-like-authority, and real-resolver cases, classify repository-wide baselines,
and then issue the prerequisite decision. Until that happens, E3 must not begin from
this handoff.
