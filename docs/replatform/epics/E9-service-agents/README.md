# E9 — Long-running Service Agents

**Status:** `backlog`
**Depends on:** E7; SVC-007 additionally requires `E10-REALTIME-FOUNDATION`
**Tickets:** SVC-001 through SVC-008 (SVC-001..007 are control-plane). **SVC-008 split on 2026-09-09** into **SVC-008a** — the `SandboxProvider` process primitive (`tickets/SVC-008a-design.md`; the work belongs to **E4/CLI's port lane**, and it is the specified fix for E7-F034) — and **SVC-008b**, the daemon-side supervisor + capability widening that E9-F002 requires. 008b depends on 008a; both resolve to the ticket id `SVC-008` for every register guard. **Both are SHIPPED** (008a `088de1084`; 008b `tickets/SVC-008b-result.md`). A service job can now be offered to a worker and is supervised as a service — **and E9-F002 stays open**: its resolve criterion is a conjunction and only the T0 conjunct holds, because the never-re-minted effect authority bounds a service to 240 s (SVC-008 §9.1, unruled).
**SVC-002 is SHIPPED** (`tickets/SVC-002-result.md`): the desired-state reconciler and its arming path, with `service_instances_live_service_uq` as the duplicate-placement authority and the `desired_state='running'` predicate `serviceSourceIsAdmitted` never had. **It does NOT move the exit gate**: nothing creates a service (SVC-007), nothing writes a `service_generations` definition (SVC-007, so every pass stalls at `no_generation` on a real deployment), and the loop converges once and goes quiescent because `recordServiceHealth` still has no consumer (SVC-003). It opened **E9-F003** and closed no finding.
**SVC-003 is PARTLY SHIPPED as SVC-003a** (`tickets/SVC-003a-result.md`; terrain + design in `tickets/SVC-003a-design.md`, both written by that unit because **SVC-003 had zero files on disk**). It closes the seam this README, `SVC-002-result.md` §7 and `SVC-008b-result.md` §7 all named: a service observation now PROJECTS. A worker event that witnesses an instance fact moves `service_instances.status` inside the fenced ingest transaction, resolved from SVC-002's `job_id`/`attempt_id` attribution rather than the worker's caller-controlled payload, fenced on the instance's generation (the fence SVC-002 handed over), and refused when the FROZEN transition table forbids the move — which is what stops a late event resurrecting a terminal instance into `service_instances_live_service_uq` alongside its replacement (DE-12). It arms `canTransitionServiceInstanceStatus`, which had **zero production callers**. It **CLOSES E9-F001** on both conjuncts, and **OPENS E9-F004** — external review of PR #410 found that the frozen table makes `stopping` the SOLE predecessor of `stopped` while **no frozen worker event can assert `stopping`**, so the first revision refused every normal service stop and wedged the instance permanently; it survived a named positive control and thirteen killed mutants because the only end-to-end case drove `service_instance_lost`, the one status reachable from everything. **E9-F005** (a failed attempt with no service event stranding the instance) was filed and resolved in the same commit. **It does NOT move the exit gate**: nothing still creates a service or writes a generation (SVC-007), and **SVC-003 stays OPEN** — its Outcome is a five-way conjunction and only `health` is delivered. Not delivered: the **liveness deadline** (a worker that goes silent without emitting anything still leaves its instance live forever — the largest omission, SVC-008b §6.2 handed it here), graceful stop (SVC-005), checkpoint request (unstorable — SVC-004), and bounded lease renewal (only its negative half is pinned: a health event does not extend ownership). E9-F002 and E9-F003 are untouched.
**SVC-003 remains PARTLY SHIPPED after SVC-003b** (`tickets/SVC-003b-result.md`; design in `tickets/SVC-003b-design.md`). It delivers the **liveness deadline**, the omission SVC-003a named as its largest: a live `service_instances` row whose worker has stopped being observed is now driven `lost` **by a clock rather than by an event**, leaves `service_instances_live_service_uq`, and is replaced by SVC-002 unchanged **in the same tick**. TWO WINDOWS THAT NEVER SUBSTITUTE: an instance that HAS been observed is aged against a short liveness window; one that has NEVER been observed has no liveness fact to age and is aged against `created_at` under a separate, longer admission window — collapsing them in either direction kills either starting services or working ones, and both collapses are mutation-pinned. The deadline lives BESIDE THE RECONCILER, inside its own tick: the projection is edge-triggered by events and the whole failure is their absence, and the lease reaper is structurally blind to it because `lease-renewal.ts` renews on a driver separate from the supervise loop, whose `unknown ⇒ EMIT NOTHING` arm is the silent case itself. `L-T8` measures rather than asserts that the shipped reaper terminalizes the attempt and the job and touches `service_instances` not at all. It **OPENS E9-F007** (the deadline terminalizes the INSTANCE and does not fence the WORKER, so a silent-but-still-renewing worker overlaps its replacement — the replacement row is protected by the split-brain refusal, the old worker external effects are not), **E9-F008** (three of the six frozen control-command kinds have ZERO producers, `checkpoint` cannot be persisted at all, and a repository docstring says otherwise) and **E9-F009** (a `lost` row records the STATUS and not the AUTHOR, so a deadline kill and a worker-reported loss are indistinguishable after the fact — the instance-specific half is delivered, the DURABLE half deliberately is not). ★ External review of PR #413 found a REAL defect in the diff and it is recorded rather than folded in: the bounded sweep ordered by `created_at`, so healthy instances — which never leave the live set — filled the batch forever and a silent instance created after them was NEVER INSPECTED. That is PR #406's starvation bug rebuilt one function along, beside its own correction note. **It does NOT move the exit gate** and **SVC-003 STAYS OPEN**: two of the five Outcome conjuncts — graceful stop and checkpoint request — are blocked on E9-F008, and bounded lease renewal is unchanged (each renewal extent is bounded and always was; the TOTAL is unbounded and bounding it needs SVC-005 TTL/budget columns).
**SVC-007 is PARTLY SHIPPED as SVC-007a** (`tickets/SVC-007a-result.md`; terrain + design in `tickets/SVC-007a-design.md`, both written by that unit because **SVC-007 had zero files on disk**). It makes false the one sentence `SVC-002-result.md` §7, `SVC-003a-result.md` §2 and this README all carried: **something creates a service, and something writes a generation.** An org admin can create a service and its immutable generation-1 definition in ONE transaction; SVC-002's reconciler then converges it into exactly one instance and one `service` job built from the STORED definition (proven through `createServiceReconciler(...).tick()` — the same sweeper the composition root drives, with its admitted-organization enumerator stubbed, so what is proven is that a created service ENTERS `listReconcilableServices`'s window rather than that the process wiring runs — not only through the per-service pass); and the operator can move desired state through the FROZEN `SERVICE_DESIRED_TRANSITIONS` table — arming `canTransitionServiceDesiredState`, which had **zero production callers**, the second dead frozen fence this epic has armed. A stop also reaches the SHIPPED `requestCancellation({graceful:true})` channel, so it is not a button that moves a column and nothing else — and the desired-state write, that cancellation and the instance terminalization are ONE transaction under the service's own row lock, which is the fix for the P1 external review of PR #412 raised (the first revision released the lock between them, so a concurrent `stopped → running` in that gap meant the older stop drained a job the operator had already resumed). Review also found that `validate(...)` middleware ran BEFORE the authority gate; the body schema is now parsed inside the handler after it. It **OPENS and RESOLVES E9-F006** in the same commit: `requestCancellation` FINALIZES rather than drains when there is no fenced worker, emitting no event, so SVC-003a's attempt-terminal backstop could not fire and the instance stayed non-terminal inside `service_instances_live_service_uq` forever — stop-then-resume would have converged nothing on every tick, and that is the NORMAL path today because E9-F002 keeps service jobs unleased. **It does NOT move the exit gate and does not claim it**: nothing rolls a generation (SVC-005), nothing accepts a TTL or a checkpoint (SVC-004/SVC-005), the view carries no checkpoint/budget/restart history, ~~no `activity_log` row is written for a control action (DE-01)~~ — **corrected by SVC-007b below: `E9-F010` measured that decline false and both mutating service routes now write one; the three JOB-008/submission mutations beside them still do not** — create is not idempotent, no `E10-REALTIME-FOUNDATION` claim is made, and — the largest — **no service job is leased anywhere in its suite**, so the DAEMON half of "created, supervised, projected" is still unexercised. **SVC-007 stays OPEN.** E9-F002, E9-F003 and E9-F004 are untouched.
**SVC-007 remains PARTLY SHIPPED after SVC-007b** (`tickets/SVC-007b-result.md`). It is an AUDIT unit, and its first job was to test the claim it inherited rather than build on it. SVC-007a had declined an `activity_log` row for its routes and given a mechanical reason — `jobAuditBridge.recordAcceptedActivity` requires a `fence: ActiveFenceRequest`, a service create has no attempt and a desired-state change has no fence, so the row was *"currently unwritable from here"*. **Half of that is right and half is measured false, and the false half is `E9-F010`.** The fence requirement is real; it is the BRIDGE's requirement, not the TABLE's. `insertActivityLog` takes a plain `Db`, `aoa_app` holds `GRANT SELECT, INSERT ON activity_log` (`0213:98`, re-affirmed `0214:166`) under no RLS — `0245`'s own header says so — and a **fenceless transactional `activity_log` write already ships on the distributed path**: `stageJobInputFiles`, measured at **2** production callers against `jobAuditBridge`'s **0**. So the fenced bridge was the deviation and the direct transactional write the norm. The replacement for JOB-013's receipt is not a weaker guard but a stronger one: a control action is RE-REQUESTED, not replayed, so the audit row is written in the SAME tenant transaction as the mutation and the two cannot be written apart — pinned by extending SVC-007a's own T1/T13 rollback probes to count audit rows. Both **MUTATING** service routes now write one row (`service.create` / `service.desired_state`), on the two verdicts that can mutate and on no refusal, carrying the operator's `reason` — which a RESUME previously put nowhere durable, the half of external review's P1 that SVC-007a fixed only for the process log. Twelve mutants, all killed; SVC-007a's T9 positive control green under every one. It also **OPENS E9-F011**: `createServiceWithinTenant`'s `null` branch COMMITS a service with no generation — the permanent `no_generation` wedge — while its docstring said the transaction rolls back; unreachable by construction today, so the docstring is corrected and the fix left to SVC-005's rollout, which makes the conflict constructible for the first time. **It does NOT move the exit gate and does not claim it.** `E9-F010` **STAYS OPEN**: three mutating endpoints on the same router — job submission, `drain` and worker `revoke` — still write nothing durable, and they are unwired rather than blocked. Half of a conjunction is not it. Everything SVC-007a listed as not delivered is still not delivered, `jobAuditBridge` still has zero production callers, and DE-01 is untouched — its `audit` clause is about policy-denial events, not mutating actions. **SVC-007 stays OPEN.**
**SVC-005 is PARTLY SHIPPED as SVC-005a** (`tickets/SVC-005a-result.md`; terrain + design in `tickets/SVC-005a-design.md`, both written by that unit because **SVC-005 had zero files on disk** — the fourth E9 ticket in that condition). It makes false the one sentence SVC-002's design, SVC-007a's `updateServiceDesiredState` docstring and the DE-12 register row all carried: **`services.generation` has no writer**, on which E0-F013's founder ruling dropped DE-12's audit conjunct as VACUOUS. An org admin can now roll a service to its next generation — the immutable generation-N+1 definition and the `services.generation` compare-and-set commit in ONE transaction under the service's own row lock, and the live instance is drained through the SAME `requestCancellation({graceful:true})` + attempt-terminal-backstop composition SVC-007a's operator stop uses. ★★★ **But the writer is the easy half, and the ticket is named for the other one.** E9's acceptance — *"no two generations may perform external effects simultaneously"* — is a claim about two PROCESSES, and the dangerous moment is not the bump (which performs no external effect) but a generation N+1 instance STARTING beside a generation-N worker that never stopped. Overlap of two PLACED generations was already structurally impossible under `service_instances_live_service_uq`; the hole is **E9-F007**, where SVC-003b's deadline condemns a silent instance BY A CLOCK and the row leaves that index while its worker may still be running. So the fence lives at **placement**: step **4b** of `reconcileServiceWithinTenant` refuses a **cross-generation** placement while a previous generation's instance is terminal-by-ASSUMPTION and its attempt is non-terminal, using the new durable `service_instances.terminalized_by` (migration `0279`) to tell a worker's own witnessed stop from a clock's assumption — which is **E9-F009's instance-specific durable half**, delivered here because the fence needs it as an INPUT rather than as telemetry. ★ **It is a stall, not a wedge**: it clears when the old attempt goes terminal, because `classifyFence` returns `attempt_terminal` before any other test, and lease expiry plus `reapExpiredLeases` reaches that state without the worker's cooperation. ★ **It weakens nothing**: SVC-003a's stale-generation refusal still compares against the INSTANCE's generation (both directions measured after a real bump), and SVC-003b's SAME-generation replacement is deliberately unfenced — E9-F007 §3 ruled that overlap the smaller harm and SVC-005a does not reopen it. **It OPENS E9-F012**: a closed fence stops the old worker WRITING, not its PROCESS, so the acceptance clause has an un-closeable half and **the clause is NOT claimed** — what is claimed is *"no two generations are PLACED while the older one is un-drained or unwitnessed"*. ★★★ **External review of PR #415 found a REAL fail-open in the diff and it is recorded rather than folded in**: the first revision had ONE `worker_event` terminal author, but the daemon emits `service_instance_lost` when `inspect` could not describe the sandbox OR when "a full stop ladder ended with the process still observed `running`" — so the fence would have read the worker’s own report that THE PROCESS SURVIVED CANCEL AND KILL as proof that it stopped. Fixed in the AUTHOR rather than the fence (`worker_stopped` / `worker_unconfirmed`, derived from the status written so the default is fail-closed); `R-T7c` reds on the original defect while `R-T7` stays green, which is exactly the asymmetry that let a fully green suite ship it. Review also raised that the roll writes no `activity_log` row (P2) — real, and NOT closed here. It was pre-existing when measured: at base `c27feeea8` and at this branch's pre-merge head, neither sibling control on the router wrote one either — not asserted about any other branch. ★ **That measurement is superseded by the merge of SVC-007b (#414)**, which wires an `activity_log` row into both MUTATING sibling routes (`service.create` / `service.desired_state`). The roll route is therefore now the ONLY mutating service route on this router that writes nothing durable, and it joins the unaudited-route class `E9-F010` tracks as its still-open half. It is deliberately left as FOLLOW-UP rather than wired in a merge resolution, which is not the place to add behaviour. A wrong mutation prediction is also recorded rather than quietly fixed (the witness test lives in two places, so either single-site mutant was survived and the SQL half was covered by nothing until `R-T7b` was added). ★★★ **A SECOND review round found a FALSE CLAIM OF ENFORCEMENT, which is worse than a missing check**: the author list is spelled in THREE places (the TypeScript constant, the Drizzle `check()` literal, migration `0279`'s DDL) and three separate records described that as reconciled — but `T-P5` read only the migration, so the schema comment's claim that its literal was reconciled *"by an assertion that asserts set EQUALITY"* was false, and it gave the next reader a reason NOT to write the assertion. The same comment also said **three** authors over an `IN`-list holding **four** — the token `worker_event` had been swept and the bare word `three` had not. Made TRUE rather than re-worded: `T-P5c` reads the Drizzle literal, `T-P5d` reconciles the two DDL copies, and mutant **P2a** (a fifth author added to the Drizzle literal alone) was observed RED on both while `T-P5` stayed green — the divergence that was invisible to the whole tree before. See `SVC-005a-result.md` §3c. **It does NOT move the exit gate and does not claim it**, and **SVC-005 stays OPEN**: worker drain is blocked on E9-F008 (the frozen `drain`/`graceful_stop` kinds have zero producers and a TYPE-level narrowing against them), replace-BEFORE-stop is structurally unreachable under the live-instance index, hard runtime/spend limits and stuck-stop force-kill are unbuilt, and no service job is leased anywhere in its suite (E9-F002). E9-F001–F008 are untouched — as are **E9-F010** and **E9-F011**, which arrived with SVC-007b in the same merge and which SVC-005a neither opened nor moved; E9-F009 gains a delivered/not-delivered split and stays OPEN.
**Exit gate:** desired state, generation, placement, health, restart, checkpoint, drain, budgets, UI, and the 72-hour D4 continuity/reconciliation canary pass without public ingress.

## Mandatory planning brief

The E9 plan models desired service, immutable generation, service instance, attempt, and lease separately. Health never renews ownership. JOB-009 placement is re-evaluated on replacement; owner/locality/credential/fallback constraints persist. D4 runs for 72 wall-clock hours across control-plane/worker restarts, partition, E2B pause/resume or replacement, drain, generation rollout, checkpoint restore, and budget/TTL stop. This is checkpoint-and-reconcile continuity under the accepted E2B caveat, not a promise of one uninterrupted sandbox. SVC-007 cannot claim reconnect-safe continuity until `E10-REALTIME-FOUNDATION` passes. Service is a mandatory private-beta workload: E9/D4 blocks REL-005 even when its exposure flag is off. Public ingress remains unrepresentable.
