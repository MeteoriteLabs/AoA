# E0 QA Results

Each QA campaign creates an immutable `<YYYY-MM-DD>-<lane>-<scope>-<sha12>-a<attempt>.md` from the QA-result template. E0 requires one final D0/repository verification record at its completion revision.
