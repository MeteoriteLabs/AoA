# E1 Findings

New findings use IDs `E1-F001`, `E1-F002`, and so on.

## E1-F001 — PRT-002 Step-2 illustrative transition maps diverge from the E0 lifecycle JSON authority

- **Severity:** Medium (would produce an incorrect state machine if transcribed literally).
- **Blocks gate:** No.
- **Blocks execution until amended:** Yes (execution paused for a plan amendment per operator directive; PRT-002 must not be implemented from the divergent examples).
- **Discovered during:** E1 controller read-back / pre-implementation cross-check (before PRT-001).
- **Evidence:** The canonical lifecycle authority is `docs/architecture/distributed-execution-lifecycles.json` (Decision #121; frozen E0/FND-001 output). The E1 `implementation-plan.md` PRT-002 Step 2 (Task 2) embeds *illustrative* expected transition maps that omit real edges present in that JSON and, in one case, assert a phantom edge the JSON does not contain:
  - **attempt:** plan omits `pending→expired`, `leased→cancelled`, `cancel_requested→succeeded`; plan asserts `offered→cancel_requested`, which the JSON does **not** have (JSON has `offered→cancelled`).
  - **browser_session:** plan omits `queued→expired`, `leased→cancelled`, `starting→cancelled`, `active→cancelled`, `waiting_approval→cancelled`, `cancel_requested→succeeded`.
  - **service_instance:** plan asserts `pending→stopped` (a phantom; JSON has `pending→lost`) and omits `leased→failed`.
  - `job`, `lease`, and `service_desired` illustrative maps already match the JSON.
- **Affected tickets:** PRT-002 (result ledger + tests + `states.ts`).
- **Root cause / resolution:** The plan's own **normative** hardening amendment ("PRT-002 — separate lifecycle machines") already states the E0 JSON is authoritative — "Generate transition constants/predicates from the E0 JSON authority **or** validate byte-for-byte semantic parity with it," and "The E0 JSON owns these edge conditions." The Step-2 examples are non-normative and stale. Proposed disposition: amend PRT-002 Step 2 to (a) correct the attempt/browser/service-instance illustrative maps to the exact JSON edges and (b) require a parity test that loads `distributed-execution-lifecycles.json` and asserts the embedded `states.ts` maps/guards/terminals equal the JSON's `allowed`/`guards`/`terminal` sets exactly (the same authority-cross-check pattern PRT-004 uses for the shared canonicalizer). Runtime `states.ts` embeds literal maps (no `fs`); the `.test.ts` performs the parity load. **Resolved: decision [E1-D001](decisions.md) locked and PRT-002 Step 2 amended (2026-08-09) by the operator acting as custodian / Integration Gate Owner.**

## E1-F002 — PRT-006 golden-journey parity is vocabulary/enum membership, not full-object schema parse; `emits` vocabulary is broader than the worker-event union

- **Severity:** Medium (literal implementation of two Step-6 bullets would fail against the frozen fixtures).
- **Blocks gate:** No.
- **Blocks execution until amended:** Yes (PRT-006 `golden-journeys.test.ts` design).
- **Discovered during:** E1 controller read-back / pre-implementation cross-check (before PRT-001).
- **Evidence:** The frozen FND-004 golden-journey fixtures (`tests/fixtures/distributed-execution/*.json`, immutable E0 output validated by `schema-v1.json`) use identity encodings and an emission vocabulary that differ from the E1 wire schemas:
  1. **`emits` vocabulary:** `steps[].emits` in the fixtures contains 4 names outside PRT-004's worker-event union — `artifact_transfer_rejected`, `quarantine_grant_issued`, `quarantine_receipt_finalized`, `replacement_lease_activated` (artifact/quarantine/lease **operation** concepts owned by PRT-003/005/007, not worker events). `expectedEvents[].eventType` additionally uses `budget_exhausted`, `lease_lost`, `cancel_requested`, `producer_safety_rejected`, `provider_pause_observed` — control-plane journey events, not worker→control-plane wire events. The plan's Step-7 note ("golden journeys pass once event-name mapping includes `network_denied`") understates this.
  2. **`source` shape/encoding:** the fixture `Source` is `{ kind, runId?, issueId? }` with ULID-style IDs (`run_…`, `issue_…`) and no `requestedBy`/`executionPrincipal`; it therefore **cannot** parse through the full `executionSourceV1Schema` (UUID-branded IDs + mandatory typed principals). Likewise fixture events use `^[0-9A-HJKMNP-TV-Z]{26}$` IDs and an integer `fenceToken`, versus the E1 UUID/base64url wire encoding.
- **Affected tickets:** PRT-006 (`golden-journeys.test.ts`). (PRT-004's digest cross-check is unaffected: it canonicalizes the raw fixture event objects and reproduces the committed `eventDigest` regardless of E1 wire schema.)
- **Root cause / resolution:** The fixtures are immutable frozen E0 authority, so the PRT-006 test must conform to them. The two Step-6 bullets ("source parses through `executionSourceV1Schema`…" and "every `emits` value parses as a known worker event type") should be reframed as **vocabulary/enum-membership parity**, not full-object schema parse: assert fixture `source.kind` ∈ the `ExecutionSourceV1` discriminant set, and assert each `emits` value ∈ a reviewed "known distributed-execution emission vocabulary" = the worker-event type union ∪ the frozen non-event operation/receipt names above. **Resolved: decision [E1-D002](decisions.md) locked and PRT-006 Step 6 amended (2026-08-09) by the operator acting as custodian / Integration Gate Owner.**

## E1-F004 — PRT-003 local extension byte-sizer diverges from the E0 RFC-8785 canonical authority: `jobEnvelopeV1Schema` fails OPEN on non-canonicalizable extension values (floats, unsafe integers, lone surrogates)

- **Severity:** High (fail-open at a `.strict()` security-critical wire envelope; admits values the program's canonical/event pipeline cannot process).
- **Blocks gate:** No (epic gate). **Blocks PRT-003 approval:** Yes (reviewer disposition `changes_requested`).
- **Discovered during:** PRT-003 independent review (reviewed revision `d381a3a46f16f5ef05c26a0b8101be63509f927a`).
- **Context:** PRT-003 must bound each extension value at "≤16,384 **RFC-8785-canonical** UTF-8 bytes" (plan hardening amendment, [implementation-plan.md](implementation-plan.md) line 71). Because the shared `canonical-json.ts` is a PRT-004 deliverable, `job.ts` carries a small **self-contained** canonical serializer (`canonicalString`/`canonicalJson`/`canonicalByteLength`, lines ~161–196) used only to size extension values (the implementer's own Concern #1 / Deviation 1). The program pins "RFC-8785 canonical JSON" to the **frozen E0 implementation** `scripts/check-distributed-execution-foundation.mjs` (`canonicalizeJson`), and the amendment's PRT-004 clause (line 97) **explicitly enumerates** "lone surrogates, unsafe-integer boundary … `-0`" as the exact edge cases where the two canonicalizers must **not** diverge.
- **Evidence (independently reproduced against the reviewed revision):**
  - **In-subset agreement is perfect** — a 23-case battery (nested objects/arrays, multibyte `é`/astral emoji, combining NFD, every control-char escape, integers, `-0`, booleans, `null`, key-ordering variations, unicode keys) produced **byte-for-byte identical** canonical strings and byte counts to E0; the real-schema value byte-budget boundary matches E0's count exactly at 16,384 pass / 16,385 fail for ASCII **and** multibyte (`é`). So the sizer is correct for representable values.
  - **Out-of-subset DIVERGES on accept/reject.** E0 `canonicalizeJson` **rejects** floats ("float is not allowed"), unsafe integers ("unsafe integer is not allowed"), and lone/broken surrogates ("lone … surrogate in string"). The local `canonicalJson` **silently accepts** all of them (numbers via `String(value)` with only a `Number.isFinite` guard; strings emit surrogate code units verbatim, and `TextEncoder` then substitutes U+FFFD). The structure walk `addExtensionValueStructureIssues` only flags non-**finite** numbers, so floats, unsafe integers, and lone-surrogate strings pass unflagged.
  - **The divergence reaches the REAL `jobEnvelopeV1Schema`.** `jobEnvelopeV1Schema.safeParse(job)` **returns `success: true`** for a job whose extension value is `1.5`, `9007199254740992` (2⁵³), a lone-surrogate string `"\uD800"`, `{ rate: 0.25 }`, or `[9007199254740992]` — every one of which E0 declares non-canonicalizable. It also **propagates through `leaseOfferV1Schema`** (a lease offer whose nested job carries a float extension value is accepted).
  - **Concrete miscount for unsafe integers:** the local sizer serializes input `9007199254740995` as `"9007199254740996"` (double-precision rounding), so the "canonical bytes" it charges against the budget do not even correspond to the supplied integer.
- **Impact:** (1) A `.strict()` security-critical envelope fails **open**, admitting extension values that have **no** RFC-8785-subset canonical form. (2) The "≤16,384 RFC-8785-canonical bytes" invariant is meaningless for such values (phantom / miscounted size), a budget-bypass surface. (3) Downstream break: the same job, once embedded in an event and digested via the authoritative E0/PRT-004 canonicalizer (`eventDigest = SHA-256 of RFC-8785 canonical JSON`, amendment line 95), will **throw** — a job PRT-003 declared valid cannot traverse the event pipeline. This is exactly the silent two-canonicalizer divergence the program's cross-implementation cross-check (line 97) exists to prevent, and the implementer's self-report claim that "the byte semantics already match the amendment" holds only for in-subset values.
- **Affected tickets:** PRT-003 (fix now); PRT-004 (the shared `canonical-json.ts` unification is the eventual convergence, and its cross-implementation digest cross-check would surface any residual gap).
- **Root cause / resolution:** The local sizer is a third canonicalizer whose accept/reject boundary was never aligned with the frozen E0 authority. **Required fix (within PRT-003 scope, no PRT-004 pre-emption):** make an extension value that is not canonicalizable under the frozen RFC-8785 subset **fail closed** — reject non-integer numbers, non-safe integers, and lone/broken surrogate strings, matching E0's `canonicalizeNumber`/`canonicalizeString` reject behavior (making the local `canonicalJson`/`canonicalString` throw on those is minimal, since `addExtensionArrayIssues` already converts a canonicalizer throw into a "not canonicalizable" issue; ideally also flag the offending path in `addExtensionValueStructureIssues`). Add tests asserting float / unsafe-integer / lone-surrogate extension values are rejected.
- **Status: RESOLVED (resolving revision `e62921b1743f9cbc33e6ecf79348a7f4bf5d5483`).** `job.ts` `canonicalString` now throws on a lone high surrogate (not followed by a low) and on a stray low surrogate; `canonicalJson` throws on `!Number.isInteger` (float) and `!Number.isSafeInteger` — byte-for-byte with the E0 authority's `canonicalizeString`/`canonicalizeNumber`, and the existing `addExtensionArrayIssues` try/catch turns the throw into a fail-closed "not canonicalizable" `custom` issue at the extension value path. Independently re-verified at PRT-003 review **attempt 2 (`approved`)**: `1.5`, `9007199254740992`, `"\uD800"`, `"\uDC00"`, `{rate:0.25}`, `[9007199254740992]` now return `success:false` on BOTH `jobEnvelopeV1Schema` and `leaseOfferV1Schema` (nested-job + own-extensions, issue path at the extension value); in-subset values still pass with 23/23 byte-for-byte E0 agreement and the value-budget boundary unchanged (16,384/16,385); valid astral emoji pairs are not falsely rejected. Diff scoped to `job.ts` (+26) + `job.test.ts` (+13 assertions) + ledger; focused 93/93, full 124/124, typecheck/build/boundary all exit 0.
- **Non-blocking follow-up (PRT-004): RESOLVED-IN-FULL.** The local `job.ts` extension byte-sizer was a second canonicalizer duplicating the E0 semantics. PRT-004 Deliverable B unified `job.ts` onto the shared `canonical-json.ts` (single canonicalizer), and Deliverable C additionally extracted the bounded-extension container/refiner into the shared `extensions.ts` (single refiner) — so the package now carries **one** canonicalizer AND **one** extension refiner across job/lease/events. Independently re-verified at PRT-004 review attempt 2 (`approved`, `0a00e3f3a61f1e532b11fb526e209589f7a0fe69`): grep confirms the sole canonicalizer definitions are in `canonical-json.ts` and the sole extension-refiner definitions are in `extensions.ts`; the cross-implementation digest cross-check reproduces all 50 committed `eventDigest`s across 9 fixtures with zero mismatches. This follow-up is complete; no residual drift.

## E1-F003 — PRT-002 `index.ts` uses `export *`, defeating the package's explicit-public-exports review convention

- **Severity:** Medium (review integrity). The functional wire surface is currently correct — I verified `export *` re-exports exactly the intended 48 identifiers and leaks no transition-map/guard/private helper — so this does **not** block the E1 gate. It does, however, require a fix before PRT-002 approval (reviewer disposition `changes_requested`), because it violates a stated, twice-documented convention on the one file whose diff the reviewer must scrutinize, and it compounds across the remaining tickets.
- **Blocks gate:** No. **Requires fix before PRT-002 approval:** Yes.
- **Discovered during:** PRT-002 independent review (reviewed revision `5288189b73b2fad3709efecf002fef6dcdb24d39`).
- **Evidence:**
  - The plan's File responsibility map ([implementation-plan.md](implementation-plan.md) line 48) states the epic-wide constraint: `index.ts` is **"Explicit public exports only."**
  - PRT-006 Step 8 (line 1435) restates it with rationale: "Update `index.ts` to explicitly export every public constant, schema, predicate, helper, and inferred type from `ids`, `states`, `job`, `events`, `policy`, `artifacts`, `capabilities`, and `version`. **Do not use `export *`; explicit exports make review of wire-surface changes visible.**"
  - Actual PRT-002 `index.ts` (lines 2–3): `export * from "./ids.js";` / `export * from "./states.js";`. The PRT-002 `index.ts` diff therefore shows two wildcards, not the 48 identifiers that became public wire contract; the reviewer had to import the built package to enumerate the actual surface — precisely the review-blindness the convention prevents.
  - `index.ts` is a `Modify` target in **six** tickets (PRT-002/003/004/005/006/007). PRT-003 introduces the **security-critical strict envelopes** (`source.ts`/`job.ts`/`wire-safety.ts`: placement matrix, secret-handle objects). Establishing `export *` at PRT-002 lets those land invisibly in each index diff until a PRT-006 big-bang conversion.
- **Affected tickets:** PRT-002 (fix now); PRT-003…PRT-007 (maintain explicit exports as each module is added).
- **Root cause / resolution:** Replace the two PRT-002 `export *` lines with explicit named exports of the `ids` + `states` public surface — values via `export { … } from "./ids.js"` / `"./states.js"`, and inferred **types** via `export type { … }` (required: the root `tsconfig.json` sets `isolatedModules: true`, so re-exporting a type-only symbol without `export type` errors) — keeping the existing explicit `version` export. The exact 48-name list is already enumerated in the PRT-002 ledger's Delivered scope, so the fix is mechanical. **Counter-reading (noted for the operator):** the "Do not use `export *`" instruction textually sits in PRT-006, and PRT-006's conversion list includes `ids`+`states`, so a "defer to PRT-006" reading is defensible; the reviewer nonetheless requires explicit exports now, per the epic-wide line-48 constraint, the stated review-visibility rationale (which applies at every index diff, not only the last), and to avoid compounding the blindness across PRT-003…PRT-007. The operator/custodian may override to the deferral reading and lock it as a decision. **Resolved in commit `26f6f61d921b3185d4b697e09b45dba27eaec688`** — `packages/worker-protocol/src/index.ts` now uses explicit `export { … }` (50 runtime values) + `export type { … }` (32 inferred types) with no `export *`. Independently re-verified at PRT-002 review attempt 2: the exported wire-surface membership is byte-identical to the pre-fix `export *` surface (built `dist/index.js` runtime keys = `union(version,ids,states)` = the recorded 50-name set; `dist/index.d.ts` re-exports the same 32 types; no private transition-map/guard internals leak), and only `index.ts` + the ledger changed. PRT-002 approved on `26f6f61d9`.

## E1-F005 — PRT-004 worker-event extensions under-enforce the ONE bounded-extension container: `workerEventV1Schema` fails OPEN on value depth/array-items/object-keys/key-bytes and has NO combined budget (job/lease enforce all five)

- **Severity:** High (fail-open at a `.strict()` inbound worker→control-plane wire envelope — the batched, high-volume, less-trusted surface the bounded container exists to fence — on five of the amendment's exact per-value/aggregate extension limits).
- **Blocks gate:** No (epic gate). **Blocks PRT-004 approval:** Yes (reviewer disposition `changes_requested`).
- **Discovered during:** PRT-004 independent review (reviewed revision `590f8162f1234beffd83a759c03bc743357181d1`).
- **Context:** The hardening amendment ([implementation-plan.md](implementation-plan.md) line 71) defines THE bounded-extension container with EXACT limits — "at most 16 extensions; namespace 1–100 UTF-8 bytes; schema version 1–1,000,000; each value has at most **8 container levels, 128 array items, 64 object keys, 100 UTF-8 bytes per key**, and 16,384 RFC-8785-canonical UTF-8 bytes; the **combined canonical value budget is 65,536 bytes**." PRT-004 Step 3 (line 962) requires the worker-event base to carry "**the explicit bounded extension field**" — the same phrase PRT-003 uses for the job/lease envelopes (lines 844/866) and the same ONE container. No E1 decision sanctions a lighter event variant.
- **Evidence (independently reproduced against the reviewed revision, built `dist/`):** `events.ts` applies a bespoke `refineEventExtensions` (lines 383–410) to every event's `extensions` instead of the job/lease `addExtensionArrayIssues` (`job.ts` 219–252). It enforces only count ≤16, unique namespace, unknown-critical-fails-closed, recursive credential-key rejection, and per-value canonical ≤16,384 bytes — and OMITS the value structural walk (`addExtensionValueStructureIssues`) and the combined budget. Comparing the built `workerEventV1Schema` vs `jobEnvelopeV1Schema` on identical extension arrays:

  | Extension value | `workerEventV1Schema` | `jobEnvelopeV1Schema` |
  |---|---|---|
  | container depth 9 | **accepted** | rejected |
  | 129 array items | **accepted** | rejected |
  | 65 object keys | **accepted** | rejected |
  | 101-UTF-8-byte key | **accepted** | rejected |
  | combined >65,536 bytes (5 × 13,200) | **accepted** | rejected |
  | per-value 16,385 canonical bytes | rejected | rejected |
  | namespace 101 bytes / schemaVersion 0 or 1,000,001 / 17th ext / unknown `critical:true` / recursive credential key | rejected | rejected |

  The first five DIVERGE (event fails open where job fails closed); the rest agree. (The shared `wireExtensionSchema` — namespace/schemaVersion/critical shape — is common, so those agree; the divergence is exactly the per-value structural + combined limits carried only by `addExtensionArrayIssues`.)
- **Impact:** (1) A `.strict()` inbound wire envelope admits extension values with no bound on nesting depth, array/object breadth, or key size, and no aggregate budget. (2) The combined-budget gap is *worse* on events than on jobs: events are submitted in **batches of up to 500** and the batch schema imposes no extension budget either, so one event may carry 16 × 16,384 = 262,144 bytes (4× the intended 65,536) and a single batch ~131 MB of extension bytes — an inbound amplification/DoS surface. (3) The depth gap admits ~8,000-level nesting within the 16 KB per-value budget — unbounded-by-contract recursion during the recursive `canonicalizeJsonV1` validation itself and in downstream receiver-side digest recompute (JOB-005). (4) It re-creates the exact anti-pattern E1-F004 closed for the canonicalizer: a SECOND, divergent implementation of the ONE container contract on a wire surface.
- **Affected tickets:** PRT-004 (fix now). Same class carries forward to any later surface that carries `extensions`.
- **Root cause / resolution:** `refineEventExtensions` is a partial re-implementation of the bounded container that silently dropped the value-structure and combined-budget checks. **Required fix:** make the worker-event `extensions` enforce the FULL amendment container (depth ≤8, ≤128 array items, ≤64 object keys, ≤100 UTF-8 key bytes, ≤16,384 canonical bytes per value, combined ≤65,536), identically to job/lease. **Preferred (same lesson as the single canonicalizer):** extract the container refinement into a SINGLE shared module/schema used by `job.ts`, `lease*`, and `events.ts`, so the ONE contract has ONE implementation; delete the bespoke `refineEventExtensions`. Add event-schema tests asserting depth 9 / 129 items / 65 keys / 101-byte key / combined >65,536 each fail at boundary+1 and pass at the boundary — the same boundary battery `job.test.ts` already carries. Consider whether the batch schema also needs an aggregate cap (out of scope for the per-event fix but worth a note).
- **Status: RESOLVED** (resolving revision: the E1-F005 fix commit on `claude/epic-e1-worker-protocol-6a5d96`; repository history pins the SHA). The ONE bounded-extension container — element schema `wireExtensionSchema`, array schema `wireExtensionsArraySchema`, and the FULL refiner `addWireExtensionArrayIssues` (the reviewer-correct job refiner + its value structural walk relocated verbatim, byte-identical) — now lives in the new single-source-of-truth module `packages/worker-protocol/src/extensions.ts`. `job.ts` (envelope + lease) and `events.ts` all import and apply `addWireExtensionArrayIssues`; the bespoke `refineEventExtensions` is deleted. `workerEventV1Schema` now enforces the full container identically to `jobEnvelopeV1Schema`: `events.test.ts` adds the boundary battery asserting depth 9 / 129 array items / 65 object keys / 101-UTF-8-byte key / combined >65,536 each REJECT and the exact limits (depth 8 / 128 / 64 / 100-byte / 16,384 / combined 65,536) ACCEPT, and that the five previously-accepted violating shapes now fail on the event schema. Job behavior is byte-identical (relocation only; all 60 job tests incl. the 13 E1-F004 out-of-subset + 16,384/16,385 boundary still green). The package now carries ONE canonicalizer AND ONE extension refiner. Recursive plaintext-credential rejection stays at the envelope level via each consumer's `addForbiddenWireKeyIssues` (unchanged, byte-identical to the reviewed job topology). A batch-level aggregate cap is intentionally left to PRT-007 (per-operation transport concern), per the coordinator directive. **Independently re-verified and CONFIRMED RESOLVED at PRT-004 review attempt 2 (`approved`, reviewed revision `0a00e3f3a61f1e532b11fb526e209589f7a0fe69`):** the adversarial event-vs-job probe shows the five previously-diverging shapes (depth 9 / 129 array items / 65 object keys / 101-UTF-8-byte key / combined >65,536) now REJECT on BOTH `workerEventV1Schema` and `jobEnvelopeV1Schema` and ACCEPT at the exact limits (8/128/64/100/16,384/65,536) — no remaining divergence; a normalized diff proves the refiner was relocated verbatim (job byte-identical, 60/60 job tests green); grep confirms exactly ONE canonicalizer AND ONE extension refiner; the digest cross-check is unaffected (9 fixtures / 50 events / 0 mismatches); focused 144/144, full 208/208, typecheck/build/boundary all exit 0.
- **Note on Deviation #3 (non-blocking, corrected):** the reviewer is correct — the implementer's original Deliverable-B rationale claimed `addExtensionValueStructureIssues` "independently rejects a non-plain-object extension value," which is factually wrong: a `Date`/`Map`/class-instance extension value is **accepted** by BOTH surfaces (`Date`/`Map` → `"{}"`) because `isPlainObject` and the canonicalizer's `typeof==="object"` branch are equivalent after the null/array checks, so the old local `canonicalJson` `isPlainObject` gate and the shared `canonicalizeJsonV1` agree on ALL inputs and the `{}`-vs-throw divergence never existed. The Deliverable-B swap is byte-identical with no exceptions; the ledger's Deviation section has been corrected to say so. Inert (wire values arrive as JSON, never constructed `Date`/`Map`); not a defect.

## E1-F006 — PRT-004 ticket-result `Reviewed revision:` line carries trailing annotation that the plan's Task-8 Step-1 verification regex rejects

- **Severity:** Low (documentation/tooling format; the reviewed revision is substantively correct and independently verifiable).
- **Blocks gate:** No.
- **Discovered during:** E1 integration gate (Task 8) Step-1 ticket-completeness verification, gate revision `93c5e9f2763a16ce17507fde11b8cac770d5478a`.
- **Evidence:** The implementation plan's Task-8 Step-1 block extracts each ticket's reviewed revision with the anchored regex `(?m)^\*\*Reviewed revision:\*\*\s*` + `` `? `` + `([0-9a-f]{40})` + `` `? `` + `\s*$`. Six ledgers (PRT-001/002/003/005/006/007) have a clean `**Reviewed revision:** `<sha>`` line and match. PRT-004's line is `**Reviewed revision:** `0a00e3f3a61f1e532b11fb526e209589f7a0fe69` (attempt 2, `approved`; attempt 1 reviewed `590f8162f1234beffd83a759c03bc743357181d1`, `changes_requested`)` — the trailing ` (attempt 2, …)` annotation defeats the regex's `\s*$` anchor, so `Match.Success` is `false` and the literal Step-1 block throws `PRT-004-result.md lacks an exact reviewed revision` (empirically reproduced).
- **Substance verified independently:** A tolerant extraction (first 40-hex after the header) yields `0a00e3f3a61f1e532b11fb526e209589f7a0fe69`, which `git cat-file -e …^{commit}` confirms is a commit and `git merge-base --is-ancestor …HEAD` confirms is an ancestor of HEAD; PRT-004's Status is `complete`, Disposition `approved`, with a distinct implementer≠reviewer. The ticket is genuinely complete/approved; only the summary line's format is non-conforming.
- **Affected tickets:** PRT-004 (ledger, frozen at `complete`); the Task-8 plan verification tooling.
- **Root cause / resolution:** The reviewer appended attempt/history annotation onto the summary `Reviewed revision:` field instead of keeping the summary field to the bare backticked latest SHA (the attempt-1/attempt-2 detail already lives in the Review-attempt-history table). Because the ledger is frozen at `complete`, it is not rewritten (a correction would be a new finding/record, not an edit). Disposition (analogous to E0-F001's template-vs-gate-regex format conflict): **non-blocking**; the Task-8 Step-1 regex should tolerate trailing annotation after the 40-hex (capture the first hex; drop the `$` anchor), and future reviewers should keep the `Reviewed revision:` summary line to a bare backticked SHA. Does not block the gate.

## E1-F007 — worker-protocol package-smoke check (`scripts/check-worker-protocol-package.mjs`) is RED on the gate revision: (a) the consumer smoke never provisions the declared `zod` dependency and (b) the `worker-consumer` fixture hard-asserts the stale PRT-001 public surface — **E1 GATE BLOCKER**

- **Severity:** High.
- **Blocks gate:** **Yes** (E1 integration gate). This is the sole E1 gate blocker.
- **Discovered during:** E1 integration gate (Task 8) Step-2, gate revision `93c5e9f2763a16ce17507fde11b8cac770d5478a`. `node scripts/check-worker-protocol-package.mjs` → exit 1 (and 3/3 red in the D0 critical-suite stability loop).
- **Context:** `check-worker-protocol-package.mjs` (PRT-001 deliverable) is a REQUIRED Step-2 E1-scope gate command ("packed protocol import smoke") and a member of the D0 3× critical-suite stability loop. It packs the built package, enforces the tarball allow-list, then extracts the tarball into minimal `server`/`worker` consumer fixtures that import the public root export. It was last exercised at PRT-001 (reviewer PASS) when the package re-exported only the two version constants and had **no** runtime import. It is **not** wired into CI (only the boundary checker is), and **no** later ticket (PRT-002…007) re-ran it — so both regressions below have been latent since PRT-002.
- **Evidence (independently reproduced on the gate revision):**
  - **Defect (a) — declared `zod` dependency not provisioned.** The checker copies only the extracted `@armyofagents/worker-protocol` into `<tmp>/<consumer>/node_modules/@armyofagents/worker-protocol` (lines 162-164) and runs the fixture with `cwd` under `os.tmpdir()`. Since PRT-002 the package's `dist/*.js` import `zod`; Node's ESM resolver walks up from the temp consumer and cannot find `node_modules/zod` (`Cannot find package '…/AppData/Local/Temp/node_modules/zod/index.js' … ERR_MODULE_NOT_FOUND`). Both consumers fail at module resolution before their own assertions run. This is environment-agnostic (Linux `/tmp` has no `zod` up-tree either). A controlled repro that additionally copies the repo's installed `zod` into the consumer `node_modules` makes **server-consumer pass** ("server-consumer OK"), isolating this to the checker's provisioning of the package's declared dependency — the tarball itself is correctly shaped (allow-list, `dist`-only, no `src`/`*.test.*`, `dependencies == ["zod"]`, name correct all pass).
  - **Defect (b) — stale public-surface assertion in `worker-consumer.mjs`.** With `zod` provisioned, `worker-consumer` then throws `AssertionError: public surface must be exactly the version constants`: fixture lines 15-19 assert `Object.keys(protocol).sort()` equals exactly `["MIN_PROTOCOL_VERSION","PROTOCOL_VERSION"]` (the PRT-001 surface), but the current public surface is **275** exports (all ids/states/job/events/artifacts/policy/capabilities/transport/errors schemas). `server-consumer.mjs` carries no exact-surface assertion and passes once `zod` is present.
- **Impact:** A REQUIRED E1-scope gate command and a D0 stability-loop member are RED on the gate revision. Both defective files (`scripts/check-worker-protocol-package.mjs`, `tests/fixtures/worker-protocol-import/worker-consumer.mjs`) are E1-touched, so per DEC-01/DEC-03 this is a regression that is never waivable, and it is not a DEC-03 environment artifact (it would fail on Linux CI too). The E1 gate decision is therefore `fail`; E1 remains in `gate_review`.
- **Scope note (encapsulation not broken):** the tarball-shape and exports-map encapsulation assertions the check chiefly protects all pass; the failure is confined to the import-execution leg (missing declared dep) and a stale exact-surface assertion. The protocol wire contract itself is otherwise green on the gate revision (package typecheck/build; 543/543 package tests; frozen-consumer integrity + bidirectional cross-version + contract-manifest all pass). This is a harness/fixture regression, not a protocol-contract defect — but a red REQUIRED E1-scope check still blocks the gate under the no-conditional-pass rule.
- **Affected tickets:** PRT-001 (owns `check-worker-protocol-package.mjs` + the two consumer fixtures).
- **Root cause / resolution:** A scoped remediation ticket (against PRT-001's smoke deliverables) must (1) provision the declared `zod` dependency into each consumer's `node_modules` (copy the installed `zod`, matching a real dependency install), and (2) replace `worker-consumer.mjs`'s hard-coded surface list with a check that tolerates the real 275-name surface (e.g. assert the two version constants are present + retain the deep-subpath encapsulation assertions, or regenerate the expected set from the built `dist/index.js`). Consider wiring `check-worker-protocol-package.mjs` into the always-on CI policy job so this cannot regress unnoticed again. Then re-run the Task-8 gate on the resolving revision (creating a superseding QA record + completion handoff).
- **Status: OPEN — blocks E1 completion.**
