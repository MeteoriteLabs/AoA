# QA Result — D0 E1-completion rollup, revision b03262692882, attempt 2

**Date (UTC):** `2026-08-09`
**Epic:** `E1-worker-protocol`
**Record path:** `docs/replatform/epics/E1-worker-protocol/qa/2026-08-09-d0-e1-completion-b03262692882-a2.md`
**Scope slug:** `e1-completion`
**Revision:** `b03262692882a7ce17834131ad358d3aecf07f5b`
**Attempt:** `2`
**Supersedes:** `docs/replatform/epics/E1-worker-protocol/qa/2026-08-09-d0-e1-completion-93c5e9f2763a-a1.md`
**Lane:** `D0`
**Result:** `pass`
**Failure class:** `none`
**Campaign start (UTC):** `not_applicable`
**Campaign end (UTC):** `not_applicable`

> This file is immutable from its first commit. A correction, rerun, changed decision, or changed revision creates a higher attempt and links this path through `Supersedes`.

This attempt re-runs the E1 integration gate after the attempt-1 blocker (finding **E1-F007** — the packed-import smoke was RED) was fixed on `233e65b2b` and independently confirmed resolved on `b03262692`. All immutable a1 records are preserved unchanged; this record supersedes the a1 QA record.

## Topology and environment

D0 is a hermetic component gate: no control-plane replicas, workers, object store, realtime/admission stores, or provider. E1 is a new leaf workspace package (`packages/worker-protocol`) plus additive root config; it builds a versioned wire **contract**, not a distributed runtime. No image or desktop artifact is built. No network provider, customer data, or live credential is used (D0-T05). No secrets are recorded.

- **Candidate revision:** `b03262692882a7ce17834131ad358d3aecf07f5b` on branch `claude/epic-e1-worker-protocol-6a5d96` (= the a1 gate revision `93c5e9f2763a…` + the E1-F007 fix `233e65b2b` + its resolution-confirmation `b03262692`; the a1 gate-fail-evidence commit `9224bd771` is an ancestor, so the a1 immutable records exist in history). Program base (E0-complete tip) `c32bbe087` verified an ancestor of HEAD. PRT-007 `BASELINE_SOURCE_SHA` `b7a842870ce7509d8baa75409e0ab19da375c88a` verified a bare 40-hex commit and an ancestor of HEAD.
- **Gate-owner environment:** Windows 11, the OneDrive worktree `…/.claude/worktrees/epic-e1-worker-protocol-6a5d96` (path length 111). Node v24.14.0, pnpm 9.15.4. `node_modules` present; `pnpm install --frozen-lockfile` a no-op in sync. The dependency-free protocol checkers run under stock Node (`node:*` only); the package-smoke checker provisions the declared `zod` dependency into its temp consumers by **offline copy** from the monorepo (no network, no package manager).
- **Contract hashes (current):** `docs/contracts/worker-protocol/v1/manifest.sha256` — `conformance.json` = `872f4e47dc036f6491adbfe8200647ce9dc39955a83277d52ae17f28a7772f71`; `operations.md` = `57d7b048ab65972558029105ccbef30ae9fc1c7e8cb962a54029fad97b4ce775` (unchanged from a1).
- **Frozen-consumer contract hashes:** `tests/fixtures/worker-protocol-consumers/v1/manifest.sha256` pins 18 files; the bundled self-contained `dist/index.js` = `3c854028f3f1c0e7a792bda328515dc4c801e41bf0b0f3c3ee79a3ac7b7294e9` (239,198 bytes; zod 3.24.2 + esbuild 0.28.1; `dependency-lock.json.sourceSha == BASELINE_SOURCE_SHA`) — unchanged from a1. `pnpm check:frozen-worker-protocol-v1` re-verified OK.
- **E1-touched scope (`c32bbe087..HEAD`):** `packages/worker-protocol/**`, `scripts/**` (worker-protocol checkers), `tests/fixtures/worker-protocol-{import,consumers}/**`, `docs/**`, and additive root config (`package.json`, `pnpm-lock.yaml`, `vitest.config.ts`, `.github/workflows/pr.yml`, `.gitattributes`, `.gitignore`). **Zero** `server/`, `ui/`, or `packages/db/` source files touched. The E1-F007 fix (`233e65b2b`) touched only `scripts/check-worker-protocol-package.mjs`, `tests/fixtures/worker-protocol-import/worker-consumer.mjs`, `.github/workflows/pr.yml`, and `findings.md` — no wire-contract source, no frozen fixture, no PRT ledger.

### Frozen schedule and samples (D4/D6)

| Field | Value |
|---|---|
| Schedule manifest path | `not_applicable` |
| Schedule manifest SHA-256 | `not_applicable` |
| Timezone | `not_applicable` |
| Expected samples | `not_applicable` |
| Observed samples | `not_applicable` |
| Missing samples | `not_applicable` |
| Numerator | `not_applicable` |
| Denominator | `not_applicable` |
| Exclusion count and stable fault-window reasons | `not_applicable` |

### D6 mandatory and advertised rows

`not_applicable` — D0 hermetic component gate; no availability SLI, support matrix, or workload rows.

## Commands

Independently re-run by the gate owner on the exact recorded revision `b03262692882a7ce17834131ad358d3aecf07f5b`. The tracked/untracked worktree was byte-clean before and after the gate (`git status --porcelain` empty; `git diff --exit-code` 0).

### Step-1 ticket-completeness verification

| Command | Exit code | Result summary |
|---|---:|---|
| `git rev-parse HEAD` | `0` | `b03262692882a7ce17834131ad358d3aecf07f5b` |
| Step-1 per-ledger Status/Disposition/reviewer/ancestor checks (tolerant extractor) | n/a | PRT-001…007 present; each `Status: complete`, `Disposition: approved`, implementer≠reviewer; each reviewed revision a commit + ancestor of HEAD; ticket blob SHAs unchanged from a1 (ledgers frozen/untouched) |
| Literal plan Step-1 PowerShell block | `1` | Still throws `PRT-004-result.md lacks an exact reviewed revision` — PRT-004's annotated `Reviewed revision:` line (finding **E1-F006**, non-blocking; carried forward from a1). Substance re-verified with a tolerant extractor: reviewed `0a00e3f3…` a commit + ancestor |
| `git cat-file -e b7a842870…^{commit}` + `git merge-base --is-ancestor b7a842870… HEAD` | `0` / `0` | BASELINE_SOURCE_SHA is a commit and ancestor of HEAD |

### Step-2 focused E1-scope suites (all green)

| Command | Exit code | Result summary |
|---|---:|---|
| `pnpm check:distributed-foundation` | `0` | `distributed execution foundation: PASS` |
| `pnpm check:worker-protocol-boundary` | `0` | `worker protocol boundary: PASS` |
| `node --test scripts/check-worker-protocol-boundary.test.mjs` | `0` | tests 50 / pass 50 / fail 0 |
| `pnpm --filter @armyofagents/worker-protocol test:run` | `0` | Test Files 17 passed (17); Tests 543 passed (543) |
| `pnpm --filter @armyofagents/worker-protocol typecheck` | `0` | `tsc --noEmit` clean |
| `pnpm --filter @armyofagents/worker-protocol build` | `0` | `tsc` clean |
| **`node scripts/check-worker-protocol-package.mjs`** | **`0`** | **`worker protocol package: PASS`** — zod provisioned offline; tolerant surface + live `jobEnvelopeV1Schema.safeParse({})` (E1-F007 RESOLVED) |
| `node --test scripts/update-worker-protocol-contract-manifest.test.mjs` | `0` | tests 13 / pass 13 / fail 0 |
| `node scripts/update-worker-protocol-contract-manifest.mjs --check` | `0` | `worker-protocol contract manifest: OK` |
| `node --test scripts/check-frozen-worker-protocol-consumer.test.mjs` | `0` | tests 12 / pass 12 / fail 0 |
| `pnpm check:frozen-worker-protocol-v1 -- --source-sha b7a842870…` | `0` | `frozen worker-protocol v1 consumer: OK (sourceSha b7a842870…, zod 3.24.2, esbuild 0.28.1)` |

### Step-2 integration signals (all green)

| Command | Exit code | Result summary |
|---|---:|---|
| `pnpm install --frozen-lockfile` | `0` | in sync ("Done in 3.4s"); pre-existing plugin-sdk dev-bin WARNs only |
| `pnpm -r typecheck` | `0` | all packages (adapters, plugin-sdk, shared, plugin examples, worker-protocol, server, ui, cli) clean |
| `pnpm -r build` | `0` | recursive build; network-free |
| `pnpm build` (authoritative root) | `0` | root build; byte-clean after |

### Step-2 repository suite (DEC-03) and whitespace

| Command | Exit code | Result summary |
|---|---:|---|
| `pnpm test:run` | `1` | Tests 166 failed / 14439 passed / 39 skipped (14644); Files 539 failed / 1560 passed / 8 skipped (2107). worker-protocol project **543/543 green** (17 files). All failures are pre-existing non-E1 Windows-local environment (drizzle-orm `ERR_REQUIRE_CYCLE_MODULE` cycle + embedded-PG MAX_PATH); **zero E1-touched test-file failures**. Identical profile to a1. Classified `environment` (DEC-03) — non-blocking. |
| `git diff --check` | `0` | no whitespace/conflict errors |

### Step-2 D0 3× critical-suite stability loop (all green)

| Suite | run 1 | run 2 | run 3 |
|---|---:|---:|---:|
| `pnpm check:worker-protocol-boundary` | `0` | `0` | `0` |
| `node --test check-worker-protocol-boundary.test.mjs` | `0` | `0` | `0` |
| `pnpm --filter @armyofagents/worker-protocol test:run` | `0` | `0` | `0` |
| **`node scripts/check-worker-protocol-package.mjs`** | **`0`** | **`0`** | **`0`** |

All four designated critical suites pass three consecutive times with identical results and zero flaky/retried tests (D0-R03 satisfied). The package-smoke suite — RED 3× in a1 — is now GREEN 3×.

### Worktree clean after gate

| Command | Exit code | Result summary |
|---|---:|---|
| `git status --porcelain` | `0` | empty — clean worktree (built `dist/` gitignored; the smoke cleans its `wpp-*` temp dirs) |
| `git diff --exit-code` | `0` | no tracked change |

## Assertions and evidence

| Requirement ID | Class | Required value/condition | Observed value | Evidence | Result |
|---|---|---|---|---|---|
| D0-T01 | REQUIRED | Each ticket's focused tests + affected-package typecheck/build + boundary + changed contract-manifest checks pass with zero failures | All PRT focused suites + PRT-001's packed protocol import smoke now green | commands above; A08 `PASS` | `pass` |
| D0-T02 | REQUIRED | Lifecycle-owning ticket tests the complete legal/illegal transition matrix | PRT-002 `states.test.ts` (19 tests): full Cartesian for all 6 machines + job reason guards + terminal immutability + byte-for-byte JSON-authority parity (E1-D001) + forbidden cross-lifecycle-edge rejection | package test:run | `pass` |
| D0-T03 | REQUIRED | Secret/path-validator-owning ticket passes ≥10,000 deterministic generated vectors (argv/URL/header/nested/extension) | PRT-003 wire-safety/job canary corpus + PRT-005 path corpus (≥10,000 seeded recursive string vectors each) green in the 543/543 package suite | package test:run | `pass` |
| D0-T04 | REQUIRED | Protocol-schema-owning ticket covers every affected valid/invalid conformance vector | PRT-007 `conformance.json` (42) + `contract.test.ts` (47) + bidirectional `cross-version.test.ts` (53) + frozen integrity green; manifest `--check` OK | package test:run; A10/A12 | `pass` |
| D0-T05 | REQUIRED | Hermetic inputs — no network provider, customer data, or live credential | protocol checkers `node:*`-only offline; the package smoke provisions `zod` by offline `fs.cpSync` (no network/package manager); no secrets recorded | checker imports; environment | `pass` |
| D0-R01 | REQUIRED | `pnpm -r typecheck` + `pnpm test:run` + `pnpm -r build` on the exact revision; DEC-03-governed | typecheck 0; recursive build 0; `test:run` exit 1 with all failures in pre-existing non-E1 Windows-local environment paths; worker-protocol 543/543; zero E1-touched test-file failure | D0-R01/DEC-03 rows | `pass` |
| D0-R02 | REQUIRED | Authoritative root `pnpm build` passes; no tracked-byte change; network-free | `pnpm build` exit 0; `git status`/`git diff --check` byte-clean after build | B16; clean-after check | `pass` |
| D0-R03 | REQUIRED | Every designated critical suite passes three consecutive executions with zero flaky/retried tests | boundary, boundary-mutation, package `test:run`, AND package-smoke each green 3× | 3× stability loop | `pass` |
| D0-R04 | REQUIRED | Worktree byte-clean after the gate; every command/exit/count retained | `git status --porcelain` empty; `git diff --exit-code` 0; all commands/exits/counts retained here | clean-after check | `pass` |
| H-04 | HARD | Secret containment — zero known secret-canary values in envelopes/argv/URLs/events | Exercised at the contract layer: `wire-safety` recursive credential-key rejection + the job/event/transport canary corpora + envelope-level `addForbiddenWireKeyIssues`, all green; no secret in any QA artifact | package test:run; PRT-003/004/007 | `pass` |
| H-01/H-02/H-03/H-05/H-06/H-09 | HARD | Distributed-runtime invariants (tenant isolation, lease authority, single executor, sandbox/network boundary, cleanup) | `not_applicable` at E1 — E1 defines the wire CONTRACT, not the runtime; proved by owning tickets at the D1+ distributed-runtime gates | program design; test-gates D1+ | `recorded` |
| H-07 | HARD | Hosted exclusions impossible while disabled | `not_applicable` at E1 — established at the E0 foundation layer; E1 constructs no runtime surface | E0 handoff | `recorded` |
| H-08 | HARD | Supply chain — only approved digests with verified provenance | Partially exercised: the frozen v1 consumer pins zod 3.24.2 + esbuild 0.28.1 + lockfile/package integrity and re-verifies byte-identical determinism; full image signing/provenance is D5+ | `check:frozen-worker-protocol-v1`; A12 | `recorded` |
| H-10 | HARD | No failing run overwritten/hidden/converted to pass without a new immutable QA record | The a1 `fail` record is preserved unchanged; this a2 `pass` record supersedes it via `Supersedes` (verified a1 QA/handoff bytes untouched by the fix commits) | a1 records; this record | `pass` |
| E1 exit gate — package contract | REQUIRED | Package build/typecheck/tests pass; the packed public surface and consumer import smoke hold | build/typecheck/543-test pass; packed consumer import smoke now green (zod provisioned + tolerant-but-enforcing surface + live zod validation) | A05/A06/A07; A08 | `pass` |
| E1 exit gate — frozen conformance | REQUIRED | Every current/frozen-consumer vector pinned by hash; frozen baseline independent; bidirectional cross-version `baseline_established` | contract-manifest `--check` OK; frozen-consumer integrity OK + 12/12 mutation; cross-version 53/53 `baseline_established` | A10/A11/A12; package test:run | `pass` |
| E1 exit gate — source boundary | REQUIRED | Runtime source contains no Node/server/db/adapter/UI imports | `check:worker-protocol-boundary` PASS + 50/50 mutation corpus, green 3× | A03/A04; stability loop | `pass` |
| E1 exit gate — execution-source provenance | REQUIRED | All six source variants round-trip with typed principals; only `task_run` carries `runId`/`issueId`; fabricated/cross-source provenance fails closed | PRT-003 `source`/`job` suites green in the 543/543 run | package test:run | `pass` |
| DEC-01 | REQUIRED | Gate owner records pass/fail/blocked_external on one exact revision; no REQUIRED-suite failure unless DEC-03-covered; no hard-invariant failure | Decision `pass` on `b03262692…`; zero HARD-invariant violation; the a1 blocker (E1-F007) is resolved; the `test:run` failures carry no E1-logic regression (DEC-03) | this record; DEC-03 row | `pass` |
| DEC-03 | REQUIRED | REQUIRED repository-suite failures ⊆ attributed baseline in the gate environment; a new or epic-touched failure is `fail` | The repository `test:run` failures are pre-existing non-E1 Windows-local environment (no E1-touched test-file failure), identical to the a1/E0 profile; the E1-scope focused checks (incl. the package smoke) are all green | DEC-03 statement; test:run classification | `pass` |

### DEC-03 honesty statement

- **(a) The gate is Windows-local / operator-directed; DEC-03's formal authority (Linux CI) was not exercised.** The repository `pnpm test:run` failure profile (539 files / 166 tests, dominated by the drizzle-orm `require() ES Module in a cycle` / `ERR_REQUIRE_CYCLE_MODULE` artifact plus embedded-PG MAX_PATH unavailability on the deep OneDrive path) is the same known Windows-local environment limitation recorded for E0 (finding E0-F005; CLAUDE.md Test Patterns) and for E1 attempt 1 — **not** a code-logic regression. The worker-protocol project runs cleanly on the deep path (no DB) and is 543/543 green inside the full run.
- **(b) The E1-scope gate is genuinely green on this revision.** Every focused E1-scope check — including the package smoke that blocked a1 (E1-F007) — passes, and passes 3× in the stability loop. The E1-F007 fix is a scoped harness/fixture/CI remediation that makes the packaging genuinely correct (offline `zod` provisioning that fails closed if unavailable; a tolerant-but-enforcing consumer surface that also runs live zod validation) and neuters no packaging/encapsulation check; independently re-verified by the gate owner (file inspection + empirical all-green run) and by the ticket's own independent adversarial re-gate (see finding E1-F007). Both defective files were E1-touched; the fix touched no wire-contract source, frozen fixture, or PRT ledger.
- **(c) A Linux-CI run is recommended to formalize the repository baseline.** Running the gate order on Linux CI at this revision (packages built, datastore up) would formalize the authoritative repository baseline. The E1-F007 fix additionally wires `check-worker-protocol-package.mjs` into the CI `verify` job (after `pnpm -r build`), so this smoke now runs on Linux CI on every PR and cannot silently rot again. If a Linux-CI result differed from this Windows-local judgment, it would create a superseding QA record (and completion handoff) with `Supersedes` pointing here.

## Failures

No E1-scope product, harness, provider, or E1-introduced failure on this revision. The a1 blocker **E1-F007** (packed-import smoke) is RESOLVED (resolving revision `233e65b2b`, confirmed `b03262692`) and re-verified green (exit 0, 3× stable). The repository `pnpm test:run` failures are classified `environment` (Windows-local): 166 failed tests / 539 failed files, all pre-existing non-E1 paths (drizzle `require(esm)` cycle + embedded-PG MAX_PATH); first causal error class `ERR_REQUIRE_CYCLE_MODULE`; zero E1-touched test-file failures; worker-protocol 543/543 green. Non-blocking finding **E1-F006** (the literal plan Step-1 regex throws on PRT-004's annotated `Reviewed revision:` line) carries forward from a1; substance independently re-verified (commit + ancestor).

## Cleanup

No provider resources, sandboxes, object uploads, or leases exist at D0 (hermetic). The package-smoke checker's ephemeral `wpp-*` temp directories (now also containing the offline-copied `zod`) are self-cleaned (`fs.rmSync` in `finally`); no leftover temp dirs and the worktree is byte-clean after the gate. Retained controlled artifacts (session scratchpad, not committed): per-command logs `A02…A12`, `B13…B16`, `C17b-repo-testrun.log`, and the attempt-1 repro scripts. No secrets recorded.

## Gate effect

This run **permits** E1 completion. Every applicable D0 REQUIRED condition (D0-T01…T05, D0-R01…R04) plus the E1 README exit-gate criteria (package contract; frozen conformance vectors hash-pinned + independent frozen baseline + bidirectional cross-version `baseline_established`; source boundary; execution-source provenance) passes on the single revision `b03262692882a7ce17834131ad358d3aecf07f5b`. The HARD invariant applicable at the protocol layer (H-04 secret containment; H-10 evidence integrity) holds; distributed-runtime and hosted-exclusion invariants (H-01/H-02/H-03/H-05/H-06/H-07/H-09) are `not_applicable`/deferred to their owning tickets at D1+ (E1 builds the wire contract, not the runtime), and H-08 supply chain is partially exercised via the frozen-consumer pin. There is no conditional pass; the sole residual is the DEC-03 Linux-CI formalization recommendation (now backstopped by the new CI `verify`-job smoke step), which does not block the D0/E1-scope decision. This QA record is paired with the completion handoff `handoffs/2026-08-09-epic-completion-b03262692882-a2.md` (Decision `pass`) and supersedes the attempt-1 `fail` QA record.
