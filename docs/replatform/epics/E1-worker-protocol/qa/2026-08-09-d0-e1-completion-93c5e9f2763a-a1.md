# QA Result — D0 E1-completion rollup, revision 93c5e9f2763a, attempt 1

**Date (UTC):** `2026-08-09`
**Epic:** `E1-worker-protocol`
**Record path:** `docs/replatform/epics/E1-worker-protocol/qa/2026-08-09-d0-e1-completion-93c5e9f2763a-a1.md`
**Scope slug:** `e1-completion`
**Revision:** `93c5e9f2763a16ce17507fde11b8cac770d5478a`
**Attempt:** `1`
**Supersedes:** `none`
**Lane:** `D0`
**Result:** `fail`
**Failure class:** `harness`
**Campaign start (UTC):** `not_applicable`
**Campaign end (UTC):** `not_applicable`

> This file is immutable from its first commit. A correction, rerun, changed decision, or changed revision creates a higher attempt and links this path through `Supersedes`.

## Topology and environment

D0 is a hermetic component gate: no control-plane replicas, workers, object store, realtime/admission stores, or provider. E1 is a new leaf workspace package (`packages/worker-protocol`) plus additive root config; it builds a versioned wire **contract**, not a distributed runtime. No image or desktop artifact is built. No network provider, customer data, or live credential is used (D0-T05). No secrets are recorded.

- **Candidate revision:** `93c5e9f2763a16ce17507fde11b8cac770d5478a` on branch `claude/epic-e1-worker-protocol-6a5d96`. Program base (E0-complete tip) `c32bbe087` verified an ancestor of HEAD. PRT-007 `BASELINE_SOURCE_SHA` `b7a842870ce7509d8baa75409e0ab19da375c88a` verified a bare 40-hex commit and an ancestor of HEAD.
- **Gate-owner environment (focused + repository-suite lanes):** Windows 11, the OneDrive worktree `…/.claude/worktrees/epic-e1-worker-protocol-6a5d96` (path length 111). Node v24.14.0, pnpm 9.15.4. `node_modules` present; `pnpm install --frozen-lockfile` a no-op in sync. The dependency-free protocol checkers run under stock Node (`node:*` only).
- **Contract hashes (current):** `docs/contracts/worker-protocol/v1/manifest.sha256` — `conformance.json` = `872f4e47dc036f6491adbfe8200647ce9dc39955a83277d52ae17f28a7772f71`; `operations.md` = `57d7b048ab65972558029105ccbef30ae9fc1c7e8cb962a54029fad97b4ce775`.
- **Frozen-consumer contract hashes:** `tests/fixtures/worker-protocol-consumers/v1/manifest.sha256` pins 18 files; the bundled self-contained `dist/index.js` = `3c854028f3f1c0e7a792bda328515dc4c801e41bf0b0f3c3ee79a3ac7b7294e9` (239,198 bytes; zod 3.24.2 + esbuild 0.28.1; `dependency-lock.json.sourceSha == BASELINE_SOURCE_SHA`). `pnpm check:frozen-worker-protocol-v1` re-verified OK.
- **E1-touched scope (`c32bbe087..HEAD`):** `packages/worker-protocol/**`, `scripts/**` (worker-protocol checkers), `tests/fixtures/worker-protocol-{import,consumers}/**`, `docs/**`, and additive root config (`package.json`, `pnpm-lock.yaml`, `vitest.config.ts`, `.github/workflows/pr.yml`, `.gitattributes`, `.gitignore`). **Zero** `server/`, `ui/`, or `packages/db/` source files touched (verified with `git diff --name-only c32bbe087..HEAD`).

### Frozen schedule and samples (D4/D6)

| Field | Value |
|---|---|
| Schedule manifest path | `not_applicable` |
| Schedule manifest SHA-256 | `not_applicable` |
| Timezone | `not_applicable` |
| Expected samples | `not_applicable` |
| Observed samples | `not_applicable` |
| Missing samples | `not_applicable` |
| Numerator | `not_applicable` |
| Denominator | `not_applicable` |
| Exclusion count and stable fault-window reasons | `not_applicable` |

### D6 mandatory and advertised rows

`not_applicable` — D0 hermetic component gate; no availability SLI, support matrix, or workload rows.

## Commands

Independently re-run by the gate owner on the exact recorded revision. The tracked/untracked worktree was byte-clean before and after the gate (`git status --porcelain` empty; `git diff --exit-code` 0).

### Step-1 ticket-completeness verification

| Command | Exit code | Result summary |
|---|---:|---|
| `git rev-parse HEAD` | `0` | `93c5e9f2763a16ce17507fde11b8cac770d5478a` |
| Step-1 filename-set + Status/Disposition/reviewer/ancestor checks (per-ledger, tolerant extractor) | n/a | PRT-001…007 present; each `Status: complete`, `Disposition: approved`, implementer≠reviewer; each reviewed revision a commit + ancestor of HEAD |
| Literal plan Step-1 PowerShell block | `1` | Throws `PRT-004-result.md lacks an exact reviewed revision` — trailing annotation on PRT-004's `Reviewed revision:` line defeats the `\s*$` anchor (finding **E1-F006**, non-blocking; substance re-verified with a tolerant extractor) |
| `git merge-base --is-ancestor b7a842870… HEAD` + `git cat-file -e b7a842870…^{commit}` | `0` | BASELINE_SOURCE_SHA is a commit and ancestor of HEAD |

### Step-2 focused E1-scope suites (MUST be green)

| Command | Exit code | Result summary |
|---|---:|---|
| `pnpm check:distributed-foundation` | `0` | `distributed execution foundation: PASS` |
| `pnpm check:worker-protocol-boundary` | `0` | `worker protocol boundary: PASS` |
| `node --test scripts/check-worker-protocol-boundary.test.mjs` | `0` | tests 50 / pass 50 / fail 0 |
| `pnpm --filter @armyofagents/worker-protocol test:run` | `0` | Test Files 17 passed (17); Tests 543 passed (543) |
| `pnpm --filter @armyofagents/worker-protocol typecheck` | `0` | `tsc --noEmit` clean |
| `pnpm --filter @armyofagents/worker-protocol build` | `0` | `tsc` clean |
| **`node scripts/check-worker-protocol-package.mjs`** | **`1`** | **FAIL** — `ERR_MODULE_NOT_FOUND: zod` (declared dep not provisioned) + stale `worker-consumer` exact-surface assertion. Finding **E1-F007** (gate blocker) |
| `node --test scripts/update-worker-protocol-contract-manifest.test.mjs` | `0` | tests 13 / pass 13 / fail 0 |
| `node scripts/update-worker-protocol-contract-manifest.mjs --check` | `0` | `worker-protocol contract manifest: OK` |
| `node --test scripts/check-frozen-worker-protocol-consumer.test.mjs` | `0` | tests 12 / pass 12 / fail 0 |
| `pnpm check:frozen-worker-protocol-v1 -- --source-sha b7a842870…` | `0` | `frozen worker-protocol v1 consumer: OK (sourceSha b7a842870…, zod 3.24.2, esbuild 0.28.1)` |

### Step-2 integration signals (must be green — "does the new leaf integrate")

| Command | Exit code | Result summary |
|---|---:|---|
| `pnpm install --frozen-lockfile` | `0` | in sync ("Done in 3.4s"); pre-existing plugin-sdk dev-bin WARNs only |
| `pnpm -r typecheck` | `0` | all packages (adapters, plugin-sdk, shared, plugin examples, worker-protocol, server, ui, cli) clean |
| `pnpm -r build` | `0` | recursive build; network-free |
| `pnpm build` (authoritative root) | `0` | root build; byte-clean after |

### Step-2 repository suite (DEC-03) and whitespace

| Command | Exit code | Result summary |
|---|---:|---|
| `pnpm test:run` | `1` | Tests 166 failed / 14439 passed / 39 skipped (14644); Files 539 failed / 1560 passed / 8 skipped (2107). worker-protocol project **543/543 green** (17 files). All failures are pre-existing non-E1 Windows-local environment (drizzle-orm `require(esm)` cycle + embedded-PG MAX_PATH); **zero E1-touched test-file failures**. Classified `environment` (DEC-03). |
| `git diff --check` | `0` | no whitespace/conflict errors |

### Step-2 D0 3× critical-suite stability loop

| Suite | run 1 | run 2 | run 3 |
|---|---:|---:|---:|
| `pnpm check:worker-protocol-boundary` | `0` | `0` | `0` |
| `node --test check-worker-protocol-boundary.test.mjs` | `0` | `0` | `0` |
| `pnpm --filter @armyofagents/worker-protocol test:run` | `0` | `0` | `0` |
| **`node scripts/check-worker-protocol-package.mjs`** | **`1`** | **`1`** | **`1`** |

Three of the four designated critical suites pass three consecutive times with identical results and zero flaky/retried tests; the package-smoke suite fails deterministically all three runs (E1-F007), so D0-R03 is not satisfied.

### Worktree clean after gate

| Command | Exit code | Result summary |
|---|---:|---|
| `git status --porcelain` | `0` | empty — clean worktree (built `dist/` is gitignored) |
| `git diff --exit-code` | `0` | no tracked change |

## Assertions and evidence

| Requirement ID | Class | Required value/condition | Observed value | Evidence | Result |
|---|---|---|---|---|---|
| D0-T01 | REQUIRED | Each ticket's focused tests + affected-package typecheck/build + boundary + changed contract-manifest checks pass with zero failures | PRT-002…007 focused suites, typecheck, build, boundary, contract-manifest all green at the gate; **PRT-001's focused "packed protocol import smoke" (`check-worker-protocol-package.mjs`) is RED** (E1-F007) | commands above; A08 log | `fail` |
| D0-T02 | REQUIRED | Lifecycle-owning ticket tests the complete legal/illegal transition matrix | PRT-002 `states.test.ts` (19 tests): full Cartesian for all 6 machines + job reason guards + terminal immutability + byte-for-byte JSON-authority parity (E1-D001) + forbidden cross-lifecycle-edge rejection — green in the 543/543 run | package test:run | `pass` |
| D0-T03 | REQUIRED | Secret/path-validator-owning ticket passes ≥10,000 deterministic generated vectors (argv/URL/header/nested/extension) | PRT-003 `wire-safety`/`job` canary corpus + PRT-005 path corpus (≥10,000 seeded recursive string vectors each) run inside the green package suite | package test:run (543/543) | `pass` |
| D0-T04 | REQUIRED | Protocol-schema-owning ticket covers every affected valid/invalid conformance vector | PRT-007 `conformance.json` (42 cases) + `contract.test.ts` (47) + bidirectional `cross-version.test.ts` (53) + frozen-consumer integrity all green; manifest `--check` OK | package test:run; A10/A12 | `pass` |
| D0-T05 | REQUIRED | Hermetic inputs — no network provider, customer data, or live credential | Protocol checkers are `node:*`-only and offline; no secrets recorded | checker imports; environment | `pass` |
| D0-R01 | REQUIRED | `pnpm -r typecheck` + `pnpm test:run` + `pnpm -r build` on the exact revision; DEC-03-governed | typecheck 0; recursive build 0; `test:run` exit 1 with all failures in pre-existing non-E1 Windows-local environment paths; worker-protocol 543/543; zero E1-touched test-file failure | D0-R01/DEC-03 rows | `pass` |
| D0-R02 | REQUIRED | Authoritative root `pnpm build` passes; no tracked-byte change; network-free | `pnpm build` exit 0; `git status`/`git diff --check` byte-clean after build | B16 | `pass` |
| D0-R03 | REQUIRED | Every designated critical suite passes three consecutive executions with zero flaky/retried tests | boundary, boundary-mutation, and package `test:run` each green 3×; **package-smoke suite RED 3×** (E1-F007) — not all critical suites stable | 3× stability loop | `fail` |
| D0-R04 | REQUIRED | Worktree byte-clean after the gate; every command/exit/count retained | `git status --porcelain` empty; `git diff --exit-code` 0; all commands/exits/counts retained here | clean-after check | `pass` |
| H-04 | HARD | Secret containment — zero known secret-canary values in envelopes/argv/URLs/events | Exercised at the contract layer: `wire-safety` recursive credential-key rejection + the job/event/transport canary corpora + envelope-level `addForbiddenWireKeyIssues`, all green; no secret in any QA artifact | package test:run; PRT-003/004/007 | `pass` |
| H-01/H-02/H-03/H-05/H-06/H-09 | HARD | Distributed-runtime invariants (tenant isolation, lease authority, single executor, sandbox/network boundary, cleanup) | `not_applicable` at E1 — E1 defines the wire CONTRACT, not the runtime; these are proved by their owning tickets at the D1+ distributed-runtime gates | program design; test-gates D1+ | `recorded` |
| H-07 | HARD | Hosted exclusions impossible while disabled | `not_applicable` at E1 — established at the E0 foundation layer (E0 QA H-07); E1 constructs no runtime surface | E0 handoff | `recorded` |
| H-08 | HARD | Supply chain — only approved digests with verified provenance | Partially exercised: the frozen v1 consumer pins zod 3.24.2 + esbuild 0.28.1 + lockfile/package integrity and re-verifies byte-identical determinism; full image signing/provenance is D5+ | `check:frozen-worker-protocol-v1`; A12 | `recorded` |
| H-10 | HARD | No failing run overwritten/hidden/converted to pass without a new immutable QA record | This `fail` record is committed immutably at first write; no prior record altered | this record | `pass` |
| E1 exit gate — package contract | REQUIRED | Package build/typecheck/tests pass; the packed public surface and consumer import smoke hold | build/typecheck/543-test all pass; **the packed consumer import smoke is RED** (E1-F007) | A05/A06/A07; A08 | `fail` |
| E1 exit gate — frozen conformance | REQUIRED | Every current/frozen-consumer vector pinned by hash; frozen baseline independent; bidirectional cross-version `baseline_established` | contract-manifest `--check` OK; frozen-consumer integrity OK + 12/12 mutation; cross-version 53/53 `baseline_established` | A10/A11/A12; package test:run | `pass` |
| E1 exit gate — source boundary | REQUIRED | Runtime source contains no Node/server/db/adapter/UI imports | `check:worker-protocol-boundary` PASS + 50/50 mutation corpus, green 3× | A03/A04; stability loop | `pass` |
| E1 exit gate — execution-source provenance | REQUIRED | All six source variants round-trip with typed principals; only `task_run` carries `runId`/`issueId`; fabricated/cross-source provenance fails closed | PRT-003 `source`/`job` suites green in the 543/543 run | package test:run | `pass` |
| DEC-01 | REQUIRED | Gate owner records pass/fail/blocked_external on one exact revision; a REQUIRED failure is `fail`; epic-touched failure is always `fail`; no hard-invariant failure | Decision `fail` on `93c5e9f2763a…`; the blocking failures (D0-T01, D0-R03, E1 package-contract exit gate) are epic-touched (E1-F007) → never waivable; zero HARD-invariant violation | this record | `fail` |
| DEC-03 | REQUIRED | REQUIRED repository-suite failures ⊆ attributed baseline in the gate environment; a new or epic-touched failure is `fail` | See the DEC-03 honesty statement. The repository `test:run` failures are pre-existing non-E1 Windows-local environment (no E1-touched test-file failure), so the repository suite itself does not fail the gate; the gate fails on the **focused** E1-scope package-smoke (E1-F007), not on the repository baseline | DEC-03 statement; test:run classification | `recorded` |

### DEC-03 honesty statement

- **(a) The gate is Windows-local / operator-directed; DEC-03's formal authority (Linux CI) was not exercised.** The repository `pnpm test:run` failure profile here (539 files / 166 tests, dominated by the drizzle-orm `require() ES Module in a cycle` / `ERR_REQUIRE_CYCLE_MODULE` artifact plus embedded-PG MAX_PATH unavailability on the deep OneDrive path) is the same known Windows-local environment limitation recorded for E0 (finding E0-F005; CLAUDE.md Test Patterns), **not** a code-logic regression. The worker-protocol project runs cleanly on the deep path (no DB) and is 543/543 green inside the full run.
- **(b) The E1 gate FAIL is NOT a DEC-03 environment artifact.** The blocker (E1-F007) is a focused E1-scope check (`check-worker-protocol-package.mjs`) that fails deterministically for two environment-agnostic reasons — the declared `zod` dependency is never provisioned into the temp consumer, and `worker-consumer.mjs` hard-asserts the stale PRT-001 public surface (275 exports now). Both would fail identically on Linux CI (`/tmp` has no `zod` up-tree; the surface is 275 everywhere). Both defective files are E1-touched, so DEC-01/DEC-03 make this a non-waivable `fail`.
- **(c) A Linux-CI run is recommended to formalize the repository baseline.** Running the gate order on Linux CI at this revision would formalize the authoritative repository baseline; it would not change the E1 decision, because the E1-scope package-smoke would still be red until E1-F007 is fixed. When E1-F007 is resolved, re-running the Task-8 gate (on Linux CI ideally) produces a superseding QA record and completion handoff with `Supersedes` pointing here.

## Failures

- **E1-F007 (gate blocker, class `harness`).** `node scripts/check-worker-protocol-package.mjs` exit 1. First causal error: `ERR_MODULE_NOT_FOUND` resolving `zod` from the temp consumer (checker does not provision the declared dependency); secondary, once `zod` is provisioned: `AssertionError: public surface must be exactly the version constants` in `tests/fixtures/worker-protocol-import/worker-consumer.mjs` (hard-coded PRT-001 two-constant surface vs the current 275-export surface). Both files are E1-touched; latent since PRT-002 because the smoke was never re-run and is not wired into CI. The shipped protocol wire contract is otherwise green (543/543 package tests, frozen-consumer + cross-version + contract-manifest all pass). Stable finding: `../findings.md` E1-F007. Owning ticket: PRT-001.
- **Repository `pnpm test:run` (class `environment`, non-blocking under DEC-03).** 166 failed tests / 539 failed files, all pre-existing non-E1 Windows-local paths (drizzle `require(esm)` cycle + embedded-PG MAX_PATH). First causal error class: `ERR_REQUIRE_CYCLE_MODULE`. Zero E1-touched test-file failures; worker-protocol 543/543 green.
- **E1-F006 (non-blocking, class documentation/tooling).** The literal plan Step-1 block throws on PRT-004's annotated `Reviewed revision:` line; substance independently re-verified (commit + ancestor). Stable finding: `../findings.md` E1-F006.

## Cleanup

No provider resources, sandboxes, object uploads, or leases exist at D0 (hermetic). Ephemeral `wpp-*` temp directories created by the package-smoke checker are self-cleaned (`fs.rmSync` in `finally`). Retained controlled artifacts (session scratchpad, not committed): per-command logs `A02…A12`, `B13…B16`, `C17-repo-testrun.log`, and the two repro scripts confirming E1-F007's root cause. No secrets recorded.

## Gate effect

This run **blocks** E1 completion. One REQUIRED focused E1-scope check (`check-worker-protocol-package.mjs` — the packed protocol import smoke, also a D0 stability-loop member) is RED on the single revision `93c5e9f2763a16ce17507fde11b8cac770d5478a` (finding E1-F007), failing D0-T01, D0-R03, and the E1 "package contract" exit-gate bullet; both defective files are E1-touched, so DEC-01/DEC-03 make it non-waivable and it is not a DEC-03 environment artifact. Every other applicable D0 REQUIRED condition (D0-T02/T03/T04/T05, D0-R01/R02/R04), the HARD invariant applicable at the protocol layer (H-04; H-10 evidence integrity), the integration signals (`pnpm -r typecheck`/`pnpm -r build`/root `pnpm build`/frozen-lockfile), and the remaining E1 exit-gate criteria (frozen conformance, source boundary, execution-source provenance) pass on this revision. There is no conditional pass. This QA record is paired with the completion handoff `handoffs/2026-08-09-epic-completion-93c5e9f2763a-a1.md` (Decision `fail`); E1 remains in `gate_review`. The non-blocking DEC-03 Linux-CI formalization caveat and finding E1-F006 do not affect the decision.
