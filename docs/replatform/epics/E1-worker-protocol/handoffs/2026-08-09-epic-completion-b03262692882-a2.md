# Handoff — E1-worker-protocol epic completion

**Date (UTC):** `2026-08-09`
**Epic:** `E1-worker-protocol`
**Record path:** `docs/replatform/epics/E1-worker-protocol/handoffs/2026-08-09-epic-completion-b03262692882-a2.md`
**Gate slug:** `epic-completion`
**Reviewed revision:** `b03262692882a7ce17834131ad358d3aecf07f5b`
**Attempt:** `2`
**Supersedes:** `docs/replatform/epics/E1-worker-protocol/handoffs/2026-08-09-epic-completion-93c5e9f2763a-a1.md`
**Decision:** `pass`
**Gate owner role:** `Integration Gate Owner`
**Gate owner identity:** `E1 integration-gate owner subagent (Claude)`

> This file is immutable from its first commit. A correction, rerun, changed decision, or changed revision creates a higher attempt and links this path through `Supersedes`.

The gate owner did not implement or review any PRT ticket, nor author the E1-F007 fix. This attempt re-runs the Task-8 gate after the attempt-1 blocker (E1-F007 — packed-import smoke RED) was fixed on `233e65b2b` and confirmed on `b03262692`. All seven ticket ledgers were re-confirmed `complete`/`approved` with a distinct implementer≠reviewer and an ancestor reviewed revision (the literal plan Step-1 block still throws on PRT-004's annotated `Reviewed revision:` line — finding **E1-F006**, non-blocking; substance re-verified with a tolerant extractor). Every focused E1-scope suite, the integration signals, the repository suite (DEC-03), and the D0 3× stability loop were independently re-run on the recorded revision. The gate owner independently verified the E1-F007 fix is real and neuters no packaging/encapsulation check (source inspection of the checker + fixture, plus the empirical all-green run and 3× stability). The blob SHAs below were recomputed with `git rev-parse HEAD:<path>`; each reviewed implementation SHA was confirmed a commit and an ancestor of HEAD.

**This gate passes.** Every focused E1-scope check — including the packed protocol import smoke that blocked a1 — is green (and green 3× in the stability loop). E1 moves `gate_review → complete`.

## Included ticket results

| Ticket | Ticket-result path | Ticket-result Git blob SHA | Reviewed implementation SHA | Latest review disposition |
|---|---|---|---|---|
| PRT-001 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-001-result.md` | `7feb5859eabbbf6bd45b1d2155a2d3c37f48ffe9` | `7e0f37e1b1e78564ddbda9708485b592087a5380` | `approved` |
| PRT-002 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-002-result.md` | `5023c9e35cada1b391da85eac3440260013ee9ee` | `26f6f61d921b3185d4b697e09b45dba27eaec688` | `approved` |
| PRT-003 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-003-result.md` | `165414ad1bf2f986d8c2e526a0dcf2a637ad46bd` | `e62921b1743f9cbc33e6ecf79348a7f4bf5d5483` | `approved` |
| PRT-004 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-004-result.md` | `741c1a3feecf61bfa354b133144d0676728f709b` | `0a00e3f3a61f1e532b11fb526e209589f7a0fe69` | `approved` |
| PRT-005 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-005-result.md` | `0ecaa2f3191ae762b165c701e5717dea82a87127` | `36d3733e06555ca2b43529734d79f559a4e940a8` | `approved` |
| PRT-006 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-006-result.md` | `edf7e653ad3c865944e8c723a1a5f6c1f4aa1f4e` | `3cda34467978031c4b04a5f856a771c8d071fe08` | `approved` |
| PRT-007 | `docs/replatform/epics/E1-worker-protocol/tickets/PRT-007-result.md` | `f5f6b47fead8218b48631bb8809c9e043eba3ffd` | `c68053421ac53c5b49066b041c8fbcdd920dad62` | `approved` |

Ticket-result blob SHAs are unchanged from attempt 1 (the ledgers are frozen at `complete`; the E1-F007 fix touched none of them). PRT-007 two-commit sequence — `BASELINE_SOURCE_SHA` = `b7a842870ce7509d8baa75409e0ab19da375c88a` (source commit, bare 40-hex, a commit and ancestor of HEAD); the freeze commit `c68053421…` is the reviewed revision.

## QA evidence

| QA record | QA revision | Lane | Attempt | Result |
|---|---|---|---:|---|
| `docs/replatform/epics/E1-worker-protocol/qa/2026-08-09-d0-e1-completion-b03262692882-a2.md` | `b03262692882a7ce17834131ad358d3aecf07f5b` | `D0` | `2` | `pass` |
| `docs/replatform/epics/E1-worker-protocol/qa/2026-08-09-d0-e1-completion-93c5e9f2763a-a1.md` (superseded) | `93c5e9f2763a16ce17507fde11b8cac770d5478a` | `D0` | `1` | `fail` |

## Threshold decision

| Requirement ID | Class | Required value/condition | Observed value | Evidence record | Decision |
|---|---|---|---|---|---|
| D0-T01 | REQUIRED | Each ticket's focused tests + typecheck/build + boundary + contract-manifest pass with zero failures | All PRT focused sets green; PRT-001's packed-import smoke now green (E1-F007 resolved) | QA `…-b03262692882-a2.md` | `pass` |
| D0-T02 | REQUIRED | Lifecycle-owning ticket tests the full legal/illegal matrix | PRT-002 full Cartesian + JSON-authority parity + forbidden cross-edge (19 tests) green | QA record | `pass` |
| D0-T03 | REQUIRED | ≥10,000 vectors for owned secret/path validators | PRT-003 wire-safety/job + PRT-005 path canary corpora green in the 543/543 package suite | QA record | `pass` |
| D0-T04 | REQUIRED | Every affected valid/invalid protocol conformance vector | PRT-007 conformance (42) + contract (47) + cross-version (53) + frozen integrity green; manifest `--check` OK | QA record | `pass` |
| D0-T05 | REQUIRED | Hermetic inputs — no network provider, customer data, or live credential | protocol checkers `node:*`-only offline; the package smoke provisions `zod` by offline `fs.cpSync` (no network/package manager); no secrets | QA record | `pass` |
| D0-R01 | REQUIRED | `pnpm -r typecheck` + `pnpm test:run` + `pnpm -r build`; DEC-03-governed | typecheck 0; recursive build 0; `test:run` exit 1 all pre-existing non-E1 environment; worker-protocol 543/543; zero E1-touched test-file failure | QA record | `pass` |
| D0-R02 | REQUIRED | Authoritative root `pnpm build` passes; no tracked-byte change; network-free | `pnpm build` 0; byte-clean after | QA record | `pass` |
| D0-R03 | REQUIRED | Every designated critical suite passes 3 consecutive runs, zero flaky | boundary + boundary-mutation + package `test:run` + package-smoke all green 3× | QA record | `pass` |
| D0-R04 | REQUIRED | Byte-clean worktree after the gate; all commands/exits/counts retained | `git status --porcelain` empty; `git diff --exit-code` 0; all retained | QA record | `pass` |
| H-04 | HARD | Secret containment — zero known secret-canary values in envelopes/argv/URLs/events | wire-safety + job/event/transport canary corpora + envelope credential-key rejection green; no secret in artifacts | QA record | `pass` |
| H-01/02/03/05/06/07/09 | HARD | Distributed-runtime + hosted-exclusion invariants | `not_applicable` at E1 (wire contract, no runtime constructed); proved by owning tickets at D1+; H-07 established at E0 | QA record | `recorded` |
| H-08 | HARD | Supply chain — approved digests/provenance | Partial: frozen v1 consumer pins zod/esbuild + integrity + deterministic re-bundle; full image signing is D5+ | QA record | `recorded` |
| H-10 | HARD | No failing run overwritten/hidden without a new immutable record | a1 `fail` record preserved unchanged; this a2 `pass` supersedes via `Supersedes` (a1 QA/handoff bytes verified untouched by the fix commits) | a1 + a2 records | `pass` |
| E1 exit gate — package contract | REQUIRED | Package build/typecheck/tests pass; packed public surface + consumer import smoke hold | build/typecheck/543-test + packed consumer import smoke all green | QA record | `pass` |
| E1 exit gate — frozen conformance | REQUIRED | Vectors hash-pinned; frozen baseline independent; bidirectional cross-version `baseline_established` | contract-manifest OK; frozen integrity OK + 12/12; cross-version 53/53 `baseline_established` | QA record | `pass` |
| E1 exit gate — source boundary | REQUIRED | Runtime source has no Node/server/db/adapter/UI imports | boundary PASS + 50/50 mutation, green 3× | QA record | `pass` |
| E1 exit gate — execution-source provenance | REQUIRED | Six source variants round-trip; only `task_run` carries `runId`/`issueId`; fabricated/cross-source fails closed | PRT-003 source/job suites green | QA record | `pass` |
| DEC-01 | REQUIRED | Owner records pass/fail on one revision; REQUIRED failure is fail unless DEC-03-covered; no hard-invariant failure | `pass` on `b03262692…`; zero hard-invariant violation; a1 blocker resolved; `test:run` failures carry no E1-logic regression | QA record | `pass` |
| DEC-03 | REQUIRED | REQUIRED repository-suite failures ⊆ attributed baseline in the gate environment; new/epic-touched failure is fail | Repository `test:run` failures are pre-existing non-E1 Windows-local environment (no E1-touched test-file failure), identical to a1/E0; E1-scope focused checks all green | QA record (DEC-03 statement) | `pass` |

A handoff cannot pass with any required condition/command failure or HARD/INITIAL failure. There is no required-condition or hard-invariant failure at this revision; the only non-blocking caveat is the DEC-03 Linux-CI formalization recommendation (now backstopped by the new CI `verify`-job package-smoke step).

## Decisions and findings

**Locked decisions (plan-alignment, no wire-contract change; approved by the operator acting as custodian):**
- **E1-D001** — PRT-002 lifecycle maps are authoritative from `docs/architecture/distributed-execution-lifecycles.json`, enforced by a byte-for-byte parity test.
- **E1-D002** — PRT-006 golden-journey checks are vocabulary/enum-membership parity, not full-object parse.

**Findings:**
- **E1-F001…F005** — resolved during PRT-002/003/004/006 review (see `findings.md`); E1-F004 (fail-open extension byte-sizer) and E1-F005 (event-extension under-enforcement) were High and are RESOLVED-and-re-verified; none blocks this gate.
- **E1-F006 (Low, non-blocking, OPEN)** — PRT-004's `Reviewed revision:` summary line carries trailing attempt annotation that the literal Task-8 Step-1 regex rejects; substance independently re-verified (commit + ancestor) with a tolerant extractor. Recommend the Step-1 regex tolerate trailing annotation and future reviewers keep the summary line to a bare backticked SHA. Does not block completion.
- **E1-F007 (High, RESOLVED)** — the attempt-1 blocker. `scripts/check-worker-protocol-package.mjs` was RED because (a) the consumer smoke never provisioned the declared `zod` dependency and (b) `worker-consumer.mjs` hard-asserted the stale PRT-001 two-constant surface. Fixed on `233e65b2b` (parent = the a1 gate-fail-evidence commit `9224bd771`): `zod` is provisioned into each consumer by **offline copy** from the monorepo, failing **closed** if unavailable; the fixture now asserts version constants + a stable 7-export sample-presence check + a live `jobEnvelopeV1Schema.safeParse({})` (exercising the provisioned zod) + retains the deep-subpath encapsulation assertions; and the smoke is wired into the CI `verify` job after `pnpm -r build`. Independently re-verified RESOLVED by the gate owner (source inspection + empirical all-green run, package smoke exit 0 and 3× stable) and by the ticket's own adversarial re-gate: the tarball allow-list, undeclared-dep, deep-subpath (`ERR_PACKAGE_PATH_NOT_EXPORTED`), broken-surface, and zod-fails-closed checks all still ENFORCE — no check was neutered. The fix touched only the checker, the fixture, `pr.yml`, and `findings.md`; the wire-contract source, the frozen v1 fixture, and all PRT ledgers are untouched.

## Compatibility and rollout

- **Protocol/schema compatibility:** E1 introduces `@armyofagents/worker-protocol` v1 (`PROTOCOL_VERSION = 1`, `MIN_PROTOCOL_VERSION = 1`). The v1 contract (`docs/contracts/worker-protocol/v1/`) and the independent hash-pinned frozen consumer are `baseline_established`; the first freeze records no fictional prior-version proof. The only runtime dependency is `zod@3.24.2`; `pnpm-lock.yaml` carries the package importer. Frozen-consumer bundle `dist/index.js` = `3c854028…7294e9` (239,198 bytes; zod 3.24.2 + esbuild 0.28.1) with `dependency-lock.json.sourceSha == BASELINE_SOURCE_SHA`; the first and every later contract change must run the current candidate against the oldest supported frozen consumer in both directions, and unsupported critical behavior must fail during negotiation.
- **Boundary:** runtime source under `packages/worker-protocol/src` imports only `zod` + relative modules (boundary checker green 3× + 50/50 mutation corpus); zero `server`/`ui`/`packages/db` source touched. No database schema, HTTP route, scheduler, worker, provider SDK, browser, or UI added.
- **CI:** the E1-F007 fix wires `node scripts/check-worker-protocol-package.mjs` into the CI `verify` job (after `pnpm -r build`, which builds this leaf), in addition to the pre-existing always-on `policy`-job boundary checker + mutation corpus — so both the dependency boundary and the packaging/import smoke run on every PR.
- **Rollback/disable path:** E1 is additive leaf-package + additive root config; it constructs no runtime and changes no existing behavior. Reverting the E1 commits removes the package with no downstream migration.
- **Residual risk (non-blocking):** the authoritative repository full-suite/integration baseline per DEC-03 should run on Linux CI; this gate ran Windows-local per the operator's explicit directive. A Linux-CI run at this revision is recommended to formalize; if it differed, it would create a superseding QA record and completion handoff (`Supersedes` → these records). The DEC-03 environment `test:run` failures carry no E1-logic regression.

## Next unblocked work

E1 is **complete**. Its consumption dependency epics are unblocked at the epic-planning level (their implementation dependencies are not automatically satisfied).

- **E2 (Tenant kernel)** is independent of E1 and may already be running from the accepted E0 completion handoff; it does not serialize behind E1.
- Once **both E1 and E2** are green on main, the next planning order is:
  1. **E3/E4 core planning** against both accepted handoffs.
  2. **JOB-001 / JOB-002 / WRK-001** core bootstrap, then **JOB-009 / JOB-003 / WRK-004**.
  3. **E6 DEP-000 through DEP-004** and the named **`E6-D1-FOUNDATION`** gate.

No downstream implementation begins merely because E1 types compile; each dependency epic's gate must be green on main.
