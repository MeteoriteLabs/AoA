# Handoff - E1 frozen-consumer checker corrective fix-round 2 candidate

**Date (UTC):** `2026-08-10`

**Candidate code revision:** `7d649a01802bc2062e91ded370ec7d6385c72931`

**Supersedes:** a4 only if the distinct reviewer accepts this candidate

**Decision:** `awaiting_review`

**Gate owner:** `TBD - distinct reviewer`

This handoff is intentionally non-passing. It neither completes prerequisite P2 nor
authorizes JOB-001 or any other E3 ticket.

## Candidate correction

- Smoke retains absolute executable/module paths, authenticated-before-execute ordering,
  local cwd, timeout/kill, output cap, and stripped injection/secret inputs. Its wrapper
  now clears all JavaScript-visible environment keys before a dynamic import; the same
  valid probe observes zero keys on the Windows host and a Linux Node 24 container.
- Each focused corpus process owns one unique temp parent. Every temp fixture/repository
  is its descendant; assertions and final cleanup inspect/remove only that exact parent.
  Shared-prefix enumeration and deletion are removed.
- The separate staggered regression runs two complete `30/30` corpora, holds B on a live
  repository while A exits, proves B remains intact, then proves both owners remove only
  their own parent and the orchestrator leaves only its four expected files.
- Default and inherited-signing corpora pass `30/30`; the isolation lane passes `2/2`;
  actual checker, boundary/package/manifest, typecheck, and build pass. The fresh full
  repository test run retains only the honestly recorded Windows collection failure.
- The protected immutable diff is empty. No frozen artifact, protocol dependency, or E3
  implementation changed.

## Evidence paths

- Result ledger: `docs/replatform/epics/E3-job-control/prerequisites/E1-frozen-checker-correction-result.md`
- QA candidate: `docs/replatform/epics/E1-worker-protocol/qa/2026-08-10-d0-e1-frozen-checker-correction-7d649a01802b-a5.md`
- Finding: `docs/replatform/epics/E1-worker-protocol/findings.md#e1-f008---frozen-consumer-checker-couples-immutable-e1-evidence-to-a-later-consumers-working-tree-and-installed-dependencies`
- Implementation report: `.superpowers/sdd/implementation-plan/prereq-p2-e1-frozen-checker-report.md`

## Reviewer action required

Review the exact evidence revision, independently run the zero-environment and staggered
ownership cases plus the retained hostile corpus, and issue the prerequisite decision.
Until then, P2 remains non-passing and E3 must not begin from this handoff.
