# Inbox Hub — Full Content Test Report (2026-07-04)

Execution of the CONTENT-AUDIT.md test matrix on qa-inbox-e2e (:3399): all 19 semantic types + bypass semantics + work_question surface. Two executors (instant/API/seeds + sequential Scout-Claude flows), followed by a browser UI pass.

## Executor A — instant/API/seed types

### approval_request (hire, QAB) — **PASS**
- Test: POST /agent-hires on QAB (requireBoardApproval=true) -> assert NO in-tx emit (latency gap), waiting lane, title/summary templates, approve closes; second hire rejected to test reject path
- Observed: Agent PendingHire-QA created pending_approval + hire_agent approval pending. LATENCY-GAP FINDING CONFIRMED with nuance: code has no hub emit in the hire path (routes/agents.ts:1121 approvalsSvc.create; zero hub refs in agents.ts), and an in-process probe (POST then direct DB polls, no hub GETs) found NO hub row at +48ms, row materialized at +302ms — an external connected client's scan-on-read (live-event-driven sidebar-badges/hub refetch) filled the gap; on an idle instance the item would wait for the next hub/counts/badges GET. Item rendered in QAB waiting_on_you: title 'Review hire agent approval', summary 'Agent: PendingHire-QA', ownerPool=board, priority normal. POST /api/approvals/:id/approve flipped agent pending_approval->idle and hub item -> archived; second hire's reject also archived its item.
- Evidence: agent ccee8d71-adb3-4921-8a24-780cddaf5119; approval 2453aff5-f3c6-43da-9be9-47ef65143f12 (hub item 70d225e5, archived); probe hire approval adb30eaf (agent ba16ef34, rejected, hub item archived); probe timing: 'POST done at +42ms' / '+48ms: no hub row yet' / 'HUB ROW PRESENT at +302ms'

### approval_request (plain, QAL) — **PASS**
- Test: POST /api/companies/QAL/approvals type=budget_override_required -> assert in-tx emit (instant), templates, reject closes
- Observed: In-tx emit proven: hub row present +5ms after the POST returned (probe: POST 34ms, row found at +39ms, before any hub GET). Title 'Review budget override required approval', summary 'Approval type: budget override required' (no agent name in payload -> type fallback template), ownerPool=board, priority normal, status open. POST /api/approvals/:id/reject -> hub item archived.
- Evidence: approval eded95e4-50e3-4383-8761-3b99a7b848dd; hub item 2fe02ada-7691-42c7-aa79-afcbb602d225; SQL post-reject: status=archived

### join_request — **PASS**
- Test: POST /invites {allowedJoinTypes:both} -> POST /api/invites/<token>/accept as agent -> assert waiting lane + templates; reject via /join-requests/:id/reject closes
- Observed: join_requests row pending_approval + in-tx hub emit. Waiting_on_you lane: title 'Review JoinBot-QA', summary 'agent join request', ownerPool=board, status open. POST /api/companies/QAL/join-requests/:id/reject -> status rejected, hub item archived in-tx, waiting lane empty on next GET.
- Evidence: invite c16271dc (token aoa_invite_pbgeezu3); join request 5baa476e-6f7c-4955-81fb-d8328f11e807; hub item a5859ce5-7561-4cf5-988f-73006d6996ec -> archived

### run_failed — **PASS**
- Test: FailBot process agent (cmd /c exit 1) + auto-dispatched todo issue -> assert EVENT-DRIVEN emit (verified by direct SQL before any hub GET or sidebar-badges), notifications lane, title/priority; close via supersession by a newer run
- Observed: Run failed within ~12s of issue create; hub row already in DB via direct SQL with zero hub GETs made (event-driven emit at heartbeat setRunStatus terminal chokepoint confirmed). Title 'FailBot run failed', summary 'Process exited with code 1', priority HIGH, sourceType heartbeat_run, notifications lane. Close path (supersede valve): second wakeup produced a new failed run; on the next notifications-lane GET the older item was archived and the newest run's item stayed open.
- Evidence: agent c7a881c5-38ea-4281-b48b-e5c37602e3fe; issue 788b3315; run1 20d61788 -> hub item 019dc328 (archived after supersession), run2 2a3dbf43 -> hub item 3cf5f998 (open)

### run_complete — **PASS**
- Test: OkBot process agent (cmd /c echo ok) + todo issue -> run_complete appears; dispatch a SECOND task -> assert supersede valve archives the older item on next notifications-lane GET
- Observed: Run succeeded in seconds; item in notifications lane: title 'OkBot run complete', summary 'Run finished successfully', priority normal, sourceType heartbeat_run. After task 2's run landed, the next notifications-lane GET archived item 1 (superseded) while item 2 remained open — supersede valve works exactly as audited.
- Evidence: agent 74b4fef5-2e63-462d-b44b-fe4a4d1afba4; issues 6205a122 / e4890d9f; run1 a217694a -> item b4fcfc07 archived; run2 94f9d34b -> item 6e9445dd open

### budget_alert — **PASS**
- Test: PATCH budgetMonthlyCents 12000 -> sidebar-badges scan -> high item; PATCH 10000 -> same item updates to urgent; PATCH 0 + badges + hub GET -> assert NEVER closes (no reconciler); restore budget 0
- Observed: After budget=12000 + GET /sidebar-badges (the only producing route): title 'Budget approaching limit', summary '92.58% used (11109/12000 cents)', priority high, ownerPool=board, sourceType company_budget, sourceId=companyId. After budget=10000 + badges: SAME row updated in place to urgent, '111.09% used (11109/10000 cents)'. After budget=0 + badges + notifications-lane GET: item still open with the stale 111.09% summary — nothing closes it (no reconciler for company_budget), audited never-closes behavior confirmed. Note: month spend is 11109 cents (grew past the recipe's 10291 baseline from concurrent executor runs), so percentages differ from the recipe's 85.8/102.9 estimates.
- Evidence: hub item 16d70ef1-ab38-4ce4-a45d-e1821ae27bdd (single row through all three phases); company budgetMonthlyCents confirmed 0 at end

### suggestion — **PASS**
- Test: POST /suggestions/detect -> in-tx emits in suggestions lane; close one via hub action API (resolve) and one via source dismiss (reconciler)
- Observed: Detect returned {detected:3, created:2}; two new items appeared in suggestions lane in-tx: 'Workload is uneven across agents' (summary='Scout-Claude has 6 open tasks while FailBot has 1.') and 'Scout-Claude is carrying 6 open tasks' (summary=evidence text). Hub action POST /hub-items/:id/action {action:resolve, expectedVersion:0} -> status resolved + audit row. Source POST /suggestions/:id/dismiss -> in-tx reconcile archived the second item. Observation: two duplicate pre-existing open 'No identity memory exists yet' items with DIFFERENT suggestion sourceIds (ed6c6c41 / 32e9fd59) — the category+actionPayload dedupe did not collapse them across detector runs; minor finding.
- Evidence: items 6fa02ae7 (resolved via hub action, audit:true) and 4b877202 (archived via source dismiss of suggestion 51f98ef0)

### stale_work — **PASS**
- Test: SQL-backdate a QAL todo issue updated_at to now()-25h -> suggestions-lane GET scan emits; PATCH the issue -> assert reconcileIssue heals/closes
- Observed: Created dedicated issue '[SEED] Stale QA task' as todo (create with in_progress was rejected: 'in_progress issues require an assignee'), SQL-backdated 25h. Next suggestions-lane GET scan-emitted: title 'Stale task: [SEED] Stale QA task', summary 'No recent human or crew progress.', ownerPool=board (unassigned). PATCH /api/issues/:id (note: company-prefixed PATCH returns 'Issue endpoint is unprefixed') -> item was ALREADY archived when checked by direct SQL immediately after the PATCH, before any lane GET — confirming the in-tx targeted reconcileIssue on issue update, not a GET-path sweep. Lane GET then showed zero stale_work items.
- Evidence: issue 43b2fbc4-c29b-43ac-aa31-5aa20e811486; hub item 874d901c-a36d-4b3b-9b37-69eb04608c6b open->archived; backdated updated_at 2026-07-03T04:55:37Z, healed at PATCH 2026-07-04T05:56:11Z

### mention — **PASS**
- Test: POST discussion 'Mention QA' with '@Board please review the hub QA results' -> notifications lane; assert it NEVER closes (no reconciler)
- Observed: Mention emitted: title 'You were mentioned in a thread', summary '@Board in thread', owner local-board, sourceType discussion, composite sourceId threadId:entryId:userId as audited. Never-closes confirmed: still open after /hub-items/counts + /sidebar-badges + notifications-lane GET sweeps (issue/discussion reconcilers are semanticType-scoped away from mention). Per-item GET also returns it (lane notifications). Recorded as the audited behavior — only manual/personal-state/autopilot can clear it.
- Evidence: discussion 1993f43d-51cd-4996-8b25-6b939ef414ad, entry 2619b35d; hub item e4d59ae0-04e9-4861-bba5-8a7ec2346936, sourceId '1993f43d...:2619b35d...:local-board', status open at end

### marketplace_op — **PASS**
- Test: GET /api/marketplace/catalog (532 items, cdn, sync success) -> install skill:anthropic/brand-guidelines on QAL -> wait terminal -> item appears; no closer exists (audited)
- Observed: Install operation reached status 'success' within ~5s; installCompleted fired: title 'brand-guidelines installed', summary 'Marketplace install completed successfully.', owner local-board, notifications lane, sourceId 'install_completed:<operationId>:local-board' (event+ref+owner composite as audited). No reconciler for marketplace_operation — item left open; recorded as audited never-closes behavior.
- Evidence: operationId fbcc5c65-3aad-410d-950c-1d43cb3853e9 (GET /marketplace/install/<id> status=success); hub item b1decf15-3ba0-4597-a3d4-48e255456deb

### human_input_needed — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: registry entry exists, zero createNotification callers) -> assert renders in waiting_on_you lane
- Observed: Seeded row appeared in waiting_on_you lane with title '[SEED] human_input_needed render test', status open; survived GET-path reconcilers (discussion reconciler is scoped to discussion_pending). Real discussion id used in source_id so the thread deep-link resolves.
- Evidence: hub item c431db6d-d31a-49d5-9a96-809b279df954; type thread.human_input_needed, source_id '1993f43d...:qa1'

### scope_proposal — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: scope proposals surface as thread entries + crew_dispatch approval, never their own hub item) -> assert waiting_on_you lane render
- Observed: Seeded row appeared in waiting_on_you lane, title '[SEED] scope_proposal render test', status open, stable across sweeps.
- Evidence: hub item 174d6a8d-03da-47be-b4b8-1a034e71a5fc; type thread.scope_proposal_posted, source_id '1993f43d...:qa-scope1'

### extraction_failed — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: real CLI-extraction failure only sets extractionStatus=failed + transient live event — genuine producer gap vs the 'founder notified via notifications table' contract) -> assert notifications lane render
- Observed: Seeded row appeared in notifications lane, title '[SEED] extraction_failed render test', status open. The producer gap itself is a real finding worth filing: extraction failures never reach the Inbox in this build.
- Evidence: hub item 992945dc-1f68-4a9e-8949-f79c7ae56b3b; type discussion.extraction_failed, source_id '1993f43d...:qa-xf1'

### reminder — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: checkReminders exists but nothing schedules/invokes it; no create-reminder route) -> assert notifications lane render
- Observed: Seeded row appeared in notifications lane, title '[SEED] reminder render test', status open.
- Evidence: hub item 8f0e0711-15b1-4e04-a513-bebdf224c399; type internal_agent.reminder, source_type internal_agent_reminder, source_id qa-reminder-1

### proactive — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: all 8 proactive check functions exported but never invoked — no 4h scheduler wired) -> assert SUGGESTIONS lane render (proactive maps there, not notifications)
- Observed: Seeded row appeared in suggestions lane (as audited), title '[SEED] proactive render test', status open; per-item GET confirms lane=suggestions.
- Evidence: hub item 40aba939-c28c-49d6-82f6-3174630df277; type internal_agent.proactive, source_type internal_agent_check, source_id 'blocked_task_scan:local-board'

### routine_outcome — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: routine.tick/sweep.tick are crew wake-trigger event names only; no registry entry, routines.ts never emits) -> assert notifications lane render
- Observed: Seeded row appeared in notifications lane, title '[SEED] routine_outcome render test', status open — the hub read path keys on semantic_type as audited, so the row renders despite no registry write entry.
- Evidence: hub item f28c932f-f1f9-4922-a827-e0a848b6d485; type routine.tick, source_type routine, source_id qa-routine-1

### legacy_other — **SEEDED-RENDER-ONLY**
- Test: SQL seed (producer-dead: internal_agent.action_result mapping has zero writers; buildNotificationHubEmit rejects unknown types, so the catch-all is reachable only by pre-existing rows/seeds) -> assert notifications lane render
- Observed: Seeded row appeared in notifications lane, title '[SEED] legacy_other render test', status open — catch-all sink renders without a registry crash.
- Evidence: hub item 7570da4f-e182-4004-9156-b86f219e5313; type internal_agent.action_result, source_type internal_agent_action, source_id qa-legacy-1

### Executor A notes
CLEANUP: QAL budgetMonthlyCents restored to 0 (its original value); no code changes, no server restarts, no Scout-Claude/Scout-Codex usage (Scout run_complete rows observed in the lane belong to Executor B). All 7 SQL-seeded rows + the stale-work issue are titled with the '[SEED] ' prefix. LEFT IN PLACE (intentional test residue): QAB agent PendingHire-QA (idle, from the approve path); QAL agents FailBot + OkBot with their latest run hub items open (FailBot run_failed 3cf5f998, OkBot run_complete 6e9445dd — they are the agents' latest runs so the supersede valve correctly keeps them open); QAL issues 'fail run QA-A', 'ok run QA-A 1/2', '[SEED] Stale QA task' (healed/archived hub item); discussion 'Mention QA' + its open mention item; budget_alert item 16d70ef1 open with stale '111.09%' summary (never-closes is the audited behavior — do not mistake it for drift); marketplace skill brand-guidelines installed on QAL + its open marketplace_op item; the 7 seeded rows left open for the UI render pass; QAB's pre-existing 'RejectHire' approval untouched. SURPRISES/FINDINGS: (1) The hire-approval latency gap is real in code (no in-tx emit at routes/agents.ts:1121) but in practice was masked within ~300ms by a connected client's live-event-triggered scan-on-read — probe proof: no hub row at +48ms, present at +302ms with zero hub GETs from the probe; on a headless/idle instance the gap is unbounded. (2) POST /issues rejects status=in_progress without an assignee ('in_progress issues require an assignee') — recipe's stale_work seed works with todo. (3) Issue PATCH must use unprefixed /api/issues/:id; the company-scoped path returns 'Issue endpoint is unprefixed'. (4) Duplicate open 'No identity memory exists yet' suggestion hub items exist with distinct suggestion source rows (ed6c6c41 / 32e9fd59) — cross-run dedupe gap in the suggestion engine, worth filing. (5) QAL month spend is now 11109 cents (recipe said 10291), so budget percentages differ from the recipe's estimates — assertions used the live formula output which matched exactly ('92.58% used (11109/12000 cents)' / '111.09% used (11109/10000 cents)'). (6) An additional '[SEED] Which pricing tier...' agent_runtime_decision item is present in the QAL waiting lane — seeded by another executor, not part of this pass. Final counts: QAL open=20/unread=18, QAB open=2/unread=1.

## Executor B — Scout-Claude runtime-decision flows

### agent_runtime_decision (bypass — routing disabled) — **PASS**
- Test: PATCH Scout-Claude runtimeConfig.runtimeDecisionRoutingEnabled=false, dispatch risky task ('Bypass QA: run command', must run hostname), poll waiting lane during + after run for ZERO new agent_runtime_decision items; record run outcome; restore flag.
- Observed: Run 203d6640 ran 05:47:14->05:49:57Z and finished status=succeeded with ZERO agent_runtime_decision hub items across all polls (waiting lane stayed at 0) and ZERO agent_runtime_decisions DB rows for the run. Run completed unsupervised — no AoA permission prompt was ever raised. Note: the CLI's own local sandbox blocked its tool calls (result text: 'sandbox is blocking all outbound HTTP calls... and env-var expansion'), so the task content (hostname) was not actually executed; that is a property of this box's CLI sandbox, not of AoA routing — the routing-bypass assertion holds. Flag restored to true and verified.
- Evidence: task 46d60bb9-0544-4960-9fa4-85a6ca1cf0f7; run 203d6640-4204-4e0b-b61f-173d9b69bb9b (succeeded); SQL count of agent_runtime_decisions WHERE run_id=203d6640... = 0; 6 lane polls at ~20s cadence all '0 runtime_decision items'; runtimeConfig after restore = {contextMode:standard, runtimeDecisionRoutingEnabled:true}

### agent_runtime_decision (routed permission control) — **PASS**
- Test: With routing re-enabled (trust rules empty), dispatch risky task; assert decision + hub item appear in waiting_on_you with audited content; answer allow_once via POST /agent-runtime-decisions/:id/answer; assert answered->relayed, hub item push-close archives, run continues.
- Observed: Permission decision 8db03ca1 (kind=permission, timeoutPolicy=escalate, adapter claude_local) raised ~18s after dispatch. Hub item b108b31f appeared in waiting_on_you lane, priority high, title = adapter prompt title ('Bash: echo "AGENT_ID=$AOA_AGENT_ID"...'), summary 'Agent wants to run Bash: ...' — matches audit template (decision.title / decision.summary). Answer required expectedSourceRevision+nonce from the decision GET (schema packages/shared/src/validators/hub.ts:119-138). allow_once: answeredAt 05:51:48.657Z -> relayed 05:51:49.432Z (<1s, live in-run broker) -> hub item archived 05:51:49.442Z (BUG-2 push-close). Run 7d8c49b0 continued running afterward.
- Evidence: task 1b9f2b5b-f47d-4021-aa41-9da3dc8f9068; decision 8db03ca1-72b9-4c92-af53-5f5b6e0a8fb3 (sourceRevision 0->1, status created->answered->relayed); hub item b108b31f-b4d0-4c6a-8960-d3f0cc6ec622 open->archived; run 7d8c49b0-39d6-4e5d-8b8b-c94d6191833d still 'running' post-relay; trust-rules GET = []

### agent_error (via permission-timeout expiry) — **PASS**
- Test: Leave the run's follow-up permission prompt unanswered; SQL fast-forward expires_at; wait 30s sweeper; assert decision cancelled, waiting item archived, agent_error 'Permission request timed out' appears in notifications lane; confirm no-closer behavior; cancel remaining run.
- Observed: Follow-up decision 7a659951 (created) expired by the sweeper: status -> cancelled, relay_error 'timeout policy escalated the run'; its waiting hub item abbcbdde archived 05:53:35.645Z (push-close); new agent_error hub item bd6e9b95 emitted OPEN in notifications lane, priority high, title 'Permission request timed out: Bash: curl -s -H "Authorization: Bearer $AOA_API_KEY"...', summary 'timeout policy escalated the run' — exact audited template. Run 7d8c49b0 cancelled ('Cancelled by control plane'). agent_error has no reconciler and stayed open through end of pass = audited never-closing behavior, PASS with note. SURPRISE: the claude CLI process survived the control-plane cancel and raised 2 more decisions over the next ~15 min (7ca7208f 'Write: ...aoa_me.ps1' at 05:54:24, then 7a95baf0), flapping the run back to 'running'; 7ca7208f self-expired (second agent_error emitted); a manual POST /api/heartbeat-runs/7d8c49b0/cancel settled it (single cancel sufficed) and its run-cancel side-effect cancelled 7a95baf0 + archived its item.
- Evidence: SQL UPDATE expired 1 row (7a659951, timeout_policy=escalate); decision 7a659951 final status=cancelled; hub items abbcbdde (archived 05:53:35.645Z) + bd6e9b95-fc5b-411c-b905-56a52bf8c087 (agent_error, open, high); zombie decisions 7ca7208f-6dc0-4044-b87d-1ffddbe4f301 (expired->cancelled) + 7a95baf0-1f90-4f8b-aebc-85db61c07bad (cancelled on final run cancel); final state: 0 active runs, 0 non-terminal decisions

### agent_runtime_decision (work_question, seeded) — **SEEDED-RENDER-ONLY**
- Test: Seed agent_runtime_decisions row (kind=work_question, status=shown, options Pro/Team, real dead run id, timeout_policy=escalate) + matching hub row using the exact producer pattern (source_unique_key companyId:runtime_decision:decisionId:agent_runtime_decision:); verify waiting-lane render, GET-reconcile does not close a non-terminal source; answer via API; record relay behavior against a dead run.
- Observed: Render + answer surface verified: seeded item showed OPEN in waiting_on_you (title '[SEED] Which pricing tier should the landing page feature?', high) and the GET-path reconcile correctly did NOT archive it (status 'shown' is non-terminal). Decision GET returned kind/work_question, options [{pro},{team}], sourceRevision 1. POST answer {kind:'work_question', answer:{selectedOptionId:'pro'}, expectedSourceRevision:1, nonce} accepted -> status answered, sourceRevision 2. DIVERGENCE from the expected 'relay_failed + urgent re-emit': NO relay attempt ever occurred — decision stayed 'answered' (relayedAt/relayError null) and the hub item stayed OPEN/high for 72s of polling. Root cause confirmed in code: relay is executed only by the in-run broker (server/src/services/heartbeat.ts:315-435 waitForAnswer -> markRelayed/markRelayFailed); markRelayFailed has no other production caller, so an answer against a dead run silently stalls forever ('answered' is non-terminal, expires_at was NULL, so neither reconcile nor the sweeper cleans it). Founder experience: the question remains in Waiting-on-you as answered-but-never-picked-up with no error surfaced. Matches the W5a known-blocker list (waitForAnswer poll model).
- Evidence: decision e3b35e23-8d05-43ae-9aca-08dce9576f21 (run_id 7d8c49b0 = real cancelled run); hub item 4db0ed6a-0009-4fe2-a22e-5401fa179e04; answer response {status:answered, sourceRevision:2}; 6 polls x12s all 'answered / hub open'; cleanup: SQL flip to cancelled -> GET reconcile archived hub item at 05:59:35.739Z

### discussion_pending — **PASS**
- Test: Create discussion 'Extraction QA B' with rich paste content; kick CLI extraction via POST .../entries/:eid/reprocess; assert hub item title/summary/owner per audit; approve all extracted items via POST .../discussions/:did/approve; assert in-tx reconcile closes the hub item.
- Observed: Entry created extractionStatus='skipped' (QA-BUG-015 confirmed), reprocess flipped it to pending, CLI extraction completed in ~20s producing 4 pending items (decision 'Ship hub tabs Friday', tasks 'Update inbox docs' + 'Fix SPA fallback 404', insight 'Reconcile sweeps only run on GET'). Hub item appeared in waiting_on_you with EXACT audited template: title 'Review 4 pending items in Extraction QA B', summary '4 extracted items need review.', owner local-board, sourceId = discussion id. Approve-all returned {approved:4, rejected:0, tasksCreated:2, memoryItemsCreated:2}; pendingItemCount -> 0; hub item archived in-tx (reconcileDiscussion) at 06:01:53.992Z. Close path fully verified.
- Evidence: discussion 32866860-f255-4ebe-9496-cebc91d42160; entry 26c6cd0a-ca25-4c71-882c-63012ca3af84; hub item 77c7a02c-8430-41f8-9cb2-b6d32cfdaa95 (open->archived); item ids c5a663e1/ce097d79/bbfc5ad3/3a98ef3a; created tasks 8656f31b-c103-4995-ab4e-5c7ca27e6a37 + 01556a52-bf82-4650-9c34-ae08cd65b08d; memory items 8529ee73 + 05fd73a6

### approval_request (crew_dispatch, W1c Assist autonomy) — **PASS**
- Test: Create thread, PATCH autonomyLevel=1, post '@Adjutant please propose crew work for this'; poll up to 8 min for 'Review crew dispatch approval'; verify approving flips parked planning tasks to standard + enqueues dispatch + closes the hub item.
- Observed: Live Adjutant path converged in ~40s (no SQL fallback needed): approval b533bf6e (type crew_dispatch, pending) + hub item ed29cb29 in waiting_on_you, title 'Review crew dispatch approval', summary 'Approval type: crew dispatch' (audited template — payload has no agent name). Payload carried ONLY taskIds+threadId+scopeVersionId+proposedByAgentId (memory candidates absent, D12 honored). Both tasks ('Build the hero widget', 'Write the pricing docs') were created work_mode=planning status=todo assigned to crew agent Engineer (e053668e, aoa/claude_local). Approve: POST /api/approvals/:id/approve (NOTE: the company-scoped /companies/:cid/approvals/:id/approve returns 404 'Not found' — resolve routes are mounted unprefixed) -> both tasks flipped planning->standard, TWO dispatch wakeups enqueued (one per task, payload->>'issueId' correct), hub item archived 06:04:09.419Z. Wakeups subsequently ended 'skipped_autonomy' — the crew agent's own autonomy gate skipped actual run spawn on this instance, so dispatch was verified at the flip+wakeup-enqueue level (no heartbeat runs started).
- Evidence: thread 1ae31ff2-a159-4d7c-bb5f-d2642fcd56c3; approval b533bf6e-aa3f-4bd0-96e2-69b8b5d9f2a8 pending->approved; hub item ed29cb29-31c1-48b6-a2de-a79c6193deea open->archived; tasks c320e01f + 3b680ede (planning->standard); wakeups 849b5af8 + bbe9bcdf (queued -> skipped_autonomy); appeared on poll 2 of 20s cadence (~40s)

### Executor B notes
CLEANUP: Scout-Claude runtimeConfig.runtimeDecisionRoutingEnabled restored to TRUE (verified via GET). All 4 QA tasks cancelled via PATCH /api/issues/:id (46d60bb9 bypass, 1b9f2b5b routed, c320e01f + 3b680ede crew-dispatch). Final state verified: 0 running/queued heartbeat runs company-wide, 0 non-terminal agent_runtime_decisions. Seeded work_question rows are '[SEED]'-prefixed (decision e3b35e23 flipped to cancelled with relay_error '[SEED] QA cleanup'; its hub item archived by reconcile). Residue intentionally left as evidence: 2 unassigned tasks + 2 memory items from the extraction approval; open agent_error items (incl. 2 new ones from this pass — no closer exists, audited behavior); [SEED] scope_proposal + human_input_needed waiting items belong to the other executor, untouched. SURPRISES worth filing: (1) claude CLI process survives control-plane run cancel and kept raising runtime decisions for ~15 min, flapping run 7d8c49b0 running<->cancelled; each zombie decision generates its own agent_error on expiry (2 extra items); a manual second cancel settled it. (2) work_question answered against a dead run stalls silently at 'answered' forever — no relay_failed, no urgent re-emit, hub item stays open (relay only happens via the live in-run broker, heartbeat.ts:315-435; markRelayFailed has no other production caller). (3) Route quirks: approve/reject approvals routes are unprefixed (/api/approvals/:id/approve; the company-scoped variant 404s), issue PATCH likewise unprefixed (/api/issues/:id, server literally replies 'Issue endpoint is unprefixed'). (4) Crew dispatch wakeups ended 'skipped_autonomy' — crew (aoa) agents don't spawn heartbeat runs from task-dispatch wakeups on this instance, so Assist-approve dispatch is observable only as wakeup rows. Nothing left running; no repo/code changes made.
