# Inbox Hub Content Coverage Audit (2026-07-04)
Four-agent audit: producers, UI rendering, lifecycle, live-trigger recipes. Environment: qa-inbox-e2e :3399.

## 1. Per-type producers & closers

### approval_request (waiting_on_you)
- **Producers:** EVENT: routes/approvals.ts:113 (POST /approvals create, in-tx emitHubItem(buildApprovalHubEmit)), :337 (request-revision re-emit), :397 (resubmit re-emit); mcp/tools/approval-tools.ts:51 via syncApprovalHubItem (MCP approval create/update while status pending/revision_requested); services/thread-agent-actions.ts:845-848 (W1c Assist autonomy: system-created crew_dispatch approval after scope-draft auto-apply). SCAN-ON-READ: services/hub-approval-requests.ts:8-21 emitOpenApprovalHubItems (all pending/revision_requested approvals, cap 50) called from routes/hub-items.ts:110 (GET list, waiting lane or no lane), :141 (GET counts) and routes/sidebar-badges.ts:70. NOTE: agent-hire approvals created at routes/agents.ts:1121 (approvalsSvc.create, type hire_agent) have NO event emit — they only materialize on the next scan-on-read.
- **Product trigger:** Any approval enters pending: founder/agent posts an approval via REST or MCP; agent hire when company.requireBoardApprovalForNewAgents=true; Assist-level (autonomy 1) thread scope-draft auto-apply raising ONE crew_dispatch approval; revision-request or resubmit re-opens it.
- **Content:** title: `Review ${spaced(approval.type)} approval` (underscores→spaces, e.g. "Review crew dispatch approval"); summary: `Agent: ${name}` when payload.name/agentName present, else `Approval type: ${spaced(approval.type)}`; ownerPool: "board"; sourceActor = requestedByAgentId/UserId; priority normal. (hub-source-producers.ts:96-115)
- **Closers:** reconcileApproval (hub-items.ts:1366-1379: terminal unless status pending/revision_requested, or row gone) invoked targeted in-tx on settle: approve routes/approvals.ts:165, reject :284, MCP approve/reject via approval-tools.ts:54 syncApprovalHubItem. crew_dispatch approve side-effect (services/approvals.ts:56-230) dispatches the parked planning tasks but the hub close is the same route reconcile. Founder-authority for manual resolve/archive (HUB_AUTHORITY_BY_TYPE); EXCLUDED from autopilot (founder-gated, hub-autopilot.ts:233/351). No periodic full sweep for approvals — only targeted reconciles + open re-emits.

### discussion_pending (waiting_on_you)
- **Producers:** EVENT only: services/extraction.ts:493 (in-tx after CLI extraction inserts items + bumps pendingItemCount); services/discussions.ts:1792 (moveEntryToDiscussion — target thread gains pending items); services/internal-agent/tools/submit-extracted-items.ts:195 (agent submit_extracted_items tool inserts pending items). No scan-on-read materializer. Legacy registry also maps thread.artifact_needs_review → discussion_pending (notification-registry.ts:119-124) but that feed is dead (see gaps).
- **Product trigger:** A discussion entry finishes extraction with ≥1 pending discussion_extracted_items (paste/write/voice/MCP entry → CLI extraction), an aoa-agent submits extracted items, or an entry with pending items is moved into another thread.
- **Content:** title: `Review ${count} pending ${count === 1 ? "item" : "items"} in ${title}` (title = discussion.title or "Discussion"); summary: `${count} extracted ${count === 1 ? "item needs" : "items need"} review.`; owner = discussion.ownerUserId; scopeKey = discussion scopeId. (hub-source-producers.ts:135-158)
- **Closers:** reconcileDiscussion (hub-items.ts:1444-1471: terminal when pendingItemCount <= 0 or row gone; also heals count/title/owner drift) invoked targeted in-tx at discussions.ts approveItems (1215), rejectItems (1312), reprocessEntry (1477), reprocessAllEntries (1589), moveEntry source+target (1794/1798). Owner authority; autopilot-eligible but default rule disabled. No GET-path sweep for sourceType discussion — a pendingItemCount decrement outside those five functions leaves the item stale until manual action.

### join_request (waiting_on_you)
- **Producers:** EVENT only: routes/access.ts:2147 (join-request insert, in-tx), :2182 (invite-replay update re-emits when row is back to pending_approval). No scan-on-read materializer.
- **Product trigger:** A human or agent submits a join request to a company (POST join-request / invite claim) landing in pending_approval.
- **Content:** title: `Review ${subject}` where subject = agentName ?? `${adapterType ?? "Agent"} request` (agent) or requestEmailSnapshot ?? "Human join request"; summary: `${spaced(request.requestType)} join request`; ownerPool: "board". (hub-source-producers.ts:117-133)
- **Closers:** reconcileJoinRequest (hub-items.ts:1418-1442: terminal when status != pending_approval or row gone) invoked in-tx on approve (access.ts:2562) and reject (access.ts:2636). Founder authority; excluded from autopilot (founder-gated).

### human_input_needed (waiting_on_you)
- **Producers:** NONE. Only mapping is the legacy registry entry thread.human_input_needed → human_input_needed (packages/shared/src/notification-registry.ts:137-142), fed exclusively by buildNotificationHubEmit → createNotification (server/src/services/notifications.ts:63) / notificationService().create — which have ZERO live call sites in server/src.
- **Product trigger:** Intended: a crew/thread agent asking the human a question (thread.human_input_needed). Currently unreachable — no code emits it.
- **Content:** Would be caller-supplied title/message via buildNotificationHubEmit (notification-registry.ts:21-45; sourceId `${relatedEntityId}:${userId}:${type}:${uuid}`, owner = input.userId). No actual template exists.
- **Closers:** Nothing source-driven (defaultSourceType "discussion" but the reconcile("discussion") sweep is semanticType-scoped to discussion_pending, hub-items.ts:1556-1567). Manual resolve/archive (owner authority) or autopilot only — moot, since nothing produces it.

### scope_proposal (waiting_on_you)
- **Producers:** NONE. Registry entry thread.scope_proposal_posted → scope_proposal (notification-registry.ts:113-118) rides the same dead createNotification feed. Real scope proposals surface as thread scope-draft entries + the crew_dispatch approval_request (thread-agent-actions.ts:826-848), never as a scope_proposal hub item.
- **Product trigger:** Intended: Adjutant posting a scope proposal into a thread. Unreachable in current product — the scope flow emits approval_request instead.
- **Content:** No template exists (would be caller-supplied via buildNotificationHubEmit).
- **Closers:** None (no producer). Manual/autopilot only in principle.

### agent_runtime_decision (waiting_on_you)
- **Producers:** EVENT: services/agent-runtime-decisions.ts emitHubItem (628-643) called from createPrompt:707 (new permission/work_question prompt), answerPrompt:870, markRelayFailed:918, markRelayed:946, expireDuePrompts:983, cancelActiveForRun:1047 (re-emits refresh the still-open row; the storm-guarded change-aware emit no-ops otherwise). Upstream: heartbeat.ts runtime-decision bridge createPrompt at 385/396 (permission) and 461 (work_question), reachable only when the adapter runtime bridge is on (claude_local W5b hooks / codex_local W5c app-server, per-agent runtimeConfig.runtimeDecisionRoutingEnabled). SCAN: none — but GET list/counts run svc.reconcile(sourceType "runtime_decision") (routes/hub-items.ts:68,111,143).
- **Product trigger:** A supervised agent run hits a permission gate (or asks a work question) during a heartbeat run with runtime-decision routing enabled; founder must Allow once / Allow always / Deny (or answer).
- **Content:** title: decision.title (adapter prompt title, default "Runtime decision", agent-runtime-decisions.ts:684); summary: decision.summary ?? promptText ?? relayError; relatedEntity heartbeat_run; sourceActor agent; priority: "urgent" when status relay_failed else "high". (agent-runtime-decisions.ts:628-643)
- **Closers:** BUG-2 push-close (fix 650412410, per BUGS.md 2026-07-04): closeProjectedHubItem (650-661, targeted reconcile) fires on markRelayed:947, expireDuePrompts:1003, cancelActiveForRun:1048. reconcileRuntimeDecision → runtimeDecisionSourceSnapshot (agent-runtime-decisions.ts:510-532): terminal exactly on TERMINAL_STATUSES {relayed, expired, cancelled}. Safety net: GET-path reconcile sweep, semanticType-scoped so it never archives the agent_error timeout follow-up (hub-items.ts:1560-1567). Timeout engine: 30s index.ts sweeper (index.ts:1058-1081 expireDuePrompts, run-cancel side-effect). Founder authority; excluded from autopilot.

### run_failed (notifications)
- **Producers:** EVENT (BUG-5 fix da9de0159): heartbeat.ts setRunStatus terminal chokepoint ~1692-1712 — buildTerminalRunHubEmit for status failed/timed_out → emitHubItem (best-effort try/catch, never fails the run). SCAN-ON-READ: hub-legacy-alerts.ts:36-42 emitLegacyAlertHubItems (latest run per non-terminated agent, failed/timed_out) called ONLY from routes/sidebar-badges.ts:71. Both route through buildFailedRunHubEmit so rows are byte-identical (change-aware emit dedupes).
- **Product trigger:** An agent heartbeat run finishes failed or timed_out (adapter crash, CLI error, watchdog timeout).
- **Content:** title: `${agentName} run ${run.status.replace(/_/g, " ")}` (e.g. "Scout run timed out"); summary: run.error ?? `Latest run status: ${run.status}`; sourceActor agent; priority "high". (hub-source-producers.ts:188-202)
- **Closers:** reconcileHeartbeatRun (hub-items.ts:1384-1416): terminal when run status leaves HEARTBEAT_ACTIONABLE_STATUSES OR the run is superseded by a newer run for that agent (isSuperseded, the noise valve) — swept on GET list notifications lane (routes/hub-items.ts:114). Manual resolve/archive (owner authority); autopilot-eligible (heartbeat_run source resolves agentId for the trust gate, hub-autopilot.ts:217-227).

### budget_alert (notifications)
- **Producers:** SCAN-ON-READ ONLY: hub-legacy-alerts.ts:55-79 emitLegacyAlertHubItems — when company.budgetMonthlyCents > 0 and month-to-date cost_events utilization ≥ 80%. Called ONLY from routes/sidebar-badges.ts:71 (NOT from hub-items GET list/counts). No event-driven emit — budget-hooks.ts / budget_incidents feed a different emitter, not the hub, despite hub.ts's "budget.incident_created" comment.
- **Product trigger:** Company monthly budget ≥80% consumed (urgent at ≥100%), noticed the next time any client loads the sidebar badges.
- **Content:** title: "Budget approaching limit"; summary: `${pct.toFixed(2)}% used (${monthSpendCents}/${monthBudgetCents} cents)`; ownerPool "board"; sourceType "company_budget", sourceId = companyId (one row per company); priority "urgent" (≥100%) else "high". (hub-source-producers.ts:232-244)
- **Closers:** NOTHING automatic — sourceType "company_budget" has NO entry in SOURCE_RECONCILERS (hub-items.ts:1533-1541), so the item never auto-closes even if spend drops, the month rolls over, or the budget is raised. Manual resolve/archive (owner authority) or autopilot only. Re-emits keep refreshing the % while open.

### agent_error (notifications)
- **Producers:** EVENT: agent-runtime-decisions.ts:987-999 — escalate-visible follow-up when expireDuePrompts parks/escalates a timed-out permission prompt (timeoutPolicy park_run/escalate; Decisions #105/#106 amended, BUG-2). Shares sourceType/sourceId with the runtime-decision item deliberately. Legacy registry mapping thread.crew_failed → agent_error (notification-registry.ts:125-130) is a DEAD feed — crew failures post an inputType:"system" thread entry via crew-failure-card.ts instead of a hub item.
- **Product trigger:** A runtime permission request times out under a park_run/escalate timeout policy: the waiting-lane item closes and this notification explains what happened.
- **Content:** title: `Permission request timed out: ${updated.title}`; summary: updated.relayError ?? "timeout policy parked the run"; relatedEntity heartbeat_run; sourceActor agent; priority "high". (agent-runtime-decisions.ts:987-999)
- **Closers:** NONE automatic — reconcile("runtime_decision") is deliberately semanticType-scoped to agent_runtime_decision so it never instantly archives this follow-up (hub-items.ts:1560-1567 comment). Manual resolve/archive (owner authority) or autopilot.

### mention (notifications)
- **Producers:** EVENT x3: (1) services/issues.ts:2189 — @human mention in a task comment, per-recipient sourceId `${taskId}:${userId}`; (2) services/threads.ts:317 (processMentions) — @Name in a discussion entry resolving to a company-member human, sourceId `${threadId}:${entryId}:${userId}`; (3) services/internal-agent/tools/notify-owner-tool.ts:73 — Commander's notify_owner tool to the thread owner, sourceId = threadId (also publishes internal_agent.notification live event). Registry mappings thread.mention / internal_agent.* are the dead createNotification feed.
- **Product trigger:** A human is @-mentioned in a task comment or thread entry (self-mentions skipped), or Commander explicitly notifies a thread owner.
- **Content:** (1) title "You were mentioned in a comment", summary = comment body.slice(0,200); (2) title "You were mentioned in a thread", summary `${mention.raw} in thread`; (3) title `Thread notification${level && level !== "info" ? ` [${level}]` : ""}`, summary = message. All natural-owner (ownerUserId = recipient), priority normal.
- **Closers:** NONE automatic — sourceTypes are "issue"/"discussion" but those reconcilers only sweep semanticType stale_work / discussion_pending respectively (hub-items.ts:1556-1567), so mention rows are never source-closed. Manual resolve/archive, per-user read/dismiss/snooze, or autopilot.

### marketplace_op (notifications)
- **Producers:** EVENT: services/marketplace-notifications.ts:49-57 hub.emit per founder (fallback "local-board"), sourceId `${eventKind}:${ref}:${ownerUserId}`. Live call sites: installRequested (routes/marketplace-company.ts:464, routes/marketplace-installs.ts:251/322), installCompleted (services/marketplace-install/orchestrator.ts:273/344), installFailed (orchestrator.ts:291/366), updateAvailable (marketplace-update-checker.ts:88/145, marketplace-install/crew-updater.ts:205), updateCompleted (skill-auto-updater.ts:153). updateFailed helper has ZERO call sites (unreachable). No scan-on-read.
- **Product trigger:** Marketplace install requested/completed/failed, catalog update available, or skill auto-update completed — notifies every founder.
- **Content:** Per helper (marketplace-notifications.ts:74-126): `${catalogItemName} installed` / `Install requested: ${catalogItemName}` (msg `User ${requestingUserId} requested installation of ${catalogItemName}.`) / `${catalogItemName} install failed` (msg = error) / `Update available: ${catalogItemName}` (msg `${fromVersion} → ${toVersion} is available.`) / `${catalogItemName} updated` / `${catalogItemName} update failed`. ownerUserId = each founder.
- **Closers:** NONE automatic — sourceTypes marketplace_operation/marketplace_update have no reconciler. Manual resolve/archive (owner authority) or autopilot.

### run_complete (notifications)
- **Producers:** EVENT ONLY (BUG-5 fix da9de0159, per BUGS.md resolution log 2026-07-04): heartbeat.ts setRunStatus terminal chokepoint ~1692-1712, buildTerminalRunHubEmit → buildCompletedRunHubEmit when status "succeeded" (adapter-agnostic). The legacy sidebar-badges scan emits failures only — success is never scan-materialized.
- **Product trigger:** An agent heartbeat run finishes with status succeeded.
- **Content:** title: `${agentName} run complete`; summary: "Run finished successfully"; sourceActor agent; priority "normal". (hub-source-producers.ts:204-218)
- **Closers:** reconcileHeartbeatRun supersession — "succeeded" stays in HEARTBEAT_ACTIONABLE_STATUSES so the item remains open only while it is the agent's LATEST run; a newer run archives it (the noise valve, hub-items.ts:39-48 + isSuperseded 1410-1412), swept on GET list notifications lane (routes/hub-items.ts:114). Manual or autopilot otherwise.

### reminder (notifications)
- **Producers:** Producer code EXISTS but is UNREACHABLE: services/internal-agent/proactive.ts:568 (checkReminders fires due internal_agent_reminders → hub emit, sourceId = reminder.id). checkReminders is exported (internal-agent/index.ts:33) but no scheduler, route, or interval invokes it anywhere in server/src (tests only). Registry internal_agent.reminder feed is also dead. Effectively NO live producer.
- **Product trigger:** Intended: a Commander reminder (internal_agent_reminders.triggerAt due) fires. Currently never fires — no sweep calls checkReminders, so reminders sit "pending" forever.
- **Content:** title: "Reminder"; summary: reminder.content; ownerUserId = reminder.userId. (proactive.ts:566-576)
- **Closers:** None automatic (sourceType internal_agent_reminder has no reconciler). Manual/autopilot — moot while unreachable.

### extraction_failed (notifications)
- **Producers:** NONE. Registry entry discussion.extraction_failed is "active" (notification-registry.ts:47-52) but its only feed (createNotification) has zero callers. The real extraction-failure paths (services/extraction.ts:431 terminal CLI failure, :521 catch-all) only mark the entry extractionStatus="failed" + stash extractionError in sourceInfo + publishLiveEvent("discussion.extraction.failed") — a transient SSE event, never a persistent hub item.
- **Product trigger:** Intended: CLI extraction of a discussion entry exhausts retries. Today the founder only finds out via the DiscussionDetail UI badge / transient live event, not the Inbox.
- **Content:** No template exists.
- **Closers:** N/A (no producer). Would be manual/autopilot only (sourceType "discussion" sweep is scoped to discussion_pending).

### routine_outcome (notifications)
- **Producers:** NONE. hub.ts:80 comments "routine.tick / sweep.tick", but those are aoa-agent WAKE trigger event names (services/internal-agent/aoa-agents/triggers.ts:10-11,41-43), not hub emits; services/routines.ts contains no hub/emit call, and no registry entry maps to routine_outcome.
- **Product trigger:** Intended: a scheduled routine/sweep run reporting its outcome. Unreachable — nothing emits it.
- **Content:** No template exists.
- **Closers:** N/A (no producer).

### legacy_other (notifications)
- **Producers:** NONE live. Two theoretical feeds, both dead: (1) registry entry internal_agent.action_result → legacy_other (notification-registry.ts:65-70) via createNotification (zero callers); (2) mapPersistedNotificationType returns "legacy_other" for unknown legacy types (notification-registry.ts:159-164) — a read-side mapping for old persisted rows, not a write producer; buildNotificationHubEmit itself REJECTS unknown types (throws unprocessable, notification-registry.ts server-side :7-19).
- **Product trigger:** Designed as the catch-all sink for un-mapped legacy notification types. No current write path reaches it.
- **Content:** Caller-supplied title/message via buildNotificationHubEmit; no template.
- **Closers:** Manual/autopilot only (no reconciler) — moot.

### suggestion (suggestions)
- **Producers:** EVENT only: services/suggestions.ts:874 — in-tx emitHubItem(buildSuggestionHubEmit) per suggestion row inserted by the detector run (runAllDetectors: 8 detectors, deduped by category+actionPayload key). Registry thread.spinoff_suggested → suggestion is the dead createNotification feed. No scan-on-read.
- **Product trigger:** The suggestion engine detects a pattern (goal_gap, pipeline_bottleneck, memory_gap, pattern_detected, budget_optimization, recurring_work, risk_flag, workload_balance) on its Home-load / periodic run and inserts a pending suggestions row.
- **Content:** title: suggestion.title (detector-authored, verbatim); summary: suggestion.evidence; no owner override → resolves to first-human-ancestor/founder; priority normal. (hub-source-producers.ts:160-170)
- **Closers:** reconcileSuggestion (hub-items.ts:1473-1490: terminal when suggestions.status != "pending" or row gone) invoked targeted in-tx on expiry (suggestions.ts:822 expireOldPendingSuggestions in runAllDetectors), apply/execute (:924), and dismiss (:971). Owner authority; autopilot-eligible.

### stale_work (suggestions)
- **Producers:** SCAN-ON-READ ONLY: services/hub-stale-work.ts:9-28 emitStaleWorkHubItems — issues in todo/in_progress with updatedAt older than 24h (STALE_WORK_THRESHOLD_MS). Called from routes/hub-items.ts:117 (GET list, suggestions lane or no lane, cap = query.limit), :142 (GET counts, UNCAPPED limit=null) and routes/sidebar-badges.ts:72 (uncapped). No event-driven emit.
- **Product trigger:** A task sits untouched in todo/in_progress for >24h, surfaced the next time anyone loads the hub or sidebar badges.
- **Content:** title: `Stale task: ${issue.title}`; summary: "No recent human or crew progress."; owner = assigneeUserId else ownerPool "board"; sourceActor = assigneeAgentId when present. (hub-source-producers.ts:172-186)
- **Closers:** reconcileIssue (hub-items.ts:1492-1521: terminal when the issue is no longer old-and-open — status left todo/in_progress, updatedAt bumped within 24h, or row gone) invoked targeted in-tx on issue update (issues.ts:1355), delete (:1480), checkout (:1538), release (:1735), addComment (:1829 — comments bump updatedAt). Owner authority; autopilot-eligible. No GET-path sweep of sourceType issue, but every issue mutation path reconciles in-tx.

### proactive (suggestions)
- **Producers:** Producer code EXISTS but is UNREACHABLE: services/internal-agent/proactive.ts:113 createNotificationIfAllowed (hub emit, sourceId `${checkKind}:${userId}`, respects notificationPreference "silent") is called by the 8 proactive check functions (blockedTaskScan:129, budgetThresholdAlert:185, staleWorkDetection:234, dependencyChainGaps:277, memoryConflictScan:366, workloadImbalance:406, morningDigest:467) — ALL exported via internal-agent/index.ts:26-34 with ZERO invocations in server/src (no 4-hour scheduler, no route, tests only). Registry internal_agent.proactive + persisted alias internal_agent_proactive are the dead createNotification feed. Cockpit READS these rows (cockpit.ts:404-430) but nothing writes them.
- **Product trigger:** Intended: Commander's proactive check interval (default 4h per CLAUDE.md) scanning blocked tasks, budget thresholds, stale work, dependency gaps, memory conflicts, workload imbalance. Currently never fires.
- **Content:** Per-check caller-supplied title/message, e.g. blockedTaskScan passes counts of blocked tasks; ownerUserId = recipient userId; sourceType "internal_agent_check". (proactive.ts:95-122)
- **Closers:** None automatic (no reconciler for internal_agent_check). Manual/autopilot — moot while unreachable.

## 1b. Producer gaps

DEAD FEED (systemic): the entire persistent-notification registry write path is orphaned — createNotification (server/src/services/notifications.ts:63) and notificationService().create have ZERO call sites in server/src outside tests, so every semantic type whose ONLY producer is a registry mapping is unproducible: (1) human_input_needed (thread.human_input_needed), (2) scope_proposal (thread.scope_proposal_posted), (3) extraction_failed (discussion.extraction_failed — the real failure path extraction.ts:431/:521 only sets extractionStatus=failed + a transient publishLiveEvent, no persistent Inbox item, contradicting the 'founder notified via notifications table' contract), (4) routine_outcome (no registry entry at all; routine.tick/sweep.tick are aoa-agent wake triggers in triggers.ts, routines.ts never emits), (5) legacy_other (internal_agent.action_result mapping; also buildNotificationHubEmit throws on unknown types so the 'catch-all sink' can only be reached by read-side mapping of pre-existing rows, never by a new write). Registry entries thread.artifact_needs_review, thread.crew_failed, thread.mention, thread.spinoff_suggested are likewise dead aliases of types that have separate live producers or none.

UNREACHABLE PRODUCERS (code exists, no trigger): proactive + reminder — all 8 proactive check functions incl. checkReminders/morningDigest (proactive.ts:129-568) are exported (internal-agent/index.ts:26-34) but no scheduler, interval, or route calls them; the documented 4-hour proactive loop is not wired in this worktree, so internal_agent_reminders never fire into the hub and cockpit's 'Proactive findings' card reads rows nothing writes. Also marketplaceNotifications.updateFailed (marketplace-notifications.ts:118-125) has zero call sites.

PRODUCER LATENCY GAP: agent-hire approvals (routes/agents.ts:1121) create the approval WITHOUT an in-tx hub emit — the approval_request item only appears on the next scan-on-read (hub GET list/counts or sidebar-badges), unlike route/MCP/Assist-created approvals which emit in-tx.

NEVER-CLOSING TYPES (no reconciler; manual/personal-state/autopilot only): budget_alert (sourceType company_budget absent from SOURCE_RECONCILERS — stays open after spend normalizes or month rolls over; also produced ONLY by sidebar-badges.ts:71, never by hub-items GET), mention, marketplace_op, reminder, proactive, agent_error (the runtime-decision sweep is deliberately semanticType-scoped away from it per BUG-2/D105-106). For mention specifically, the issue/discussion reconcilers exist but are scoped to stale_work/discussion_pending, so mention rows are permanently source-orphaned.

CONDITIONAL REACHABILITY: agent_runtime_decision requires the adapter runtime bridge (per-agent runtimeConfig.runtimeDecisionRoutingEnabled + claude_local W5b hooks or codex_local W5c app-server). Per BUGS.md resolution log (2026-07-04): BUG-1 codex handshake fixed (0ac91a759), BUG-2 stale decision items fixed via push-close + terminality=TERMINAL_STATUSES (650412410), BUG-3 storm fixed via change-aware emit setWhere (f3368867c), BUG-5 run_complete/run_failed now event-driven at the setRunStatus chokepoint (da9de0159); BUG-6 (codex supervised turn completes empty) remains open — codex-path runtime decisions currently produce no real work product.

AUTOPILOT: defaults fully off (mode off, every rule action 'none'/disabled, minTrustScore 100 — hub-autopilot.ts:49-60); founder-gated types (approval_request, join_request, agent_runtime_decision) are hard-excluded (hub-autopilot.ts:233, :351), and evaluateOpenItems runs on every hub GET list/counts (routes/hub-items.ts:119/144, limit ≤25).

## 2. Per-type UI rendering

### approval_request
- **List row:** ShieldQuestion icon chip; title 'Review <type> approval'; summary 'Agent: <name>' or 'Approval type: <type>'; badges 'Approval' + priority; unread dot; date. (hub-source-producers.ts:96-115, ownerPool=board)
- **Reading pane:** Full HubViewer: viewerKind caption 'approval', title, summary, why-section (curationReason/curationPriorityReason), Priority/Status/Source/Version meta, audit timeline when resolved/archived. All lifecycle buttons: Mark unread (when read), Dismiss, Snooze, Resolve, Archive, plus Claim (ownerPool=board, unclaimed) / Release (claimed).
- **Tab:** REAL viewer. approvalTab(approval.id) -> ApprovalDetailCore embedded (HubTabBody 'approval' case, D3), with onOpenTab hand-offs so linked task / hired agent open sibling tabs. crew_dispatch approvals flow through this same type. fullLink /approvals/:id is a real route (App.tsx:200) but is DEAD CODE in the tabbed shell: HubHomeTab always passes onOpenFull, so the Link branch in HubViewer.tsx:274-281 never renders.
- **Issues:** None material. Best-wired type.

### discussion_pending
- **List row:** MessageSquare icon; title 'Review N pending item(s) in <thread>'; summary 'N extracted items need review.'; badges 'Discussion' + priority.
- **Reading pane:** Full HubViewer, all lifecycle buttons (owner-authority; Claim only if ownerPool=board which this producer doesn't set — ownerUserId instead, so no Claim).
- **Tab:** REAL viewer. threadTab(discussion.id) -> embedded ThreadDetail (D2). fullLink /discussions/:id real (App.tsx:186). Caveat documented in HubTabBody:105-108: thread-internal links open in ThreadDetail's own right-side viewer, not sibling hub tabs.
- **Issues:** Minor only: in-thread link hand-off to sibling tabs deferred.

### join_request
- **List row:** UserPlus icon; title 'Review <agentName|email>'; summary '<type> join request'; badges 'Join request' + priority. Lane waiting_on_you, founder authority, ownerPool=board (Claim button shows).
- **Reading pane:** Generic HubViewer only (viewerKind 'notification' caption). All lifecycle buttons incl. Claim/Release. No approve/deny action anywhere in the pane.
- **Tab:** PLACEHOLDER. joinRequestTab factory exists (hubViewerModel.ts:117) but HubTabBody falls through to TabLoadingPlaceholder ('Preparing viewer… A dedicated join_request viewer is coming soon.') — E3 deferral confirmed (HubTabBody.tsx:210). fullLink () => null — no route at all.
- **Issues:** DEAD END for a founder-gated waiting_on_you item: live producer (buildJoinRequestHubEmit), but Open full = infinite spinner and fullLink=null, so there is no path from the hub to actually approve/deny the join request. Only generic resolve/archive/dismiss.

### human_input_needed
- **List row:** FileQuestion icon; label 'Needs input'; waiting_on_you lane.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** Would be REAL (threadTab -> ThreadDetail) IF relatedEntityId (discussion id) is set; fallback rawSource would hand the notification-registry COMPOSITE sourceId (`entityId:userId:type:eventId`, notification-registry.ts:25-27) to threadTab -> broken thread fetch. fullLink sourceLink('/discussions') uses the RAW sourceId, so a registry-produced item gets /discussions/<composite> — a broken deep link (agent_error shows the correct resolveTabId-based pattern).
- **Issues:** NO LIVE EMITTER found: nothing in server/src creates 'thread.human_input_needed' notifications (registry entry only, packages/shared/src/notification-registry.ts:137). Registry comment in hubRegistry.tsx:124-126 ('sourceId is the discussion id') contradicts buildNotificationHubEmit, which ALWAYS composites. rawSource fallback + raw-sourceId fullLink are both wrong for the registry format — latent bugs that fire the day an emitter ships.

### scope_proposal
- **List row:** GitPullRequest icon; label 'Scope proposal'; waiting_on_you lane.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** Same wiring as human_input_needed: threadTab via preferRelated(rawSource); real ThreadDetail if relatedEntityId set, broken composite otherwise. fullLink sourceLink('/discussions') = same raw-composite broken-link pattern.
- **Issues:** Same as human_input_needed: no live emitter for 'thread.scope_proposal_posted' notifications (scope proposals are discussion entries, not notifications, today), rawSource fallback and fullLink wrong for the registry composite format. Latent.

### agent_runtime_decision
- **List row:** Bot icon; label 'Runtime decision'; waiting_on_you, founder authority. Live producer (agent-runtime-decisions.ts:627-637, relatedEntityType='heartbeat_run').
- **Reading pane:** SPECIAL-CASED: HubViewer embeds RuntimeDecisionPanel inline (HubViewer.tsx:134-136) — fetches the decision by item.sourceId, shows Permission/Question header + status chip, promptText, facts grid (Tool/Command/Path/Network/Risk), and live actions: Allow once / Allow always / Deny, or answer textarea + Send answer (optimistic-concurrency via expectedSourceRevision + nonce; disabled unless status created/shown/relay_failed). Resolve/Archive/Claim/Release are HIDDEN (showLifecycleActions=false, HubViewer.tsx:88-89) — but Dismiss / Snooze / Mark unread are STILL shown.
- **Tab:** REAL. runtimeDecisionTab(item.id) (keyed on HUB ITEM id, not decision id) -> HubTabBody resolves the full row via resolveHubItem (lane list ∪ 24-entry openedItemCache, InboxHub.tsx:568-572) and renders RuntimeDecisionPanel. If the row can't be resolved -> TabLoadingPlaceholder. fullLink () => null.
- **Issues:** (a) Dismiss/Snooze remain available on a pending permission prompt — semantically dubious next to hidden resolve/archive. (b) Persisted tabs break on reload: useHubTabs persists tabs in localStorage but openedItemCache does not persist, so a rehydrated runtime_decision tab shows 'Preparing viewer…' until the user re-selects the row in a lane. (c) Cache eviction (>24 opens) has the same effect.

### run_failed
- **List row:** AlertCircle icon; title '<Agent> run failed/timed out'; summary = run error; priority HIGH (producer hardcodes); badges 'Run failed' + 'high'.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** DEGRADES TO GENERIC NOTIFICATION BODY — always. tabKind 'run' needs agentId from item.relatedEntityType==='agent' (hubRegistry.tsx:353-359), but buildFailedRunHubEmit (hub-source-producers.ts:188-202) sets sourceActorType/sourceActorId ('agent'/agentId) and NEVER relatedEntityType/relatedEntityId. Verified: zero relatedEntity writes in hub-source-producers.ts. So every run_failed 'Open full' lands on HubNotificationBody ('No dedicated viewer for this item yet. Open it from the list to review details.') even though RunDetailContainer (D4b) is fully built. fullLink () => null.
- **Issues:** E1 gap CONFIRMED and it is 100% of rows, not an edge case. Highest-impact type gap: a priority-high failure item whose real viewer exists but is unreachable. Fix is one line in the producer (emit relatedEntityType='agent', relatedEntityId=run.agentId) — the registry already prefers it.

### budget_alert
- **List row:** AlertCircle icon; title 'Budget approaching limit'; summary '<pct>% used (spend/budget cents)'; priority urgent at >=100% else high; ownerPool=board (Claim shows).
- **Reading pane:** Generic HubViewer, all lifecycle buttons + Claim.
- **Tab:** REAL-ish. budgetTab() -> BudgetCapsSection (company budget settings). Company-scoped, not incident-scoped — fine since sourceId IS the companyId. fullLink '/settings?tab=budget' real (settings route + budget redirect App.tsx:201).
- **Issues:** None significant; tab shows caps config rather than the triggering incident detail.

### agent_error
- **List row:** AlertCircle icon; label 'Agent error'.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** REAL when produced: threadTab via preferRelated(firstSegment) -> ThreadDetail. fullLink is the ONLY discussion-derived one done correctly — built from resolveTabId (hubRegistry.tsx:187-190) -> /discussions/:id.
- **Issues:** No live emitter: 'thread.crew_failed' notifications are never created (crew failures write a discussion ENTRY via crew-failure-card.ts, not a notification). Type is currently dead inventory; wiring is correct for when it lands.

### mention
- **List row:** Bell icon; label 'Mention'.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** REAL, dual-target: hubTabForItem refines — sourceType 'issue' + no relatedEntityId -> taskTab(taskId) -> TaskDetail; otherwise threadTab(threadId) -> ThreadDetail. firstSegment parse matches both producer composites (issues.ts:2193 `taskId:userId`; threads.ts:321 `threadId:entryId:userId`). fullLink () => null.
- **Issues:** Thread mentions DROP the entryId — threadTab supports an entryId param (hubViewerModel.ts:127) but hubTabForItem never passes it, so no scroll-to-mention. fullLink null despite both targets having real routes.

### marketplace_op
- **List row:** Sparkles icon; label 'Marketplace'. Live producers: 6 marketplace.* types (marketplace-notifications.ts + install orchestrator).
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** PLACEHOLDER. marketplaceOpTab factory exists; HubTabBody falls to TabLoadingPlaceholder (E3 deferred). resolveTabId correctly takes the SECOND composite segment (operation id). fullLink () => null.
- **Issues:** Live producer + dead-end viewer + no fullLink: an install-failed notification gives the user no path to the operation or the marketplace page.

### run_complete
- **List row:** CheckSquare icon; title '<Agent> run complete'; summary 'Run finished successfully'; priority normal.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** Same E1 degradation as run_failed -> generic HubNotificationBody (no relatedEntityType='agent' from buildCompletedRunHubEmit). fullLink () => null.
- **Issues:** (a) Same one-line producer fix as run_failed. (b) hubRegistry.tsx:231 comment 'Reserved type; when produced…' is STALE — buildTerminalRunHubEmit routes succeeded runs here and is live via heartbeat.ts:1700.

### reminder
- **List row:** Bell icon; title 'Reminder'; summary = reminder content (so the reading pane at least shows the content). Live producer: proactive.ts fires due reminders with a direct hub emit, sourceId=reminder.id.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** PLACEHOLDER. reminderTab exists; HubTabBody -> TabLoadingPlaceholder (E3 deferred). fullLink '/commander' real route but generic — does not locate the reminder.
- **Issues:** Live producer, spinner tab. Lowest of the E3 four in practice because the summary carries the whole reminder text and /commander is a plausible destination.

### extraction_failed
- **List row:** AlertCircle icon; label 'Extraction failed'.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** REAL when produced via registry with relatedEntityId: threadTab -> ThreadDetail (preferRelated(firstSegment) also survives the composite). fullLink sourceLink('/discussions') uses RAW sourceId -> /discussions/<entityId:userId:type:eventId> BROKEN for registry-format rows (latent, since onOpenFull bypasses fullLink today).
- **Issues:** (a) fullLink raw-composite bug (should mirror agent_error's resolveTabId pattern). (b) DISCREPANCY: no live emitter of 'discussion.extraction_failed' notifications found anywhere in this worktree's server/src, despite CLAUDE.md stating extraction failure notifies via the notifications table — worth verifying whether the emitter was lost in the notifications->registry migration.

### routine_outcome
- **List row:** Rocket icon; label 'Routine'.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** GENERIC NOTIFICATION BODY. tabKind is 'routine' but hubTabForItem case 'routine' returns notificationTab (no factory, hubRegistry.tsx:368-370); HubTabBody's 'routine' case would be a placeholder anyway. fullLink sourceLink('/routines') -> /routines/:routineId is a real route (App.tsx:181) using raw sourceId.
- **Issues:** Reserved — no live producer (routine.tick/sweep.tick never emit hub items). Two dead layers (missing factory + placeholder case) but zero user impact today.

### legacy_other
- **List row:** Bell icon in the LIST (HUB_REGISTRY), label 'Notification' — catch-all sink.
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** Generic HubNotificationBody by design (tabKind 'notification'). fullLink () => null.
- **Issues:** (a) The generic body shows ONLY tab.title + boilerplate — it ignores the item's summary/message even though payload.hubItemId + resolveHubItem are available in HubTabBody. (b) Icon mismatch: list row shows Bell, its tab shows Sparkles (KIND_ICON.notification = Sparkles).

### suggestion
- **List row:** Lightbulb icon; title = suggestion title; summary = evidence; badges 'Suggestion' + priority. Live producers: suggestion engine scan + cockpit (relatedEntityId=suggestion.id).
- **Reading pane:** Generic HubViewer, all lifecycle buttons. No accept/dismiss-suggestion action.
- **Tab:** PLACEHOLDER. suggestionTab exists; HubTabBody -> TabLoadingPlaceholder (E3 deferred). fullLink '/home' is a real route but does not anchor/scroll to the suggestion.
- **Issues:** The suggestions lane's PRIMARY type opens an infinite 'Preparing viewer…' spinner. Combined with proactive's generic body, the entire suggestions lane has no working dedicated viewer.

### stale_work
- **List row:** CheckSquare icon; title 'Stale task: <title>'; summary 'No recent human or crew progress.'; badges 'Stale task' + priority; Claim shows when unassigned (ownerPool=board fallback).
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** REAL. taskTab(issue.id) -> TaskDetail (active; scope-handoff URL items re-emit as hub browser tabs via handleTaskHandoff). fullLink /issues/:id real (App.tsx:176).
- **Issues:** None material. Artifact/asset handoffs inside TaskDetail are no-ops until hub artifact tabs exist (documented).

### proactive
- **List row:** Sparkles icon; label 'Proactive'. Live producer (proactive.ts checks via internal_agent.proactive registry entries).
- **Reading pane:** Generic HubViewer, all lifecycle buttons.
- **Tab:** GENERIC NOTIFICATION BODY (tabKind 'notification' — deliberate, 'inline in Home' per registry comment). Because HubNotificationBody drops the summary, the proactive insight's content is visible ONLY in the Home reading pane. fullLink '/commander' real but generic.
- **Issues:** Content-less tab: opening a proactive insight full-screen shows LESS than the preview did. Same generic-body summary gap as legacy_other.

## 2b. Cross-cutting UI

LANES/RAIL: HubRail = Home + 3 lanes (visibleLanes pref filters; Mail row reserved/disabled; collapsible, persisted aoa:hub:rail-collapsed). COUNT BUG: the only badge is on 'Waiting on you' and it shows counts.open — but the server counts() (hub-items.ts:1696) counts ALL open items company-wide (RBAC-scoped, NO lane filter), so the waiting_on_you badge is inflated by notifications+suggestions items. counts.unread is fetched but shown only on the Home overview card; no per-lane badges, no unread badge on the rail. GROUPING: groupMode pref (auto/source/scope/type/none) sent to the server (groupKey computed server-side); client buildHubListEntries (hubTypes.ts:47) never groups high/urgent items, requires >=3 per group, renders a collapsed header 'N <label> / M unread' + curationGroupSummary, expandable per group. CURATION: curationReason + curationPriorityReason render in the reading pane 'Why you are seeing this' section and as the Home 'Needs you most' sub-line; curationGroupLabel/Summary drive group headers; curatedByAgentId / curatedAt / curationRevision are fetched but never displayed (no curation provenance in UI). UNREAD: brand dot on rows (!readAt), auto mark-read on row click (optimistic broad cache patch in InboxHub markRead), 'Mark unread' in the reading pane, group unread counts. SLA: slaAt exists on HubItemListRow (hub-items.ts:47) and is rendered NOWHERE — no due chip, no overdue styling, no sort affordance; grep confirms only tests/API type reference it. DENSITY: comfortable 92px / compact 76px rows. SEARCH: '/' shortcut focuses; 250ms debounce; server-side q scoped to active lane + historyStatus; hidden on Home — no cross-lane/global hub search. HISTORY TABS: Open/Resolved/Archived buttons per lane (?status= in URL); audit timeline fetched only for a selected resolved/archived item and shown in the reading pane. HOME OVERVIEW (HubHome): Open/Unread stat cards, lane jump buttons, 'Needs you most' = items[0], Autopilot status (mode, handledToday, last 5 actions with deadline-gated Undo). BUG: InboxHub.tsx:517 sets items=[] whenever activeLane===null, and Home only renders when activeLane===null — so HubHome's topItem is ALWAYS null and 'Needs you most' permanently says 'Nothing needs attention right now.' READING PANE ARCHITECTURE: row click = select/preview only (HubShell deliberately does NOT pass onOpenItem to HubList, per the single-click-preview comment; HubList's onOpenItem prop is dead code); dedicated tabs open only via 'Open full' -> handleOpenItem -> hubTabForItem. Because HubHomeTab always supplies onOpenFull, every registry fullLink is dead code in this shell (latent broken composite links in human_input_needed/scope_proposal/extraction_failed included). TABS: 12-tab cap, per-company localStorage persistence (useHubTabs); runtime_decision + notification bodies depend on the NON-persisted 24-row openedItemCache, so persisted tabs of those kinds degrade to placeholder after reload. Reading-pane meta shows raw sourceType string and internal 'Version' number (developer-facing).

## 2c. UI gaps (ranked)

RANKED BY USER IMPACT: (1) run_failed (and run_complete) always degrade to the generic 'No dedicated viewer' body — buildFailedRunHubEmit/buildCompletedRunHubEmit (server/src/services/hub-source-producers.ts:188-218) set sourceActorType/Id instead of relatedEntityType='agent'/relatedEntityId=agentId, which hubRegistry's run case requires; RunDetailContainer is built and unreachable; run_failed is priority-high failure triage; one-line producer fix. E1 confirmed at 100% of rows, not just id-less edge cases. (2) Home 'Needs you most' is permanently empty (items=[] when activeLane===null, InboxHub.tsx:517 vs HubHome items[0]) — the Home overview centerpiece never works. (3) suggestion -> TabLoadingPlaceholder (E3 deferred, confirmed at HubTabBody.tsx:210-215): the suggestions lane's primary type opens an infinite spinner; with proactive's content-less generic body the whole suggestions lane lacks any working dedicated viewer. (4) join_request -> placeholder AND fullLink null: a founder-gated waiting_on_you item with no path to approve/deny from the hub at all. (5) marketplace_op + reminder -> placeholders (E3 confirmed) with live producers; reminder mitigated by summary carrying the content + /commander fullLink; marketplace_op has neither. (6) slaAt never rendered anywhere — a decision-queue product with no deadline/overdue affordance. (7) Rail badge shows the company-wide open count on the waiting_on_you lane only (server counts() has no lane filter) — misleading triage number; unread counts absent from the rail. (8) Generic HubNotificationBody drops the item summary (affects proactive, legacy_other, degraded run rows) — the full tab shows less than the preview. (9) Persisted runtime_decision tabs render 'Preparing viewer…' after reload (openedItemCache not persisted; also >24-open eviction) — a founder decision surface that silently breaks. (10) Latent broken fullLinks: human_input_needed / scope_proposal / extraction_failed build /discussions/<raw composite sourceId> instead of using resolveTabId (agent_error at hubRegistry.tsx:187-190 is the correct pattern); dead today because onOpenFull bypasses fullLink and 2 of 3 types have no live emitter — plus the discrepancy that NO 'discussion.extraction_failed' / 'thread.human_input_needed' / 'thread.scope_proposal_posted' / 'thread.crew_failed' emitters exist in server/src despite registry entries (extraction-failure notify vs CLAUDE.md claim worth verifying). (11) Minor: mention drops entryId (no scroll-to-mention); TabLoadingPlaceholder's spinner + 'Preparing viewer…' implies imminent loading for kinds that will never load (honesty: should be a static 'not built yet' state); runtime_decision still offers Dismiss/Snooze on pending permission prompts while resolve/archive are correctly hidden; routine_outcome's tabKind 'routine' is doubly dead (no factory + placeholder case); HubList's onOpenItem prop is dead code; list icon (Bell) vs tab icon (Sparkles) mismatch for legacy_other/notification kind; stale hubRegistry comment claiming run_complete is 'Reserved' when it is live via heartbeat.ts:1700.

## 3. Lifecycle & preferences

### Actions
SURFACES (server/src/routes/hub-items.ts): PATCH /companies/:cid/hub-items/:id/state (personal), POST /:id/action (shared lifecycle), POST /:id/undo, POST /hub-items/bulk-action, GET /:id/audit, GET list/counts/:id — all board-user only (requireBoardUserId; agents/MCP rejected 401). RBAC visibility (hub-items.ts visibilityConds): founder = all, team_lead = owned OR led-department scopeKey, team_member = owned only.

PERSONAL STATE (per-user, sparse hub_item_user_state row; hub-items.ts applyPersonalState, schema packages/shared/src/validators/hub.ts hubUserStateSchema): read/unread, snooze(until: any ISO datetime — no future check; snoozedUntil <= now treated as not snoozed)/unsnooze, dismiss/undismiss. ALLOWED FOR EVERY SEMANTIC TYPE on any visible item, no version guard, no authority gate, no audit row (only counter invalidation + hub.counts.changed personal_state_changed). Legacy mirror: read/unread/dismiss/undismiss also patch hubItems.readAt/dismissedAt when actor === legacy userId owner.

SHARED LIFECYCLE (hub-items.ts recordLifecycleAction; hubActionSchema = resolve|archive|claim|release): requires expectedVersion (version-guarded UPDATE, mismatch or non-open status → 409 with currentVersion), optional idempotencyKey (replay returns the cached audit id, no second transition/side-effect — checked both pre-tx and in-tx), audit row (hubAudit: actorType/actorId/action/authorityBasis/reason/priorState incl. claim fields/undoDeadline) + activity_log `hub_item.{action}` in the SAME transaction; live events + counter invalidation after commit.
- Authority gate: resolve/archive on founder-authority types — approval_request, join_request, agent_runtime_decision (HUB_AUTHORITY_BY_TYPE in packages/shared/src/hub.ts) — 403 for non-founders ("route or escalate"). All other 16 types: any Owner (route resolves actorIsFounder = implicit local_trusted/instance-admin OR perms.isFounder).
- claim: founder-only + item.ownerPool === "board" + not already claimed (else 409). release: claimant or founder. ownerPool "board" is set by emitters for approval_request, join_request, budget_alert, and unassigned stale_work (server/src/services/hub-source-producers.ts).
- IMPORTANT: agent_runtime_decision is NOT type-blocked server-side — the UI hides Resolve/Archive/Claim/Release for it (HubViewer.tsx `showLifecycleActions = !isRuntimeDecision`, viewer instead renders RuntimeDecisionPanel allow-once/allow-always/deny/answer), but a founder can POST resolve/archive via API, closing the hub item while the runtime decision stays unanswered (source later handled only by the timeout sweep). Same class of gap: resolving/archiving an approval_request or join_request hub item does NOT decide the source approval — no sideEffect is wired anywhere — and the emit upsert is gated to still-open rows, so the still-pending approval never reappears in ANY user's hub (shared archive by one founder hides it for the whole board; it remains decidable only at /approvals).

UNDO (undoAction): needs auditId + expectedVersion; window = audit.undoDeadline = 8s (HUB_UNDO_WINDOW_MS, hub-items.ts:52); expired → 409 "Undo window has expired"; blocked if audit.irreversibleSideEffects/relayResult non-null (inert — see gaps); restores priorState (status/version/resolvedAt/archivedAt/claimedByUserId/claimedAt) under the version guard, writes an "undo" audit (authorityBasis "undo_window") + activity_log. No authority/type gate on undo itself. Personal dismiss/snooze "undo" is client-side inverse (undismiss/unsnooze via useHubItemMutations.undoPersonalState) — no deadline. The HubShell undo bar persists indefinitely; clicking after 8s just surfaces the 409.

BULK (bulkAction, hubBulkActionSchema): 1–50 items, discriminated per action — resolve/archive/claim/release (expectedVersion required per item, else 400) route through recordLifecycleAction (full authority/claim gates apply per item); dismiss + snooze(until) route through applyPersonalState. Per-item try/catch → {succeeded, failed, skipped} summary + per-item error status; bulkId echoed/generated. UI bulk bar (HubShell.tsx) exposes only Archive/Dismiss/Snooze(fixed +24h); bulk resolve/claim/release exist API-only. Failures surface only as "N succeeded, N failed".

SYSTEM TRANSITIONS: the reconciliation sweeper (hub-items.ts reconcile, SOURCE_RECONCILERS for approval/heartbeat_run/runtime_decision/join_request/discussion/suggestion/issue) archives items whose source is terminal/gone via the same version-guarded path (actorType "system", action "reconcile_close", 409s from concurrent user actions swallowed) and heals title/summary/owner/ownerPool/scopeKey/revision drift. Runs lazily on GET list/counts per lane.

### Autopilot
POLICY (server/src/services/hub-autopilot.ts + routes/hub-autopilot.ts): per-company hub_autopilot_policies row — mode off|assist|drive (default off) + exactly one rule per ALL 19 semantic types (normalizePolicy backfills), each {action: none|resolve|archive, minTrustScore default 100, enabled default false}. GET policy/actions = any board member; PATCH/reset = founder-only (requireFounder → 403) + activity-logged.

FOUNDER-GATING: HUB_AUTOPILOT_FOUNDER_GATED_TYPES = approval_request, join_request, agent_runtime_decision (packages/shared/src/hub.ts). Triple-enforced: schema rejects an enabled non-none rule for them (hubAutopilotRuleSchema superRefine), evaluate() escalates before rule lookup, evaluateOpenItems skips them entirely. UI (HubShell settings) renders them as "Founder-gated / Escalation only" with no controls.

EVALUATION: runs lazily on every hub GET list/counts (evaluateAutopilotRefresh → evaluateOpenItems, capped at min(limit,25) open items; failures logged, never break the request). Pipeline: mode off → noop; founder-gated → escalate; rule disabled/action none → noop; unsafe action → escalate; no resolvable source agent → escalate; no trust score → escalate; trustScore.currentScore < minTrustScore → escalate; mode assist → escalate ("explains but does not auto-handle"); mode drive → auto_handle.

TRUST GATE + EFFECTIVE COVERAGE: resolveSourceAgent only uses input.sourceAgentId (never passed by evaluateOpenItems) or a heartbeat_runs lookup when sourceType === "heartbeat_run" — so in the real refresh loop ONLY run_complete / run_failed items can ever pass; every other enabled rule (mention, budget_alert, marketplace_op, suggestion, stale_work, etc.) perpetually escalates with "No trusted source-agent context", and escalations are counted but persisted/surfaced nowhere.

EXECUTION + UNDO: applyEvaluation acts via recordLifecycleAction with actorType "autonomy", actorId "autopilot", authorityBasis `autopilot_policy:{type}:{mode}`, decisionContext (semanticType/source/sourceAgentId/minTrustScore), idempotencyKey `autopilot:{cid}:{itemId}:{version}:{action}` (version-scoped dedupe), and the standard 8-second undoDeadline. 409s during the sweep are skipped. handledToday = count of autonomy resolve/archive audit rows since UTC day start. Home panel (HubHome.tsx) shows mode, handledToday, last 5 autopilot actions (listRecentActions, ≤50) with an Undo button gated client-side on undoDeadline — practically the button vanishes 8s after a background action, so autopilot undo is near-unusable (see gaps).

### Notification prefs
MODEL (packages/shared/src/notification-preferences.ts; per-user per-company notification_preferences row via server/src/services/notification-preferences.ts; routes server/src/routes/notification-preferences.ts — GET/PATCH/reset /notifications/preferences/me, any board member, own row, activity-logged): rules enumerate ALL 19 HUB_SEMANTIC_TYPES (defaults: deliveryMode "realtime", toastEnabled true), deliveryMode realtime|digest|silent per type, quietHours {enabled default false, start 18:00, end 09:00, timezone UTC — IANA-validated, start===end means always-quiet}, digest {enabled default true, cadence "daily"}.

TOASTS (ui/src/lib/hub-toast-bridge.ts + ui/src/pages/InboxHub.tsx:221-241): hub.item.changed WS event → onHubItemChanged → RBAC hydrate via GET /hub-items/:id (404s ignored) → shouldToastHubItem: suppress if item not open ("closed"), rule missing/toastEnabled false or deliveryMode silent ("silent"), deliveryMode digest ("digest"), or quiet hours active ("quiet_hours"); else toast (dedupeKey hub:{cid}:{itemId}:{version}, warn tone for high/urgent, deep-link /inbox/{lane}/{id}). CRITICAL SCOPE LIMIT: the ONLY onHubItemChanged consumer is InboxHub — hub toasts fire exclusively while the Inbox page is mounted; LiveUpdatesProvider globally handles only query invalidation (hub.item.changed / hub.counts.changed / hub.digest.changed, ui/src/context/LiveUpdatesProvider.tsx:698-720). All 19 types can toast in principle since every emit publishes "created"; in practice routine_outcome never toasts because nothing ever emits it (see gaps).

DIGEST (server/src/services/hub-items.ts queueDigestDeliveries + notification-digest.ts): on every changed emit of an OPEN item, candidates = owner + all founders + team leads leading the item's scopeKey; queue per user when their digest.enabled AND rule.deliveryMode === "digest" OR (realtime AND quiet hours currently active); "silent" queues nothing. Dedup via onConflictDoNothing; publishes hub.digest.changed. Consumption is PULL-ONLY: GET /notifications/digest/me (unacked + still-open + still-RBAC-visible items) and POST .../ack (ack-all, activity-logged); rendered in the HubShell settings panel ("Pending digest" top-5 + Acknowledge digest). The digest.cadence field ("daily") is stored and settable but NO scheduler/sender consumes it anywhere in server/src — there is no scheduled digest delivery.

UI PANEL (HubShell.tsx, gear in the lane header): hub prefs (default landing home/3 lanes, visible lanes ≥1, grouping auto/source/scope/type/none, density comfortable/compact, showAutopilotEntry — per-user via /hub-items/preferences/me) + full Autopilot section (mode + all 19 per-type rules; founder-gated rows read-only) + Notification preferences sub-panel: per-type Delivery select + Toast checkbox (disabled unless realtime — consistent with the bridge), quiet hours (enabled/start/end/timezone as free-text inputs, no validation feedback), digest enable + pending list + Acknowledge + Reset. Note the panel PATCH routes are not founder-gated for autopilot display but the server rejects non-founder autopilot PATCHes with 403 — the UI does not hide the autopilot controls from non-founders.

### History
Per-lane header segmented control Open | Resolved | Archived (HubShell.tsx:328, ?status= URL param, InboxHub getRouteHistoryStatus) re-queries the same lane with status=resolved/archived (RBAC + search + pagination all still apply; dismissed/snoozed filters are irrelevant there since the personal-state exclusions only bite visually on open sets). Selecting a resolved/archived item shows the standard reading pane PLUS an "Audit" timeline section (HubViewer.tsx:167-196) fetched from GET /hub-items/:id/audit (enabled only for resolved/archived — InboxHub auditQuery). Timeline rows show: action (resolve/archive/claim/release/undo/reconcile_close), actorId, authorityBasis (e.g. "founder", "owner", "system_reconciliation", "autopilot_policy:{type}:{mode}", "undo_window"), reason, timestamp — newest first. So after close a founder can see WHO closed it, under what authority, why, and whether the system reconciler or Autopilot did it. Limits: the audit route projection (hub-items.ts getAudit) omits actorType, autonomyLevel, decisionContext, priorState and idempotencyKey — autopilot decision context (trust threshold, source agent) is inspectable only via the Home "recent autopilot actions" list (last ≤50 autonomy actions) and the raw hubAudit table; the viewer timeline can't distinguish a user "resolve" from an autonomy "resolve" except by actorId string ("autopilot") and authorityBasis. activity_log also carries hub_item.{action}/hub_item.undo rows for the company Activity page. Deep-linked resolved items still preview via the 24-row openedItemCache. There is NO UI view for personally dismissed or snoozed items (API supports includeDismissed/includeSnoozed query params, but no control sets them), and the shared status "snoozed" exists in HUB_ITEM_STATUSES + applyGuardedTransition yet nothing ever sets it — snooze is personal-only.

### Lifecycle gaps
1) runtime_decision viewer-hidden actions are NOT server-blocked: a founder can resolve/archive an agent_runtime_decision hub item via POST /:id/action, closing it while the decision is unanswered (hub-items.ts authority gate only checks founder-vs-not, never per-type action legality). 2) Resolve/archive of approval_request / join_request hub items has zero effect on the source approval and the open-gated emit upsert never resurrects a non-open row → one founder's archive permanently hides a still-pending approval from every user's hub (decidable only at /approvals); reconcile closes but never reopens. 3) The sideEffect / irreversibleSideEffects / relayResult machinery is inert: no production caller passes sideEffect, and recordLifecycleAction discards its return value without persisting it to the audit row — so undo's "This action cannot be undone" guard (hub-items.ts:1084) can never trip. 4) Dead code: recordAndAct (hub-items.ts:1253-1347) returns at line 1282; ~60 lines after it (duplicate authority gate, phase-1/phase-2 tx) are unreachable. 5) Autopilot Drive effective coverage is only heartbeat_run-sourced types (run_complete/run_failed): evaluateOpenItems never passes sourceAgentId and resolveSourceAgent only resolves heartbeat_run sources, so every other enabled rule permanently escalates — and escalations are counted but persisted/surfaced nowhere. 6) routine_outcome has prefs rules, autopilot rules, and a lane mapping but NO production emitter (only tests); its claimed sources (routine.tick/sweep.tick) are absent from NOTIFICATION_REGISTRY, so its toast/digest/autopilot settings are dead controls. 7) Hub toasts fire only while the Inbox page is mounted (sole onHubItemChanged consumer = InboxHub.tsx) — elsewhere in the app, realtime-preference items produce no toast at all. 8) digest.cadence is stored but unused — no scheduled digest delivery exists; digest is a pull list buried in the settings panel. 9) Autopilot undo is effectively unusable: 8s undoDeadline vs background evaluation on GET refresh; the Home list renders Undo only inside that window. 10) Bulk API supports resolve/claim/release but the UI bulk bar offers only archive/dismiss/snooze; bulk authority failures collapse into an anonymous "N failed" count. 11) Claim is founder-only, so the board-pool claim/release mechanism provides no delegation to team leads who can already SEE scoped items. 12) hubUserStateSchema.snooze accepts past datetimes (silently a no-op). 13) Asymmetric undo: personal dismiss/snooze undo has no deadline; shared actions 8s. 14) The audit route omits actorType/autonomyLevel/decisionContext/priorState, limiting post-hoc inspection in the viewer. 15) Autopilot policy controls render for non-founders in HubShell (server 403s the PATCH, but the UI doesn't gate or explain). 16) Shared status "snoozed" + applyGuardedTransition("snoozed") are unreachable (nothing sets shared snooze), and dismissed/snoozed items have no UI surface despite includeDismissed/includeSnoozed API params. Files: server/src/routes/hub-items.ts, server/src/services/hub-items.ts, server/src/services/hub-autopilot.ts, server/src/services/notification-{preferences,digest}.ts, server/src/services/hub-source-producers.ts, packages/shared/src/hub.ts, packages/shared/src/notification-{preferences,registry}.ts, packages/shared/src/validators/hub.ts, ui/src/pages/InboxHub.tsx, ui/src/components/hub/{HubShell,HubViewer,HubHome,HubList,RuntimeDecisionPanel}.tsx, ui/src/lib/hub-toast-bridge.ts, ui/src/context/LiveUpdatesProvider.tsx, ui/src/hooks/useHubItemMutations.ts.

## 4. Live trigger recipes (test matrix)

### approval_request — instant-api
- **Trigger:** Instant path A (hire on QAB, requireBoardApproval=true): POST http://127.0.0.1:3399/api/companies/a088f22d-3243-4b57-b483-06e408f1f0dd/agent-hires -H 'content-type: application/json' -d '{"name":"PendingHire-QA","adapterType":"claude_local"}' -> agent created pending_approval + hire_agent approval pending + immediate hub emit (routes/agents.ts:1053-1140; NOTE plain POST /agents bypasses the gate - must use /agent-hires). Instant path B (works on QAL too): POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/approvals -d '{"type":"budget_override_required","payload":{"reason":"QA"}}' (createApprovalSchema allows hire_agent|approve_ceo_strategy|budget_override_required; crew_dispatch is blocked from external create). Any pending/revision_requested approval is also re-projected by the scan emitOpenApprovalHubItems on every GET /hub-items (waiting lane), /hub-items/counts, and /sidebar-badges. QAB already has 1 pending hire_agent approval (bc557b1f-4e0b-4c88-87f8-07282e7a0a5a) live right now.
- **Verify:** Waiting-on-you lane; title 'Review hire agent approval' / 'Review budget override required approval'; summary 'Agent: <name>' or 'Approval type: <type>'; ownerPool=board (Claim button). 'Open full' deep-links to /QAB/approvals/:id ApprovalDetail with Approve/Reject. Approving via POST /api/approvals/:id/approve flips the QAB agent pending_approval->idle and the hub item reconciles to resolved on the next hub GET (proven in prior sweep, scenario 3.1).

### approval_request (crew_dispatch variant, W1c) — minutes-agent-run
- **Trigger:** Real path (system-internal only): 1) POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/discussions -d '{"title":"Assist dispatch QA","entry":{"inputType":"write","rawContent":"We need to ship X: task one build the widget, task two write the docs"}}' (new threads default useControllerPath=true); 2) PATCH /api/companies/<QAL>/discussions/<id> -d '{"autonomyLevel":1}'; 3) POST an entry '@Adjutant please propose crew work for this' -> controller runs Adjutant (claude) -> propose_crew_work -> create_scope_draft thread action -> W1b auto-accept at Assist creates the tasks as work_mode=planning and enqueues ONE crew_dispatch approval + hub item (thread-agent-actions.ts:825-870). SQL fallback if Adjutant won't converge: INSERT INTO approvals (company_id,type,status,payload,updated_at) VALUES ('a09f6b78-a9ac-4c54-81cc-eba3a14cf677','crew_dispatch','pending','{"threadId":"<tid>","taskIds":["<planning-task-id>"]}'::jsonb,now()); then GET /hub-items -> scan projects it (run SQL via: cd C:/Users/TK/.aoa/wt/inbox-hub/packages/db && node -e with require('postgres')('postgresql://paperclip:paperclip@127.0.0.1:54329/paperclip') - no psql.exe on this machine).
- **Verify:** ONE 'Review crew dispatch approval' item in Waiting; the created tasks show the amber Planning pill on the Tasks list. Approve via /approvals/:id/approve -> tasks flip planning->standard + dispatch wakeups enqueue (heartbeat runs appear); reject -> tasks stay planning. Hub item resolves on next hub GET.

### discussion_pending — minutes-agent-run
- **Trigger:** Entries are created extractionStatus='skipped' (QA-BUG-015) so you must explicitly kick extraction: 1) POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/discussions -d '{"title":"Extraction QA","entry":{"inputType":"paste","rawContent":"Decision: we ship hub tabs Friday. Task: update the inbox docs. Task: fix the SPA fallback 404. Insight: reconcile sweeps only run on GET."}}' -> note discussion id + entry id from the response; 2) POST /api/companies/<QAL>/discussions/<did>/entries/<eid>/reprocess (or POST .../discussions/<did>/reprocess for all entries) -> flips entry to pending and runs the CLI extractor (claude, authed on this box; the 45s extraction sweeper is also a backstop). On success items insert with status=pending, pendingItemCount>0, and the hub item emits inside the same tx (extraction.ts:493).
- **Verify:** Waiting-on-you lane; title 'Review N pending items in Extraction QA'; summary 'N extracted items need review.'; owner = local-board (thread owner). Deep link opens DiscussionDetail with the pending item cards. Approve/reject all items (POST .../extracted-items approve routes) -> pendingItemCount hits 0 -> hub item reconciles closed.

### join_request — instant-api
- **Trigger:** Two calls, no auth needed in local_trusted: 1) POST http://127.0.0.1:3399/api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/invites -d '{"allowedJoinTypes":"both"}' -> response contains token; 2) POST http://127.0.0.1:3399/api/invites/<token>/accept -d '{"requestType":"agent","agentName":"JoinBot-QA","adapterType":"claude_local"}' -> join_requests row status=pending_approval + hub emit in the same tx (access.ts:2147). Human variant also works ({"requestType":"human"}) because local_implicit passes the auth guard (userId falls back to 'local-board').
- **Verify:** Waiting-on-you lane; title 'Review JoinBot-QA'; summary 'agent join request'; ownerPool=board. Sidebar join-request badge increments. Approving via the join-requests approve route (POST /api/companies/<cid>/join-requests/<id>/approve) creates the agent and the hub item reconciles closed on next GET.

### human_input_needed — hard
- **Trigger:** PRODUCER-DEAD in this build - registry entry 'thread.human_input_needed' exists (shared/notification-registry.ts:137) but zero write sites call createNotification/hub.emit with it. Pragmatic UI-test seed via SQL (node + postgres driver, connstring postgresql://paperclip:paperclip@127.0.0.1:54329/paperclip): INSERT INTO notifications (company_id,user_id,type,title,message,summary,semantic_type,source_type,source_id,source_unique_key,owner_user_id,status,delivered_at) VALUES ('a09f6b78-a9ac-4c54-81cc-eba3a14cf677','local-board','thread.human_input_needed','Crew needs your input','Which pricing tier should we target?','Which pricing tier should we target?','human_input_needed','discussion','<realDiscussionId>:qa1','a09f6b78-a9ac-4c54-81cc-eba3a14cf677:discussion:<realDiscussionId>:qa1:human_input_needed:','local-board','open',now()); use a REAL discussion id in source_id so the viewer/tab deep link resolves. Safe from reconcilers (only runtime_decision/heartbeat_run source types get swept).
- **Verify:** Waiting-on-you lane (authority=owner); item renders with the seeded title/summary; registry tab resolves to the DiscussionDetail thread tab. Resolve/archive via POST /hub-items/:id/action {action:'resolve',expectedVersion:<n>} closes it permanently (no source to re-project).

### scope_proposal — hard
- **Trigger:** PRODUCER-DEAD - 'thread.scope_proposal_posted' is registered but scope-proposal writers (scope-proposal-writer.ts, thread-post-scope-proposal tool) only create discussion entries + live events, never a hub row; proposals surface as thread cards and (at Assist) as the crew_dispatch approval instead. Seed via SQL exactly like human_input_needed but with type='thread.scope_proposal_posted', semantic_type='scope_proposal', title 'Scope proposal ready for review', source_id '<realDiscussionId>:qa-scope1' (+ matching source_unique_key 'a09f6b78-...:discussion:<realDiscussionId>:qa-scope1:scope_proposal:').
- **Verify:** Waiting-on-you lane; deep link resolves to the thread (DiscussionDetail) where a real scope_proposal entry card would live. Lifecycle actions work; nothing re-opens it.

### agent_runtime_decision — minutes-agent-run
- **Trigger:** Claude only (BUG-6: codex model turns are empty so codex raises no decisions; prior BUG-1 codex handshake also blocked this path). Preconditions already true on this instance: AOA_RUNTIME_DECISION_ROUTING=1 + Scout-Claude runtimeConfig.runtimeDecisionRoutingEnabled=true. Trigger: POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/issues -d '{"title":"QA hub runtime decision","description":"Your only action: run exactly powershell -Command \"Invoke-WebRequest -Uri https://example.com -UseBasicParsing | Out-Null\" then report done.","assigneeAgentId":"58bdbab6-4668-43a5-929e-e67678b97e0b","status":"todo"}' -> auto-dispatch gate wakes the agent; the claude PreToolUse hook raises a permission decision in ~15-60s -> hub emit (agent-runtime-decisions.ts:631). 18 archived examples already in the DB.
- **Verify:** Waiting-on-you lane, priority high; RuntimeDecisionPanel in the viewer shows tool+command+risk with Allow once / Allow always / Deny (permission kind). Answer allow_once (POST /api/companies/<cid>/agent-runtime-decisions/<id>/answer) -> decision answered->relayed, the blocked run CONTINUES, and the hub item push-closes (archives) on the terminal transition (BUG-2 fix: closeProjectedHubItem). allow_always additionally creates a 90d trust rule visible at the trust-rules API.

### run_failed — instant-api
- **Trigger:** Deterministic, no LLM: 1) POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/agents -d '{"name":"FailBot","adapterType":"process","adapterConfig":{"command":"cmd","args":["/c","exit","1"]}}' (QAL creates idle directly, no approval); 2) POST /api/companies/<QAL>/issues -d '{"title":"fail run","assigneeAgentId":"<FailBot id>","status":"todo"}' (auto-dispatch) or POST /api/agents/<id>/wakeup. Non-zero exit -> run failed in seconds -> event-driven hub emit at the terminal-status chokepoint (heartbeat.ts:1686-1714, BUG-5 fix) AND re-projected by the legacy scan on GET /sidebar-badges (latest-run-per-agent, statuses failed/timed_out).
- **Verify:** Notifications lane; title 'FailBot run failed'; summary = run error ('Process exited with code 1'); priority high; source actor = the agent. Resolving while the agent's LATEST run is still failed will re-open on the next sidebar-badges scan - dispatch a succeeding run (exit 0) afterwards to make resolve stick.

### budget_alert — instant-api
- **Trigger:** QAL has 10291 cents of real cost_events this month already, but budgetMonthlyCents=0 (scan skipped). 1) PATCH /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677 -d '{"budgetMonthlyCents":12000}' (-> 85.8% >= 80% warning, priority high) or '{"budgetMonthlyCents":10000}' (-> 102.9%, priority urgent); 2) GET /api/companies/<QAL>/sidebar-badges - the ONLY route that runs emitLegacyAlertHubItems (hub-legacy-alerts.ts:66) - simply loading any QAL page in the UI fires it. Reset budgetMonthlyCents to 0 afterwards or every page load keeps re-emitting.
- **Verify:** Notifications lane; title 'Budget approaching limit'; summary '<pct>% used (<spend>/<budget> cents)'; ownerPool=board; urgent when >=100%. sourceType company_budget, sourceId = companyId (one row per company, refreshed change-aware). Resolve re-opens while the condition persists - lowering spend%/raising budget then resolving makes it stick.

### agent_error — minutes-agent-run
- **Trigger:** Only live producer = a permission decision timing out under park/escalate policy (agent-runtime-decisions.ts:985-1000; the W5 claude bridge creates decisions with policy=escalate - confirmed on live rows). 1) Raise a claude permission decision (see agent_runtime_decision recipe) and DO NOT answer; 2) either wait out the ~5min SLA or fast-forward via SQL: UPDATE agent_runtime_decisions SET expires_at = now() - interval '1 second' WHERE status IN ('created','shown') AND company_id='a09f6b78-a9ac-4c54-81cc-eba3a14cf677'; 3) the 30s timeout sweep (index.ts RUNTIME_DECISION_TIMEOUT_SWEEP) expires it -> parked/cancelled -> emits an agent_error hub item ('escalate-visible'). 3 open agent_error items already exist live from yesterday's timeouts. (thread.crew_failed also maps here but crew failures post thread entries, not notifications - not a live producer.)
- **Verify:** Notifications lane; title 'Permission request timed out: <decision title>'; summary 'timeout policy escalated the run' (or parked); priority high; relatedEntity = the heartbeat run. Simultaneously the waiting-lane agent_runtime_decision item closes (push-close) and the blocked run is released/cancelled per policy.

### mention — instant-api
- **Trigger:** Thread path (works single-user): POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/discussions -d '{"title":"Mention QA","entry":{"inputType":"write","rawContent":"@Board please review the hub QA results"}}' (or POST an entry to an existing thread). parseMentions matches @Board; 'Board' is the local user's name in the user table -> hub emit (threads.ts:315-327), no self-mention skip on this path. CAVEATS: the ISSUE-COMMENT mention path (issues.ts:2118-2213) requires enableTeams=true (PATCH /api/companies/<cid>/enable-teams {"enabled":true} - currently false on both companies) AND skips self-mentions by user actor - so the sole local-board human can never fire it via curl; only an agent-authored comment containing @Board would. The crew notify_owner tool (notify-owner-tool.ts:73) also emits semanticType mention.
- **Verify:** Notifications lane; title 'You were mentioned in a thread'; summary '@Board in thread'; owner local-board; sourceType discussion with sourceId <threadId>:<entryId>:<userId> (per-recipient dedupe). Deep link resolves to the thread. Resolve sticks (event-driven, no reconciler).

### marketplace_op — setup-required
- **Trigger:** Catalog is live-synced on this instance (GET /api/marketplace/catalog/status -> success, source=cdn, 532 items). 1) GET /api/marketplace/catalog and pick a skill item id; 2) POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/marketplace/install -d '{"catalogItemId":"<id>"}' - as local_implicit board the request-approval gate is bypassed and the install orchestrator runs -> on terminal state it fires marketplaceNotifications.installCompleted or installFailed -> hub emit per founder (marketplace-notifications.ts:48-57; local-board holds the founder role on both companies). A broken/unfetchable source item yields install_failed; either terminal event produces the hub item. update_available/update_completed come from the crew/skill auto-updaters (background) - not directly drivable.
- **Verify:** Notifications lane; title '<item name> installed' / '<item name> install failed' (message = error text); owner local-board; sourceId encodes event type + operationId so repeat installs of the same item dedupe per event. GET /api/companies/<cid>/marketplace/install/<operationId> confirms the operation state matches.

### run_complete — instant-api
- **Trigger:** Same harness as run_failed but exit 0: POST /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/agents -d '{"name":"OkBot","adapterType":"process","adapterConfig":{"command":"cmd","args":["/c","echo","ok"]}}' then create a todo issue assigned to it (auto-dispatch). Run succeeds in seconds -> buildCompletedRunHubEmit at the heartbeat terminal chokepoint (BUG-5 is FIXED in this build - 2 open run_complete items already live). A trivial claude task also works but takes minutes; avoid codex (runs succeed but BUG-6 means no useful output).
- **Verify:** Notifications lane; title 'OkBot run complete'; summary 'Run finished successfully'; priority normal; sourceType heartbeat_run + sourceId = run id (one item per run). Also re-projected by the notifications-lane reconcile on GET /hub-items. Resolve sticks (terminal source).

### reminder — hard
- **Trigger:** PRODUCER-DEAD in this build: the only emitter is checkReminders (proactive.ts:526-579) and NOTHING invokes it - no scheduler interval, no route; there is also no POST route to create internal_agent_reminders (only GET/PATCH-cancel at internal-agent.ts:966-1023). Seed via SQL: INSERT INTO notifications (company_id,user_id,type,title,summary,message,semantic_type,source_type,source_id,source_unique_key,owner_user_id,status,delivered_at) VALUES ('a09f6b78-a9ac-4c54-81cc-eba3a14cf677','local-board','internal_agent.reminder','Reminder','Follow up on the hub QA sweep','Follow up on the hub QA sweep','reminder','internal_agent_reminder','qa-reminder-1','a09f6b78-a9ac-4c54-81cc-eba3a14cf677:internal_agent_reminder:qa-reminder-1:reminder:','local-board','open',now());
- **Verify:** Notifications lane; title 'Reminder' with the seeded content as summary; owner local-board. Registry viewer is the generic notification detail. Resolve/dismiss sticks.

### extraction_failed — hard
- **Trigger:** PRODUCER-DEAD for the hub: a failing CLI extraction marks the entry extractionStatus='failed' + sourceInfo.extractionError and publishes a TRANSIENT live event 'discussion.extraction.failed' (extraction.ts:416-442), but no code path writes a 'discussion.extraction_failed' notification/hub row (zero createNotification callers). You can still produce the underlying failure state (temporarily rename the claude CLI / point PATH away, then reprocess an entry - DiscussionDetail shows the classified failure copy) but NO hub item appears. Seed the hub row via SQL: type='discussion.extraction_failed', semantic_type='extraction_failed', source_type='discussion', source_id='<realDiscussionId>:qa-xf1' (+ matching source_unique_key), title 'Extraction failed for an entry', owner local-board.
- **Verify:** Notifications lane; deep link should resolve to the DiscussionDetail (reprocess affordance) for the real discussion id used in source_id. Note in results that the producer gap itself is a finding: extraction failures currently never reach the hub.

### routine_outcome — hard
- **Trigger:** PRODUCER-DEAD - reserved for routine.tick/sweep.tick, which exist only as crew trigger-matching event types (aoa-agents/triggers.ts), never as hub emits; the tabbed-architecture plan itself lists it 'no producer, untestable'. There is also no active notification-registry entry for it, so even createNotification cannot mint one. Seed via SQL with semantic_type='routine_outcome' directly (the hub read path keys on semantic_type, not type): type can be 'routine.tick', source_type 'routine', source_id 'qa-routine-1', owner local-board, company QAL.
- **Verify:** Notifications lane; renders via the registry's routine_outcome entry (generic detail; RoutineDetail tab deferred). Lifecycle actions stick.

### legacy_other — hard
- **Trigger:** No live producer: the only registry mapping is 'internal_agent.action_result' and nothing writes it; new createNotification calls with unknown types are REJECTED 422 by the registry (notification-registry.ts activeEntryForWrite), so the catch-all sink can only contain pre-registry legacy rows or seeds. Seed via SQL: INSERT INTO notifications (...same columns...) VALUES ('a09f6b78-a9ac-4c54-81cc-eba3a14cf677','local-board','internal_agent.action_result','Commander action result','Created task from your request','Created task from your request','legacy_other','internal_agent_action','qa-legacy-1','a09f6b78-a9ac-4c54-81cc-eba3a14cf677:internal_agent_action:qa-legacy-1:legacy_other:','local-board','open',now());
- **Verify:** Notifications lane (catch-all); generic viewer; confirms the unknown-type sink renders without a registry crash. Lifecycle actions stick.

### suggestion — instant-api
- **Trigger:** POST http://127.0.0.1:3399/api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/suggestions/detect (founder-gated; local_implicit passes) -> runs all 8 detectors synchronously and hub-emits each newly inserted suggestion in-tx (suggestions.ts:843-877). On these fresh companies memory_gap ('No identity memory exists yet') fires reliably; 3 suggestion items are already open live. Deduped by category+actionPayload, so a re-run creates nothing new until state changes. (thread.spinoff_suggested also maps here but has no live producer.)
- **Verify:** Suggestions lane; title = suggestion title, summary = evidence text. Viewer offers generic lifecycle actions plus Open link; POST /suggestions/:id/accept or /dismiss transitions the source and the hub item reconciles. Known gap from prior sweep: no category-specific action affordance in the viewer.

### stale_work — instant-api
- **Trigger:** Threshold = updatedAt older than 24h with status todo/in_progress (hub-stale-work.ts:6-7). QAL has in_progress issues from 2026-07-03T23:xx that cross the line naturally late on 07-04; to force it now, SQL-backdate (any API PATCH would bump updatedAt): UPDATE issues SET updated_at = now() - interval '25 hours' WHERE id='7917d667-6425-4243-ad5c-58cf6cb20cb5'; then GET /api/companies/a09f6b78-a9ac-4c54-81cc-eba3a14cf677/hub-items?lane=suggestions (or /hub-items/counts or /sidebar-badges) - the stale-work scan emits on those reads.
- **Verify:** Suggestions lane; title 'Stale task: Flow D: trivial completion'; summary 'No recent human or crew progress.'; owner = assignee human else board pool; source actor = assignee agent. Deep link opens the task. Resolving while the issue is still stale re-opens on the next scan - touch the issue (PATCH anything, bumping updatedAt) to make resolve stick.

### proactive — hard
- **Trigger:** PRODUCER-DEAD in this build: all six proactive checks + createNotificationIfAllowed (proactive.ts:95-122) are exported but never invoked - no interval, no route, no Commander wiring (internal_agent_config.proactiveIntervalMinutes=240 exists but nothing reads it at runtime). Seed via SQL: type='internal_agent.proactive', semantic_type='proactive', source_type='internal_agent_check', source_id='blocked_task_scan:local-board', source_unique_key 'a09f6b78-a9ac-4c54-81cc-eba3a14cf677:internal_agent_check:blocked_task_scan:local-board:proactive:', title 'Blocked tasks need attention', owner local-board, company QAL.
- **Verify:** Suggestions lane (proactive maps there, not notifications); renders with the seeded title; per-check sourceId means a re-seed of the same check dedupes onto the same row. Lifecycle actions stick.

### Untestable types

Six types cannot be generated by any live producer on this instance and need the SQL seed side-door (node + the postgres driver from C:/Users/TK/.aoa/wt/inbox-hub/packages/db against postgresql://paperclip:paperclip@127.0.0.1:54329/paperclip — no psql.exe exists; the embedded-postgres package ships only initdb/pg_ctl/postgres): (1) human_input_needed — registry entry exists, zero write sites; (2) scope_proposal — proposals are thread entries + (at Assist) a crew_dispatch approval, never their own hub item; (3) extraction_failed — CLI-extraction failure marks the entry failed and fires only a transient live event; no notification/hub row is ever written (real producer gap worth filing); (4) routine_outcome — routine.tick/sweep.tick exist only as crew trigger events, and no registry entry maps to it, so even createNotification can't mint one (plan doc itself says 'no producer, untestable'); (5) reminder — checkReminders is the sole emitter and nothing schedules or exposes it, and there's no create-reminder route (reminders GET/cancel only); (6) proactive — all proactive check functions are exported but never invoked (no interval/route/Commander wiring). Additional constraints: agent_runtime_decision and agent_error are claude-only right now (BUG-6: codex model turns are empty, so codex raises no runtime decisions; use Scout-Claude 58bdbab6-4668-43a5-929e-e67678b97e0b); the issue-comment @mention path is un-fireable by the sole local_trusted human (enableTeams=false by default AND a self-mention skip for user actors — only agent-authored comments can trigger it), so use the thread-entry mention path instead; crew_dispatch approvals cannot be created via API (CREATABLE_APPROVAL_TYPES excludes it) — only the Assist-autonomy Adjutant pipeline or a direct SQL approvals insert (which the GET-path approval scan then projects into the hub); marketplace_op update_available/update_completed variants come from background auto-updaters and aren't directly drivable, but install_completed/install_failed are (catalog is CDN-synced and healthy). Note also that SQL-seeded rows must avoid sourceType 'runtime_decision'/'heartbeat_run' (GET-path reconcilers would archive rows whose source doesn't exist).
