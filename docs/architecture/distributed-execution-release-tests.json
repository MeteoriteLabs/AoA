{
  "version": 1,
  "note": "Release-test deferral manifest for the E0 foundation checker (REL-FOUNDATION-GATE, GO-BOOK S9 unit 1). Every REL ticket NAMED by a Critical/High crossing in distributed-execution-threat-controls.json that is NOT yet written on disk (docs/replatform/epics/E11-hardening-release/tickets/<REL-ID>-design.md) MUST appear in `deferred` with a non-empty reason. When a ticket's design doc lands, its entry MUST be removed in the SAME commit — the stale-deferral guard reds otherwise. REL-004 is written and therefore is NOT declared here. An ABSENT manifest is a FAIL (fail-closed), not an empty allow-list.",
  "deferred": {
    "REL-001": {
      "reason": "End-to-end cross-tenant and secret-exposure adversarial gate across coding+browser+service. Deps BRW-006 (Sprint 7) and SVC-007 (Sprint 8) are unshipped — the hostile-tenant browser/service adversarial suite cannot be authored before those workloads exist (program-design.md #### REL-001)."
    },
    "REL-002": {
      "reason": "Multi-tenant load/fairness/SLO model with worker churn and object-store latency. Dep SVC-006 (Sprint 8) is unshipped — there is no service-reconciliation limit to model yet (program-design.md #### REL-002)."
    },
    "REL-003": {
      "reason": "Staging DR + migration rehearsal with stale-fence rejection and measured RPO/RTO. Its deps (DEP-006, MIG-002, E10-REALTIME-FOUNDATION) have landed, so it is scheduled as the NEXT Sprint-9 unit (unit 2). This deferral is TRANSITIONAL: it MUST be removed in the same commit that lands REL-003-design.md, or the stale-deferral guard reds (program-design.md #### REL-003)."
    },
    "REL-005": {
      "reason": "Private-beta campaign + evidence pack + the kill-switch write path (GO-BOOK S5 debt row). Depends on REL-001/002/003/004 — the last Sprint-9 unit; deferred until all four land (program-design.md #### REL-005)."
    }
  }
}
