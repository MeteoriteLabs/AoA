# Universe — DESIGN state evidence coverage

September 12, 2026. **Completed documentary evidence inventory; DESIGN remains open.** This report is the DESIGN portion of the approved bounded baseline-preparation batch. It records existing evidence and missing proof; it does not record a new visual acceptance, run any UAT script, authorize E1.1 implementation, or change release scope. The [preparation record](bounded-engineering-preparation.md) records the original proposal and subsequent bounded batch authorization under which this report was authored.

Codex is the evidence author. TK owns product and execution-scope acceptance. TK-managed Claude supplies independent technical review; this report does not substitute for that review or claim it occurred. No acceptance is inferred from authoring the report.

## Evidence boundary and attribution

The documentation worktree inspected was `C:/Users/TK/OneDrive/Desktop/Claude Data/Paperclip-AoA/AoA-2.5/.worktrees/universe-interface`, at HEAD `3431c66f2e3a262c9a7f1119acaa0aaecfdbdc2a`, initially clean. This is the **documentation revision inspected**, not a source-base adoption decision. The preparation proposal separately identifies source pin `183e46a9c65fc3105c7e3d125629276814df7dbb` and candidate `9200a66c42633019349de937a8b97979acac0f7a`; this DESIGN report does not qualify either runtime. The excluded `universe-e1-1` worktree was not inspected or used.

| Evidence ID | Record and exact scope |
|---|---|
| D1 | [UI decisions U01–U10](ui-review-decisions.md): agreed product/design direction, with explicit gaps and no production certification. Later corrections control earlier mock proposals. |
| D2 | [UI state review](ui-state-review.md): the main interactive mock was created and reviewed; additional states are written specifications, explicitly not visual approval or runtime proof. Routine faithful application does not require restarting TK's review. |
| D3 | [Motion and interaction](motion-and-interaction.md): planned transitions, recommended timing, reduced-motion behavior and unchecked connected acceptance requirements. |
| D4 | [User acceptance plan](user-acceptance-plan.md): mapped scripts, evidence requirements and product roles; every run and per-slice acceptance record remains `not_run`. |
| H1 | D1/U10 reports a historical standalone headless sequence: Ready → Launch design, header drag, eight resize edges, pin, maximize/restore, minimize/overview restore, close, and Needs you open/close, without script errors. It also reports transparent blob hover, three layers and breathing. Host icons were absent in the standalone screenshot. No raw execution log, reviewed asset hash, embedded-host route or complete variant manifest is supplied by that record. This is a limited reported mock check, not a newly verified run. |
| H2 | [Earlier UI history](history/ui-review-before-consolidation.md) reports chat maximize/restore, overflow and history-scroll styling checks for `universe-unified-dock.html`; it labels voice as simulated and other iterations as syntax-only or not browser-validated. It records settings, previews and motion that the rebuild cannot silently remove. This historical inventory is subordinate to D1, not an alternate specification. |

No browser, mock script, application server, runtime test, dependency installation or provider session was run for this report. No rendered artifact was inspected: the required link between the actual reviewed revision and its embedded-host entry route could not be established from these records. Reading documentation and checking file metadata is not visual inspection.

### Available asset identity, without inferred review provenance

The named files exist in `C:/Users/TK/.codex/visualizations/2026/09/09/01a08745-71f3-7bb1-8a25-bfa8c3dc5f64/`. The following SHA-256 values identify the bytes available during this inventory only. They do **not** establish that those bytes are the version TK reviewed. File timestamps are metadata, not acceptance timestamps.

| Available file | Bytes | Last-write UTC | Current SHA-256 | Reviewed revision / actual-host route |
|---|---|---|---|---|
| `universe-clean-panels.html` | 38,364 | 2026-09-11 10:12:20 | `9a206c226052261b73c677d6609999621dbc80cd6825537193099da06bdb87b3` | Not established; D1 identifies filename and directory date only. |
| `universe-evening-review.html` | 96,794 | 2026-09-10 22:22:33 | `2885e99119974e6f14fa35dc81931915bb36623a718ab281393cbccbb4a79054` | Not established; H2 identifies it as an earlier review mock only. |

The directory also contains other named iterations and screenshots. Their existence does not establish state coverage, accepted appearance or a successful interaction. No asset was copied, modified, rendered, or promoted to a signed-off baseline. **Comparison against the exact reviewed artifact is blocked pending provenance; the accepted D1 intent remains usable.**

## Classification

These categories describe different evidence, not a ladder that turns design agreement into technical proof:

- **User-reviewed baseline:** D1/D2 explicitly preserve TK's reviewed experience direction. This applies to the listed intent, not every failure state or current file byte.
- **Specified-not-shown:** a concrete design exists, but the inspected records provide no attributable rendering of this state. Rendering and its visual comparison remain pending; this does not assert that no rendering ever existed elsewhere.
- **Shown-untested:** a mock presentation is documented, but required interaction, state-chain or actual-host checks are missing. Where H1/H2 report a limited standalone check, it is stated separately rather than erased or upgraded to host proof.
- **Actual runtime proof:** an identified integrated candidate/host/fixture/run with expected-versus-actual observations. **None is established by this report.** Static source, mock labels, historical check summaries and future UAT scripts do not qualify.

## State evidence matrix

The last column names required future evidence, not executed checks. All mapped UAT runs remain `not_run`. A blocked artifact comparison and an unattempted runtime run are recorded separately.

| State | Existing design evidence and classification | Material gap / next evidence and owning slices |
|---|---|---|
| Initial workspace loading | **Specified-not-shown**, D2: stable tray silhouette, quiet opening status, reachable Settings/account, no placeholder autofocus. | Render a delayed-load fixture and confirm no invented counts or arrival headline; UAT-02/03/18, E1.1–2. |
| Panel loading | **Specified-not-shown**, D2: real title/type, shared header, bounded skeleton; move/resize/minimize/close remain available. | Observe loading during all lifecycle controls and entry paths, not just a static skeleton; UAT-02/04, E1.1/E2.4/E4. |
| Empty authorized workspace | **User-reviewed baseline** for the D1/U01 composition (expanded top tray, centered blob, compact composer together). **Specified-not-shown** for an identified zero-data fixture with absent rails/zero badges under D2. | Confirm a truly empty authorized fixture keeps recovery and explicit voice start; distinguish from restored-data failure; UAT-02/03, E1.4–5. |
| Empty overview / search no match | **Specified-not-shown**, D2: empty text only inside opened overview, no zero badge; no match differs from query failure and preserves search text. | Render both empty states and exercise dismiss/focus return and recovery; UAT-02/03, E1.4. |
| Deleted / unavailable reference | **Specified-not-shown**, D2: unavailable body/preview with safe close or return; no stale thumbnail. | Render deletion separately from denied access and confirm registry/reference behavior; UAT-02/04/10, E1.4/E2/E4. |
| Revoked access | **Specified-not-shown**, D2: replace private content immediately, clear inaccessible previews/caches, no retry as permission workaround. | Actual revoked-reference fixture across panel, preview, selected context and source links; UI observation plus canonical denial proof, UAT-02/06/10/19, E1.4/E2/E4/E8.2. |
| Workspace restore error / retry | **Specified-not-shown**, D2: restore failure near tray; Retry reads; temporary view has saving disabled and cannot overwrite stored work with empty data. | Visual fixture then acknowledged recovery/no-overwrite evidence, UAT-02/05, E1.2. |
| Renderer error / retry | **Specified-not-shown**, D2: contained view failure, working header, canonical Task status separate; retry renderer or open source. | Demonstrate view failure while Task still runs; retry must not rerun work. Include artifact derivative failure with downloadable authorized original; UAT-02/04/10/12, E1.1/E2/E4/E5.2. |
| Send or attention-answer pending / rejected / uncertain | **Specified-not-shown**, D2: destination and pending state visible, newer draft retained, rejected outcome not a success transcript; resolve only on canonical acknowledgement. | Acknowledgement-loss and rejected-write fixtures with one canonical outcome and recovered drafts; UAT-05/16, E1.3/E2.2/E2.4/E7.3. |
| Offline / unsaved / save conflict | **Specified-not-shown**, D2: device-local changes marked unsaved; existing authorized content retained; per-property conflict recovery rather than silent overwrite. | Render distinct stale, unsaved and conflict cases; two-tab/save-failure/reload proof follows in UAT-02/05/17, E1.2–3/E8.1. |
| Voice connecting / reconnecting / unavailable | **Specified-not-shown**, D2/D3: truthful adjacent status, Stop accessible, typed chat works, no fake speech/listening or silent provider fallback. D1/U08 preserves these semantics; H2 says voice is simulated. | Presentation fixtures can review copy only. Actual provider reconnect/outcome reconciliation requires separately qualified VOICE/CMD runs, UAT-07/08/09, E3/E8.1. |
| Browser connection / stream loss / reconnect | **Specified-not-shown**, D2: actual session identity, marked-stale last frame, input disabled; reconnect checks current authority without replayed clicks. | Actual local/cloud session and ownership evidence is separate from a mock frame, UAT-13/14, E6.1–3. No BROWSER/CLOUD gate closes here. |
| Narrow viewport and text scaling | **Specified-not-shown**, D2: below 640 CSS px usable width, one fitted foreground panel and overview recovery; preferred normal geometry survives. Composer/header priority and caption/attention separation are specified. Numeric sizes are recommendations. | Review 1440×900, 1024×768 and 390×844 CSS px plus 200% text scaling; measure occupied chrome and test retained geometry. Existing screenshot filenames alone prove none of this. UAT-02/04/07/18, E1.0/E1.1/E1.5/E1.6/E8.2. |
| Keyboard, touch and focus | **Specified-not-shown**, D2/D3: distinct control labels/order, focus return on close/minimize/menu dismissal, keyboard movement/resize outside editors, continuous hover/focus/tap corridors, capture cleanup. | Keyboard/touch/screen-reader runs, native zoom and input exclusion; show visible focus separately from header-only selection styling. UAT-02/03/04/07/18, E1.0/E1.1/E1.4–6/E8.2. |
| Reduced motion | **User-reviewed baseline** for reduced-motion support in D1/U09–10. **Specified-not-shown** for each D3 parity state and OS preference precedence. | Observe instant travel/hide/restore, static status/highlight, no loss of information or control, OS reduced motion overriding app full motion. Timing values are unmeasured starting points. UAT-02/17/18, E1.0/E1.6/E8.1–2. |
| Compact chat | **User-reviewed baseline**, D1/U07: aligned Add/input/voice/Send, latest caption above, continuous Expand path; compact remains input-visible. **Shown-untested** for complete state-chain behavior: D1 names chat movement/resize chains as a gap. | Verify stable bottom return after maximize, retained draft/cursor and no duplicate caption overlay; UAT-07, E1.5. No current mock inspection confirms this. |
| Expanded / maximized chat | **User-reviewed baseline**, D1/U07: scrollable history/fixed composer, independent move/resize, reachable Tuck/Maximize/Compact. **Shown-untested** against required host/state chains; H2 reports only earlier standalone maximize/restore and scroll styling checks. | Chain Compact → Expanded → Maximize → Restore → Compact across viewport changes with geometry/draft retained; UAT-05/07/18, E1.2–3/E1.5. |
| Tucked chat / independent blob and captions | **User-reviewed baseline**, D1/U02/U07: tray restores chat; tuck does not hide blob/captions or end voice; independent toggles. **Shown-untested** for complete independent recovery and interaction chains. | Run Tuck → tray restore, covered/frontmost chat behavior and selectable captions; verify below-blob fallback while blob hidden. Qualified live voice variant remains separate; UAT-03/07/08/17, E1.5/E3/E8.1. |
| Panel open / repeat entry / selection | **User-reviewed baseline**, D1/U03–06: authorized stable identity, bounded placement, one existing Task view reused, header-only selection. **Shown-untested** for required actual-host paths; H1 reports Ready → Launch design only, not all five required Task routes. | Same Task via Work, Inbox, Needs you, artifact source-task link and Open panels after reopen/zoom/overlap; UAT-04, E1.1/E1.4/E2.4. Intermittent defect remains open. |
| Panel drag / eight-edge resize / pin | **User-reviewed baseline**, D1/U04: header-only drag, eight edges/corners, content input excluded, pin preserves automatic placement but allows human manipulation. **Shown-untested** for full host matrix, with H1's limited standalone checks recorded. | Multiple zooms, overlap, long content, pin/arrange/manual move, pointer cancel/lost capture/blur and keyboard/touch alternatives; UAT-04/06/18, E1.1/E1.6/E2.4. |
| Panel minimize / restore / close / reopen | **User-reviewed baseline**, D1/U03–04: minimize retains registry/draft/geometry, restore same instance, close removes view rather than canonical work. **Shown-untested** for full host/persistence behavior, with H1's limited minimize/overview restore/close sequence. | Repeat through every route; retain draft/source and preferred geometry through renderer suspension/reload; no animation-only hide or duplicate panel. UAT-04/05/18, E1.1–4/E2.4. |
| Panel maximize / restore | **User-reviewed baseline**, D1/U04: fill usable workspace, retain reachable tray/Commander, restore prior geometry/viewport and preserve other work. **Shown-untested** for required integrated behavior; H1 reports standalone maximize/restore only. | Exact prior geometry/camera after maximize, resize viewport and restore, with other panels and inputs intact; UAT-04/05/18, E1.1–2/E1.6. |
| Requested reveal / highlight / arrangement | **User-reviewed baseline**, D1/U05: bring requested off-screen reference into readable view, transient distinct glow, pins/manual work respected, no routine camera theft. **Specified-not-shown** for a complete attributable off-screen/minimized/typing/deferred-action chain. | Actual bounded viewport context and input-safe camera action with undo; D3 rapid-action/cancellation/reduced-motion parity, UAT-04/06/18, E1.6/E2.1. |

## Material gaps and recommended disposition

1. **Reviewed-asset provenance is incomplete.** Keep D1 as the accepted intent. Recover the original review/host reference and bind it to exact asset bytes before claiming a pixel/state comparison. The hashes above make available files identifiable, but do not resolve that historical link. Do not ask TK to reapprove unchanged controls merely because the author lacks evidence.
2. **Failure and constrained-space states are not visually evidenced.** Loading, empty-data variants, revocation, errors, retry/reconnect, conflicts, narrow layout and accessibility/reduced-motion parity still need identified renderings. The material experience checks are legible simultaneous composer/captions/attention on narrow screens, immediate privacy redaction, honest retry status, and independent chat recovery. Apply D1/D2 faithfully; bring TK only a concrete departure or tradeoff with a recommendation when evidence reveals one. This inventory found no verified new visual deviation and invents no product decision.
3. **The intermittent Task-controls defect remains open.** Historical standalone success cannot outweigh reported embedded-host failures. Closure needs all five UAT-04 routes after reopen, zoom and overlap, with exact host/candidate/fixture/state-chain evidence, plus TK's explicit acceptance of reproduction/fix evidence. Existing source/component tests cannot close it.
4. **Lost settings, previews and motion remain in scope.** H2 shows why the clean rebuild is an incomplete authority. Use UAT-02 and UAT-17's full retained-setting inventory during the future candidate comparison. No restoration or field-by-field consumption has been verified here.
5. **No implementation or integration gate is closed.** Real React Flow behavior, persistence, permission enforcement, provider/browser/isolated-host reality and sustained resource measurements remain in their owning slices. The matrix supports a bounded next-batch proposal; it does not establish that E1.0 or DESIGN is complete or that E1.1 coding may begin.

Next handoff: TK-managed Claude reviews this attribution and the separate BASE results; Codex resolves technical findings against evidence. The combined packet should present the exact source-base recommendation, remaining E1.1-relevant gates and proposed next batch to TK. Preserve current acceptance as pending. Any future rendering, implementation, fixture or runtime qualification must stay within its own approved scope; this documentary report grants none of those actions.

## Verification performed for this report

Read the preparation proposal, D1–D4 and the cited historical records; verified the documentation HEAD and initial worktree status; listed existing visualization file metadata and calculated the two SHA-256 hashes above. Checked this new Markdown file for trailing whitespace and relative-link targets. No application code or mock asset was edited. Repository typecheck, tests and build were not run in this DESIGN subtask because its approved scope excludes runtime checks; baseline execution evidence belongs in the separate BASE report. All actual-host visual and UAT runs remain `not_run`.
