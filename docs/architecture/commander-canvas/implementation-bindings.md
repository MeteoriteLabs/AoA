# Universe — implementation bindings and shared-contract review

Planning source: `183e46a9c65fc3105c7e3d125629276814df7dbb` on replatform. Source inspection is not runtime qualification. All paths below are repository-relative. Proposed outputs do not exist at this base unless marked as modifications. Read with [coding plans](coding-plans/README.md), [self-review](planning-self-review.md) and [branch procedure](planning-branch-proposal.md).

## Shared integration files

| Boundary | Existing source / proposed change | Owner |
|---|---|---|
| Application entry | `ui/src/App.tsx` defines `:companyPrefix` under `Layout` and `home`/`commander` children. Add `universe` there, using the existing company route context. Proposed host `ui/src/pages/Universe.tsx` owns composition, not task execution. | E1.0/E1.1 |
| Real UI fixture | `tests/e2e/helpers/seed-company.ts` supplies `seedCompany`/cleanup. Follow `tests/e2e/commander-error-states.spec.ts` and navigate `/${company.issuePrefix}/universe`. Fake CLI fixtures prove UI wiring only. Authenticated second-identity configuration proves access separately. | E1.0, E8.2 |
| API registration | `server/src/app.ts` mounts per-company handlers directly on its api Router; `server/src/routes/index.ts` and `server/src/services/index.ts` supply exports, not the mount. Register handlers beside existing domain handlers with existing auth middleware. Shared schemas/types export through their actual package indices. | Each owning slice; E0.1 collision review |
| DB export/generation | `packages/db/src/schema/index.ts`, `packages/db/src/index.ts`; define Drizzle schema then generate migration. Inspect generated FK/index/backfill behavior. No handwritten schema SQL or lockfile-only update. | E1.2/E1.3/E4/E5/E7/E8 by table |
| Company-scoped presentation state | `server/src/routes/goals.ts`, `home-board-layout.ts`, `notification-preferences.ts` provide company access and service conventions. Layout/drafts/preferences require additional authenticated-user and destination ownership on every query. They do not join the distributed execution table set merely because they are new. | E1.2/E1.3/E8.1 |
| Distributed authority | `server/src/db/tenant-context.ts` exports `runInTenant`/`runInTenantReadOnly`; `packages/db/src/repositories/tenant/index.ts` supplies callback-local repositories. Resolve `companies.organizationId` server-side. Never use companyId as organizationId or an owner connection as the non-owner pool. | CMD/DAT/BROWSER producers |
| RLS boundary | `server/src/db/rls-tenant.ts:89` freezes eight new-path execution tables and explicitly excludes legacy company tables (CAV-005). Human intake uses selected company-scoped A; worker-publication-plan.md recommends application scope for derivative/index/receipt and tool grants; a separately proposed organization-owned follow-up ledger remains a reviewed extension, not an existing automatic registry. Before its migration: decide its data class with the tenant owner, bind exact generator/grant/policy changes and test real non-owner isolation. No blanket retrofit or unscoped tenant accessor. | BASE plus ledger owner, unassigned |
| Existing Commander context | `contextAssemblyService(db).assembleContext(companyId,options)` in `server/src/services/internal-agent/context-assembly.ts`; agent-loop imports it. Add bounded resolved context as an optional option, preserving current memory/summary/budget fields. | E2.1 |
| Outcome observation | Existing `conversationService(db)` methods read submission and explicitly linked reply. E2.2 supplies GET observation, fingerprint and fenced run linkage. POST chat can resume work and is never an observation substitute. | E2.2 |
| Durable events | Existing `server/src/services/live-events.ts`, `server/src/realtime/live-events-ws.ts` and `ui/src/context/LiveUpdatesProvider.tsx`. Append is best effort; snapshot/canonical records establish truth. No second event bus or assumed transactional outbox. | E2.3/E7.2 |

## Contract compatibility decisions

**Migration-policy provenance:** Pinned `CLAUDE.md` Rule 1, `AGENTS.md` §6 and Decision #122 (including September 1 amendment) explicitly permit idempotent cluster/security DDL that the pinned generator cannot emit in delta-free `--custom` migrations, under that decision's conditions. Tables/columns/indexes/FKs remain generated. The older AGENTS text supplied in this session lists only guards/data backfills. This planning pass authors no migration and does not silently choose broader execution permission. Before affected implementation, reconcile the applicable instruction baseline and bind Decision #122's exact reviewed security steps, role/grant allowlists and boot certificates. Generator inability is already documented; it is not evidence that replatform lacks a sanctioned security path. New table topology/privileges still need explicit review. No runtime bootstrap or unreviewed grant is a substitute.

These are proposed implementation signatures, not shipped exports. They take precedence over illustrative snippets with shorter local aliases; keep all producers/consumers synchronized in the owning increment.

| Shared value | Producer → consumer | Required mapping and regression |
|---|---|---|
| Panel identity | E1.1 → all renderers | `{companyId,kind,id,version?}` is presentation identity. The reference controller's task/artifact/browser union must expand explicitly for accepted panel adapters (intake, blocks, executable tool, attention), with strict schemas and a canonical owner for each. Renderer type alone is not resource authorization. One reference maps to one registry instance across all five task entry routes. |
| Domain context reference | E2.1 → E3/E4/E5 | `UniverseReference={kind:'task'|'artifact'|'message',id,versionId?}`. Adapter maps panel artifact `version` to `versionId`; rejects missing immutable version for generation/refinement. Browser/presentation-only refs cannot be cast to task IDs. |
| Block reference | E5.1 → E2.1 | `artifact_version {artifactId,versionId}` maps explicitly to `artifact {id,versionId}`; task `{taskId}` maps to `{kind:'task',id}`. Live task `updatedAt` is an observation, not a fabricated mutation revision. |
| Canonical outcome | E2.2 → E3/E4/E5 | State is unknown/accepted/running/succeeded/failed. Submission, message and run IDs are not artifact identities. Persisted linked reply `outputRefs` or a qualified fenced output commit supplies exact artifact/version; missing identity blocks publication, never substitutes latest. |
| Voice identity | E3.1 → E3.2/E3.3 | Company/user/conversation/session/generation + utterance/revision fence transport. Map finalized utterance to one persisted submission ID. Partial text and reconnect never submit by themselves. |
| Browser identity | E6.0 → E6.1–E6.4 | Descriptor carries lifecycle revision/controller epoch; input also carries page/frame revision. Validate at execution after queueing. Actual adapter/relay/profile wire bindings remain qualification outputs. |
| Preferences | E8.1 → E1/E3/E6/E7 | Import `UniversePreferences`, snapshot, defaults, resolver and `useUniversePreferences`. Do not introduce alternate enum spelling or local defaults. `/preferences/me` scope is company + authenticated user; absent record revision zero. Build foundation /1 before consumers, finish UI /2 with them. |
| Geometry versus overview order | E1.2 → E1.4 | E1.1 declares Panel.openedOrdinal and State.nextOpenedOrdinal; E1.2 allocates persisted ordinals under its layout lock and returns them with the counter and per-open operationIndex in LayoutAck/receipts. E1.1 AuthorizedLayoutSnapshot/hydrateLayout retain persisted ordinals; reconcileOpeningAck matches the journaled operationId/index/key/local generation, never current key alone. The local reducer uses optimistic ordinals reconciled against that acknowledgement. Persist `openedOrdinal` independently of z-order. Focus changes z-order; overview stays stable. Close/reopen creates new ordinal; minimize/restore keeps it. |

## Block and executable-tool checkpoint producer

E1.2 defines a separate proposed `universe_panel_checkpoints` table in `packages/db/src/schema/universe.ts`: UUID id, companyId/userId/conversationId, panelKey, sourceVersionId, schemaVersion, revision (zero on absence), bounded JSON data, timestamps; unique owner/company/conversation/panelKey/sourceVersionId. This is personal presentation state under the same owner checks as layout. It is not a task table, job queue or grant store. E5.1 supplies the reviewed `BlockCheckpoint` payload schema; E5.2 supplies a registered, inert tool checkpoint schema. Neither renderer registers arbitrary executable validators.

E1.2's `universeStateService` adds `getCheckpoint(scope,key,sourceVersionId)` and `saveCheckpoint(scope,key,input)`. Proposed GET/PATCH `/api/companies/:companyId/universe/conversations/:conversationId/panels/:panelKey/checkpoint`; GET query identifies sourceVersionId; PATCH body `{sourceVersionId,schemaVersion,expectedRevision,data}`. Authenticate conversation owner and exact accessible source version, decode panelKey once as a bounded opaque identifier, validate registered schema and serialized size ≤32 KiB. Under row lock compare expectedRevision and update or return 409; absent row expects zero. No unscoped raw checkpoint endpoint.

On close, retain checkpoint and unsent form inputs according to draft policy; remove only the live panel. New source version gets separate state. Minimize, navigation and reload cannot replay an action. E1.3 stores pending action/submission metadata separately from editable checkpoint content. Consumer sees `{revision,data}` and retains local dirty variant on conflict. Whole-layout patches do not carry checkpoint JSON; the 64 KiB layout patch bound remains independent.

Implementation tests: same-source two-tab conflict preserves both variants; different instances/versions independent; reload restores input without dispatch; same-company other-user denied; foreign source/version denied; 32 KiB boundary and unknown schema rejected; close/reopen retains state; reset Appearance does not delete it. Add `server/src/__tests__/universe-checkpoints.integration.test.ts` and E5 connected host test. The migrated DB fixture is E1.2's actual PostgreSQL setup. This producer is specified concretely in E1.2/1 and its slice outline after H1 reconciliation; it has not been implemented.

## Draft destination adapters

E1.3 owns all five strict draft kinds: commander/task text, question, runtime_decision and approval. Its [structured answer contract](coding-plans/e1-3.md#structured-answer-contract) binds payload fields, size limits, source versions, pending-attempt identity and canonical acknowledgement before E7.3 consumes them. The text-only Draft/afterAcknowledgement example does not implement answer forms. These are saved answer fields and pending attempt IDs, not new answer APIs. Each source adapter supplies its existing validation/maximum payload and submission/expected-version semantics. WorkQuestionPanel, RuntimeDecisionPanel and canonical approval UI continue to own governed submission. Unauthorized/expired/resolved destinations retain or purge sensitive local draft under existing permission policy but never replay an answer. Test same-company different-user denial, stale expected version, unmount/reload, duplicate reply and revoked access at submit.

Checkpoint content and a text/answer draft are not stored twice. E5 field data lives in the checkpoint; its pending `clientActionId`/`clientSubmissionId` mapping is separate recovery metadata. E1.3 does not treat generated form data as text and clear it after an unrelated Commander reply.

## Source and output inventory

The generated inventory below records investigation anchors from all 31 slice outlines checked against the pinned Git object. Additional exact modifications and outputs are specified in each linked coding addendum. Of 82 outline investigation-anchor entries, 70 exist at the pinned base and 12 refer to Universe planning documents newly bundled in this packet; no runtime source anchor is missing. An existing anchor means only that the file exists, not that the proposed feature is implemented. A proposed output absent at the base is a collision check, not an instruction to create it during planning.

| Slice | Existing source or bundled planning input | Proposed outputs absent at base |
|---|---|---|
| [E0.1](coding-plans/e0-1.md) | `docs/architecture/commander-canvas/readiness-review.md` **Bundled planning input; absent from base**<br>`docs/replatform/epics/E10-desktop-migration-realtime/findings.md` | `docs/architecture/commander-canvas/implementation-bindings.md` |
| [E1.0](coding-plans/e1-0.md) | `docs/architecture/commander-canvas/ui-review-decisions.md` **Bundled planning input; absent from base**<br>`docs/architecture/commander-canvas/motion-and-interaction.md` **Bundled planning input; absent from base** | `docs/architecture/commander-canvas/ui-state-review.md` |
| [E1.1](coding-plans/e1-1.md) | `ui/src/components/hub/HubShell.tsx`<br>`ui/src/components/commander/CommanderTaskFocusPane.tsx`<br>`ui/package.json` | `ui/src/components/universe/UniverseWorkspace.tsx`<br>`ui/src/components/universe/PanelFrame.tsx`<br>`ui/src/components/universe/panel-state.ts`<br>`ui/src/components/universe/panel-history.ts`<br>`ui/src/components/universe/__tests__/panel-history.test.ts`<br>`ui/src/components/universe/__tests__/panel-state.test.ts`<br>`tests/e2e/universe-panels.spec.ts` |
| [E1.2](coding-plans/e1-2.md) | `ui/src/hooks/useHomeBoardLayout.ts`<br>`server/src/routes/goals.ts`<br>`server/src/services/goals.ts`<br>`packages/db/src/schema/internal_agent.ts` | `packages/db/src/schema/universe.ts`<br>`packages/shared/src/validators/universe.ts`<br>`server/src/services/universe-state.ts`<br>`server/src/routes/universe.ts`<br>`ui/src/api/universe.ts`<br>`ui/src/components/universe/useUniverseState.ts`<br>`server/src/__tests__/universe-state.integration.test.ts`<br>`server/src/__tests__/universe-checkpoints.integration.test.ts` |
| [E1.3](coding-plans/e1-3.md) | `ui/src/components/commander/CommanderInput.tsx`<br>`ui/src/api/internal-agent.ts`<br>`packages/shared/src/validators/internal-agent.ts` | `server/src/services/universe-drafts.ts`<br>`ui/src/components/universe/useUniverseDraft.ts`<br>`server/src/__tests__/universe-drafts.integration.test.ts`<br>`ui/src/components/universe/__tests__/draft-recovery.test.ts` |
| [E1.4](coding-plans/e1-4.md) | `ui/src/api/internal-agent.ts`<br>`ui/src/api/issues.ts`<br>`ui/src/api/artifacts.ts`<br>`ui/src/api/hub-items.ts` | `ui/src/components/universe/UniverseTray.tsx`<br>`ui/src/components/universe/OpenPanelsOverview.tsx`<br>`ui/src/components/universe/PanelPreviewRail.tsx`<br>`tests/e2e/universe-navigation.spec.ts` |
| [E1.5](coding-plans/e1-5.md) | `ui/src/components/commander/CommanderInput.tsx`<br>`ui/src/components/InternalAgentPanel.tsx`<br>`ui/src/api/internal-agent.ts` | `ui/src/components/universe/CommanderSurface.tsx`<br>`ui/src/components/universe/CommanderBlob.tsx`<br>`ui/src/components/universe/CommanderCaptions.tsx`<br>`tests/e2e/universe-commander-surfaces.spec.ts` |
| [E1.6](coding-plans/e1-6.md) | `docs/architecture/commander-canvas/motion-and-interaction.md` **Bundled planning input; absent from base** | `ui/src/components/universe/viewport-controller.ts`<br>`ui/src/components/universe/motion-tokens.ts`<br>`ui/src/components/universe/__tests__/viewport-controller.test.ts`<br>`tests/e2e/universe-motion.spec.ts` |
| [E2.1](coding-plans/e2-1.md) | `server/src/services/internal-agent/agent-loop.ts`<br>`packages/shared/src/validators/internal-agent.ts`<br>`ui/src/api/internal-agent.ts` | `packages/shared/src/types/universe-context.ts`<br>`packages/shared/src/validators/universe-context.ts`<br>`server/src/services/internal-agent/universe-context.ts`<br>`server/src/__tests__/universe-context.test.ts` |
| [E2.2](coding-plans/e2-2.md) | `server/src/routes/internal-agent.ts`<br>`server/src/services/internal-agent/agent-loop.ts`<br>`packages/db/src/schema/internal_agent.ts`<br>`ui/src/api/internal-agent.ts` | `server/src/services/internal-agent/request-outcome.ts`<br>`server/src/__tests__/universe-request-outcome.integration.test.ts` |
| [E2.3](coding-plans/e2-3.md) | `server/src/services/live-events.ts`<br>`server/src/services/internal-agent/proactive.ts`<br>`ui/src/api/hub-items.ts` | `server/src/services/universe-catchup.ts`<br>`ui/src/components/universe/useUniverseReconciliation.ts`<br>`server/src/__tests__/universe-catchup.test.ts` |
| [E2.4](coding-plans/e2-4.md) | `ui/src/components/commander/CommanderTaskFocusPane.tsx`<br>`ui/src/components/TaskDetail.tsx`<br>`ui/src/api/issues.ts` | `ui/src/components/universe/TaskConversationPanel.tsx`<br>`tests/e2e/universe-task-routes.spec.ts` |
| [E3.1](coding-plans/e3-1.md) | `ui/src/components/settings/sections/CommanderSection.tsx`<br>`server/src/services/secrets.ts`<br>`server/src/routes/internal-agent.ts` | `server/src/services/universe-voice.ts`<br>`server/src/services/voice/openai.ts`<br>`ui/src/components/universe/useVoiceSession.ts`<br>`server/src/__tests__/universe-voice.test.ts` |
| [E3.2](coding-plans/e3-2.md) | `ui/src/api/internal-agent.ts`<br>`server/src/services/internal-agent/agent-loop.ts` | `ui/src/components/universe/voice-state.ts`<br>`server/src/services/voice/transcript-reconciliation.ts`<br>`tests/e2e/universe-voice-lifecycle.spec.ts` |
| [E3.3](coding-plans/e3-3.md) | `server/src/services/secrets.ts` | `server/src/services/voice/gemini.ts`<br>`server/src/services/voice/elevenlabs.ts`<br>`server/src/__tests__/voice-provider-conformance.test.ts` |
| [E3.4](coding-plans/e3-4.md) | `docs/architecture/commander-canvas/voice-provider-research.md` **Bundled planning input; absent from base** | `docs/architecture/commander-canvas/speech-modes-qualification.md` |
| [E4.1](coding-plans/e4-1.md) | `server/src/routes/assets.ts`<br>`server/src/services/assets.ts`<br>`packages/db/src/schema/assets.ts`<br>`ui/src/api/assets.ts` | `server/src/services/universe-intake.ts`<br>`server/src/__tests__/universe-intake.integration.test.ts`<br>`ui/src/components/universe/ArtifactIntakePanel.tsx` |
| [E4.2](coding-plans/e4-2.md) | `docs/architecture/commander-canvas/format-matrix.md` **Bundled planning input; absent from base**<br>`server/src/routes/assets.ts`<br>`server/src/services/asset-serving-safety.ts` | `server/src/services/universe-derivatives.ts`<br>`server/src/__tests__/universe-format-matrix.test.ts`<br>`ui/src/components/universe/ArtifactPanel.tsx`<br>`packages/db/src/schema/universe_asset_index.ts`<br>`server/src/services/universe-asset-index.ts`<br>`server/src/__tests__/universe-asset-index.integration.test.ts` |
| [E4.3](coding-plans/e4-3.md) | `server/src/services/artifacts.ts`<br>`server/src/routes/artifacts.ts`<br>`packages/db/src/schema/artifacts.ts`<br>`ui/src/api/artifacts.ts` | `ui/src/components/universe/ArtifactVersions.tsx`<br>`server/src/__tests__/universe-artifact-revisions.test.ts`<br>`docs/architecture/commander-canvas/generation-qualification.md` |
| [E5.1](coding-plans/e5-1.md) | `docs/architecture/commander-canvas/panel-contract.md` **Bundled planning input; absent from base** | `packages/shared/src/validators/universe-blocks.ts`<br>`ui/src/components/universe/blocks/registry.tsx`<br>`ui/src/components/universe/blocks/__tests__/registry.test.tsx` |
| [E5.2](coding-plans/e5-2.md) | `docs/architecture/commander-canvas/panel-contract.md` **Bundled planning input; absent from base** | `ui/src/components/universe/tools/IsolatedToolHost.tsx`<br>`server/src/services/universe-tool-capabilities.ts`<br>`tests/e2e/universe-tool-isolation.spec.ts` |
| [E6.0](coding-plans/e6-0.md) | `packages/browser-runtime/src/launch-guard.ts`<br>`docs/replatform/epics/E8-browser-automation/findings.md` | `docs/architecture/commander-canvas/browser-qualification.md`<br>`tests/e2e/universe-browser-qualification.spec.ts` |
| [E6.1](coding-plans/e6-1.md) | `packages/browser-runtime/src/launch-guard.ts`<br>`docs/replatform/epics/E8-browser-automation/findings.md` | `server/src/services/universe-browser-sessions.ts`<br>`server/src/__tests__/universe-browser-authority.test.ts` |
| [E6.2](coding-plans/e6-2.md) | `docs/architecture/commander-canvas/integration-contracts.md` **Bundled planning input; absent from base** | `ui/src/components/universe/BrowserPanel.tsx`<br>`server/src/services/universe-browser-stream.ts`<br>`tests/e2e/universe-cloud-browser.spec.ts` |
| [E6.3](coding-plans/e6-3.md) | `docs/architecture/commander-canvas/integration-contracts.md` **Bundled planning input; absent from base** | `ui/src/components/universe/LocalBrowserConnection.tsx`<br>`tests/e2e/universe-local-browser.spec.ts` |
| [E6.4](coding-plans/e6-4.md) | `server/src/services/assets.ts`<br>`server/src/services/secrets.ts`<br>`docs/replatform/epics/E8-browser-automation/findings.md` | `server/src/services/universe-browser-lifecycle.ts`<br>`server/src/__tests__/universe-browser-lifecycle.test.ts`<br>`tests/e2e/universe-browser-files.spec.ts` |
| [E7.1](coding-plans/e7-1.md) | `server/src/services/routines.ts`<br>`server/src/routes/routines.ts`<br>`ui/src/api/routines.ts`<br>`packages/db/src/schema/routines.ts` | `server/src/services/internal-agent/tools/universe-routine-actions.ts`<br>`server/src/__tests__/universe-routine-actions.test.ts` |
| [E7.2](coding-plans/e7-2.md) | `server/src/services/routines.ts`<br>`packages/db/src/schema/routines.ts`<br>`server/src/services/live-events.ts` | `server/src/services/universe-followup-intent.ts`<br>`server/src/__tests__/universe-followup-recovery.integration.test.ts` |
| [E7.3](coding-plans/e7-3.md) | `server/src/services/internal-agent/proactive.ts`<br>`server/src/services/hub-items.ts`<br>`server/src/services/notification-preferences.ts`<br>`ui/src/lib/hub-toast-bridge.ts` | `server/src/services/universe-attention-projection.ts`<br>`ui/src/components/universe/AttentionRail.tsx`<br>`ui/src/components/universe/AttentionQuestion.tsx`<br>`server/src/__tests__/universe-attention-migration.test.ts` |
| [E8.1](coding-plans/e8-1.md) | `ui/src/pages/SettingsPage.tsx`<br>`ui/src/components/settings/SettingsLayout.tsx`<br>`ui/src/context/ThemeContext.tsx`<br>`server/src/services/secrets.ts` | `ui/src/components/settings/sections/UniverseSection.tsx`<br>`packages/shared/src/validators/universe-preferences.ts`<br>`server/src/services/universe-preferences.ts`<br>`server/src/__tests__/universe-preferences.test.ts` |
| [E8.2](coding-plans/e8-2.md) | `package.json`<br>`docs/architecture/commander-canvas/grooming-review.md` **Bundled planning input; absent from base**<br>`docs/architecture/commander-canvas/motion-and-interaction.md` **Bundled planning input; absent from base** | `tests/e2e/universe-release.spec.ts`<br>`docs/architecture/commander-canvas/release-evidence.md` |

## Reconciled producer order

- E1.1 produces registry, measured viewport and optimistic order; E1.2 produces authoritative layout ordinals/receipts and checkpoint storage. AuthorizedLayoutSnapshot includes viewport plus selected/maximized/order; E1.1 hydrates registry and camera together, and onStateChange/onViewportCommit map to E1.2 lifecycle/order/presentation/viewport operations under one revision/receipt boundary. See [complete layout write/read contract](coding-plans/e1-2.md#complete-layout-writeread-contract).
- E2.1/1 defines shared context types/resolution after BASE; client capture consumes E1.1 data and E1.3/1's draft snapshot contract. E1.6 consumes the projection for navigation. E1.6 is never an E2.1 prerequisite.
- E1.3/1 publishes all five draft payloads; E1.3/2 recovery integrates after E2.2/1. Consumers may build against the contract early but cannot claim complete recovery.
- E5.1 local renderer needs E1.1/E4 source contracts; durable state needs E1.2; contextual actions need E1.3/E2.1/E2.2. Local calculator success does not close the V1 slice.
- E8.1/1 publishes personal preferences and their reduce-only policy inputs; E7.3/1 owns the source-authorized attention service, GET route, validated client, projection tests and shared delivery policy. E7.3/2 owns the consuming React surfaces/source-answer forms; it does not produce the GET route. E2.3 consumes that projection; E7.3/1 never calls the Universe catch-up service. E8.1/2 UI and E7.3 delivery tests complete together without making the initial preference contract cyclic.

## Distribution binding clarification

API mounts belong in `server/src/app.ts`, not the route barrel. Existing `.github/workflows/pr.yml` validates changes; `.github/workflows/docker.yml` and `.github/workflows/release.yml` are additional app-image/npm publication seams to inspect at BASE. Their existence does not publish the proposed converter/host/worker assets. Each owning slice still needs an exact build artifact, workflow owner, digest/version/compatibility and rollback record before qualifying its distribution.

## Artifact and durable-follow-up qualification decisions

E4.1 uses selected A: company/actor/destination-scoped human intake with canonical asset, ledger pointer and audit in one authorized application transaction. See [E4.1](coding-plans/e4-1.md#constraints-and-source-evidence). The [worker-publication proposal](worker-publication-plan.md) now recommends application-scoped derivative/index/receipt and E5 grant/action-link records, with existing non-owner distributed ledgers and separate canonical publication. This is pending independent/security acceptance; it does not infer that human intake A approved other stores. Current aoa_app SELECT on legacy assets/artifacts does not support a normal tenant-transaction INSERT. Human intake never supplies an owner fallback for distributed execution. Any non-owner grants or cross-role intent/receipt protocol require an exact reviewed authority trace before coding.

E4.2/2 owns proposed `packages/db/src/schema/universe_asset_index.ts`, `server/src/services/universe-asset-index.ts:searchAuthorizedAssetChunks` and `server/src/__tests__/universe-asset-index.integration.test.ts`. E4.1 provides original/destination/status contracts first; E4.2 implements authorized indexing; E4.1/2's indexing acceptance then integrates it. Neither producer waits for its own consumer acceptance. All retrieval, snippets/counts, context/RAG and caches preserve live source destination access; private asset indexing never implicitly writes Memory. Vector/embedding/storage-class and distribution qualification remain open.

Media generation remains V1. Read the amended #104 deployment/credential boundary, then qualify each proposed media execution/provider path or obtain its specific amendment. The older blanket keyless wording does not itself mandate a scope cut, and the cloud extraction amendment does not itself authorize direct media APIs.

E7.2's exhaustive terminal/reopen writer map and per-writer-marker versus canonical-outbox decision is an explicit architecture gate. The source count is unverified, not assumed to be 18. Routine-run qualification may land first, but issue-source follow-ups remain required for complete V1. E7.1 exports the real private cron helper deliberately and qualifies a bounded in-flight guard on the configured scheduler interval.

E8.1's soundEffects/spokenAnnouncements are reduce-only inputs to the shared Inbox/Commander policy. E7.3 owns the invariant test and server claim/attempt enforcement; E8.1 only stores inputs and binds controls. No third delivery authority is created.

## Worker publication producer binding

E4.2/2 owns proposed packages/db/src/schema/universe_publications.ts, packages/shared/src/validators/universe-publication.ts, server/src/services/universe-publication.ts and server/src/__tests__/universe-publication.integration.test.ts. E4.3/1 owns canonical artifact/version integration and server/src/__tests__/universe-artifact-publication.integration.test.ts. These supplement the representative inventory above. The detailed addendum defines prepare/observe/reconcile/cancel/get responsibilities, states, copy/receipt recovery and source qualifications. E2.2 consumes exact published result references; no reverse dependency requires the common producer to wait for version UI. No worker ordinal or wire artifactId is an application asset/version ID.

### Output identity and revocation producer obligations

[The M1/M3 follow-up](worker-publication-identity-revocation.md) proposes E2.2-owned packages/db/src/schema/universe_output_bindings.ts, server/src/services/internal-agent/output-binding.ts and server/src/__tests__/universe-output-binding.integration.test.ts. E4.1/E4.2 security work owns server/src/services/universe-publication-auth.ts and server/src/__tests__/universe-publication-revocation.integration.test.ts, plus qualified modifications to every actual permission writer. These supplement the representative output inventory. Initial writer inventory is not exhaustive or accepted; enablement requires complete coverage and supported credential-mode evidence. Lock-order and exact action/slot definitions are authoritative proposals in that addendum, not already-shipped APIs.

## D3 voice/media producers

[Policy proposal](voice-media-policy.md) supplements the representative inventory with E8.1/1-owned packages/shared/src/validators/voice-media-policy.ts, server/src/services/voice-media-credentials.ts and server/src/__tests__/voice-media-credentials.test.ts; E3.1-owned packages/db/src/schema/universe_voice_sessions.ts, server/src/services/voice/session-registry.ts, server/src/services/voice/budget-lifecycle.ts and server/src/__tests__/voice-session-authority.integration.test.ts. E8.1/1 publishes policy/credential producer before E3 and E8.1/2's completed UI. Existing Secrets binding exemptions and process-local budget hooks are insufficient by themselves. These files are proposed, not source evidence; actual schema/role/stop/admission qualification and policy approval remain open.
