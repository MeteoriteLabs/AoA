Universe — review: worker-publication M1/M2/M3 reconciliation
Reviewed SHA: a033e17c0a19d5f44de2be3837fd81b3ca16398c ("reconcile worker publication review contracts"), local == origin/codex/universe-interface. Compared against baseline: 0d8876cdf333ba433c792127e8b4b264fb3a5d22. Pinned source base: 183e46a9c65fc3105c7e3d125629276814df7dbb (unchanged, still ancestor). Change: one docs-only commit, 13 files, +191/−11; 0 non-docs; 2 new files (worker-publication-identity-revocation.md = M1/M3 contract; worker-publication-review-report.md = my prior report preserved verbatim). Scope: the M1/M2/M3/LOW-1/LOW-2 reconciliation on the distributed worker-publication path. Human intake A untouched (E4.1 files unchanged, verified). Planning text only; no runtime/qualification executed.

Verdict
The reconciliation resolves all five prior findings coherently and source-accurately. M2 is essentially closed as a design contract. M1 and M3 are now concrete, decidable proposed contracts whose runtime acceptance remains correctly contingent on their own named gates. LOW-1 is correctly overturned — my prior finding was wrong (the composite job FK does cascade). No Critical/High, no undisclosed inconsistency, no over-claim, and no grant/protocol/scope expansion. The proposal is internally consistent and decidable enough for security review; it does not claim runtime readiness.

Answer to "are the remaining writer/CMD/credential qualifications sufficient gates?" — Yes, as gates: they are specified fail-closed (an uncovered writer, credential mode, or unbound CMD adapter leaves that path blocked/unavailable). What is not yet proven — and the plan does not claim — is that the gates are satisfiable with complete coverage; that is the qualification work, and its load-bearing residual is exhaustive permission-writer discovery (M3-residual).

Source-claim verification (accurate at base)
Claim	Verified	Result
LOW-1 pushback: composite job FK .onDelete("cascade")	job_artifacts.ts:126-131 — orgJobFk: foreignKey({columns:[organizationId,jobId], foreignColumns:[jobs.organizationId,jobs.id]}).onDelete("cascade"); comment "this composite is the SOLE job FK, ON DELETE cascade (E2-D09)"; :21 "NO single-column FK to jobs.id"	CONFIRMED — codex right; my prior LOW-1 was wrong
M1: finishTurn only updates turnStatus; not an acceptance op	conversation.ts:232-235 finishTurn(...).set({turnStatus:status}); claimTurn:186-189 issues turn_claim_token	CONFIRMED (a new acceptance method is genuinely required)
M3 seed inventory (11 access-authority files) exist	conversation-authz, permissions, access, team, org-hierarchy, board-claim, threads, discussions, companies, authz, middleware/auth — all present	CONFIRMED (accurate seed; plan does not claim exhaustive)
M3: conversation-authz preserves founder/bearer distinction	conversation-authz.ts:14-16,24,31,34 "MCP & agent BEARER TOKENS are always team_member … a founder-created MCP key … must NOT grant founder reach"	CONFIRMED (source-policy preservation is real, not aspirational)
M3: companies.ts performs teardown / membership+grant writes	companies.ts company create/teardown, founder-membership, org handling (lines 80-98, 379-381)	CONFIRMED (accurate)
Requirement-by-requirement assessment
M1 — exact action tuple / persisted slot / claim-fenced binding / cancellation & first-artifact idempotency / E2-E4 acyclic — RESOLVED (contract), CMD-gated (runtime)
Exact action tuple: PublicationAction tagged tuple — ["universe-action-v1", companyId, "commander", conversationId, userMessageId] or […, "processing", derivativeId, processingAttempt]; canonicalActionIdentity = SHA-256(UTF-8 JSON(tuple)); UUIDs normalized lowercase; attempt a positive safe int; "Never compute the action digest from mutable processor output." Concrete and unambiguous.
Persisted slot identity: "Output slots are server-issued UUIDs persisted with the canonical action before dispatch, not ordinal positions"; "An action has an immutable allowed slot set"; an extra worker file "cannot appear merely because a worker returned an extra file." Closes the wire-ordinal/extra-output hole.
Claim-fenced accepted binding: acceptOutput(bindingId, expectedRevision, verifiedCompletion) — only the canonical action owner; adapter reads the exact committed jobArtifact via the non-owner tenant repo; "Verified completion is server-internal data, never trusted from a worker callback payload or model output"; inside the app txn it rechecks auth + current turn-claim ownership + cancellation and CAS-writes the binding; pending accepts once, replay returns same, other reference conflicts, cancelled cannot accept, copy/observation cannot accept. Correctly notes existing finishTurn is insufficient and E2.2 must add a transaction-aware result-acceptance method.
Cancellation / first-artifact idempotency: unique (companyId, canonicalActionIdentity, outputSlot) binding; regressions enumerate cancel-first-rejects-accept, accept-first/cancel-before-publication-blocks-publication, crash-after-accepted-binding-recovers-same-first-artifact, stale-claim-cannot-accept, worker-ordinal-collision-no-effect, extra/unregistered-slot-denied.
E2/E4 ownership without a completion cycle: E2.2 owns universe_output_bindings.ts + output-binding.ts (accept output identity); E4 later creates the canonical artifact and repairs the reply projection — "this separation prevents an E2/E4 acceptance cycle." Verified acyclic: "the service module has no dependency on E4 publication or artifact UI" and e2-2.md "E2 implementation has no import from the E4 publication service." E4.2 processing calls the same service (processing variant).
Residual (plan's own gate): the authoritative CMD/replatform result-acceptance adapter is unbound — "The exact integration is still CMD-gated"; "a query of the latest attempt or successful-looking worker event is not equivalent." → MEDIUM-A.
M2 — default-deny promotion tuples / disguised-other tests / truthful limits — RESOLVED (design contract)
Default-deny allowlist tuple (7 fields): (processorId, processorBuild, registeredOutputSlotSchema, validatedContentType, contentValidatorVersion, hashPolicyVersion, promotionPolicyVersion); "No wildcard tuple and no policy supplied by model/worker"; "Missing tuple or unsupported validation rejects promotion."
Disguised-other: "other is not an exception … only after every allowlist predicate and content/provenance validation passes"; negative fixtures for "credential, log and trace material labelled other with spoofed filename/MIME; known forbidden lineage or failed validator must deny"; regression row updated accordingly.
Truthful validation limits (the key honesty check): "Hash policy verifies the exact accepted bytes … it is not a content-safety detector"; "Do not promise detection of arbitrary secrets from a checksum or MIME sniff; an output contract without a defensible validator/source boundary remains unavailable." This correctly does not over-claim byte-level secret detection and defaults to unavailable. Essentially closed as a contract; the exact per-processor validator/lineage boundary is the remaining Retention/promotion qualification.
M3 — company/credential barriers / lock ordering / source-policy preservation / gate sufficiency — SUBSTANTIALLY RESOLVED (contract), coverage-gated (runtime)
Company barrier: companies row as shared/exclusive lock (avoids a second ACL truth store); publication takes a shared row lock + freshly evaluates canonical company/actor/destination/source permissions in the same app txn; every permission writer takes an exclusive company-row lock before changing authoritative rows; multi-company in sorted UUID order; deletion conflicts with the lock; "a deleted company never causes implicit recreation."
Credential barrier: "Global identity/session/key/admin authority cannot be protected by a company row alone." Supported modes acquire shared locks on actual credential/principal rows before the company barrier; revocation writers take conflicting locks in the same order; external/cached/non-atomic modes are gated ("bind a durable locally authoritative revocation mechanism … before supporting background publication for that mode"); "Do not claim a stale req.actor or expiration timestamp proves current permission." This closes the stale-actor hole honestly.
Complete lock ordering: explicit global order — credential/principal rows (stable type/id) → company rows (UUID) → canonical action/claim → output binding → publication receipt → destination/source rows (stable type/id) → artifact/version/index rows; "CAS checks occur after locking"; "No tenant DB transaction, converter or storage/network copy is opened while holding this final application transaction." Deadlock-safe and correctly keeps network/tenant I/O outside the lock (consistent with step-4-copy-before-step-5-commit).
Source-policy preservation: conversation-authz founder/bearer rules preserved and reused by E4.1's raw-asset guard "including intended founder/bearer distinctions" — verified against source, not aspirational.
Gate sufficiency: the barrier's guarantee ("revoke commits first → publication denied; publication commits first → authorized at that instant; later reads denied") holds only if every permission writer joins the barrier. The plan is honest this is not yet exhaustive ("verified seed inventory, not an exhaustive writer certification"; "If any active writer cannot join the barrier, that destination/credential mode remains gated") — fail-closed. So the gate is sufficient as a gate; the load-bearing residual is proving writer-inventory completeness. → MEDIUM-B.
LOW-1 — OVERTURNED (my prior finding was wrong)
The composite (organizationId, jobId) FK is the sole job FK and calls .onDelete("cascade") (job_artifacts.ts:126-131). My prior review looked at the single-column organizations FK (restrict, line 20) and missed the composite. Codex's rejection is correct — and it strengthens the design: because the job FK cascades, copying provenance into the durable receipt is necessary (not merely defensive) for a published result to survive job deletion. No action.

LOW-2 — ACCEPTED with a concrete check
Static callback verification added: "trace the artifact/version publisher registration to trusted source imports at the composition root. Negative tests submit forged publisher names/operation tags in worker/model/frame data and prove they cannot select a callback … No dynamic import/eval/plugin registry." Adequate.

Propagation & regression (clean)
e2-2.md adds the M1 output-binding producer obligation (E2.2-owned schema/service/test; "no import from the E4 publication service"; "do not infer [acceptance] from finishTurn or worker commit").
e4-2.md/e4-3.md consume the same credential/company/action/binding/publication lock order and the default-deny tuples; both add disguised-other + forged-callback checks; both restate human intake A unchanged.
implementation-bindings adds "Output identity and revocation producer obligations" (E2.2-owned binding; E4.1/E4.2 security-owned auth/revocation test) marked "Initial writer inventory is not exhaustive or accepted … Lock-order and exact action/slot definitions are authoritative proposals … not already-shipped APIs."
independent-review-response.md dispositions all five findings; preserves my prior report verbatim ("no reviewer text is silently corrected").
Counts 69/69; anchors 82 = 70 runtime (all exist) + 12 bundled; V1 scope (30 / E3.4) unchanged; E4.1 human-intake A untouched; over-claim scan on the new file clean; output-binding/universe_output_bindings attributed consistently to E2.2.
Findings (severity-ranked)
No Critical/High. No undisclosed inconsistency. Both are the plan's own named gates, elevated for security acceptance.

[MEDIUM-A — plan's own gate] Generated-path acceptance remains contingent on the unbound CMD authoritative result-acceptance adapter. M1 now specifies the binding/claim/CAS contract, but the actual "which committed attempt/slot is the accepted output" decision needs a transaction-aware result-acceptance method in E2.2 plus the replatform distributed result adapter. Honestly gated ("still CMD-gated"; generated publication "remains blocked" until bound). Security acceptance of the generated path must not precede that adapter being bound and tested (claim/CAS/cancellation/competing-attempt). Evidence: worker-publication-identity-revocation.md §M1; e2-2.md "M1 detailed follow-up"; plan gate "Accepted generated output."
[MEDIUM-B — plan's own gate, highest confidentiality priority] The revocation barrier's guarantee is contingent on exhaustive permission-writer + credential-mode coverage. The company/credential-row lock protocol is sound, but its confidentiality guarantee fails if even one production permission writer does not take the exclusive company lock (or a credential mode lacks a durable revocation row). The plan flags this ("not exhaustive … all supported writers and credential modes must be bound and tested before acceptance") and is fail-closed, so it is a sufficient gate; the residual is that proving writer-inventory completeness across direct DB writers, bulk/admin/bootstrap paths and every credential mode is inherently hard. Secondary consideration for review: the barrier imposes a broad blast radius (modify every permission writer) and per-company single-row serialization (contention) — weigh coverage completeness and performance. Evidence: worker-publication-identity-revocation.md §M3; plan revocation section + gate "Revocation serialization."
[LOW — verification note, not a defect] Confirm the E2.2 output-binding → E4 dependency stays one-way at implementation. output-binding.ts lives under server/src/services/internal-agent/ (E2.2) and is consumed by E4.2 processing; the plan states the service has no dependency on E4/artifact UI (acyclic). Confirm at implementation that E4.2's use is a one-way call and no reverse import appears (matches the plan's own statement).
(My prior LOW-1 is withdrawn as incorrect; LOW-2 is satisfied.)

Standing caveats (unchanged; not defects)
Planning text with proposed, unrun tests; the identity/revocation contracts are authoritative proposals, not shipped APIs; the writer inventory is a verified seed, not exhaustive or certified.
Open gates (all named): CMD accepted-output adapter (M1); exhaustive writer/credential-mode barrier + two-connection race proof (M3); processor admission; retention/promotion validator+lineage; isolated-host grants; accountable security owner (unassigned).
No grant or protocol expansion; human intake A unchanged; release scope unchanged; implementation still requires TK's explicit approval. This review confirms internal consistency and decidability for security review — not runtime readiness.
Reviewed a033e17c0a19d5f44de2be3837fd81b3ca16398c; baseline 0d8876cdf333ba433c792127e8b4b264fb3a5d22; source base