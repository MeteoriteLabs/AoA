# E3.1 OpenAI realtime qualification and integration implementation plan

> **For agentic workers:** Use `superpowers:executing-plans` only after explicit implementation approval, one checked increment at a time. This packet authorizes documentation only; do not run providers, install dependencies, change branches, or reuse the premature E1.1 worktree.

**Goal:** Connect explicit OpenAI voice sessions to the existing authorized Commander conversation, with truthful readiness and confirmed work outcomes.

**Architecture:** Speech is replaceable transport/presentation around Commander. Provider sessions do not own task execution, idempotency, approvals or conversation permission. Qualification must establish a speech-purpose credential boundary before any provider SDK binding is written.

**Tech stack:** Existing TypeScript/React, Express and repository test runners. Provider model, SDK, transport and credential issuance details are outputs of qualification, not selected dependencies.

**Spec:** [E3.1](../slice-plans/e3-1.md), [voice preparation](../implementation-plans/e3-voice.md), [integration contract §1](../integration-contracts.md), [settings](../settings-contract.md), [U07–U09](../ui-review-decisions.md), [motion](../motion-and-interaction.md).

## Status, constraints and evidence

V1, two original increments. Detailed planning complete for review; provider/credential binding **blocked**, runtime unexecuted. Source reference throughout: `183e46a9c65fc3105c7e3d125629276814df7dbb`, inspected using `git show`/`git grep`, not the current runtime checkout. Revalidate at the approved execution revision.

| Source anchor at that object | Verified behavior and limit |
|---|---|
| `CLAUDE.md`, Critical Rules #11 | Deployment-specific CLI credential policy: self-hosted CLI login, cloud extraction in an isolated sandbox with company key; embeddings chokepoint. It differs from the original checkout's blanket hosted-key wording. Neither text establishes a realtime speech credential class. |
| `server/src/routes/internal-agent.ts:156`, chat handler beginning at `:160`, service input at `:241` | Existing POST chat calls `agentLoopService(...).chat`; forwards actor/user role, company, conversation, submission, attachments and context. A pre-created run/thinking event precedes service acceptance: neither proves durable execution. |
| `ui/src/api/internal-agent.ts:195` `streamAgentChat` | Existing POST SSE client; do not give a voice provider its bearer session or route it directly into task mutation APIs. |
| `packages/shared/src/validators/internal-agent.ts:69` `chatMessageSchema` | Message at most 10,000 characters, page context 5,000, client submission ID 200, attachment IDs at most five. Spoken instructions must obey the same limits. |
| `server/src/services/secrets.ts:54`, `:458` | `SecretConsumerContext` and `resolveSecretValue(companyId, secretId, version, context)` enforce company, active status, consumer binding and access auditing. A generic secret resolver is not permission to invent a new voice consumer type or borrow MCP OAuth ownership. |
| `server/src/__tests__/internal-agent-chat-route-sse.test.ts` | Real route-handler/mock-service fixture proves SSE/run projection only; middleware is mocked, so it cannot certify authorization or provider behavior. |
| `docs/replatform/epics/E10-desktop-migration-realtime/findings.md:3` E10-F001 | Open/unowned: Commander distributed routing, per-user credential and synchronous result-return gaps. CMD blocks distributed acceptance, not voice controls or authorized existing conversation reads. |

No new tables are specified until retention/credential ownership is bound. Any resulting schema uses generated Drizzle migrations, shared exports/validators and tenant repository/RLS review; no raw schema DDL. Secrets, drafts, captions and panel layouts remain separate. Transcript retention defaults on under the agreed policy; AoA raw-audio recording defaults off and requires explicit opt-in. Provider retention is independent.

## E3.1/1 — Provider contract qualification

**Files:** Create, during an authorized qualification phase, `docs/architecture/commander-canvas/voice-openai-qualification.md`; proposed implementation owners `packages/shared/src/types/universe-voice.ts`, `packages/shared/src/validators/universe-voice.ts`, `server/src/services/voice/openai.ts`. Modify `settings-contract.md` only to record an accepted concrete binding, not to relocate Providers or Budget & caps. Test owner: `server/src/__tests__/voice-provider-conformance.test.ts`.

**Consumes:** E8.1 company-scoped provider readiness/preferences; E2.1 immutable selected context and E2.2 canonical submission/outcome ownership. **Produces:** One provider capability record, credential decision, redacted protocol transcript and adapter-to-wire operation map. The following is an AoA-owned *proposed* contract, not an OpenAI API signature:

```ts
export type VoiceProvider = "openai" | "gemini" | "elevenlabs";
export type VoiceCapability = {
  provider: VoiceProvider;
  connectionId: string; // authorized reference, never a key
  model: string;
  transport: "webrtc" | "websocket" | "brokered";
  partialTranscripts: boolean;
  finalTranscripts: boolean;
  interrupt: boolean;
  resumeTransport: boolean;
  usage: "reported" | "estimated" | "unavailable";
  validatedLanguages: readonly string[];
};
export type VoiceScope = {
  companyId: string; userId: string; conversationId: string;
}; // server derives company/user from the authenticated request
export type VoiceCorrelation = VoiceScope & {
  voiceSessionId: string; generation: number;
};
export type VoiceTranscript = VoiceCorrelation & {
  utteranceId: string; revision: number; text: string; final: boolean;
};
```

- [ ] **1. Pin documentary inputs.** Read current official OpenAI realtime session/authentication, WebRTC/WebSocket, conversation interruption, tool/result, usage and data-retention docs at qualification time. Record exact URL, retrieval date, model/API/SDK version, region and account entitlement. The September 10 [research](../voice-provider-research.md) is a starting bibliography, not current provider certification. No browse or paid session was performed for this addendum.
- [ ] **2. Resolve credential policy before code.** Produce a decision against pinned Critical Rule #11, Decisions #104/#117 and replatform credential taxonomy: who owns the speech connection, who may resolve it, exact purpose, session issuance location, allowed client material, expiration/revocation mechanism, provider data handling, and budget authority. Keep realtime speech separate from extraction/CLI credentials. Accountable credential/security reviewer remains unassigned; user review is required if a locked policy changes. An unavailable short-lived client mechanism must yield a reviewed broker architecture or a blocked result, never a durable browser key.
- [ ] **3. Complete the operation map.** For connect, end, interrupt, mic mute, output silence, final transcript, tool handoff, delayed result, expiry, revocation and usage, record exact provider messages/methods and ordering from the pinned docs. Capture sample redacted payloads. Verify whether session closure prevents continued billing and whether issued credentials can be invalidated mid-session; do not equate server revocation with instant provider-side invalidation.
- [ ] **4. Run the separately authorized qualification matrix.** Use two test companies A/B, users A1/A2/B1, A1's conversation, disposable provider connection and budget ceiling. For each scenario below, store timestamps, provider session ID hash, AoA correlation IDs, asserted state, billing observation and cleanup proof. Never store credential/token bodies. Begin from disabled/unconfigured state and show the expected unsupported result before adding the adapter.

| Input and method | Required evidence / failure verdict |
|---|---|
| Load Providers and Universe ten times; do not press Start/check | Zero mic requests and zero live sessions. CLI-only login remains voice-unconfigured. |
| Explicit check, then explicit Start with microphone permission granted | Check has disclosed cost behavior; Start emits connecting then connected only after transport acknowledgement. Credential disclosure limited to approved connection material. |
| Speak a finalized harmless task instruction; inject duplicate partial and final messages | One canonical submission ID and one Commander user message. Partial hypotheses produce zero submissions; provider cannot directly invoke task creation. |
| Hold Commander result for 30 seconds, then return confirmed completion | “Accepted/in progress” stays distinct from completed. Spoken completion carries the original canonical result reference. No invented success during hold. |
| Interrupt at 250/750/1500 ms into playback | Measure input-to-local-stop and provider truncation behavior; no audio from old generation afterward; original background task retains state. |
| Expire token; revoke connection; revoke A1 conversation access; replay token as B1 | New access denied; no leaked transcript; observed active-session shutdown latency and provider residual explicitly recorded. |
| Disconnect after accepted write before acknowledgement, reconnect | E2.2 read-only outcome reports original request; zero replay execution. If lookup is unavailable, report unknown and block automatic resubmission. |
| English, Hindi-English, and one additional supported language; noise and long silence | Record recognition corrections, turn segmentation and cost/latency distribution independently. No language parity claim based on a vendor list. |

- [ ] **5. Publish a qualification verdict.** Record pass/fail/unsupported per capability, maximum documented session/token duration, actual audio format/sample rate, measured p50/p95 latency, expiry and idle billing. Proposed sample size is ten repetitions per recovery case plus twenty scripted utterances per language; this is coverage, not a statistical quality guarantee. Pin the exact SDK only after this map passes, and pair any manifest change with a generated lockfile. No fake test can close VOICE.

**Recovery/rollback:** A qualification failure leaves the capability unavailable, typing works, no silent vendor fallback, temporary provider session closed and test secrets revoked. A failed cleanup is a failed qualification with an owner/escalation record. No live data is needed.

## E3.1/2 — Session integration

**Files:** Create `server/src/services/universe-voice.ts` (session admission and identity), `server/src/services/voice/openai.ts` (qualified wire adapter), `ui/src/components/universe/useVoiceSession.ts` (device/transport owner), `server/src/__tests__/universe-voice.test.ts`. Add routes in `server/src/routes/internal-agent.ts`, shared voice exports in `packages/shared/src/index.ts`, UI API methods in `ui/src/api/internal-agent.ts`; E8.1 owns preference/provider storage. Exact credential table/route binding is blocked by /1, not assigned a fictional endpoint.

**Interfaces:** Proposed server orchestration functions `startVoice(scope: VoiceScope, connectionId: string, clientStartId: string): Promise<VoiceCorrelation>` and `endVoice(scope: VoiceScope, voiceSessionId: string): Promise<void>` are application intent boundaries only. Actual start response connection material must be typed after /1 and excluded from logs/storage. A session ID is correlation, never authorization. E2.2 remains the sole submission/outcome API producer; bind its finalized exported types before writing this consumer. Do not clone them here.

- [ ] **1. Build refusal tests at the real route boundary.** Extend the route stack patterns in `internal-agent-chat-route-sse.test.ts`, but add the real authorization middleware in the new integration fixture. Seed A1/A2/B1 and company-owned connections/conversations. A1 may start A1's permitted connection; A2/B1 attempts return the canonical denied/not-found result before provider issuance. Missing config, expired capability, hard budget stop and revoked secret call the provider zero times. Schema rejection covers empty IDs, unknown fields and unsupported voice/model pairs.
- [ ] **2. Write admission and lifecycle implementation after binding.** Resolve actor from request; authorize conversation and provider connection; check budget/cap admission using its canonical owner; resolve the approved secret consumer; obtain qualified connection material; create an auditable session correlation; return acknowledgement. On any post-issuance failure, close provider session and report no accepted session. Retry the same `clientStartId` returns the same active start outcome or explicit terminal status; it cannot create two billable sessions. A proposed maximum of one active voice session per user/device is an implementation default to review, not a measured provider limit.
- [ ] **3. Connect explicit controls and preferences.** Start only from user action. Load effective provider/voice/language/style and device-local mic/speaker choice at start; validate against capability record. Opening/showing blob, chat, captions, reload and preference sync never start capture. Permission denial stays disconnected with an actionable explanation. Stop/end closes tracks, playback and transport while preserving conversation/blob. E3.2 owns fine-grained state transitions.
- [ ] **4. Bridge finalized work only.** Freeze E2.1 context and stable client submission ID once per accepted utterance. Pass content through E2.2's canonical Commander owner, preserving provenance as correlation. Duplicate provider tool calls reuse the original identity and verify payload equality; changed payload conflicts. A result may be spoken only after canonical outcome observation. Source speech cannot broaden role. Keep provider conversation context within authorized data and the established context limits.
- [ ] **5. Verify ordinary and adversarial seams.** New unit/integration tests assert provider-issued material never enters serialized settings/layout/draft/activity output, logs or canonical messages; a second company sees neither readiness nor session. Test expired token during handoff, revoke after issue but before connect, callback arriving after end, duplicate start, budget denial, server restart and unknown start acknowledgement. Provider callback loss never causes a new Commander request. Re-run the real provider matrix from /1 against actual routes, not an adapter-only harness.

**Commands after approval/tests exist:** `pnpm exec vitest run server/src/__tests__/universe-voice.test.ts server/src/__tests__/voice-provider-conformance.test.ts`; E3.2 browser lifecycle test for UI. Red expectation is missing voice entry/typed unavailable or duplicate/denied assertion failure in a deliberately failing regression; green is all assertions plus recorded real provider runs. Current main cannot run pinned-only bindings without the approved base. No commands were executed here.

**Observability:** Audit explicit start/end/check/config mutation and refusal reason with company, actor, connection reference, session, generation and canonical request/result IDs. Metrics record connect latency, interruption latency, stale-event drops and reported versus estimated usage; no raw audio or transcript in metrics. Store transcript only through the authorized conversation retention owner.

**Limits/release/rollback:** Provider TTL and usage caps come from /1 plus Budget & caps, whichever is stricter. Proposed UI start deadline 15 seconds and reconnect attempts 0 automatically until outcome/credential qualification; timeout means failure/unknown, not permission to issue again. Disable new admissions on rollback, terminate connections, retain canonical messages/tasks and audit. Run full Linux `pnpm -r typecheck`, `pnpm test:run`, `pnpm build` before runtime handoff; Windows E2E limitations are detailed in E3.2. No runtime claim is made.

## Self-review and remaining gates

- Corrected stale blanket-key-policy quotation using pinned Critical Rule #11; retained the still-missing speech-purpose authorization decision.
- Split run/thinking visibility from durable Commander acceptance; pre-created route run cannot close handoff evidence.
- Kept capability-specific readiness separate from CLI login and stopped settings loads from incurring capture/session activity.
- Open: VOICE credential/provider record (/1, reviewer unassigned), E8.1 provider/preferences binding, E2.1–2 canonical types, and CMD/E10-F001 for distributed completion. Independent work allowed: source/doc qualification and interface review only under current scope. All V1 providers remain required; OpenAI first is order, not permission to drop the others.
