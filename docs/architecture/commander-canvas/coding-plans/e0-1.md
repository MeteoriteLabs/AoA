# E0.1 — Base, integration and evidence implementation plan

**Status:** Review candidate; documentation work only. **Version:** V1. **Spec:** [slice](../slice-plans/e0-1.md), [branch decision](../planning-branch-proposal.md). **Architecture:** source-bound manifests and dependency evidence; no runtime service. **Tools:** Git object reads and Markdown validation. Writing-plans applies to this packet; runtime execution requires separate approval.

## Current execution clarification

The planning branch already exists as `codex/universe-interface`, from source pin `183e46a9c65fc3105c7e3d125629276814df7dbb`. Branch creation/publication steps below describe the original procedure and must not be repeated or reset. Candidate `9200a66c42633019349de937a8b97979acac0f7a` and local correction `b5cc42643223c433a8263564c7142761472a13d9` remain unadopted. Use the [current readiness register](../consolidated-plan-readiness.md) and [F4 proposal](../f4-fixture-repair-plan.md) for next actions. A new upstream revision requires a recorded delta review, not an automatic rebase. Documentary publication is complete; BASE acceptance is still open. The [full qualification](../full-baseline-qualification-results.md) has now run at the clean F4 repair commit: F4 passes, F5 setup timeout blocks build. The [F5 diagnostic](../f5-attribution-results.md) passed but did not clear qualification; the next gate is concrete startup/lifecycle correction review and clean qualification; the branch must still not be recreated or adopted automatically.

## E0.1/1 — Baseline and ownership

- [ ] Read the exact candidate with `git rev-parse origin/docs/replatform-program` and compare `git ls-remote --heads origin docs/replatform-program main`. Record full commits, merge-base and whether each required source exists. Current inspected candidate is `183e46a9c65fc3105c7e3d125629276814df7dbb`; ancestry is not a test certificate.
- [ ] Review the generated [binding manifest](../implementation-bindings.md). For every slice map source/proposed files, upstream gate, producer responsibility, consumer responsibility and required evidence. Unassigned engineering owners remain unassigned rather than inventing staff assignments.
- [ ] Check E10-F001 and E8-F001/F004/F011/F012 at that commit. Copy status and evidence reference, not a completed flag inferred from a ticket's existence. The actual dispatch/result/credential/approval/profile contracts must be re-reviewed when those producers change.
- [ ] Finish the full packet and author self-review before branch creation, per TK's ordering. Create a fresh `codex/universe-interface` branch from the recorded replatform commit and a separate checkout; never reset the original main checkout or use the premature worktree as the copy source. Copy only the Universe architecture directory and its top-level master-scope entry, verifying referenced documents. Stage explicit paths, inspect `git diff --cached --name-only`, then commit and push the documentation packet. Every staged path must be under the allowlisted planning paths.
- [ ] Give TK the branch, source commit and plan commit with the short Claude prompt. A reviewer's source tree must match the packet's stated baseline. Record returned findings against that plan commit.

## E0.1/2 — Contract collision and upstream integration

- [ ] For each proposed file, inspect `git cat-file -e <base>:<path>`; absence means a proposed new file, not an implemented module. All current runtime investigation anchors resolve; newly authored planning references belong to this packet, not inherited source.
- [ ] Compare `git diff --name-status <old-base> <new-replatform> -- <bound-source-paths>` before any upstream sync. Review behavior changes in touched exports, validators, schema, credentials, admission and result delivery. Update only affected plans and consumer tests; invalidate prior evidence that depended on old signatures.
- [ ] Propose one integrator for each sync in the execution assignment record. Check upstream at the start of each implementation slice and before integration; bring in updates at a reviewed boundary, never halfway through a failing test investigation. Missing producer evidence blocks its consumers, not unrelated V1 slices.
- [ ] Resolve generated migrations/lockfiles by regenerating from the merged schema/manifests in an authorized implementation environment. Do not manually renumber migration history or merge lockfile hunks. Run baseline and candidate against the same Linux environment to attribute failures; historical Windows failures are not global waivers.
- [ ] After replatform lands, merge/reconcile actual main into this same Universe branch, revalidate the bound contracts and run complete V1 acceptance before proposing Universe's merge. The earlier draft disposition is a separate user decision; no cherry-pick by default.

## Acceptance and recovery

Evidence: 31 slice rows, 69 increment links, exactly one version per slice, all code-source links at a named revision, every upstream dependency with closure evidence or an explicit open state. No runtime change in the planning publication diff. If the candidate moved, stop publication until its changed source is reviewed and the recorded base is updated; do not call a stale commit “latest.” If push fails, preserve the local planning commit and report the failure; no force push.

No code/test generation is required to complete this documentation increment. The first actual runtime slice starts only after TK's explicit approval. Reverting planning records never deletes runtime data, tasks, the mock or the unapproved draft.
