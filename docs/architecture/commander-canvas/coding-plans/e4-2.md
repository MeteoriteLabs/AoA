# E4.2 — Format processing implementation and qualification plan

**Policy reconciliation:** Pinned Decision #122 and AGENTS/CLAUDE permit narrowly conditioned idempotent security DDL in delta-free custom migrations because the generator cannot emit it. Older session-supplied AGENTS text lacks this class. The [shared binding record](../implementation-bindings.md#contract-compatibility-decisions) captures the difference. Reconcile applicable execution instructions and bind the locked decision before migration work; table/column/index/FK DDL remains generated. Do not interpret the conditional gate below as proof no supported security-migration path exists.

> **For agentic workers:** Use superpowers:executing-plans only after explicit implementation approval. All checkboxes are unexecuted; qualification involving runtime, installation or providers requires authorization first.

**Goal:** Give each accepted format a truthful preview/understanding/export disposition while retaining original bytes.

**Architecture:** A versioned capability registry selects reviewed processors. Independent processing rows link derivative assets to their source; client renderers reuse existing viewers. Complex parsers run on admitted isolated workers after qualification, never as arbitrary uploaded commands on the control-plane host.

**Tech stack:** Existing shared validators, Drizzle, asset storage, React viewers; new converter builds remain qualification-gated.

**Spec:** [E4.2 outline](../slice-plans/e4-2.md), [format matrix](../format-matrix.md), [artifact contract](../artifact-contract.md), [U04/U06/U09/U10](../ui-review-decisions.md), [motion](../motion-and-interaction.md). E4.2/1 and /2 are V1; fallback is an explicit format disposition, not a deferral of the slice.

## Evidence and dependencies

Pinned source `183e46a9c65fc3105c7e3d125629276814df7dbb`, source inspection only. `ui/src/components/viewers/viewer-registry.ts` exports `resolveViewer(ViewerInput)` and `ViewerResolution`; `SharedContentViewer.tsx` handles markdown/code/JSON/table/media/PDF/sandbox markup/Mermaid/canvas/DOCX/XLSX/download. `PdfDocumentRenderer.tsx` uses react-pdf and pdfjs-dist 5.4.296. `server/src/routes/assets.ts` exposes authorized `/assets/:assetId/render` for DOCX/XLSX only. Helpers are `renderDocxBufferToSafeHtml`, `renderXlsxBufferToSafeHtml`, `office-html-sanitize.ts`; server manifest mammoth `^1.12.0`, XLSX uses ExcelJS, not SheetJS. `OFFICE_RENDER_MAX_BYTES` defaults 15 MiB, independently of 50 MiB upload. These are source-supported paths, not measured complete-format coverage or sandbox containment.

E4.1 produces validated original asset and stage projection; E4.2/2 produces the indexing writer/retrieval evidence consumed by E4.1/2, so it does not wait for that indexing acceptance before implementing the producer. E4.3 consumes source/version lineage and native validation. BASE/security review owns the open ledger data-class/transaction choice; CMD/worker platform owners own converter admission. Actual people are unassigned. No converter can be marked enabled until its image/build, network denial, resource termination and fidelity evidence close the gate. Registry/UI/download work can proceed after approval while processor bindings remain blocked. Do not enable existing synchronous Office parser for broader hostile inputs merely because sanitization passes.

**Security architecture gate (M6):** inherit the explicit A/B/C data-class and transaction decision in [E4.1](e4-1.md#constraints-and-source-evidence), still unselected. It covers `universe_derivatives` and the proposed `universe_asset_index` as well as intake/parts. The organization-owned fields/repository paths below are conditional proposals, not accepted ownership or generic RLS registration. Pinned CAV-005 leaves legacy assets/artifacts under application checks; `aoa_app` currently has SELECT only, so a tenant ledger claim does not authorize derivative asset INSERT. A approved company-scoped same-role publication, B explicitly reviewed non-owner legacy grants, or C durable two-transaction intent/receipt reconciliation must be selected before server coding. Distributed job authority always keeps its sanctioned non-owner boundary. Require the chosen boundary's company/actor/destination, pool-reuse and crash tests; B/C also require no-context/wrong-org tests. Table/column/index/FK DDL remains generated; security DDL follows Decision #122 and its September 1 amendment. A generator limitation on security DDL alone is not proof that no supported path exists. Do not widen the frozen kernel or immutable legacy-grant manifest, adopt owner fallback, or retrofit legacy RLS.

## E4.2/1 — Format capability matrix

**Files:** Create `packages/shared/src/validators/universe-formats.ts`, `server/src/services/universe-format-registry.ts`, `server/src/__tests__/universe-format-matrix.test.ts`; modify shared exports and `ui/src/components/viewers/viewer-registry.ts`; create `ui/src/components/universe/ArtifactPanel.tsx`. Keep existing viewer body reuse, U04 header/controller ownership in E1.1.

```ts
export type FormatDisposition = "native" | "converted" | "source_only" | "download_only";
export interface FormatCapability {
  id: string; version: number; extensions: readonly string[];
  preview: FormatDisposition;
  extraction: "text" | "structured" | "provider_gated" | "none";
  nativeExport: "qualified_writer" | "original_only";
  processorId: string | null; processorBuild: string | null;
  maxInputBytes: number; limitations: readonly string[];
}
export type FormatFailure = "unsupported" | "locked" | "damaged" | "too_large"
  | "codec_unavailable" | "font_substitution" | "resource_limit" | "denied";
```

Registry schema rejects unknown keys and executable config. File extension is a routing hint; sniffed content and parser acceptance decide activation. Unknown general uploads may be safely downloadable without preview; executable code never runs automatically. `nativeExport` refers to qualified writers in E4.3, not viewer success.

The following is the complete intended V1 disposition map. “Qualify” means binding is blocked and UI offers the declared fallback until evidence; no newly listed library is approved for installation.

| Family | Route/processor plan | Fidelity fixture and fallback |
|---|---|---|
| TXT/MD/source/logs | Existing bounded code/markdown viewer; UTF-8 BOM detection and explicit encoding failure | Unicode/RTL, long lines, 2 MiB text truncation; source download, never script execution |
| JSON/YAML/XML | JSON viewer; YAML/XML bounded source first; dedicated safe structured parsers qualify separately | Invalid JSON, duplicate keys, entity expansion/external entities, YAML tags; never resolve entities/custom tags; source download |
| CSV/TSV | Existing table viewer extended with row virtualization | Quoted delimiters/newlines, RTL, formulas; raw values shown, export escaped against formula injection |
| PDF/scans | Existing PDF viewer; bounded pages; separate OCR worker qualification | Real text/scanned/rotated/password PDF, fonts and signatures; locked state without storing password; original download |
| DOCX | Existing mammoth helper moved into qualified worker for new processing pipeline | Existing hostile-link DOCX plus tables/footnotes/tracked changes; sanitized preview and visible fidelity notice |
| ODT/DOC/RTF | Qualify pinned LibreOffice headless image → PDF/text | Missing fonts, OLE/external references, broken package; original-only until converter passes |
| XLSX | Existing ExcelJS helper in qualified worker, preserving formula versus cached value labels | Multi-sheet, merged cells/formulas/cache absent; preview values are not recalculated results |
| XLS/XLSB/ODS/XLSM | Qualify dedicated workbook parser/LibreOffice; macro execution disabled | Pivot, macros, hidden sheets, huge sparse range; original-only if feature/format unsupported |
| PPTX/PPT/ODP | Qualify LibreOffice → slide PDF/thumbnails plus notes extraction | Speaker notes, fonts, embedded media, animations; preview explicitly static, native original retained |
| PNG/JPEG/WebP/GIF/AVIF | Existing image viewer with dimensions/frame gate; animation user-controlled | EXIF rotation, transparency, animated frames, malformed header; oversized decode denied |
| TIFF/HEIC/RAW | Qualify pinned image worker/codec build → PNG/WebP | Multipage TIFF, camera RAW and HEIC codec missing; list pages/limitations, original-only fallback |
| SVG/Mermaid | Existing sandboxed SVG/strict Mermaid, reviewed sanitization/network policy | SVG script/foreignObject/external URL; Mermaid click/link injection; editable source retained |
| draw.io/Excalidraw | Qualify schema adapters → static SVG plus retained source | External images/fonts and unsupported shapes; source/download fallback |
| Charts | E5.1 schema registry → accessible chart plus table; E4.3 deterministic export | Missing/nonfinite values, 10,000 points, Unicode labels; source spec/data retained |
| WAV/MP3/M4A/AAC/OGG/Opus/FLAC | Browser playback only after codec probe; qualify FFmpeg derivative/transcript separately | Codec-container mismatch, duration, corrupt timestamps; download original when unplayable |
| MP4/WebM/MOV/MKV/AVI | Qualify FFprobe/FFmpeg poster + supported media derivative; no generative claim | Rotation, long duration, unsupported codecs/VFR; poster and download fallback |
| SRT/VTT | Bounded source/timeline parser; attach explicit media reference | Overlap, negative/out-of-order timestamps, markup; display validation notice, preserve bytes |
| ZIP/TAR/GZ/7Z | Qualified sandbox manifest, explicit selective extraction only | Traversal/absolute paths/symlinks/bombs/nesting; no extraction on open; original download |
| EML/MSG/ICS | Qualify safe parsers/sanitized message or calendar preview | HTML remote pixels, nested attachments, timezone/recurrence; source/download; never send/sync |
| EPUB | Qualify package reader/text; no active scripting/remote resources | Reflow/RTL, malformed ZIP, encrypted content; locked/original fallback |
| PSD/AI/INDD/CAD/3D/notebooks/Parquet/DB | Bounded metadata/source if safely recognized, otherwise original download | Native valid specimen + malformed specimen per advertised extension; notebook cells never execute; DB never auto-connects |
| Unknown/encrypted/damaged | Non-executable download for allowed originals; distinct reason | Random bytes, wrong declared MIME, locked package; no invented preview or destructive conversion |

- [ ] Write table-driven registry tests mapping every listed extension to exactly one disposition; format family cannot disappear by default fall-through. Include allowed download for unknown versus policy-rejected upload.
- [ ] Implement explicit capability records and `resolveFormatCapability` using allowlisted ID/version/build; server returns limitations and unsupported reasons, client does not guess processor availability from extension.
- [ ] Reuse `resolveViewer` only for a validated ready derivative or approved native path; do not forward arbitrary `assetUrl` or executable `metadata` from model data. Authorize source and derivative before returning URLs.
- [ ] Implement preview/extraction/export badges independently. Unsupported preview keeps Download original visible. Encrypted/locked material has no ordinary persisted password field; a future password-handling capability would need its own reviewed transient flow.

Concrete regression based on actual repository fixture, with no fake DOCX bytes advertised as fidelity evidence:

```ts
import { readFile } from "node:fs/promises";
import { expect, it } from "vitest";
import { renderDocxBufferToSafeHtml } from "../services/docx-render.js";

it("hostile DOCX links cannot become active script", async () => {
  const bytes = await readFile(new URL(
    "./__fixtures__/docx-with-javascript-href.docx", import.meta.url));
  const html = await renderDocxBufferToSafeHtml(bytes);
  expect(html).not.toMatch(/javascript:|<script\b|\son\w+=/i);
  expect(bytes.length).toBeGreaterThan(0);
});
```

Run this in a separate test module without the mammoth mock used by `docx-render.test.ts`. It proves one parser/sanitizer case only. Existing `xlsx-render.test.ts` and `asset-render-xlsx.test.ts` supply actual workbook construction and authorized route patterns; use their ExcelJS fixture creation for formula/merged-cell assertions. Add immutable fixture hashes and expected page/sheet counts to proposed `tests/fixtures/universe-formats/manifest.json`; binary fixture creation belongs to authorized execution.

## E4.2/2 — Isolated processing and display

**Files, conditional on the security architecture gate:** create `packages/db/src/schema/universe_derivatives.ts`, proposed `packages/db/src/repositories/tenant/universe-derivatives.ts`, `server/src/services/universe-derivatives.ts`, `server/src/routes/universe-derivatives.ts`, `ui/src/api/universe-derivatives.ts`; extend E4.1 stage projection. Add `packages/db/src/schema/universe_asset_index.ts`, `server/src/services/universe-asset-index.ts` and `server/src/__tests__/universe-asset-index.integration.test.ts` inside this existing increment. Schema/shared exports and route mounting are planned; repository placement and policies follow the selected A/B/C gate rather than assuming organization ownership. Company-source integrity is mandatory for every alternative; source does not establish those new protections yet.

`universe_derivatives`: UUID id, organization/company/sourceAsset IDs, optional sourceVersionId, stage (`preview`/`extraction`/`indexing`), processorId/build, configHash, sourceHash, state, revision, attempt count, canonicalJobId nullable, outputAssetId nullable, bounded failure code/detail, timestamps. Unique `(company_id,source_asset_id,stage,processor_id,processor_build,config_hash)`; concurrent callers attach to one current operation. Retries do not change immutable source or successful output; a changed processor build makes a new derivative identity. Link canonical job only through admitted job authority, never a panel-local worker loop.

Proposed `POST /api/companies/:companyId/universe/assets/:assetId/processing/:stage/retry` accepts `{processorId,processorBuild,configHash,expectedRevision}`; server selects permitted config, rechecks source/destination/access/budget and returns stage snapshot, 409 stale revision, 403 denied, 422 unsupported processor. `GET .../processing` is E4.1 projection; proposed derivative content route resolves both derivative and live source access before delegating to scoped storage. No direct exposed derivative `assetId` may bypass source revocation: unique derivative `outputAssetId` → sourceAssetId linkage in the derivative ledger is server-only, and E4.1's `assertUniverseAssetAccess` must enforce that lineage on existing `/assets/:id/content`, `/meta`, `/render` and attachment delivery paths too. Missing/cyclic lineage denies access rather than degrading to company-only authorization.

**Indexing access contract (M12; part of E4.2/2, retained in V1):** the proposed `universe_asset_index` is a dedicated asset-search store, separate from Memory. Its chunks contain company ID, source asset/version/hash, derivative ID, immutable destination reference, processor/build, index generation, chunk ordinal, bounded text and optional qualified embedding. Organization ID/policy follows the security gate; unique `(company_id,derivative_id,index_generation,chunk_ordinal)` and a committed generation receipt prevent partial/duplicate results. Vector storage/type and exact embedding adapter bind during BASE/processor qualification; no fictitious installed vector capability or new hosted-key exception is assumed. No asset indexing code calls `writeMemoryAndIndex`, `enqueueMemoryEmbedding` or writes `memory_items`/the Memory embedding queue. Pinned `memory-access.ts:canActorSee` and Decisions #118/#119 allow broad internal visibility for company/unscoped Memory, so copying private asset text there would violate destination authority even if raw bytes remain protected.

Index admission, every source read, publication and every retrieval must call the same live `assertUniverseAssetAccess` destination/lineage policy. Search accepts server-resolved actor/company and an explicit authorized destination; it filters authorized source IDs before matching/scoring/counting and rechecks before returning text, snippets, titles, counts, embeddings or citations. Proposed `universe-asset-index.ts:searchAuthorizedAssetChunks` is the only index reader for UI search, Commander context/RAG, agent and MCP integrations; no company-only query or unfiltered post-ranking overfetch path is permitted. Cache identity includes actor, company, destination and access revision, with live reauthorization on cache hit. Revocation/deletion denies immediately even before physical index cleanup; queued/stale jobs cannot publish or revive an older generation. Missing/deleted/cyclic lineage fails closed. Explicit authorized publication to a broader artifact audience creates a separately authorized destination/index lineage and records that decision; it never retroactively broadens the private source index. Promotion to Memory still requires the existing separate governed approval flow.

- [ ] Add DB tests for two retry callers → one job/derivative, stale revision →409, same hash in different companies never deduplicates across tenants, and revoke-source denies every derivative serving path.
- [ ] After the security architecture gate, implement stage claim via the approved ledger transaction, durable job admission, output reconciliation through existing worker artifact commit, then derivative/index publication. Existing distributed job rows continue through `runInTenant`. A/B permit only the reviewed same-role publication composition; C persists/reconciles a canonical writer receipt across separate commits. Today's non-owner grants do not permit asset writes. Test crashes at every selected protocol boundary, duplicate receipt delivery and cancellation before/after publication. Unknown worker completion polls canonical outcome; it does not enqueue another converter. Cancellation names one stage; late fenced output cannot revive it.
- [ ] Implement the dedicated scoped index writer/reader and committed generation receipt above. Index only authorized bounded extraction from immutable source lineage; default stage remains `not_requested` until explicit authorized admission. Return the authoritative receipt through E4.1/2 status; successful preview/extraction alone is not successful indexing. A changed destination grant does not rewrite old chunks into a broader corpus.
- [ ] Add real-PG index tests and connected UI/context fixtures: a private-canvas canary is searchable by its authorized owner, but absent for same-company second user, foreign company, unrelated agent and external MCP key across search/count/snippet/citation/direct-chunk/context paths. Assert zero Memory/Memory-queue writes. Repeat after revoke-before-job, revoke-before-commit, revoke-after-ready, cached search and retry/late completion; missing/cyclic lineage and changed source hash deny. Prove an explicitly authorized broader publication exposes only its new lineage and leaves private-source queries denied. Preserve positive permitted-destination retrieval and independent original download/preview-failure behavior.
- [ ] Route complex processors by reviewed image identifier, not file-derived command arguments. Bind exact worker command/result envelope only after CMD qualification; classify crash/timeout/resource-limit distinctly. Logs contain processor/build, size/count/duration and IDs, never extracted body or secrets.
- [ ] Implement incremental display and cancel subscriptions/render tasks on minimize/close; jobs and original continue. Cap pages/cells/frames before allocation where feasible; output truncation must carry a visible `partial` reason. Retry only failed stage; no function in this module can submit generation.
- [ ] Test background completion without camera movement, 409 conflict with retained input, offline last-success timestamp, reload snapshot-before-events, revoked panel purge, narrow layout and keyboard download/retry. Consume E8.1 theme/density/motion immediately; reduced-motion cancels decorative spinner travel and retains textual status. U04 focus restoration and pinned geometry remain controller-owned.

Proposed numerical ceilings, not measured capacity: retain 50 MiB original and 15 MiB legacy Office-render input; worker max expanded input 200 MiB, expansion ratio 100:1, archive nesting 2, 1,000 members, no symlinks/hardlinks; images 40 megapixels/frame and 100 frames; 500 document pages, 50 sheets, 100,000 parsed cells, 10,000 displayed rows with virtualization, 2 MiB extracted text with provenance/truncation, 30-minute media probe scope. Worker 1 CPU, 512 MiB RAM, 512 MiB scratch, 64 PIDs, no network; 60-second ordinary conversion and 300-second media/OCR hard timeout, 1 converter/company and 4/worker, queue cap 20/company with explicit busy state. Platform ceiling always wins. Enforce input/resource bounds before parser where possible; inability to stop a decompression bomb before exhaustion blocks that processor. Do not advertise long-media transforms beyond qualified bounds.

## Qualification protocol and completion

Input packet for each processor: pinned source revision, container digest/library version/license record, CPU/OS/font/codec inventory, fixture SHA-256 manifest, exact admission limits, network policy, supported native outputs. Method: run valid/malformed/locked/bomb/external-resource fixture at limit−1/limit/limit+1; record exit/status, peak RSS/CPU/disk, duration, attempted network, page/sheet/frame counts; compare rendered screenshots against fixture references and reopen any generated native output. Kill worker mid-write and revoke source mid-read; retries must preserve original and publish at most one derivative. Real isolated Linux worker plus local/cloud storage evidence required; source inspection and sanitized HTML unit tests cannot close this gate. Owners: platform containment, format engineer, design/accessibility reviewer, all unassigned.

Acceptance evidence must distinguish “native preview works”, “text extraction works”, “native writer reopens”, “download only” per family. No blanket “all formats supported”. Any changed build/font/codec invalidates affected row evidence. Gate failure keeps processor unavailable; independent safe viewers and download still work, but V1 release cannot be declared complete without the accepted family dispositions and connected failures verified.

After implementation: `pnpm exec vitest run server/src/__tests__/universe-format-matrix.test.ts server/src/__tests__/docx-render.test.ts server/src/__tests__/xlsx-render.test.ts server/src/__tests__/asset-render-xlsx.test.ts`; `pnpm exec vitest run ui/src/components/viewers/__tests__`; new `tests/e2e/universe-artifact-formats.spec.ts` via real Playwright app. Linux full `pnpm -r typecheck`, `pnpm test:run`, `pnpm build`; record Windows embedded-PG skips without reclassifying. Qualification was not run in planning.

Rollback disables new processing admission while preserving original/read/download and durable pending-status reconciliation. Additive derivative schema remains; cleanup removes only unreferenced expired staging, never originals. New parser versions do not overwrite old derivatives. Self-review fixes: corrected SheetJS assumption to actual ExcelJS; separated sanitization from process containment; included every family and missing-codec fallback; closed derivative raw-route bypass in plan; no provider or converter SDK binding fabricated.
