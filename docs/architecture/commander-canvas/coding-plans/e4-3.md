# E4.3 — Immutable revisions and generation implementation plan

> **For agentic workers:** Use superpowers:executing-plans after TK explicitly approves implementation. This plan does not authorize provider sessions, dependency installation, branch changes or coding.

**Goal:** Inspect, compare, refine and export immutable artifact versions, and reconcile generated results without duplicate publication or spend.

**Architecture:** Reuse `artifacts`/`artifact_versions` and canonical request/job output authority. Add only version-concurrency/idempotency and explicit winner-selection support missing from source. Generated bytes enter E4.1/E4.2 validation/publication before they are represented as ready output.

**Tech stack:** Existing TypeScript, Drizzle/PostgreSQL, asset storage and React viewers; generation adapters/writers require separate qualification.

**Spec:** [E4.3 outline](../slice-plans/e4-3.md), [artifact contract](../artifact-contract.md), [format matrix](../format-matrix.md), [request contract](../request-contract.md), [U04/U06/U09/U10](../ui-review-decisions.md). Both increments are V1.

## Existing evidence and required corrections

At `183e46a9c65fc3105c7e3d125629276814df7dbb` (source read only), `packages/db/src/schema/artifacts.ts` defines both tables. `artifact_versions` has `(artifact_id,version_number)` unique index, parentVersionId, assetId, storageKind, SHA-256 and metadata; versions are immutable by contract. `artifacts.sourceActionId` has partial company/source-action uniqueness. `server/src/services/artifacts.ts:artifactService.addVersion` validates parent belongs to artifact and asset belongs to company, calculates max version in a transaction, inserts then always changes `currentVersionId`. A transaction without row serialization is not proof that concurrent max+1 inserts succeed; the unique index can reject one. No explicit winner-select endpoint appears in inspected artifact routes. Preserve Decisions #43/#45 rather than call existing pointer movement a complete winner policy.

Existing routes `POST /api/artifacts/:id/versions`, archive/unarchive are company+board+founder gated; MCP version route uses `assertFounderMcpPublisher`. `ui/src/api/artifacts.ts` exports `artifactsApi`, `archiveArtifact`, `unarchiveArtifact`, `uploadFileArtifact`. `server/src/__tests__/artifact-version-auth.test.ts`, `artifacts-qa.test.ts`, and `tests/e2e/artifact-lifecycle.spec.ts` provide fixtures. The e2e fake `%PDF-1.4 test` validates linkage, not PDF fidelity; do not reuse it as native-export evidence.

Dependencies: E2.1 revision references/context; E2.2 canonical request/outcome; E4.1 original assets; E4.2 format and worker validation. BASE/tenant and CMD owners remain unassigned. Manual revision UI can proceed after approval independently of blocked paid generation. V1 completion includes all accepted generation dispositions, not merely manual revisions.

**Storage authority clarification:** artifacts/versions/assets are legacy company-scoped entities. Pinned `server/src/db/rls-tenant.ts:TENANT_RLS_TABLES` explicitly excludes legacy companyId tables under CAV-005; `runInTenant` governs distributed rows and does not replace artifact route/service company and role checks. Manual version transactions use the existing artifact DB/service pattern; no organization-RLS retrofit is proposed here. Distributed rows retain their sanctioned tenant boundary. Accepted human intake and the newly proposed application-owned derivative/index/receipt classification must each satisfy their exact BASE security evidence; generated schema and Decision #122 security-migration rules apply. E1 user/company presentation stores remain separate.

**Publication composition gate (M6):** Consume E4.2/2's proposed application publication receipt/coordinator in the [worker-publication addendum](../worker-publication-plan.md). Worker bytes remain committed under non-owner authority; the application writer independently verifies exact accepted output, copies permitted bytes into canonical storage and commits asset/version/receipt/audit in its own transaction. The proposed application-owned projections and C-style boundary reconciliation require independent/security acceptance; no legacy grant expansion or owner fallback. Bind accepted output-slot/attempt and cancellation/revocation authority before coding. Manual E4.1 A is unchanged.

## E4.3/1 — Immutable revisions

**Modify:** `packages/db/src/schema/artifacts.ts`, `server/src/services/artifacts.ts`, `server/src/routes/artifacts.ts`, verified `packages/shared/src/validators/artifact.ts:createArtifactVersionSchema`, and `ui/src/api/artifacts.ts`. **Create:** `packages/shared/src/validators/universe-artifact-versions.ts`, `ui/src/components/universe/ArtifactVersions.tsx`, `server/src/__tests__/universe-artifact-revisions.test.ts`, `tests/e2e/universe-artifact-revisions.spec.ts`. Export new schemas through shared indices. Canonical version service remains the single writer for human, agent and MCP paths.

Proposed additive nullable `publicationKey` (text, ≤200) on artifact_versions, unique `(artifact_id,publication_key)` where non-null, and `publicationPayloadHash` (64-hex). Retain existing sourceActionId meaning; never overload it with arbitrary UUIDs. Add `revision` integer default 1 to artifacts for winner CAS. Generate migrations; existing rows remain valid with null publication key/revision 1. No rewrite of prior versions/hashes or source attribution.

```ts
export interface ArtifactVersionSelection {
  artifactId: string;
  selectedVersionId: string; // view/context, never a pointer mutation
  currentVersionId: string | null; // canonical winner/current pointer
  artifactRevision: number;
}
export interface VersionPublicationIdentity {
  key: string; payloadHash: string;
}
export interface SelectCurrentVersionInput {
  versionId: string; expectedArtifactRevision: number;
}
```

Manual caller publication key uses stable user action identity retained until a terminal response. Generated key is server-derived from canonical accepted output identity, not client supplied. Exact canonical fields bind to E2.2's producer; key encoding must be versioned and collision-free (canonical JSON digest, not delimiter concatenation). Reused key with altered content returns 409, never overwrites or returns misleading success.

E2.2's `SubmissionOutcome` is a turn observation (`clientSubmissionId`, `userMessageId`, `replyMessageId`, `runId`); none is itself a unique artifact output. For existing `ShowRef {kind:'artifact',id,versionId}`, read it from the explicitly linked persisted reply's `outputRefs`, verify artifact/version ownership and reuse that version directly—do not create a second version to “publish” an already published result. `server/src/services/internal-agent/output-refs.ts` can emit nullable versionId and best-effort refs. Missing artifact/version identity must be repaired by the owning result producer from the immutable commit record, never by selecting current/latest version, message order, array index or run ID. A referenced artifact with no exact generated version remains a navigational reference and cannot close generation publication.

For newly generated worker bytes not yet represented as an AoA artifact, publication key derives from verified committed `job_artifacts` identity (organization/job/attempt/identifier plus committed row ID/hash as defined in the worker-publication addendum; fence is provenance, not the publication key), source version and requested output slot. Bind exact exported protocol identity with DAT/CMD owner before coding; if its stable committed identity is unavailable, output-to-version integration remains blocked. The worker's versionNumber is a best-effort ordinal, not an artifact_versions identity. The common publication receipt also deduplicates first artifact creation, which a per-artifact unique key alone cannot protect. After one atomic application publication, persist artifact/version reference in the linked reply/outputRefs or durable canonical result projection. Do not wait for best-effort notification to establish publication.

- [ ] Add a real PostgreSQL concurrency test: two different keys produce sequential versions; same key/body concurrently returns the same version; changed body 409; foreign parent/asset fails; old rows unchanged. Instantiate `artifactService(db)` using the existing legacy DB/service boundary and test actual company/role authorization through the artifact route fixtures. The artifact-only test does not require `runInTenant`. A separate worker-publication test can reuse non-owner distributed setup from `artifact-transfer-commit.integration.test.ts`, composing the callback-local `tx` only when distributed rows are touched and after privileges/legacy checks are verified. Do not import that suite's private fixture helpers; extract shared setup only during implementation review.
- [ ] Serialize `addVersion` on artifact row before reading max. Inside one transaction check publication identity, validate parent/asset, insert immutable version, and increment artifact revision if canonical pointer changes. Use Drizzle `select().from(artifacts).where(eq(...)).for("update")`; unique indexes remain defense in depth. Deadlock retry policy belongs to server transaction layer, not automatic user resubmission.
- [ ] Preserve existing linear revision behavior for compatibility, but a supplied parent that is not current creates a branch candidate without replacing current. A generated alternative never auto-selects winner. Add explicit founder-only `POST /api/artifacts/:id/current-version` with input above; same-artifact check and expected revision compare under lock; 409 returns authoritative revision/current ID, not unrelated data. Audit `artifact.current_version_selected` once with old/new IDs and actor. Existing callers must be tested for this locked branch rule before enablement.
- [ ] Add stable publication key support to canonical version route/validator for manual UI and a server-internal parameter for canonical output reconciliation. Recheck founder/agent-specific existing tool authority at its actual entry point. Do not loosen human version route to permit an arbitrary agent token.
- [ ] Render version list, “Viewing version N”, current badge and “New version available” without switching a selected historical version. Compare opens two selected immutable references through E1.1 controller/E2.1 context; no duplicate artifact entity. Refinement begins from selected version with explicit parent. Download original uses that version's asset, not whichever version is currently newest.

Representative HTTP test uses actual fixture `seedCompany` and canonical routes; new parameters are proposed:

```ts
import { expect, test } from "@playwright/test";
import { randomUUID } from "node:crypto";
import { seedCompany } from "./helpers/seed-company";

test("repeated version publication returns one immutable version", async ({ request }) => {
  const company = await seedCompany(request, `E2E-Universe-Version-${Date.now()}`);
  const created = await request.post(`/api/companies/${company.id}/artifacts`, {
    data: { title: "Plan", type: "document", source: "founder", content: "v1" },
  });
  expect(created.ok()).toBeTruthy();
  const artifact = await created.json();
  const body = { source: "founder", content: "v2", parentVersionId: artifact.versions[0].id,
    publicationKey: randomUUID() };
  const first = await request.post(`/api/artifacts/${artifact.id}/versions`, { data: body });
  const second = await request.post(`/api/artifacts/${artifact.id}/versions`, { data: body });
  expect(first.ok()).toBeTruthy(); expect(second.ok()).toBeTruthy();
  expect((await first.json()).id).toBe((await second.json()).id);
  const reread = await request.get(`/api/artifacts/${artifact.id}`);
  const result = await reread.json();
  expect(result.versions).toHaveLength(2);
  expect(result.versions.find((v: { content: string }) => v.content === "v1")).toBeTruthy();
});
```

In `artifact-version-auth.test.ts`, retain real Express middleware fixture and test new current-version endpoint denies team_member/agent. Browser fixture above runs local-trusted founder and cannot prove those denials. Add concurrency test above on Linux PostgreSQL; mock chaining tests cannot establish row locking.

## E4.3/2 — Generation qualification

**Files:** Create `docs/architecture/commander-canvas/generation-qualification.md` at execution evidence stage, `server/src/services/universe-generation.ts`, `server/src/__tests__/universe-generation-outcomes.test.ts`; extend E4.2 registry with independently qualified native writers; create `ui/src/components/universe/ArtifactGenerationStatus.tsx`. Do not create a second jobs/outcomes table. If canonical output cannot bind exactly once to a version, add the linkage to its owning replatform repository with owner review, not a shadow canvas queue.

Planned internal boundary (not an invented vendor SDK):

```ts
export type GeneratedOutputDecision =
  | { state: "not_submitted" }
  | { state: "pending"; clientSubmissionId: string }
  | { state: "unknown"; clientSubmissionId: string; retryAllowed: false }
  | { state: "failed"; clientSubmissionId: string; retryAllowed: boolean }
  | { state: "published"; clientSubmissionId: string; replyMessageId: string;
      artifactId: string; versionId: string };
```

`universe-generation.ts` projects canonical outcome into this union and publishes only verified completed output via E4.1/E4.3/1. It does not infer success from notification, live stream or tool-channel delivery. Execution admission, budget reservation, cancellation and job fencing stay with canonical owners. Status GET/reconnect and preview retry have no submission capability.

- [ ] Define regression cases before adapters: lost submission ACK, accepted but timed out provider, cancelled yet late output, completed original with preview failure, duplicate worker event, source version changed while generating, permission revoked before publication, and publication failure after successful paid generation. Only an authoritative “not accepted/no effect” or a user-approved new attempt after disclosed uncertainty can permit another billable submission.
- [ ] Bind canonical request ID, accepted job/attempt/output identity and outcome query to E2.2. Pin generation inputs to selected immutable source IDs/hash. Persist output-to-version linkage atomically with version creation under publication key. Worker outputs that have not passed fenced commit/validation remain pending or rejected, never ready artifacts.
- [ ] Implement deterministic document/deck/workbook/PDF/chart writers as separate registry capabilities after fixture reopening; media image/audio/video generation adapters qualify separately. Native export is not external sharing. Export original may be read-only; creating a new export is governed processing; publication to an external service is a separately named, explicitly authorized action.
- [ ] Surface admitted/running/checking outcome/file ready/preview failed/failed/denied states without fabricated progress. Unknown shows “Checking whether the output was created” and no one-click blind retry. Closing/minimizing leaves canonical generation active; explicit cancel reports whether bytes/version already exist. No auto-camera movement on background completion.

Qualification is a blocking contract, owner unassigned, and requires these inputs for **each** provider or deterministic writer: exact package/API/model/build, license/terms, credential reference owner and runtime-key policy allowance, permitted region/retention/deletion behavior, input/output schema, maximum sizes/duration, cost/rate limits, idempotency or status-lookup semantics, cancellation guarantees, native-format validator and fixture manifest. Budget & caps owns limits; no duplicated cap editor here.

**Decision #104 reconciliation (M7):** read its August 8 amendment at `docs/architecture/decisions.md:939` onward together with `server/src/services/one-shot-sandbox-cli.ts`. The earlier blanket “only embeddings may use a hosted runtime key” is superseded for `cloud_auth`: company model-provider credentials may be materialized for sandbox-local CLI extraction. Self-hosted extraction remains installed-CLI-login based; direct/API extraction and shared-host fallback remain disallowed. That amendment is not blanket approval for a new direct hosted media API. Before enabling each image/audio/video provider, the product/security owner must record the permitted execution mode and credential boundary, and obtain a narrowly scoped policy amendment if the proposed direct API path is not covered. Providers readiness never follows from CLI login or cloud extraction credentials alone.

Image/audio/video generation and deterministic DOCX/PPTX/XLSX/PDF/chart writers remain in the agreed E4.3/2 V1 completion bar. The policy path, provider choices and accountable owners remain open qualification inputs; no generation family moves to V2 or disappears to close the gate. Deterministic/manual work can be implemented after its own prerequisites, but it cannot certify complete E4.3 or V1 while an accepted media capability is policy- or provider-blocked. Recommendation: resolve and record narrow per-provider execution/credential policy before adapter coding, then qualify actual outputs/billing/cancellation; do not revive the stale embeddings-only premise or assume the cloud amendment already approves media.

Method after authorization: produce a small valid artifact; validate magic/type/hash and reopen native file; record structural fidelity (DOCX paragraphs/tables, PPTX slide count/editable objects/notes, XLSX formulas/cache/styles, PDF pages, chart data/spec), then preview independently. For media record actual codec/container/dimensions/duration. Repeat same canonical action; sever network before/after provider acceptance and during retrieval; cancel at accepted/running/result-ready; force result retrieval/DB publication failure; revoke credential/access. Collect redacted provider request IDs, authoritative outcome observations, billing reconciliation and one-version count. A provider lacking status/idempotency must remain explicit `unknown` after ambiguous failure and cannot advertise automatic safe retry. No simulated provider can close billing/cancellation evidence.

Proposed limits, unmeasured: 1 generation/actor, 2/company subject to stricter platform/budget; 10 queued/company; no automatic paid retry; 10-minute request observation deadline then durable unknown; poll 2/5/10/30-second backoff while visible and server reconciliation according to canonical scheduler; no polling after revocation. Original outputs ≤50 MiB initially; larger provider output reports limit and preserves authorized remote outcome reference without silently downloading beyond quota. Native compare at most 2 rendered versions, 500 pages or 10,000 rows under E4.2 caps. No new retention window overrides company policy; publication keys remain for the lifetime of their immutable versions.

## UI, rollback and validation closure

Consume E8.1 theme/density/reduced motion in versions/status panels; no preference change mutates artifact current pointer. Keyboard version selection uses labelled buttons/listbox with current versus viewed text; compare has two explicit headings; export menu returns focus. At 200% text and 375px width stack compare panes or offer one-at-a-time tabs without discarding selection. Offline shows cached version identity/staleness and disables writes; reconnect refetches current metadata before action. 409 keeps historical selection and unsaved refinement, offers review/reload. Revocation clears data/fetch URLs and denies future downloads; downloaded copies cannot be recalled.

Rollback disables new generation and current-selection entry points; preserves status reconciliation, committed originals, versions and downloads. Keep additive nullable columns and old-reader compatibility. Existing sourceActionId/idempotency meanings remain. Migration rehearsal includes old inline/file versions, external URLs and parent branches; no version backfill invents a source request.

Focused commands after implementation: `pnpm exec vitest run server/src/__tests__/universe-artifact-revisions.test.ts server/src/__tests__/artifact-version-auth.test.ts server/src/__tests__/artifacts-qa.test.ts server/src/__tests__/universe-generation-outcomes.test.ts`; Playwright `tests/e2e/universe-artifact-revisions.spec.ts` with normal and authenticated configs. Run Linux real PostgreSQL concurrency and provider/writer evidence separately, then `pnpm -r typecheck`, `pnpm test:run`, `pnpm build`. Windows embedded-PG skips are not successes. All evidence is currently unrun.

Self-review findings fixed: no claim that existing max+1 transaction serializes callers; separated viewed/newest/current identity; supplied a new founder winner endpoint rather than cite a nonexistent one; kept generation unknown distinct from failed; preserved native reopening requirements. Parent self-review must reconcile exact publication-key field names with E2.2 before code approval. No independent reviewer is dispatched; TK manages Claude review of the complete packet.

**Worker review follow-up:** Consume the [exact output-identity and revocation proposal](../worker-publication-identity-revocation.md) and tightened default-deny promotion tuples in the [publication plan](../worker-publication-plan.md). Final publication and cancellation use the same credential/company/action/binding/publication lock order. Output binding is an E2.2 producer; security writer coverage and true canonical completion remain qualification gates. Check other-kind disguised forbidden payloads and forged callback selectors explicitly. Existing human-intake A is unchanged.
**Reconciliation qualification:** Follow [the qualification record](../worker-publication-qualification.md) for existing/missing CMD exports, expanded permission/credential writer coverage, one-way E2/E4 dependency verification and contention/implicit-lock evidence. Contract consistency is reviewed; production qualification is not complete.
