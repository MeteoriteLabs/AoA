# E8.2 — Complete V1 release qualification Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans only after explicit implementation/qualification approval. This plan does not deploy, publish, spend, start provider sessions or certify a release.

**Goal:** Qualify all 30 V1 slices on the integrated revision, including real providers/workers and recovery, before V1 acceptance and any V2 implementation.

**Architecture:** Join per-slice evidence to an immutable integrated source/host manifest and run connected journeys plus failure injection. Repository fixtures, real external qualification, migration/rollback and product acceptance are separate evidence classes; all required classes must close.

**Tech Stack:** Existing pnpm/Vitest/Playwright, real PostgreSQL, supported browsers and qualified voice/browser workers. No release automation or new dependency is assumed.

**Spec:** [V1 release](../release-plans/v1.md), [master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion](../motion-and-interaction.md), [states](../ui-state-review.md), [slice](../slice-plans/e8-2.md), [epic](../implementation-plans/e8-settings-release.md), [reset](../planning-reset.md), [audit](../planning-audit.md).

## Global constraints and source facts

- Two original increments, both V1. Full V1 means E0.1, E1.0–E1.6, E2.1–E2.4, E3.1–E3.3, E4.1–E4.3, E5.1–E5.2, E6.0–E6.4, E7.1–E7.3, E8.1–E8.2. E3.4 is V2 and cannot begin before accepted complete V1. No reduced release is implied by a blocked integration.
- Planning pin is `183e46a9c65fc3105c7e3d125629276814df7dbb`; final source pin will be the approved integrated tree, reconciled with actual replatform/main ancestry. Source and review evidence must be refreshed after material changes. Premature worktree/results are unapproved and excluded.
- At the source pin, root `package.json` has `test:run`, `build`, `typecheck`, `test:e2e`, `test:e2e:real-provider`, and `test:release-smoke`. `tests/e2e/playwright.config.ts` starts a dedicated app at port 3199 by default with fake CLI/provider fixtures. Passing it cannot prove live-provider functionality.
- That config documents Windows embedded-PG/administrative runner limitations and external `DATABASE_URL`/`AOA_E2E_FORCE_WINDOWS=1` routes. Real DB revision tests also skip Windows encoding issues. Linux is required for authoritative full-suite results; Windows user-path verification is still required where supported. Never claim a skipped suite passed.
- Existing runtime-key restrictions need E3 VOICE amendment and capability credential evidence before enabled sessions; CLI readiness does not prove voice readiness. Budget & caps owns spending limits. Commercial/managed-service approval is independent of technical support.

## E8.2/1 — Integrated evidence

**Files:** Create `tests/e2e/universe-release.spec.ts`, `docs/architecture/commander-canvas/release-evidence.md`, `docs/architecture/commander-canvas/release-evidence-manifest.json`, and `docs/architecture/commander-canvas/implementation-results/e8-2.md`. Create `tests/e2e/universe-release-real.spec.ts` only in the separately qualified real-provider configuration after verifying its actual auth/setup restrictions. Modify existing CI configuration only if needed for an approved separate credential-enabled qualification lane; no new paid run on ordinary PR events.

**Evidence interface (proposed artifact schema):**

```ts
type EvidenceStatus = "pass" | "fail" | "blocked" | "not_run";
interface UniverseReleaseEvidence {
  schemaVersion: 1;
  sourceSha: string;
  replatformSha: string;
  mainMergeBaseSha: string;
  generatedMigrationChecksums: Record<string, string>;
  environment: { os: string; browser: string; node: string; pnpm: string;
    deploymentMode: string; workerBuild: string | null;
    providerModels: Record<string, string>; testedAt: string };
  slices: Record<string, { increments: Record<string, EvidenceStatus>;
    fixtureEvidence: string[]; integrationEvidence: string[];
    residualLimits: string[]; reviewer: string | null }>;
  gates: Record<string, { status: EvidenceStatus; owner: string | null;
    upstreamEvidence: string[]; consumerEvidence: string[] }>;
  productAcceptance: { status: EvidenceStatus; acceptedBy: string | null };
}
```

Evidence references are stable artifact paths/checksums or immutable CI URLs, not transient console screenshots alone. Include failure seed, account class (never secrets), concrete expected/actual IDs and redacted traces. Empty reviewer/owner means unassigned and cannot close a gate. No existing gate state is trusted merely because an old planning register described it as closed. E0 refreshes BASE/DESIGN/CMD/BROWSER/CLOUD/PROFILE/APPROVAL/VOICE/HOST and the owning replatform full release result at the final tree.

**Required per-slice coverage matrix:** Every row includes all increments from its coding plan plus failure/recovery, settings and relevant visual review. This table identifies the decisive connected check; it does not replace the detailed unit/integration tests.

| Slice | Decisive evidence at integrated revision |
|---|---|
| E0.1 | Accepted base ancestry, tenant contract map, collisions/gate owners, migration ordering and rollback plan; refresh after upstream sync |
| E1.0 | Remaining loading/error/offline/revoked/conflict/narrow UI states; original task-entry defect reproduced/fixed in actual host |
| E1.1 | Real Task/artifact/browser panel drag/resize at zoom/overlap; one controller; all lifecycle operations and focus |
| E1.2 | Two-tab CAS layout conflict, server failure and reload recovery; no stale whole-layout overwrite |
| E1.3 | Text typed after Send survives response/reload; destination/company drafts and attachments restore safely |
| E1.4 | Stationary logo, one menu, expanded top tray, collapse/auto-hide, full registry/overview paging and hidden rails |
| E1.5 | Compact/expanded/maximized/tucked chat, independent blob/captions/voice, retained draft/cursor and recoverable controls |
| E1.6 | Requested focus/fit cancellation, pinned/manual items, all resize edges and viewport settings; OS reduced motion |
| E2.1 | Selected Task/artifact version/context matches actual request, stale/revoked context refused, canonical conversation identity |
| E2.2 | Lost ack/repeated send ->one admitted execution; mismatch conflict; unknown outcome read; real CMD routing/results |
| E2.3 | Duplicate/out-of-order/lost/replay-truncated events recover through authorized snapshots, no false completion |
| E2.4 | Same Task from Work, Inbox, Needs you, artifact source and Open panels; fixed source reply input and permissions |
| E3.1 | Real OpenAI explicit connect, scoped credential/session, transcript/tool handoff and budget/usage evidence |
| E3.2 | Interrupt/mute/silence/end/reconnect independent of Task cancel; actual input/output states and retained conversation |
| E3.3 | Gemini and ElevenLabs individually: supported/validated voices/languages, tool handoff, reconnect/revocation; no parity inferred |
| E4.1 | Authorized manual upload/retry/cancel, immutable identity and cross-company denial, original preserved |
| E4.2 | Entire accepted format matrix: parse/preview/edit/export independent claims, damaged/locked/oversized/unsupported fixtures |
| E4.3 | Source/provenance/immutable revision, branch choice, generated output unknown-outcome recovery without duplicate paid work |
| E5.1 | Every reviewed block schema/trusted renderer, local actions versus governed write, retained input and failure fallback |
| E5.2 | Real isolated origin/CSP/channel allowlist, arbitrary generated code denied privilege, revocation/resource/teardown checks |
| E6.0 | Real Browser Use compatibility with containment, local/cloud qualified transport; no public debugging bypass |
| E6.1 | Admitted browser command/result authority, canonical approval, timeout/reconciliation, no repeated uncertain clicks |
| E6.2 | Real cloud stream and control handoff; stale controller denied, lost stream/reconnect, truthful capability disclosure |
| E6.3 | Actual outbound local worker/tab-only stream, owner/epoch fencing and disconnect; no desktop leakage |
| E6.4 | Session lifetime/file transfer/profile reuse each qualified; encryption/TTL/purge/audit/revocation evidence, not signed-out substitution |
| E7.1 | Conversation create/edit/pause/run, same request idempotency, real timezone/DST/overlap and scheduler claim-crash recovery |
| E7.2 | Every terminal source kind's durable marker, lost all UI events, restarted drain, one authorized follow-up and revocation |
| E7.3 | Silent durable items, legacy migration, safe catch-up, same source answer, quiet policy and cross-device sound/speech ownership |
| E8.1 | Every retained field consumed, scope/reset/migration/failure; provider readiness truthful and Budget & caps separate |
| E8.2 | All rows/gates joined, full commands, integrated journeys, rollback and explicit product V1 acceptance |

- [ ] Freeze candidate manifest after approved integration. Record `git rev-parse HEAD`, actual main/replatform ancestry and clean scoped diff; preserve unrelated files. Inspect manifest/package/lockfile consistency (changes travel together) and generated migration ledger. Do not invent source SHA or treat remote-tracking refs as fresh deployed evidence.
- [ ] Run repository commands separately and attach full logs/exit codes: `pnpm -r typecheck`, `pnpm test:run`, `pnpm build`. Run on authoritative Linux and relevant supported Windows environment. If baseline fails, compare same command/environment on exact accepted base; identify failing test/error and changed dependency rather than grant blanket baseline waiver. A new or materially affected failure blocks release; an unrelated pre-existing failure requires explicit evidence/disposition by owner and product release reviewer, not a silent pass.
- [ ] Create deterministic connected test in standard fixture config: explicit typed request →Task→artifact, all five Task entry routes, send then keep typing, reply error/conflict, minimized state across reload, source acknowledgement. Reuse actual test helper setup already in `tests/e2e`, selected by reading neighboring Task/Commander tests at final tree; do not add a fake helper import or claim fixtures exist by name. The new test file exports no production API. Representative real-UI assertions after the fixture has selected its seeded company and opened Universe:

```ts
import { test, expect } from "@playwright/test";
import { cleanupTestCompanies, seedCompany } from "./helpers/seed-company";
test.afterEach(async ({ request }) => {
  await cleanupTestCompanies(request, /^E2E-Universe-NoCapture-/);
});
test("visual toggles do not start capture", async ({ page, request }) => {
  const company = await seedCompany(request, `E2E-Universe-NoCapture-${Date.now()}`);
  let mediaCalls = 0;
  await page.exposeFunction("recordUniverseMediaCall", () => { mediaCalls += 1; });
  await page.addInitScript(() => {
    navigator.mediaDevices.getUserMedia = async () => {
      await (window as unknown as { recordUniverseMediaCall(): Promise<void> })
        .recordUniverseMediaCall();
      throw new Error("Unexpected capture from a presentation action");
    };
  });
  await page.goto(`/${company.issuePrefix}/universe`);
  await page.getByRole("button", { name: "Commander options", exact: true }).click();
  await page.getByRole("menuitemcheckbox", { name: "Blob", exact: true }).click();
  expect(mediaCalls).toBe(0);
  await expect(page.getByRole("textbox", { name: "Message Commander" })).toBeVisible();
});
```

`seedCompany`/`cleanupTestCompanies` and `company.issuePrefix` navigation are verified in pinned `tests/e2e/commander-error-states.spec.ts`; `ui/src/App.tsx:424` registers the `:companyPrefix` parent. The child `/universe` and accessible selectors are proposed contracts E1/E8 must implement; the test fails until they exist. `Commander` is the primary chat toggle; the adjacent `Commander options` opens the Chat/Blob/Captions menu. This test opens options then toggles Blob and asserts chat remains visible. This specimen proves presentation consent boundary only. It cannot qualify microphone capture, provider audio, task dispatch or reset persistence. Add actual full connected tests for those outcomes rather than stretch its claim.
- [ ] Run `pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/universe-release.spec.ts`. Initially tests fail because required integrated behavior is absent; fix owning slices and run again, record traces on failures. Do not globally disable failures or reduce V1 expectations to make a smoke test green.
- [ ] Qualify external lanes independently: first inspect `tests/e2e/playwright.real-provider.config.ts` and existing real-provider helpers for auth/environment contract. Use a dedicated disposable company with scoped credentials and approved spend cap; list exact provider model/API revision, browser worker build, account/tier and region. Run E3 and E6 protocols from their coding plans, capturing actual microphone/playback, source result IDs, stream/controller epochs, reconnect and revocation. Fake CLI harness is not allowed in this evidence class. Tests do not send messages/publish external changes unless explicitly authorized by the qualification fixture action.
- [ ] Refresh gate owners and evidence: CMD real per-user ownership/routing/lost ack/result; BROWSER launch containment/streaming; APPROVAL canonical decision and stranded-answer recovery; CLOUD final tier policy/credential boundaries/disclosure; PROFILE actual encryption/retention/purge/access; VOICE amendment/capability/credentials/each provider; HOST real isolation; BASE/DESIGN final source/host. Old September 11 finding status is a lead, not a present verdict. Open important finding blocks its consumer row and full V1 acceptance.

## E8.2/2 — Release and recovery rehearsal

**Files:** Extend `tests/e2e/universe-release.spec.ts` with deterministic failure cases; add `tests/e2e/universe-release-recovery.spec.ts` and `docs/architecture/commander-canvas/release-recovery-runbook.md`; update evidence manifest/result. Qualification scripts are added only when the exact owned service supports bounded fault injection; do not add unsafe arbitrary production chaos endpoints.

**Integrated qualification protocol:** Run on a disposable production-shaped database/storage/worker environment, never the user's main company. Record original state by canonical IDs and hashes, not screenshots alone. Inputs: two companies, two human roles plus unauthorized caller, permitted agents, qualified OpenAI/Gemini/ElevenLabs connections, local/cloud browser sessions, signed-out and saved-profile cases, every accepted format fixture, a standing routine and follow-up. Test actions are explicit and must obey normal budget/approval. Deployment/worker/provider configuration and cleanup method are recorded before activation.

| Scenario / action | Required observation and failure disposition |
|---|---|
| Cross-company user swap and token replay | 401/403/404 as contract, no content/body/thumbnail/event/credential/readiness leak; clear accessible caches at revocation; expected security denial remains operator-only |
| Revoke while typing, provider session, worker stream and queued follow-up active | Immediate access enforcement; no unauthorized pending dispatch/click/playback; recoverable user draft per policy; already authorized task status remains canonical |
| Drop acknowledgement after Task/approval/artifact/routine commit | Read outcome by canonical key first; one mutation; newer typing remains; no retry of uncertain external side effect |
| Drop/truncate/duplicate/reorder all live events and restart replica | Replay when available; retention floor forces snapshot repair; UI converges without claiming best-effort append as outbox; follow-up still executes from durable marker |
| Kill converter, renderer and browser stream independently | Original artifact intact; renderer failure not task failure; browser input disabled while stale; restart only affected stage, no regenerated successful output |
| Control takeover then old controller delayed input | Canonical epoch rejects stale input; human typing waits for acknowledged ownership; reobserve before agent resume |
| Rapid panels/gesture cancellation | Pointer lost capture/blur/cancel releases shield, last valid state wins, no phantom panel/overlay; controls/focus/drafts remain |
| Two tabs edit layout/preferences/answer | CAS conflict retains both versions/edits, one canonical answer, no whole-state overwrite, no other-company cache reuse |
| Scheduler restart after claim / source terminal / admitted follow-up | One occurrence per scheduled instant, one run per follow-up ID, no lost due work; current permissions applied |
| Silent/digest/quiet hours/DND with two devices | Useful hub records preserved, optional speech/sound respects explicit consent, at most one unsolicited attempt; direct replies still work |
| Finish/close/return/break | Explicit checkpoint only, close never Finish/cancel, arrival never grants routine authority; return shows visible changes since acknowledged checkpoint |

- [ ] At 1440×900, 1024×768 and 390×844 CSS pixels, run the accepted full motion/state checklist with keyboard, pointer, touch, 200% text scaling and OS reduced motion. All controls reachable; measured tray/composer/captions determine usable bounds; hidden rails still reachable through tray/Inbox; bottom-right question cannot cover input. Screenshots and video accompany state assertions but cannot substitute for them. Selection tint differs from focus ring and task header never gains prohibited white selection outline.
- [ ] Measure workload using 100 panels in the registry (mix of Task/artifact/browser references), at most the implementation's qualified simultaneous live browser/voice/converter sessions. Do not start 100 paid streams to test registry capacity. Over 60 minutes, perform 200 open→minimize→restore→maximize→close cycles, 50 conversation switches and 20 connect/end cycles per enabled session kind within agreed spend limits. Sample JS heap/listeners/DOM nodes, worker process/session count and server queues every five minutes after warm-up; retain raw series and cleanup timing. Proposed acceptance: no monotonic retained session/listener growth, zero active mic/audio context after explicit End within five seconds, zero leftover view-owned workers after documented session shutdown deadline, no lost drafts/unacknowledged actions. Compare equal workloads before/after; report actual supported live counts separately from registry size.
- [ ] Measure input-to-paint/frame time on specified baseline device and browser, use E1 motion tokens (180–280ms typical transitions) as animation intent, not performance guarantees. Proposed targets: p95 pointer/input response <100ms, no UI long task >200ms during steady dragging in qualified mix, interactions remain cancellable. Any target miss is investigated, measured limits disclosed and qualified workload adjusted only through engineering/product review; do not claim 100-panel fluidity from a static rendering screenshot.
- [ ] Fresh database: apply all generated migrations, run old/new schema/readers compatibility suite, seed every new state and verify app starts. Upgrade database: restore a sanitized pre-Universe backup in disposable environment; record row counts and hashes of canonical conversations/tasks/artifacts/routines/Secrets and new preferences/drafts/layout/receipts; migrate; verify existing saved choices and no invented follow-up consent. Reapply allowed idempotent backfills; no duplicate rows. Account for concurrent writer cutover and schema lock time. No schema DDL is manually authored.
- [ ] Rehearse feature-disable while requests, voice and browser are active: hide Universe entry; stop new Universe submissions/sessions; close voice contexts; follow canonical browser/work lifetime and owner rules; keep admitted Tasks running unless explicitly cancelled; keep drafts/layout/receipts and artifacts accessible on re-enable. A single UI flag cannot alone enforce server admission: verify disabled new endpoints/session creation reject appropriately. Existing canonical app routes remain authorized and usable.
- [ ] Rehearse version rollback only to the documented compatible server/client build with all additive data intact. E7 scheduler/follow-up drains require quiescence and receipt reconciliation before legacy dispatch can resume, since old scheduler can duplicate intervals; inspect pending/admitted occurrence inventory, not just row counts. Old notification readers must not re-enable sound/voice or undo explicit silent intent. If schema or execution contract is incompatible, prefer feature-disable and forward repair; do not invent a destructive down migration.
- [ ] Re-enable approved build and compare all IDs/hashes; pending unknown outcomes reconcile before retry; no current conversation/work lost and no stale controller accepted. Restore backup only in the disposable test environment to verify recoverability and state expected recovery point/time from actual measurements. Production rollback/restore remains a separate explicit deployment authorization.
- [ ] Run full repository checks again only if implementation/recovery fixes changed code or new findings require them; otherwise preserve prior same-SHA evidence. Review each failed/blocked/not_run row with actual owner and exact scope. Remaining V1 blocker stops V1 acceptance and V2 start; unrelated independent V1 repairs/qualification may proceed.

## Exit artifact and review gate

`release-evidence.md` leads with complete/incomplete V1 status, then matrix linking all increment, external gate, UI, migration/rollback and full-check evidence. Record tested revision/build/provider versions, supported workload/format limits, unresolved risk and exact reviewer disposition. Missing live qualification, empty owner, fixture-only successful provider or skipped full suite is never “pass.” The result must include a concrete reproduction and owning fix for every important finding; material fixes invalidate affected evidence and require re-review.

The user manages Claude's independent review after the full detailed plan and self-review; do not run reviewer CLI/agents or treat this document as that review. After implementation, engineering and product acceptance must explicitly record all V1 obligations complete. Only that actual V1 acceptance permits consideration of E3.4 implementation. Commercial rollout, publishing and deployment are separate decisions and are not inferred from passing tests.

**Self-review:** Both original increments retain complete V1 scope and distinguish unit fixtures, real upstream/runtime qualification, UI evidence and deployment acceptance. Explicitly covers all 30 slices, each provider, both browser locations and saved profiles, uncertainty/revocation, all settings, long-session measurement and data-safe rollback. Concrete numbers are proposed qualification inputs/targets rather than measured claims. No runtime checks have run during this planning pass; full release readiness is not claimed.
