# E6.1 Governed browser session ownership implementation plan

> **For agentic workers:** Use `superpowers:executing-plans` after explicit implementation approval and closed runtime bindings. No code, worker, provider or branch action is authorized by this document.

**Goal:** Associate each browser task with its admitted worker/session and preserve canonical approvals and uncertain outcomes.

**Architecture:** Universe projects the canonical execution owner's state. The worker alone executes browser commands under its live lease, controller epoch and scoped approvals. Panel selection, saved profile identity and visibility are presentation/account references, never shared live control.

**Tech stack:** Existing tenant repositories, worker protocol, browser runtime, TypeScript/React; no new scheduler or standalone browser execution queue.

**Spec:** [E6.1](../slice-plans/e6-1.md), [E6.0 contract and qualification](e6-0.md), [integration §2](../integration-contracts.md), [request contract](../request-contract.md), [settings](../settings-contract.md).

## Evidence and scope

V1, two original increments; planning complete for review; admission/adapter/control delivery binding blocked. Source `183e46a9c65fc3105c7e3d125629276814df7dbb`:

- `server/src/services/job-submission.ts:116` exports `submitJobWithinTenant`; `:415` composes it inside `runInTenant`. Preserve this tenant/admission/idempotency owner instead of inserting job rows from a canvas route.
- `server/src/services/browser-job-config.ts:123` `normalizeBrowserJobInput` validates the frozen workload; default 900 seconds, server ceiling 3,600, frozen ceiling 43,200. Its allowed fields exclude cookies/control tokens.
- `server/src/services/run-execution-owner.ts:245` `RunExecutionOwnerResolver` and `:278` `createRunExecutionOwnerResolver` are task-run routing infrastructure, not a generic Commander/browser cutover. E10-F001 names missing non-task routing and credential gaps.
- `packages/browser-runtime/src/run-session.ts:139` performs one navigate/download lifecycle; `approval.ts:107` defines `ApprovalResolver`. Enabling an injected test resolver does not establish upstream delivery.
- E8 findings E8-F001 `:3` remains open/unowned; E8-F004 `:651` states existing `agent-runtime-decisions.ts` stranded-answer sweep inner-joins heartbeat `run_id` and excludes distributed decisions. Removing that correct join does not implement a distributed recovery owner.
- `server/src/__tests__/browser-job-config.test.ts` supplies exact frozen browser workload fixtures; `packages/browser-runtime/src/__tests__/run-session.test.ts` supplies recording-driver/port/order fixtures. These pure fixtures cannot prove a governed production command path.

**Files:** Create `server/src/services/universe-browser-sessions.ts` (authorized projection/orchestration over accepted upstream ports), `server/src/__tests__/universe-browser-authority.test.ts`. Reuse E6.0 shared descriptor/validators; API placement and upstream production producer edits are conditional on qualification inventory. Do not mint a guessed `/browser/start` endpoint. Add an API client in `ui/src/api/universe-browser.ts` only once route placement/auth contracts are accepted. No table is assumed necessary; if canonical lifecycle metadata lacks required fields, its owner supplies additive schema/API/migration before consumer implementation.

## E6.1/1 — Admitted session ownership

**Consumes:** Canonical task authorization/admission, job/attempt/worker identity and E6.0 qualified automation port. **Produces:** `BrowserSessionDescriptor` from E6.0 with actual task/account/placement/controller/capabilities. Proposed application projection `readBrowserSession(companyId: string, actorId: string, browserSessionId: string): Promise<BrowserSessionDescriptor | null>` is read-only; actual authenticated actor is server-derived. No descriptor value grants an execution capability.

- [ ] **1. Bind source-to-runtime relationships.** Trace the exact approved route/service from a user's authorized task request to admitted job, active attempt/lease, browser process and observable session. Record ID classes, foreign-key relations, source kind, actor/role checks and revocation owner. Verify where admission rejects unsupported browser targets and where legacy execution is suppressed. If task_run-only routing cannot represent the request, file/retain the specific upstream blocker; do not cast it to another source kind.
- [ ] **2. Write the negative admission fixture.** Reuse real tenant construction from upstream job-submission integration tests after reading its then-current setup, plus the frozen browser workload from `browser-job-config.test.ts`. Seed company A tasks T1/T2 and company B task T3, separate authorized/unauthorized members and worker bindings. An unauthorized request must refuse before normalization/provider lookup/launch; a malformed request from an authorized actor returns schema error. Add budget hard stop, paused target, unavailable worker and stale lease cases: zero browser launch each. This fully specified protocol cannot be reduced to a mock auth function returning true.
- [ ] **3. Implement projection after admission.** Create/recover one default live browser session per task and admitted execution attempt, with owned tabs inside that session. Two concurrent requests with the same canonical identity return the same session or explicit pending status; changed execution payload conflicts using canonical job submission semantics. Distinct tasks cannot share controller state even when they intentionally reference the same saved account. Source process restart gets a new session unless the owner proves actual continuation.
- [ ] **4. Wire display without authority side effects.** Read the descriptor and show task, account label or signed-out, local/cloud location, active controller and status. Opening/restoring/selecting a BrowserPanel only reads/attaches permitted view; it never submits a new task, obtains control or resumes automation. Unknown worker capability displays unavailable with reason. E8.1 view-quality preference affects stream negotiation only, never execution budget/lifetime.
- [ ] **5. Verify concurrency and scope through actual routes.** Race two starts for T1, then launch T2; expect one T1 session, one T2 session, disjoint controller epochs and explicit canonical attempt links. Change selected panel rapidly, minimize/close/reopen and inspect unchanged session/task IDs. Attempt cross-company reads and stolen session ID; canonical non-disclosing denial precedes metadata/frames. Revoke task access while panel open; detach its view/control without changing another task. Record DB IDs, worker launch count and UI instance identity together.

**Failure/recovery:** Starting stays pending until authority acknowledges; lost acknowledgement triggers read-only reconciliation, not a new submission. An ended session has an explicit ended state; “Start new” is separate admitted work. A source task deleted/denied produces safe inaccessible state and closes attachment. No renderer cleanup calls task cancel.

## E6.1/2 — Approval and recovery

**Consumes:** E8-F001 canonical authority resolution, E8-F004/job-control delivery recovery, E6.0 worker-side epoch checks. **Produces:** Correlated approval status and confirmed/unknown action outcomes, presented through canonical Inbox/Needs you surfaces. Proposed action outcome projection: `{ commandId, browserSessionId, outcome: "pending" | "confirmed" | "refused" | "unknown", canonicalResultRef: string | null }`; exact persisted authority and route come from upstream, not a second Universe decision table.

- [ ] **1. Close the approval binding with producer reviewers.** Record the actual decision table/service/type, scoped intent, answer actor checks, one-shot consumption, command delivery acknowledgement and stranded-answer sweep. Reconcile frozen journey `browser_request` authority with shipped runtime-decision authority at the same revision. E8-F004 needs actual delivery-hop recovery; a local resolver or editing an inner join cannot close it. Named owner remains unassigned until the upstream reviewer accepts responsibility.
- [ ] **2. Extend recording-driver tests for ordering.** Use `run-session.test.ts`'s real `BrowserDriver` interface to record `approval_requested → allowed → navigate/saveAs`. For deny, missing resolver, expired answer and timeout expect no effect call. Default-on policy for the new production path must be explicit in its composition root; preserve historical tests of existing default off rather than falsely saying all existing browser code enforces approvals. Existing 60-second timeout is an observed runtime default, not a promised founder-response window.
- [ ] **3. Bind runtime approval UI to existing authority.** Show the canonical pending decision in Inbox/Needs you and BrowserPanel; responding uses its route/authorization, not a parallel Universe “allow” button state. Approval receipt is distinct from worker application. Rejected/expired/undelivered answers stay visibly final or pending according to source; user must not see “resumed” before worker acknowledgement. Keyboard focus returns after acknowledged response, no animation or panel selection consumes an approval.
- [ ] **4. Execute the uncertain mutation protocol.** Add a disposable fixture form endpoint that persists a counter and operation receipt independently of the client response. Submit once, allow mutation to commit, drop browser/worker response, then reconnect. Canonical action outcome is unknown until a read/receipt proves it; counter must remain one. Never replay a click/form submission based on connection loss. Resume re-observes current page plus canonical task/action state; unresolved external effect requires a human decision with explicit uncertainty. If the target offers no observable receipt, the result remains unknown rather than “failed, retrying.”
- [ ] **5. Execute stranded-answer and takeover races.** Answer while worker disconnects, answer after task/attempt terminal, deliver duplicate answer, revoke actor before answer applies and enqueue an agent action then transfer controller. Expect zero stale action, no consumed answer accidentally granted to another attempt, and a durable recoverable/terminal status from the upstream sweep. Test manually allowed harmless action with actual local/cloud worker; pure injected resolver tests are insufficient.

**Commands after authorization and new tests:** `pnpm exec vitest run server/src/__tests__/universe-browser-authority.test.ts`; `pnpm --filter @armyofagents/browser-runtime test:run`. Real UI uses E6.2/E6.3 E2E journeys. Red cases are observable duplicate launch, action-before-approval, scope leak or blind replay; deliberately remove each guard to confirm meaningful tests during implementation review when permitted. No test ran for this plan.

## Limits, observability and rollback

One live session per task/attempt is the proposed default, not a measured capacity guarantee; worker concurrency stays canonical policy. Do not extend source 900/3,600/43,200-second TTL bounds through panel activity. Proposed view/read request timeout ten seconds displays stale/unavailable without altering task state. Execution and approval timeout/retry policy belongs to upstream owners and must be bound before runtime coding. Preserve immutable artifacts and UI Task/API issues naming; schema additions require Drizzle generation and synchronized tenant contracts.

Audit admitted/refused session association, approval reference/status, controller change and effect outcome with company/task/job/attempt/worker/session/epoch/command IDs; no typed input, secret URLs, cookies or screenshots. An audit event is not effect confirmation. Rollback disables new browser admissions, releases view/input, and drains/cancels only through explicit canonical lifecycle operations; preserve unresolved outcomes and committed results. Never use old legacy MCP browser execution as fallback.

## Self-review and remaining dependencies

Both original increments mapped. Fixed mistaken equivalences: saved account ≠ live session; view selection ≠ takeover; allowed decision ≠ applied worker command; disconnected form ≠ failed effect. Upstream BROWSER/APPROVAL and relevant CMD/routing remain blocking, with exact source mapping/real-worker evidence required. CLOUD gates cloud placement; PROFILE gates saved login reuse in E6.4. Independent projection/fixture planning can proceed; no current implementation permission. Full Linux typecheck/tests/build required at handoff, with Windows limitations reported per E6.0.
