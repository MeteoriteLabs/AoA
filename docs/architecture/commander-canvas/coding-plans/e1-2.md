# E1.2 — Revisioned canvas persistence implementation plan

**Status:** Review candidate; schema and tests are proposed, unrun. **Version:** V1. **Spec:** [slice](../slice-plans/e1-2.md), [state contract](../state-contract.md), U04–U05. **Architecture:** owner-scoped snapshots plus atomic operation receipts; renderer measurement never overwrites preferred geometry. **Stack:** Drizzle/PostgreSQL, shared Zod, React Query.

## Source and file ownership

Pinned source: `ui/src/hooks/useHomeBoardLayout.ts:15` and `server/src/services/home-board-layout.ts:38` save a whole array by user/company, without expected revision. Reuse query cancellation/error presentation patterns, not this write contract. `internalAgentConversations` at `packages/db/src/schema/internal_agent.ts:184` has company/user ownership. `goalRoutes` supplies route validation/access/audit conventions. These presentation tables use the company-scoped application boundary; replatform's frozen distributed-execution RLS table set is not automatically extended to UI state. See the exact boundary in [bindings](../implementation-bindings.md).

Create `packages/db/src/schema/universe.ts`, `packages/shared/src/validators/universe.ts`, `server/src/services/universe-state.ts`, `server/src/routes/universe.ts`, `ui/src/api/universe.ts`, `ui/src/components/universe/useUniverseState.ts`. Modify `packages/db/src/schema/index.ts`, shared validator/type exports, `server/src/services/index.ts`, `server/src/routes/index.ts` and `server/src/app.ts`. Do not edit Home persistence or widen `tenantRepositories` for personal UI state. Generate schema migrations with `pnpm db:generate`, never authored schema SQL.

## Contract

Proposed exported service `universeStateService(db)` has `get(scope)`, `apply(scope, patch)` and `getReceipt(scope, operationId)`, plus `getCheckpoint(scope,key,sourceVersionId)` and `saveCheckpoint(scope,key,input)` as specified below. `scope={companyId,userId,conversationId}` is derived from authenticated request and canonical conversation ownership, not body-supplied actor fields. V1 personal layout writes are owner-only even when another user can read a shared conversation. Register GET/PATCH `/api/companies/:companyId/universe/conversations/:conversationId/layout` and GET `.../layout/operations/:operationId`. Missing/inaccessible conversation returns the existing not-found convention; invalid operation 400, stale revision or reused ID/content mismatch 409.

```ts
export type LayoutOp =
  | { type:'open'; key:string; ref:{kind:string;id:string;version?:string}; rect:Rect }
  | { type:'geometry'; key:string; rect:Rect }
  | { type:'pin'; key:string; value:boolean }
  | { type:'minimize'; key:string; value:boolean }
  | { type:'close'; key:string }
  | { type:'order'; keys:string[] }
  | { type:'presentation'; selected:string|null; maximized:string|null }
  | { type:'viewport'; x:number; y:number; zoom:number };
export type Rect = {x:number;y:number;width:number;height:number};
export type LayoutPatch = {schemaVersion:1;operationId:string;expectedRevision:number;operations:LayoutOp[]};
export type LayoutAck = {operationId:string;revision:number;schemaVersion:1;
  opened:Array<{operationIndex:number;key:string;openedOrdinal:number}>;nextOpenedOrdinal:number};
```

Use a strict discriminated Zod union; `kind` must be the accepted reference-kind enum, not arbitrary render code. Ref authorization is checked before inclusion/preview; no content bodies, credentials, raw audio, cookies or live control tokens in layout. Proposed initial transport bounds: 200 records, 50 operations/patch, 64 KiB patch, 256 KiB snapshot, finite coordinates within ±1,000,000 and dimensions 1–8192 canvas units. Zoom 0.25–2 follows the reference controller; bounds require host qualification, not capacity claims.

### Complete layout write/read contract

GET returns E1.1's `AuthorizedLayoutSnapshot`, including `viewport:{x,y,zoom}`. Store CSS-pixel camera translation x/y within ±1,000,000 and zoom 0.25–2 separately from canvas-unit panel rectangles. A missing layout returns an authorized revision-zero snapshot with counter 1, default camera {x:0,y:0,zoom:1}, empty panels/order and null selected/maximized; GET creates no row. Resolve authorized reference titles at read time rather than trusting client-supplied display text.

Apply operations in input order to a transaction-local candidate; publish only after final invariant checks. Preserve these semantics across the server applier and E1 controller:

| Operation | Candidate-state effect |
|---|---|
| open | Validate key equals the scoped canonical reference key; new panel gets supplied normal rect and next ordinal. Existing panel keeps normal rect/ordinal, becomes visible and foreground. Both select it, move it to end of order and exit another panel's maximized mode, matching E1.1 foreground. |
| geometry / pin | Update only that field on the existing panel; geometry cannot modify a minimized/maximized target. Presentation does not imply execution permission. |
| minimize true | Hide target; clear maximized if it names target; if selected target, select last visible panel in order or null. Retain normal rect, ordinal and order membership. |
| minimize false | Restore target in normal mode, foreground/select it and clear maximized, matching E1.1 restore. |
| close | Remove only target and its order entry; clear its maximized state and choose next visible selection when necessary. No checkpoint, draft, task or artifact deletion. |
| order | Set the full z-order permutation; never rewrite ordinals or infer selection from an arbitrary order change. |
| presentation | Explicitly set selected/maximized as one pair. Non-null selected must exist and be visible; non-null maximized must equal selected and be last in order. Maximize is sent after any required restore/order operation, in the same patch. Null maximized restores normal display without changing normal rect. |
| viewport | Set only validated camera x/y/zoom; no geometry or focus change. User camera movement is disabled while maximized by the controller; a save batch may include a completed pre-maximize camera change followed by maximize. Persist that camera so restoring from maximize returns to it; do not infer an unauthorized live gesture merely from the final maximized state. |

Require final order to contain every existing panel exactly once, including minimized panels; no missing, foreign or duplicate keys. Validate the selected/maximized pair and geometry/camera bounds on the whole candidate. Invalid patches return 400 without any partial write, receipt or audit. Failed expectedRevision or reused operation identity remains 409. Transient intermediate presentation mismatch while composing restore/order/presentation is allowed only inside the atomic candidate; no invalid state becomes externally visible.

E1.2's UI adapter batches a focus change as order + presentation; maximize as visibility restoration if needed + order + presentation; restore from maximize as presentation with maximized null and preserved normal rect, and restore from minimize as minimize(false) plus final order/presentation if needed. The adapter records affected presentation fields together for conflict handling. Camera, order, selection and maximize writes participate in the same expectedRevision/operationId receipt protocol as geometry, not a second unfenced save endpoint. Safe rebase additionally validates the entire candidate: selection/maximize/order form a coupled conflict group, and camera plus maximize must remain compatible. If concurrent changes violate these rules, retain the local candidate and require resolution rather than overwriting it.

- [ ] In `universe-state.integration.test.ts`, save two panels, nondefault viewport, z-order, selection and maximized state in one batch; GET must return all those values and original normal rectangles. Restore then reload; geometry and ordinal order remain intact. Test focus-only persistence, null selection, minimize/close of selected/maximized target, invalid hidden/foreign target, non-permutation order, viewport bounds and a failed multi-operation batch with no receipt/audit.
- [ ] In `tests/e2e/universe-persistence.spec.ts`, exercise nondefault pan/zoom→focus→maximize→save→reload→restore, then lost acknowledgement and concurrent focus/camera changes. Verify snapshot hydration emits zero PATCH requests, callbacks cannot cross scope, and small-screen fitting does not replace the acknowledged preferred layout/camera. These are planned checks, not executed evidence.

## E1.2/1 — Persistence transaction

- [ ] Define `universe_layouts`: UUID id, companyId, userId, conversationId, schemaVersion=1, revision bigint=0, JSON document (viewport, order, selected, maximized and panel records including immutable openedOrdinal), nextOpenedOrdinal bigint=1 and timestamps; unique owner/company/conversation. Define `universe_layout_operations`: layoutId/companyId, operationId, canonical payload hash, acknowledged revision, complete bounded LayoutAck and timestamp; unique(layoutId,operationId). Export both, generate migration and check FK/cascade behavior on a disposable migrated database. Every service query carries companyId and authenticated userId; receipts are reached through their authorized parent layout. Never use an operation UUID as standalone authorization.
- [ ] Authenticate/access-check before receipt reads. In one company/owner-scoped Drizzle transaction ensure owner row exists (`onConflictDoNothing`), lock/read it, then look up receipt before checking expectedRevision. Equal receipt/hash returns original ack, including ordinal mapping/counter; different hash conflicts. Otherwise require matching revision, authorize refs, apply validated operations (allocate the next openedOrdinal only for a genuinely new key, advance the counter under the same lock; existing open/focus/minimize/restore keep it; close/reopen gets a new value), update revision and insert receipt atomically. A failed transaction produces neither receipt nor partial geometry.
- [ ] Use existing `insertActivityLog(tx,input)` from `server/src/services/activity-log.ts` in the transaction for a successful new operation, then `publishActivityLogged(persisted)` after commit. Receipt replay does not create repeated domain work. Never log text/artifact contents. Publication failure cannot undo the committed operation or justify replaying it.
- [ ] Add real DB tests in `server/src/__tests__/universe-state.integration.test.ts`, using the migrated PostgreSQL setup and `insertTestCompany` pattern from `home-board-layout.integration.test.ts`. Seed two companies, two users, two conversations; concurrent revision-zero writes must yield one ack and one conflict. Assert cross-owner and cross-company route/service reads and writes fail. Any referenced distributed job is read separately through the existing non-owner tenant transaction; its RLS tests remain distinct from UI-state ownership tests.

Representative service-test body, inside that real seeded fixture (the fixture supplies `svc`, authorized `scope` and seeded task key `key`):

```ts
const patch = {schemaVersion:1 as const, operationId:crypto.randomUUID(), expectedRevision:0,
  operations:[{type:'pin' as const,key,value:true}]};
const a = await svc.apply(scope, patch);
expect(await svc.apply(scope, patch)).toEqual(a);
await expect(svc.apply(scope,{...patch,operations:[{type:'pin',key,value:false}]}))
  .rejects.toMatchObject({status:409});
expect((await svc.get(scope)).revision).toBe(1);
```

Seed the referenced panel before setting the revision-zero fixture snapshot; a missing key is a validation failure, not silent creation. Test nonexistent keys, duplicate order keys, unknown schema, oversized payload and nonfinite geometry explicitly.

### Checkpoint producer in E1.2/1

- [ ] Define/export `universe_panel_checkpoints` alongside the layout tables: UUID id, companyId/userId/conversationId, panelKey, sourceVersionId, schemaVersion, revision bigint, JSON data and timestamps; unique(companyId,userId,conversationId,panelKey,sourceVersionId). This is company/owner-scoped presentation data, not an automatic addition to the frozen RLS tables. Generate its migration with the same schema change.
- [ ] Add `getCheckpoint(scope,key,sourceVersionId): Promise<{revision:number;schemaVersion:number;data:unknown}|null>` and `saveCheckpoint(scope,key,{sourceVersionId,schemaVersion,expectedRevision,data})`. GET/PATCH route: `/api/companies/:companyId/universe/conversations/:conversationId/panels/:panelKey/checkpoint`; GET uses sourceVersionId query. Authenticate the conversation owner and exact source/version access, decode the bounded opaque panelKey once, and reject unknown registered payload schemas or serialized data over 32 KiB. E5.1/E5.2 supply reviewed inert payload validators; the generic E1.2 store/route and auth tests do not wait on the renderer implementation. Do not accept executable validators from content.
- [ ] Absent row has revision zero. In one transaction insert a zero-revision sentinel if absent, lock/read it, compare expectedRevision, save validated data at revision+1 and insert its activity audit; publish after commit. Concurrent first writes yield one success/one 409; a failed transaction leaves no partial checkpoint. A lost PATCH acknowledgement is reconciled by GET and sent-payload comparison, never an automatic action retry. Matching observed content permits clearing only the unchanged local dirty copy; a later/different value preserves the local variant for resolution.
- [ ] Add `server/src/__tests__/universe-checkpoints.integration.test.ts` using E1.2's migrated PostgreSQL fixtures: company/user isolation, foreign source/version, absent-row CAS race, same-version conflict, independent versions, 32 KiB boundary, unknown schema, no action dispatch on read/reload, and close/reopen retention. These tests and service/route exports are explicit E1.2 deliverables consumed by E5.1/E5.2.
- [ ] Add ordinal allocation tests to the layout integration suite: concurrent opens at one expectedRevision, rebase allocating distinct next values, receipt replay returning the same mapping, focus retaining order, close/reopen, counter hydration and safe-integer exhaustion. Client ordinals are optimistic only; server never accepts them as authority. Return one opened entry per open operation including existing panels, in input order with its operationIndex. The client journals operationId+operationIndex+key+local generation and uses E1.1 reconcileOpeningAck; same-batch close/reopen and a late receipt cannot mutate the new incarnation. GET layout exposes E1.1 AuthorizedLayoutSnapshot, including viewport, counter, per-panel ordinal and ordered/selected/maximized state; the hook hydrates it directly, not through replayed opens.

## E1.2/2 — Conflict and reload recovery

- [ ] `useUniverseState` retains acknowledged snapshot separately from optimistic operations. Debounce transient geometry (proposed 250 ms), flush final gesture immediately, and show saving/saved/offline/conflict based on server acknowledgement, never local animation.
- [ ] On lost ack query the operation receipt first. If absent, fetch snapshot. Rebase only property changes whose base property is unchanged; same-property edits preserve local candidate and require explicit resolution. Resubmission uses a new operationId only after resolving the old operation's outcome and recomputing expectedRevision.
- [ ] Keep receipts for the life of the layout initially; do not add arbitrary TTL that permits old IDs to apply twice. Later retention needs tombstone/expiry semantics and measured storage review. Bound the client pending journal (100 patches/1 MiB proposed); on overflow stop enqueueing and preserve a downloadable/recoverable local snapshot rather than falsely showing saved.
- [ ] Reload restores the authoritative nextOpenedOrdinal/panel ordinals before new opens; optimistic receipt reconciliation updates only the matching open incarnation. Checkpoints load separately by exact source version; never embed them in whole-layout patches. Close retains checkpoint/draft data under its retention policy, new source versions get separate state, and appearance reset never clears it.
- [ ] Reload fetches authorized snapshot before rendering cached previews. A smaller viewport computes display clamps without persisting them as preferred geometry. Logout/company switch cancels in-flight requests and clears accessible cached data; offline local retention follows explicit policy, not an assumed secure browser store.
- [ ] Test two tabs changing independent properties, same-property conflict, lost-ack receipt, scope switch before reply, snapshot version mismatch and reload during save in `tests/e2e/universe-persistence.spec.ts`.

## Verification, rollback and remaining evidence

Run the proposed integration file with Vitest, the E2E file with the existing Playwright config, then full repository checks on the integrated implementation. Windows-skipped Postgres tests require Linux evidence. Tenant table registration and generated policy migration must be reviewed at the final base before schema coding. Feature-disable stops new layout writes; preserve tables and records for rollback, do not drop data as a UI rollback. No tests above were executed during planning.
