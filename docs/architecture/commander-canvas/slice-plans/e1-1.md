# E1.1 — Build one canvas and panel controller

**Detailed coding addendum:** [E1.1 implementation plan](../coding-plans/e1-1.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planning, not approved for implementation. Premature isolated code is excluded from accepted delivery and has an unresolved disposition. **Release:** Accepted V1. **Epic:** Canvas, persistence and presentation. See [planning reset](../planning-reset.md); detailed plan, branch review, independent review and explicit user coding approval remain required.

**Goal:** Build one canvas and panel controller with the observable completion criteria below.

**Architecture:** Consumes authorized content references and viewport. Produces panel operations open/focus/move/resize/pin/minimize/restore/maximize/close with stable IDs, saved geometry and selected ID; local state never grants domain authority.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e1-canvas.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

The [first-batch coding plan](../coding-plans/e1-1-canvas-controller.md) supplies concrete controller types/code, generation fencing, reference checks and React Flow bindings at the [verified planning base](../first-batch-readiness.md). Its pure reference controller was checked independently; the package/frame/host integration still requires runtime qualification.

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **BASE, DESIGN**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `ui/src/components/hub/HubShell.tsx`
- `ui/src/components/commander/CommanderTaskFocusPane.tsx`
- `ui/package.json`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `ui/src/components/universe/UniverseWorkspace.tsx`
- `ui/src/components/universe/PanelFrame.tsx`
- `ui/src/components/universe/panel-state.ts`
- `ui/src/components/universe/__tests__/panel-state.test.ts`
- `tests/e2e/universe-panels.spec.ts`

**Boundary:** Consumes authorized content references and viewport. Produces panel operations open/focus/move/resize/pin/minimize/restore/maximize/close with stable IDs, saved geometry and selected ID; local state never grants domain authority.

**Change scope:** Pin/qualify React Flow and regenerate manifest+lockfile together. Build a reducer/registry owned above renderers and one PanelFrame. Adapt first-party content without nested window chrome. Bind header-only drag, all edges, pin, min/max/close and z-order; exclude editor/browser input.

**Controller additions in E1.1/1:** declare Panel.openedOrdinal / State.nextOpenedOrdinal independently of generation/z-order, reconcile authoritative E1.2 allocation after optimistic open, and implement the [bounded generation-fenced undo/redo contract](../coding-plans/e1-1.md#undo-and-redo-contract). Reducer and persistence share ±1,000,000 coordinate bounds. Undo never resurrects closed panels or overwrites a changed remote rectangle.

## Ordered work increments

### E1.1/1 — Registry and lifecycle

**Deliverable:** Create stable content-keyed panel identity and a reducer above renderer lifetime. Store normal geometry separately from maximized presentation; route all entries and controls through the same registry.

**Acceptance:** Opening the same task twice focuses one instance; close removes only that view, minimize preserves it, restore returns exact geometry.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E1.1/2 — Pointer and focus handling

**Deliverable:** Give the frame header drag ownership and edges resize ownership. Exclude buttons/editors/browser input, handle pointer cancellation, and bring any exposed clicked panel forward.

**Acceptance:** All eight resize edges work at non-unit zoom; pinned panels remain movable by the human; no animation-only minimize.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E1.1/3 — Renderer adaptation

**Deliverable:** Adapt existing first-party content into the shared frame and qualify React Flow with manifest and generated lockfile together.

**Acceptance:** Task, artifact and browser fixtures share identical lifecycle behavior without duplicate outer headers.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Same task via Work/Inbox/attention/source/overview is one live instance; every control changes actual state after animation; user drag still works when pinned; minimized keeps draft, closed removes view only.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## User acceptance

Use this slice's row in the [formal UAT register](../user-acceptance-plan.md#per-slice-acceptance-register), its numbered script variants and tester/product-acceptance record. All runs are currently not_run. Backend-only acceptance is demonstrated through the mapped consuming journeys and technical evidence; a developer test alone cannot fill product acceptance. E8.2 aggregates the records without replacing per-slice review.

## Verification and evidence

Proposed focused commands, runnable after these tests exist:

```sh
pnpm exec vitest run ui/src/components/universe/__tests__/panel-state.test.ts
pnpm exec playwright test --config=tests/e2e/playwright.config.ts tests/e2e/universe-panels.spec.ts
```

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.
