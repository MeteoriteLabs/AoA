# E4.3 — Preserve revisions and qualify generation

**Detailed coding addendum:** [E4.3 implementation plan](../coding-plans/e4-3.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V1. **Epic:** Artifacts, formats and generation. Individual accountable engineer is not assigned by this document.

**Goal:** Preserve revisions and qualify generation with the observable completion criteria below.

**Architecture:** Consumes authorized artifact/version and canonical generation request; produces new immutable version and provenance, not in-place edits.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e4-artifacts.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **E4.1–2 including selected publication security boundary; per-provider generation execution/credential policy and adapter qualification; E2.2 for jobs**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `server/src/services/artifacts.ts`
- `server/src/routes/artifacts.ts`
- `packages/db/src/schema/artifacts.ts`
- `ui/src/api/artifacts.ts`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `ui/src/components/universe/ArtifactVersions.tsx`
- `server/src/__tests__/universe-artifact-revisions.test.ts`
- `docs/architecture/commander-canvas/generation-qualification.md`

**Boundary:** Consumes authorized artifact/version and canonical generation request; produces new immutable version and provenance, not in-place edits.

**Change scope:** Reuse immutable versions and winner policy; compare/reuse source references without identity copy. For each generation provider define submission/outcome reconciliation and billing before enabling. Preserve native export separately from preview.

## Ordered work increments

### E4.3/1 — Immutable revisions

**Deliverable:** Reuse artifact version and winner semantics, with explicit source references and authorized compare/export actions.

**Acceptance:** Duplicate completion creates one version; original/winner references stay intact.

Canonical version + publication identity/receipt must commit in one authorized writer transaction. E4.1's human-intake choice is A; if the separately selected distributed publication protocol requires cross-role reconciliation, distributed acknowledgement follows that durable receipt in a separate transaction. Existing `aoa_app` SELECT-only artifact grants cannot be treated as write authority, and this plan does not authorize INSERT grants or owner fallback.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E4.3/2 — Generation qualification

**Deliverable:** Bind each supported generation adapter to canonical submission/outcome and budget rules before enabling it.

**Acceptance:** Uncertain completion is observed before new paid submission; external publication remains an explicit governed action.

M7 disposition: preserve image/audio/video generation and deterministic DOCX/PPTX/XLSX/PDF/chart writers in V1. Decision #104's August 8 amendment allows Company credentials for isolated `cloud_auth` CLI extraction and supersedes blanket embeddings-only key wording; it does not approve every new direct media API. Product/security must select and record each provider's permitted execution/credential boundary and any required narrow policy amendment before adapter coding, then qualify real output/billing/cancellation. Provider choices and accountable owners remain open. Manual/deterministic completion cannot close E4.3 while accepted media generation is blocked; no media scope is carved out or moved to V2.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Repeat uncertain generation outcome checked before new spend; duplicate completion one version under the selected publication protocol; source/winner preserved; external export/publication remains explicit permissioned action. Every accepted V1 generation family has its execution/credential policy and provider/writer evidence; unavailable media providers remain visible qualification blocks.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## User acceptance

Use this slice's row in the [formal UAT register](../user-acceptance-plan.md#per-slice-acceptance-register), its numbered script variants and tester/product-acceptance record. All runs are currently not_run. Backend-only acceptance is demonstrated through the mapped consuming journeys and technical evidence; a developer test alone cannot fill product acceptance. E8.2 aggregates the records without replacing per-slice review.

## Verification and evidence

Proposed focused commands, runnable after these tests exist:

```sh
pnpm exec vitest run server/src/__tests__/universe-artifact-revisions.test.ts
```

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.

## Worker-publication planning follow-up

Consume the [worker-publication design and addendum](../worker-publication-plan.md). E4.2/2 owns the proposed common application receipt/coordinator; E4.3/1 integrates immutable artifact/version writing. Existing job/output records remain non-owner tenant-owned. Application derivative/index/receipt records and E5.2 grant/action-link records are proposed company/actor/destination-scoped, pending independent/security review. The addendum replaces generic A/B/C alternatives for these proposed stores with a concrete recommendation; exact admission, accepted-output, revocation, retention and HOST qualifications remain open. No implementation approval or new increment/release allocation is implied.
