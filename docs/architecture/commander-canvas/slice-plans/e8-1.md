# E8.1 — Implement scoped settings with each consumer

**Detailed coding addendum:** [E8.1 implementation plan](../coding-plans/e8-1.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V1. **Epic:** Settings, rollout and release acceptance. Individual accountable engineer is not assigned by this document.

**Goal:** Implement scoped settings with each consumer with the observable completion criteria below.

**Architecture:** Consumes user/company overrides, app theme and permitted capabilities; produces effective preference plus inheritance/readiness. No preference read triggers voice capture or a billable probe.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e8-settings-release.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **E8.1/1: BASE only; E8.1/2: corresponding E1/E3/E6/E7 consumer contracts**. E8.1/1 publishes contract/defaults/store before E7.3 consumes them; E8.1/2 binds attention controls afterward. Completed E8.1 UI is not required to start E7.3. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `ui/src/pages/SettingsPage.tsx`
- `ui/src/components/settings/SettingsLayout.tsx`
- `ui/src/context/ThemeContext.tsx`
- `server/src/services/secrets.ts`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `ui/src/components/settings/sections/UniverseSection.tsx`
- `packages/shared/src/validators/universe-preferences.ts`
- `server/src/services/universe-preferences.ts`
- `server/src/__tests__/universe-preferences.test.ts`

**Boundary:** Consumes user/company overrides, app theme and permitted capabilities; produces effective preference plus inheritance/readiness. No preference read triggers voice capture or a billable probe.

**Change scope:** Implement field matrix in settings-contract with revisioned patch/reset. Personal appearance versus shared Commander style versus provider capability/Secrets versus Budget & caps. Device-local audio selection; full retained theme/blob/grid/rail/caption options; provider checks explicit.

## Ordered work increments

### E8.1/1 — Preferences contract foundation

**Deliverable:** Bind field scope, defaults, inheritance, revisioned patch/reset and device-local audio choices before consumers implement controls.

**Acceptance:** Personal appearance, shared Commander style, provider readiness and Budget & caps remain distinct; reset preserves work.

`soundEffects`/`spokenAnnouncements` are stored personal inputs to the Inbox/Commander shared policy. E8.1 owns round-trip/reset tests for all four combinations, with no notification-policy mutation or delivery side effect. E7.3 owns the mapping and reduce-only contract/test: personal values never expand shared eligibility. Contract/store publication can precede that consumer; delivery controls remain unavailable until E7.3 connects the common evaluator. No third delivery policy is introduced while waiting.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E8.1/2 — Consumer configuration

**Deliverable:** Deliver theme/blob/grid/captions/rails settings alongside E1 and add voice/browser/attention settings with their slices.

**Acceptance:** Every retained setting has save/failure/reset/migration behavior; no setting read starts capture or a billable provider probe.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Reset preserves drafts/layout/work; top-only tray default expanded; separate visibility; unsupported provider voice marked unavailable; cross-company updates denied; saved preference migration and failed-save recovery tested.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## User acceptance

Use this slice's row in the [formal UAT register](../user-acceptance-plan.md#per-slice-acceptance-register), its numbered script variants and tester/product-acceptance record. All runs are currently not_run. Backend-only acceptance is demonstrated through the mapped consuming journeys and technical evidence; a developer test alone cannot fill product acceptance. E8.2 aggregates the records without replacing per-slice review.

## Verification and evidence

Proposed focused commands, runnable after these tests exist:

```sh
pnpm exec vitest run server/src/__tests__/universe-preferences.test.ts
```

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.
