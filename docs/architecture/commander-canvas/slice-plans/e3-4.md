# E3.4 — Plan later speech pipelines and local speech

**Detailed coding addendum:** [E3.4 implementation plan](../coding-plans/e3-4.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V2. **Epic:** Voice capability qualification and sessions. Individual accountable engineer is not assigned by this document.

**Goal:** Plan later speech pipelines and local speech with the observable completion criteria below.

**Architecture:** Produces mode capability/environment matrix and concrete provider/runtime adapter plan. Local speech is not offline control-plane execution.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e3-voice.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

- V2 implementation cannot start until all agreed V1 scope passes its release gate. Planning and qualification design may be recorded now.
- Start conditions: **Complete and verified V1 plus explicit V2 approval; E3.1–3 adapter/lifecycle contracts, E2.1–3 context/outcome/reconciliation, E8.1 settings; independent qualification of each later speech mode**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `docs/architecture/commander-canvas/voice-provider-research.md`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `docs/architecture/commander-canvas/speech-modes-qualification.md`

**Boundary:** Produces mode capability/environment matrix and concrete provider/runtime adapter plan. Local speech is not offline control-plane execution.

**Change scope:** Qualify recognition+synthesis and local runtime separately: supported hardware/OS/models, install/update, language, timing, recording and failure. Bind adapters only after qualification; retain canonical Commander authority and privacy scopes.

## Ordered work increments

### E3.4/1 — Split recognition and synthesis

**Deliverable:** Qualify a bounded speech-recognition/synthesis pipeline with transcript correction, interruption, latency, credential and data-boundary requirements.

**Acceptance:** A corrected utterance reconciles before dispatch; duplicate partial/final text cannot create two requests.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E3.4/2 — Local speech runtime

**Deliverable:** Qualify supported OS/hardware/model installation, update, resource bounds and device disconnect behavior. Keep local audio processing separate from control-plane authority.

**Acceptance:** Missing model/hardware is clearly unavailable; device loss preserves the conversation and does not duplicate work.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E3.4/3 — V2 regression and rollout

**Deliverable:** Add mode-specific configuration to existing preferences/Providers and apply the shared release checks again after all V1 scope is closed.

**Acceptance:** V1 realtime modes continue to work; disabling a new speech mode preserves transcripts and canonical tasks.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Missing model/hardware yields clear unavailability; recognition correction reconciles transcript; local device loss does not dispatch twice; mixed providers preserve authorized data handling.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## User acceptance

Use this slice's row in the [formal UAT register](../user-acceptance-plan.md#per-slice-acceptance-register), its numbered script variants and tester/product-acceptance record. All runs are currently not_run. Backend-only acceptance is demonstrated through the mapped consuming journeys and technical evidence; a developer test alone cannot fill product acceptance. E8.2 aggregates the records without replacing per-slice review.

## Verification and evidence

This slice first produces a review/qualification record. Validate its source references, requirement coverage and measured protocol outcomes; a documentation check does not certify runtime feasibility. Its coding addendum must bind actual test fixtures and commands before runtime work.

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.
