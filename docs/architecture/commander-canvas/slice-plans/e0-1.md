# E0.1 — Bind the implementation base

**Detailed coding addendum:** [E0.1 implementation plan](../coding-plans/e0-1.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V1. **Epic:** Baseline and contract binding. Individual accountable engineer is not assigned by this document.

**Goal:** Bind the implementation base with the observable completion criteria below.

**Architecture:** Produces a revision-pinned binding manifest: contract name, existing export/route, owner, gate, consumer and evidence. No runtime interface or new database writes.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e0-baseline.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

Historical source binding is recorded in [first-batch readiness](../first-batch-readiness.md). The current [planning reset](../planning-reset.md) records exact local and last-fetched provenance; no execution base has been accepted. This narrows an investigation boundary, not approval of E1.1 or any other runtime slice. Final bindings require a reviewed base decision and revalidation.

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **BASE**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `docs/architecture/commander-canvas/readiness-review.md`
- `docs/replatform/epics/E10-desktop-migration-realtime/findings.md`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `docs/architecture/commander-canvas/implementation-bindings.md`

**Boundary:** Produces a revision-pinned binding manifest: contract name, existing export/route, owner, gate, consumer and evidence. No runtime interface or new database writes.

**Change scope:** Read accepted replatform revision and tenant-access/service registration patterns; resolve each proposed file collision; record schema, producer, consumer and test at that exact revision. Record owners and closure evidence for CMD/BROWSER/CLOUD/PROFILE/APPROVAL/VOICE/HOST rather than inventing upstream tickets.

## Ordered work increments

### E0.1/1 — Baseline and ownership

**Deliverable:** Compare local, fetched main and replatform ancestry; select the accepted integration revision with the program owner. Resolve each source anchor in that tree and assign the producer and consumer for every gate.

**Acceptance:** A manifest lists all 31 slice IDs, exact source revisions and unresolved owners; no unassigned dependency is marked closed.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E0.1/2 — Contract collision review

**Deliverable:** Compare proposed modules with tenant repository exports, route registration, credential issuance, schema conventions and existing work in flight. Record whether each change is reuse, extension or new code.

**Acceptance:** Each runtime contract has one authority and a consumer test owner; no parallel Commander execution or browser approval system is introduced.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

All 31 slices have a binding/disposition; source references resolve; missing dependencies stay gated; replatform main ancestry and merge strategy recorded before branch creation.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## Verification and evidence

This slice first produces a review/qualification record. Validate its source references, requirement coverage and measured protocol outcomes; a documentation check does not certify runtime feasibility. Its coding addendum must bind actual test fixtures and commands before runtime work.

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.
