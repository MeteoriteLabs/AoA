# E3.1 — Qualify and integrate OpenAI realtime

**Detailed coding addendum:** [E3.1 implementation plan](../coding-plans/e3-1.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V1. **Epic:** Voice capability qualification and sessions. Individual accountable engineer is not assigned by this document.

**Goal:** Qualify and integrate OpenAI realtime with the observable completion criteria below.

**Architecture:** Qualification output fixes adapter operations connect/end/interrupt/mute, transcript events and token/session issuance. No endpoint, model name or SDK version is assumed from the mock; bind before provider code.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e3-voice.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **BASE and accepted /1 documentary/security bindings, E2.1–2, E8.1/1 credential and shared Budget foundations; CMD only for distributed execution. VOICE is the live completion gate after the session consumer exists, not a prerequisite to building that consumer**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `ui/src/components/settings/sections/CommanderSection.tsx`
- `server/src/services/secrets.ts`
- `server/src/routes/internal-agent.ts`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `server/src/services/universe-voice.ts`
- `server/src/services/voice/openai.ts`
- `ui/src/components/universe/useVoiceSession.ts`
- `server/src/__tests__/universe-voice.test.ts`

**Boundary:** Qualification output fixes adapter operations connect/end/interrupt/mute, transcript events and token/session issuance. No endpoint, model name or SDK version is assumed from the mock; bind before provider code.

**Change scope:** First produce a provider contract with current official APIs, session credential scope/expiry/retention/usage and allowed speech exception to existing key rules. Then add explicit start/end service and direct audio connection. Route every work request through canonical Commander; no independent speech execution tools.

## Ordered work increments

### E3.1/1 — Provider contract qualification

**Deliverable:** Document current official session, credential, audio, transcript, interruption and usage interfaces; obtain the scoped speech credential architecture binding before implementation.

**Acceptance:** The provider capability record distinguishes configured CLI access from speech readiness and records actual retention/expiry constraints.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E3.1/2 — Session integration

**Deliverable:** Implement explicit voice start/end and canonical Commander handoff using the qualified contract; connect preference UI without automatic capture or paid readiness probes.

**Acceptance:** Real audio produces one authorized Commander request and attributable result; revoked credentials fail clearly without exposing long-lived keys.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Actual audio request→Commander→task/result speech; missing/revoked provider fails clearly; no long-lived secret in browser; opening settings does not start paid session; CLI-only configured vendor shows voice unready.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## User acceptance

Use this slice's row in the [formal UAT register](../user-acceptance-plan.md#per-slice-acceptance-register), its numbered script variants and tester/product-acceptance record. All runs are currently not_run. Backend-only acceptance is demonstrated through the mapped consuming journeys and technical evidence; a developer test alone cannot fill product acceptance. E8.2 aggregates the records without replacing per-slice review.

## Verification and evidence

Proposed focused commands, runnable after these tests exist:

```sh
pnpm exec vitest run server/src/__tests__/universe-voice.test.ts
```

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.

## D3 correction scheduling

The [credential, shared Budget and session corrections](../voice-media-review-corrections.md) are authoritative for this follow-up. E8.1/1 delivers the restricted-secret resolver and BUDGET-VOICE-MEDIA producer; E3.1/2 integrates transactional session ownership after accepted documentary/security bindings. E3.1/1's live qualification closes on those real producers and consumer; it does not gate their own construction. E8.1/2 UI follows consumers. Runtime tests and implementation still require explicit authorization. Existing increment IDs and release allocation are unchanged.
