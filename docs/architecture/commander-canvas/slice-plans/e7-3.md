# E7.3 — Upgrade shared attention delivery and add Universe presentation

**Detailed coding addendum:** [E7.3 implementation plan](../coding-plans/e7-3.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V1. **Epic:** Routines, catch-up and shared attention. Individual accountable engineer is not assigned by this document.

**Goal:** Upgrade shared attention delivery and add Universe presentation with the observable completion criteria below.

**Architecture:** Consumes authorized hub items and effective shared delivery policy; produces view state/delivery intent. Direct answers remain available; no third independent policy.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e7-routines-attention.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **BASE; E3 only for speech**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `server/src/services/internal-agent/proactive.ts`
- `server/src/services/hub-items.ts`
- `server/src/services/notification-preferences.ts`
- `ui/src/lib/hub-toast-bridge.ts`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `server/src/services/universe-attention-projection.ts`
- `ui/src/components/universe/AttentionRail.tsx`
- `ui/src/components/universe/AttentionQuestion.tsx`
- `server/src/__tests__/universe-attention-migration.test.ts`

**Boundary:** Consumes authorized hub items and effective shared delivery policy; produces view state/delivery intent. Direct answers remain available; no third independent policy.

**Change scope:** Separate durable item creation from delivery policy; migrate silent/digest intent explicitly. Reuse safe projection excluding denial audit namespace. Add optional local sound/voice channels with cross-device dedup; bottom-right movable question, same canonical answer.

## Ordered work increments

### E7.3/1 — Shared policy correction

**Deliverable:** Separate durable item creation from immediate/digest/silent delivery and explicitly migrate existing user intent.

**Acceptance:** Silent retains useful work without unsolicited speech; direct answers remain available.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E7.3/2 — Attention surfaces

**Deliverable:** Project canonical authorized items into right-side previews and movable bottom-right questions; preserve Inbox acknowledgement identity.

**Acceptance:** Answering resolves the same item everywhere; opening or closing a card never approves work.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E7.3/3 — Delivery deduplication

**Deliverable:** Add personal sound/voice channels only through shared policy and cross-device delivery ownership.

**Acceptance:** Duplicate events/devices do not repeat announcements; denied audit categories stay excluded.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Silent policy retains durable work without unsolicited speech; answer ack resolves same item; opening/closing is not approval; repeated events/devices don't repeat announcement; hidden rail still accessible via Inbox.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## Verification and evidence

Proposed focused commands, runnable after these tests exist:

```sh
pnpm exec vitest run server/src/__tests__/universe-attention-migration.test.ts
```

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.
