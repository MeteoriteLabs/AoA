# E2.2 — Add read-only outcome lookup and bind execution

**Detailed coding addendum:** [E2.2 implementation plan](../coding-plans/e2-2.md) covers every numbered increment below. Read the [shared bindings](../implementation-bindings.md) and [full-packet self-review](../planning-self-review.md) with it. These remain unexecuted plans pending independent review and explicit coding approval.

**Status:** Planned, unchecked. **Release recommendation:** V1. **Epic:** Commander context, outcomes and task chat. Individual accountable engineer is not assigned by this document.

**Goal:** Add read-only outcome lookup and bind execution with the observable completion criteria below.

**Architecture:** Consumes canonical submission identity; returns observed accepted/running/succeeded/failed/unknown with linked reply/output refs. GET/lookup never calls chat(), creates run rows, claims leases or executes CLI.

**Tech stack:** Existing React/TypeScript, Express, shared validators and PostgreSQL/Drizzle where applicable; React Flow for canvas implementation. Qualification determines actual provider/worker transport versions.

**Spec:** [Master scope](../master-scope.md), [UI decisions](../ui-review-decisions.md), [motion rules](../motion-and-interaction.md), [epic preparation](../implementation-plans/e2-commander.md), [release allocation](../release-plans/README.md).

## Constraints and readiness

- This slice belongs to the recommended complete V1; a blocked subpart keeps the slice incomplete and does not permit substituting V2 work.
- Start conditions: **BASE, E2.1; CMD for distributed behavior**. External gate definitions and owning workstreams are in the [readiness register](../readiness-review.md). An unresolved gate permits investigation/specification, not a completed runtime claim.
- Preserve company/actor authorization, governed writes, canonical task/conversation ownership, budget hard stops and activity auditing. UI says Task; existing DB/API naming remains unchanged.
- Keep layout, drafts, captions, secrets and execution authority separate. No credential belongs in presentation state or generated content. No UI visibility change authorizes execution or capture.
- Follow generated Drizzle migration and synchronized db/shared/server/ui contracts. Dependency manifests and generated lockfile travel together.
- [Coding readiness](coding-readiness.md) applies before each runtime increment. Proposed modules below are ownership proposals, not existing exports or verified implementations.

## File and interface ownership

Investigation anchors at the accepted source revision:
- `server/src/routes/internal-agent.ts`
- `server/src/services/internal-agent/agent-loop.ts`
- `packages/db/src/schema/internal_agent.ts`
- `ui/src/api/internal-agent.ts`

Proposed implementation/test outputs (confirm collisions and responsibility at BASE):
- `server/src/services/internal-agent/request-outcome.ts`
- `server/src/__tests__/universe-request-outcome.integration.test.ts`

**Boundary:** Consumes canonical submission identity; returns observed accepted/running/succeeded/failed/unknown with linked reply/output refs. GET/lookup never calls chat(), creates run rows, claims leases or executes CLI.

**Change scope:** Add authenticated read-only lookup by conversation and clientSubmissionId over canonical user turn/reply/run mapping. Validate request payload fingerprint and conflict on reused identity with changed content. Preserve current admitted chat path; connect distributed results only after CMD ownership/credential contract is accepted.

## Ordered work increments

### E2.2/1 — Observation-only outcomes

**Deliverable:** Read canonical submission, turn, reply and run relationships through a tenant-authenticated lookup. Record payload identity conflicts separately from retry policy.

**Acceptance:** Repeated lookup adds zero run rows, claims and external actions; unknown outcome never silently triggers a retry.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

### E2.2/2 — Execution binding

**Deliverable:** Connect the existing admitted request path first; bind distributed routing, credentials and result origin only after CMD evidence is accepted.

**Acceptance:** Distributed completion arrives in its originating conversation once, including lost acknowledgement and owner-transfer cases.

- [ ] Follow this increment's concrete file, interface, regression and recovery steps in the linked coding addendum; record implementation evidence only after approval.

## Connected acceptance

Lost ack repeated lookup produces zero new runs/claims; same key changed args conflicts; unknown is not safe retry; live owner never duplicated; distributed target and origin conversation verified after CMD closes.

Verify through the real authorized entry points, not just direct component construction. Include denied/revoked access for domain data or actions; qualify loading/error/empty states and keyboard/reduced-motion paths for visible UI. Read the owning epic's recovery scenarios alongside these checks.

## User acceptance

Use this slice's row in the [formal UAT register](../user-acceptance-plan.md#per-slice-acceptance-register), its numbered script variants and tester/product-acceptance record. All runs are currently not_run. Backend-only acceptance is demonstrated through the mapped consuming journeys and technical evidence; a developer test alone cannot fill product acceptance. E8.2 aggregates the records without replacing per-slice review.

## Verification and evidence

Proposed focused commands, runnable after these tests exist:

```sh
pnpm exec vitest run server/src/__tests__/universe-request-outcome.integration.test.ts
```

At runtime handoff, run repository typecheck, tests and build on the integrated tree. Provider/worker tasks require actual configured environment evidence in addition to fixtures. No new test listed here has been run in this planning pass.

## Recovery and completion

Disable this feature's new entry/actions on rollback while preserving canonical records, drafts and outputs. Reconcile uncertain outcomes before replay; never use an observation as permission to retry a write. Review backward compatibility and generated migrations before data changes.

This slice closes only when every increment, its connected acceptance, consuming settings/motion/accessibility and applicable dependencies have evidence. Record the implementation revision, test environment/results, residual limits and rollback outcome. An approved plan, simulated provider or incomplete subpart cannot close the slice.
