# Provider-Neutral Internal-Agent MCP Bridge (codex/opencode parity)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Fresh implementer per milestone + two-stage review; controller code-verifies every diff vs landed source (never trust reports). Strict TDD. `- [ ]` checkboxes.

**Why:** The §17 codex acceptance proved (code-verified) that the internal-agent MCP bridge is **claude-only**. `runner.ts:108-110` hardcodes `config.args=["--mcp-config", cfgPath, …]` (a native Claude Code flag) for **every** adapter; `cli-mode.ts:120-124` does the same for the chat. `claude_local` consumes `--mcp-config`; **`codex_local` has ZERO MCP wiring** (grep of `packages/adapters/codex-local/src` for mcp/config.toml/mcp_servers/--mcp-config → empty) so codex agents run with no tools → no real output (entry stuck `processing`, 0 `discussion_extracted_items`, run still exits "completed"). `opencode_local` same class. This is a **pre-existing architectural gap, not a FX1–FX7 regression**. Fix it *right*: a provider-neutral bridge seam each CLI adapter translates into its own MCP mechanism.

**Goal:** AoA agents + the Commander chat work on `codex_local` (and the design generalizes to `opencode_local`/future CLIs) with the **claude path behavior byte-preserved**; prove §17 real-output end-to-end on **codex** live; full suite stays green.

**Worktree/branch/test/git:** `AoA-2.5/.worktrees/commander-subagent-1` @ `commander-subagent-1`. Server tests `cd <wt>/server && npx vitest run src/__tests__/<f>`; adapter tests `cd <wt> && npx vitest run packages/adapters/<a>/src/__tests__/<f>`; UI `cd <wt> && npx vitest run ui/src/__tests__/<f>`. Stage files BY NAME; NEW commits; `docs/superpowers/` gitignored → `git add -f` this plan. Schema changes (none expected) via `pnpm db:generate`. App is running on the cmdr-c-verify instance (3211/54341) for live re-verify; never touch 3100/54329.

**Verified seam (code-truth, this session):**
- `cli-mode.ts:74-98` `buildMcpConfig` returns claude-shaped `{ mcpServers: { aoa: { command:"node", args:[bridgeEntrypoint], env:{AOA_SESSION_*,AOA_AGENT_KIND?,AOA_TOOL_ALLOWLIST?,DATABASE_URL?} } } }`. The **inner** `{command,args,env}` is already provider-neutral; only the `{mcpServers:{aoa}}` wrapper + `--mcp-config` file delivery is claude-specific.
- `packages/adapter-utils/src/types.ts:138-155` `AdapterExecutionContext` has `config`, `context`, `executionTarget?`, `runtimeCommandSpec?`, `onLog`… — **no `mcpBridge`**. Optional add = backward-compatible. Re-exported by `server/src/adapters/types.ts:3-27`.
- `runner.ts:95-122`: builds `mcp=buildMcpConfig(...)`, writes claude JSON to `tmpdir()/aoa-mcp-<agentId>-<runId>.json`, sets `config.args=["--mcp-config",cfgPath,...prevArgs]`, calls `adapter.execute({…config,context:{aoaInstruction,payload}…})`; unlinks the temp file in `finally` (pinned by `aoa-runner-tmpfile-cleanup.test.ts`).
- claude-local `execute.ts:246-250,401-417`: `config.args`→`extraArgs`→appended to `claude --print … <extraArgs>` argv. So `--mcp-config <file>` reaching the claude argv is the contract to preserve.
- codex-local `execute.ts`: `:280` `prepareManagedCodexHome(...)`, `:286-288` `env.CODEX_HOME=managedCodexHome` (= `<CODEX_HOME|~/.codex>/aoa-instances/<companyId>` per `codex-home.ts:36-38`, mkdir'd `:49`, holds `auth.json` `:11-13`), `:298-301` `extraArgs` falls back to `config.args` (so the runner's `--mcp-config` **leaks** into) `:366` `["exec","--json"]` `:377` `args.push(...extraArgs)`. Codex discovers MCP via `$CODEX_HOME/config.toml [mcp_servers.<name>]` — the managed home is the injection point.
- Pinning tests: `aoa-runner.test.ts:28` (`execArg.config.args` contains `--mcp-config` — will need a documented superseded update), `cli-mode.test.ts:278-310` (`buildMcpConfig` `{mcpServers:{aoa:…}}` shape — KEEP via wrapper), `packages/adapters/codex-local/src/__tests__/execute-target.test.ts:117` (`CODEX_HOME = …/aoa-instances/company-1` — codex config.toml write MUST keep this dir), `codex-home.test.ts` (auth.json untouched by a sibling config.toml), `codex-local-adapter.test.ts` (JSONL parser — new args must not perturb), `aoa-realoutput.integration.test.ts` (claude §17 — must stay green).

**Out of scope:** opencode MCP *implementation* (mechanism not in-repo/undocumented — design must keep the neutral hand-off so an `opencode.json` translator can be added later; flagged, not built). Marketplace seeding. The separately-decided **FX-del** (no AoA deletion) — sequenced after this.

---

## MX1 — Neutral bridge spec + contract seam
**Files:** `server/src/services/internal-agent/cli-mode.ts`; `packages/adapter-utils/src/types.ts`; `server/src/adapters/types.ts`. Test: `server/src/__tests__/cli-mode.test.ts` (extend), new `server/src/__tests__/mcp-bridge-spec.test.ts`.
- [ ] **1 [verify@exec]** Re-read `buildMcpConfig`/`McpConfigParams`/`McpConfig` (cli-mode.ts:52-98); `AdapterExecutionContext` (adapter-utils types.ts:138-155) + the `server/src/adapters/types.ts` re-export list.
- [ ] **2 Failing test** `mcp-bridge-spec.test.ts`: `buildMcpBridgeSpec(params)` returns `{command:"node",args:[bridgeEntrypoint],env:{AOA_SESSION_COMPANY_ID,…,AOA_AGENT_KIND?,AOA_TOOL_ALLOWLIST?,DATABASE_URL?}}` (exact env keys per cli-mode.ts:80-94); and `buildMcpConfig(params)` STILL returns `{mcpServers:{aoa:<that spec>}}` (wrapper preserved). Run→FAIL.
- [ ] **3 Implement** factor `buildMcpBridgeSpec(params): McpBridgeSpec` (the inner object); `buildMcpConfig` becomes `{mcpServers:{aoa:buildMcpBridgeSpec(params)}}` (return shape unchanged). Add `export interface McpBridgeSpec { command:string; args:string[]; env:Record<string,string> }` + optional `mcpBridge?: McpBridgeSpec` to `AdapterExecutionContext` in adapter-utils types.ts; add `McpBridgeSpec` to the `server/src/adapters/types.ts` re-export.
- [ ] **4 Run→PASS** new test + `cli-mode.test.ts` buildMcpConfig block (lines 278-310) unchanged-green.
- [ ] **5 Regression** `cli-mode.test.ts` full + `tsc --noEmit` (server) 0. **6 Commit** `feat(mcp): provider-neutral buildMcpBridgeSpec + AdapterExecutionContext.mcpBridge seam (MX1)`.

## MX2 — Runner hands the bridge neutrally; claude path byte-preserved
**Files:** `server/src/services/internal-agent/aoa-agents/runner.ts`; `packages/adapters/claude-local/src/server/execute.ts` (only if it must consume `ctx.mcpBridge`); tests `server/src/__tests__/aoa-runner.test.ts`, `aoa-runner-tmpfile-cleanup.test.ts`.
- [ ] **1 [verify@exec]** Re-read runner.ts:95-122 + the `finally` unlink (195-205); claude-local execute.ts:246-250,401-417 (how `--mcp-config` reaches argv).
- [ ] **2 Decide+document the claude-temp-file owner.** Two valid designs — pick the minimal-blast one at exec: **(A)** runner keeps writing the claude `{mcpServers}` file + `config.args=["--mcp-config",cfgPath]` **only when `adapterType` is claude-family**, AND always passes `mcpBridge=buildMcpBridgeSpec(...)`; non-claude adapters get `mcpBridge`, no `--mcp-config` leak. **(B)** runner only ever passes `mcpBridge`; claude-local's `execute` writes its own `{mcpServers}` temp file from `ctx.mcpBridge` + adds `--mcp-config`, owning its lifecycle. (A) preserves the runner tmpfile-cleanup test with least churn; (B) is cleaner/more uniform. Record the choice + rationale in the commit.
- [ ] **3 Failing tests** Update `aoa-runner.test.ts`: the run passes `ctx.mcpBridge` (neutral spec) to `adapter.execute` for a non-claude adapter and does NOT inject `--mcp-config` into `config.args` for it; for a claude-family adapter the claude `--mcp-config` delivery is preserved (assert the argv/`config.args` invariant per the chosen design). The current `:28 expect(execArg.config.args).toContain("--mcp-config")` is a **controller-authorized superseded-test update** (it pins the claude-only delivery this milestone deliberately generalizes; A5/A6/F2 precedent) — re-assert it scoped to the claude path; add a non-claude case asserting `execArg.mcpBridge` is the neutral spec and NO `--mcp-config` leak. Run→FAIL.
- [ ] **4 Implement** per the chosen design. claude argv MUST be invariant (so `aoa-realoutput.integration.test.ts` claude path stays green). Reconcile `aoa-runner-tmpfile-cleanup.test.ts` faithfully (claude still produces+unlinks the `.json`; non-claude does not).
- [ ] **5 Run→PASS** updated runner tests + tmpfile-cleanup; **6 Regression** `aoa-*`,`runner`,`cli-mode` + tsc 0. **7 Commit** `fix(mcp): runner hands neutral mcpBridge; claude --mcp-config delivery byte-preserved (MX2)` (note the superseded `aoa-runner.test.ts:28` update + rationale + design A/B choice).

## MX3 — codex_local: real MCP wiring via managed CODEX_HOME `config.toml`
**Files:** new `packages/adapters/codex-local/src/server/codex-config-toml.ts`; `packages/adapters/codex-local/src/server/execute.ts`. Tests: new `packages/adapters/codex-local/src/__tests__/codex-config-toml.test.ts`; extend `execute-target.test.ts`/`codex-home.test.ts` only if needed (faithfully).
- [ ] **1 [verify@exec]** Re-read codex-local execute.ts:280-302,365-381; codex-home.ts (managed home path/mkdir/auth.json). Confirm the exact codex `config.toml [mcp_servers.<name>]` TOML shape from codex-cli (`command`, `args` array, `[mcp_servers.aoa.env]` table). Confirm `extraArgs` no longer carries `--mcp-config` (MX2 stopped the leak) — if a defensive strip is still warranted, note it.
- [ ] **2 Failing tests** `codex-config-toml.test.ts`: `writeCodexMcpConfigToml(managedHome, spec)` writes `<managedHome>/config.toml` containing `[mcp_servers.aoa]` `command="node"` `args=["<bridge>"]` + `[mcp_servers.aoa.env]` with every spec.env key; does NOT touch/clobber `auth.json`; idempotent/merge-safe if `config.toml` exists; targets exactly `resolveManagedCodexHomeDir(env,companyId)` (the `…/aoa-instances/<cid>` dir → `execute-target.test.ts:117` CODEX_HOME pin preserved). Plus an execute-level test: when `ctx.mcpBridge` is set, codex execute writes the config.toml into the managed home and the `codex exec` argv contains NO `--mcp-config`. Run→FAIL.
- [ ] **3 Implement** `codex-config-toml.ts` (mirror codex-home.ts auth.json write style; minimal TOML serializer or a vetted dep already in the workspace — `[verify@exec]` deps; prefer hand-rolled minimal escaping for the known small shape). In `execute.ts`, after `prepareManagedCodexHome` (~:280) and before `buildArgs`, if `ctx.mcpBridge` present → `writeCodexMcpConfigToml(managedCodexHome, ctx.mcpBridge)`. Do NOT add `--mcp-config` to codex argv. Keep `CODEX_HOME` value exactly as is.
- [ ] **4 Run→PASS** new tests + existing `codex-local-adapter.test.ts`, `execute-target.test.ts`, `codex-home.test.ts` green. **5 Regression** codex adapter suite + `aoa-*` server + tsc 0. **6 Commit** `feat(mcp): codex_local wires the internal-agent bridge via managed CODEX_HOME config.toml (MX3)`.

## MX4 — Chat path generalized (fixes F-codex-chat)
**Files:** `server/src/services/internal-agent/cli-mode.ts` (`CLI_SPAWN_ARGS` + chat file-write/spawn ~120-124,177-212). Test: `server/src/__tests__/cli-mode.test.ts` (extend; the spawn-args were previously unpinned).
- [ ] **1 [verify@exec]** Re-read CLI_SPAWN_ARGS, CLI_BINARY_MAP, the chat `buildMcpConfig`→writeFile→spawn (cli-mode.ts:177-212), and the codex per-session home story (chat has no `agent.companyId` managed-home; needs a per-session CODEX_HOME or `-c mcp_servers.*` overrides — decide using MX3's helper).
- [ ] **2 Failing tests** per-CLI: `claude_cli` → unchanged (`--mcp-config <file> -p <msg> --output-format text`); `codex` → `codex exec --json` with MCP via a per-session `config.toml` (reusing MX3's writer) or `-c mcp_servers.aoa.*` + prompt via stdin/positional (NOT `-p`); assert NO `--mcp-config`/`-p` for codex. (`opencode` left as a documented TODO returning a clear "unsupported in chat for now" or the existing behavior with a flag — do NOT silently keep the broken claude shape; `[verify@exec]` what's least-bad: prefer an explicit not-yet-supported error over a confusing broken spawn.) Run→FAIL.
- [ ] **3 Implement** restructure `CLI_SPAWN_ARGS` into per-CLI translators (neutral spec + msg → {argv, preSpawn-write}); claude byte-unchanged; codex correct. **4 Run→PASS** + `cli-mode.test.ts` full green (buildMcpConfig wrapper still pinned-green). **5 Regression** `cli-mode`,`aoa-*` + tsc 0. **6 Commit** `fix(mcp): generalize chat CLI MCP/prompt wiring; codex uses codex exec (MX4; F-codex-chat)`.

## MX5 — Live re-verify (the actual proof) + full regression
- [ ] **1** Full server suite `cd <wt>/server && npx vitest run` → 352+ files / 0 failed (the new tests add files); UI suite green; `tsc --noEmit` 0 (server + ui). Independent final code-review subagent over the MX1–MX4 cumulative diff; controller code-verifies.
- [ ] **2** On the running cmdr-c-verify instance (Phase2 PHA company, agents already `codex_local`): drop a fresh discussion entry → assert **real `discussion_extracted_items`** rows + entry terminal `extracted/completed` + `internal_agent_runs` completed + `cost_event` — the §17 hard bar **on codex** (the V2 that failed before). Capture the real rows as evidence.
- [ ] **3** Chat with the Commander (internal-agent) on codex → assert a real codex response (V1; F-codex-chat fixed). @mention + delegate wakeup still single-exec (B2/FX3 holds). Claude path: `aoa-realoutput.integration.test.ts` still green (claude argv invariant).
- [ ] **4** Update `docs/guides/board-operator/aoa-agents-acceptance.md` with the codex variant + the now-passing real-output result (tracked file, plain `git add`). Commit.
- [ ] **5** Report consolidated evidence → return to FX-del, then the finish-branch decision.

### Discipline
Fresh implementer per milestone; two-stage review; controller code-verifies every diff vs landed source + re-runs targeted+regression independently — never trust subagent reports. STOP/NEEDS_CONTEXT/BLOCKED rather than guess or weaken a test; superseded-test updates (only the controller-authorized `aoa-runner.test.ts:28` here) with documented rationale + real contract preserved. Claude path argv is the invariant — if any milestone risks changing it, STOP. opencode MCP is design-compatible but explicitly NOT implemented.
