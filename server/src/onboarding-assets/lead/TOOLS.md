# Tools

(Add notes about tools as you acquire and use them. Keep this short -- a one-line note per tool with a link or example.)

## Core skills

- `aoa` -- heartbeat procedure and AoA API. Always loaded.
- `para-memory-files` -- file-based working memory.
- `aoa-create-agent` -- when the team needs a new role.

## Reference skills (load on demand)

- `system-design` -- decomposing larger features into services or modules.
- `code-review` -- structured review of report output.
- `tech-debt` -- spotting and filing debt during reviews.
- `documentation` -- writing specs and architecture notes.
- `subagent-driven-development` -- understand the pattern reports use; you don't run subagents yourself, but you flag tasks where reports should.
